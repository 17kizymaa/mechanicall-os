# .inbox — depreciated folder send (not Headscale-as-Drive)

**Not CURRENT. Not Yes.** Headscale only carries the mesh. Bytes land here, gitignored.

| Path | Direction | Who Accepts |
|------|-----------|-------------|
| `sitters/<id>/` | sitter → operator (opt-in upload) | operator *looks*; never apply as mechanicall-os CURRENT |
| `outbox/<id>/` | operator → sitter (new app, template, …) | **sitter Accept/Decline** (AirDrop/Taildrop-shaped notify) |

`NOTICE.json` on every offer. Accept copies files and **skips live CURRENT.md**. Decline leaves the offer marked declined.

See `SHARING.md` and `python/aether_inbox.py`.
