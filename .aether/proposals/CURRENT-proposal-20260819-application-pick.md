# Proposed pick — what application is Mechanicall?

**Not law.** Operator `CURRENT.md` is SELECT / REJECTED. This file does not overwrite it.
**Trigger:** reject 2026-08-19T09:06:05Z — standardise as an application; pick from a list; definition must reward work since late July; deep research; explicit conclusion of wants; Go.
**Earlier same morning:** 08:18 actual operator Yes (not ritual); 08:34 refinement/reeval due.
**APPLY:** human picks, then `aether next` / `aether approve` with a real reason. Model must not overwrite CURRENT.

## What the 09:06 reject asked for (next steps)

1. Standardise this as **an application** (not a folder, not a protocol lifestyle).
2. Need something else than the last turns’ definitions.
3. Operator **picks from a list**.
4. The definition must **reward the work since late July**.
5. Deep research → explicit conclusion of wants.
6. Go.

## Explicit conclusion of wants (from this repo + rejects, late July → today)

| Date | Want (logged, not inferred as taste) |
|------|--------------------------------------|
| 2026-07-21 | Authority layer that is real: CURRENT, preflight, approve/reject, events |
| 2026-07-27 | Panel / first-run that is not “type aether” |
| 2026-08-03 | **One application** (seat UX). CLI is not the product. Casual CLI bet **failed**. `docs/SINGLE-APP-DISTRIBUTION.md` |
| 2026-08-10 | Not technically bound to one host. Other people’s devices: **web + Android + desktop + distro suite** |
| 2026-08-16 | Phone demo for **live client planning**; client is the planner; proposal-doc editor; Yes applies PROPOSE |
| 2026-08-18 | People-app / share; then reject desktop-only: **actual phone seat or mate/ will not work** |
| 2026-08-19 08:18 | Actual human operator Yes, not law-ritual |
| 2026-08-19 09:06 | **Application**; a **list to pick**; reward the months of work |

**Hard work already on disk that a pick must use, not throw away:** v0.2 protocol, `aether` + tests, panel TUI, seat (Tauri, incomplete), pocket APK sideloaded on A33, people-app factory, SeatMate lab, CURRENT/events/DECISIONS.

**Market (2026 HITL):** everyone else puts approval in a queue + Postgres (LangGraph, StackAI, Lindy, Copilot Studio). Your differentiator is already built: the plan is a file. That only pays if someone **opens an application** that shows it and the Yes is a button, not a sermon.

## The list (pick one)

| # | Application | What you open | Rewards July–Aug work? | Can pay a bill this week? |
|---|-------------|----------------|------------------------|---------------------------|
| **1** | **The Seat** (recommended) | One window: plan (`CURRENT.md`) + draft + Yes / Not yet + agent. Desktop and phone are the **same app**, not two products. | Yes — protocol is the engine; panel/seat/APK are faces. Already named 2026-08-03. | Yes — APK is on the A33; panel exists. Sit a **client’s** plan in that window. |
| **2** | **The sitting** | Same Seat, sold as the hour: you run a live planning session; they tap Yes; you invoice. | Yes — `mobile-planning-demo` + mum-class gate. | Yes, if a client will sit. |
| **3** | **The suite** | Same Seat on web + Android + desktop + distro (Aug 10 want). | Yes, but as a **roadmap**, not today’s ship. Session on anphuni.com stays lab. | No — not this week. |
| **4** | **A vertical app** (SeatMate-class) | A booking/charity/gig app that *uses* Mechanicall underneath. | Partial — factory exists; SeatMate is still “not an app, stack later.” | No — that app is not built. |
| **5** | **The CLI** | `aether` + files as the product. | Uses the protocol; **contradicts** the 08-03 failed casual bet. | Only if the client already lives in a terminal. |

Rejected as definitions (do not pick these as the product name): “a folder that’s yours”; “a bench for improving other people’s apps” as the *pitch*. Those can stay as **how the Seat binds a project**, not what you sell.

## Fidelity checklist

- Still one Next after a pick.
- Models never approve.
- No secrets.
- Silence ≠ permission.
- Phone is a seat of the same application, not a second CURRENT.

## If you pick 1 or 2

Suggested human follow-up (you run these, not the model):

```text
aether next <new-id-if-you-want-one>
aether approve "<real reason: this is the application I am shipping>"
```

Today’s work after that Yes: open the existing Seat face on a **client project’s CURRENT**, one job, one receipt. Do not assemble another chrome story first.

`APPLY: human must approve then merge — model must not overwrite CURRENT without human gate`
