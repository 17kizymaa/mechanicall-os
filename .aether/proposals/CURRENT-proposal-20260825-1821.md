# CURRENT proposal — 2026-08-25T18:21Z

**Not law. Not Yes.** Live `CURRENT.md` stays until a **human** applies this file.  
**APPLY: human must approve then merge — model must not overwrite CURRENT without human gate.**

Grill: `decision-tree.md` M-series. Header Next is already `sit-mvp-rack` (human M3=2). Body is still `sit-distro-gate`.

---

## Observations

- Live header: **Next `sit-mvp-rack` APPROVED.** Objective / This Next / Action id / Reject still describe **closed-tester distro** (`sit-distro-gate` remainder: LTE → Play internal → Go-git). Two clocks. Preflight **allows** `sit-mvp-rack` because the header matches — a hole until the body matches.
- `sit-distro-gate` 1–3 Done in-tree (`dev/42`). LTE still BLOCKED. Play pack ready, Console human. Production Reject.
- M-series locked: **M1=6** sit walk + SOS-pipeline face + folder cycle. **M2=1** whole UI = generated plates; Plan = typeset CURRENT (no md glyphs, no nested millwork window, no Changes); Draft = interactive twin + schema + empty TBC (models deferred). **M6=4** Imagine paints millwork. **M7=1** Imagine = Photoshop, then **freeze**; FACE-SPEC “no Imagine-mocks as the spec” **stands**. **M8=3** Headscale/tsnet **carries** the contained zip; JOIN/SEND/WAKE = symbolic frozen PNGs (recant verbose Compose strip). **M9=2** AuthKey compiled into the APK. **M10=1** gitignored inject + Headscale jail (ephemeral, `tag:stranger`, TTL, ACL); APK treated public; no R8-as-security. **M11=2** WAKE plate still POSTs wol-pi (conjure in this MVP). **M3=2** new action-id `sit-mvp-rack` (human already re-SELECT’d).
- First halt you demanded: **implementation framework review** (Imagine PNGs + CRect map) **before** assemble.
- Funnel / public wake / Play production / dual-Next / Camel PNG theft / Compose millwork-as-SOS / child CURRENT / USB-as-LTE: still Reject.
- M4 (first stranger) and M5 (Play this week) **parked** — not this Next.

## Inferences

- Sequential ≠ dual-Next. One live id: `sit-mvp-rack`. Conjure is **not** a following Next; WAKE is in this bundle (M11=2). `sit-conjure-desk` as a queued id is stale.
- Distro remainder (LTE, Console, Go-git) is **unfinished and parked**, not the Objective.
- Recant as *this Next*: leftover-chrome-as-the-Next, SOS-reskin-as-this-Next, conjure-desk-this-next, G4 “JNI stays a named gap / do not ship folder-send.” Keep: leftover-**Compose**-millwork-as-SOS, Imagine-as-spec, camel-png-theft, Funnel, pretend-JOIN-without-tsnet-Up.
- Status APPROVED is the **old** distro amendment. Applying this body is a **new** approve (real reason). Do not rubber-stamp `APPROVED: APPROVED`.
- Models do not Imagine, assemble, deploy wol-pi, or close JNI until this file is applied **and** stage 1 has a human Go on `output/`.

## Unknowns

- Headscale login-server HTTPS reachability and key TTL you will mint (never in git).
- Whether wol-pi `:7077` is up this sitting (doorbell was connection-refused earlier).
- M4/M5 if you reopen them.

## Proposed change (summary)

1. **Keep Next `sit-mvp-rack`** (already selected). Rewrite Objective / This Next / Action id / Keep / Reject / Limits / Prohibited to the M-series bundle.  
2. **Move distro remainder to parked Done/Limits** (LTE BLOCKED, Play human later, not this Next).  
3. **First allowed action:** Imagine millwork + CRect map at `dev/45_sit-mvp-rack/01_plates/output/` — **halt**. No assemble until you review.  
4. **Then** (after Go): freeze `drawable-nodpi/`, typeset Plan / schema Draft, symbolic DESK, tsnet AAR, gitignored AuthKey inject, zip-over-Headscale, WAKE→wol-pi, recant E3b (k3s off myarch), BEHAVIOURS B2 rewrite, git last.  
5. **Do not recant** Funnel, Imagine-as-spec, Camel PNG theft, Play production, dual-Next, USB-as-LTE, secrets-in-git.

## Fidelity checklist

- [x] Still **one** Next (`sit-mvp-rack`)
- [x] Models never approve / never auto-write CURRENT
- [x] No secrets in git (`.jks` / Play JSON / WOL_MAC / wake token / **Headscale preauth**)
- [x] Silence ≠ permission
- [x] First stage halt at `output/` before assemble
- [x] JOIN `connected` only if tsnet Up
- [x] Imagine = paint, not spec
- [x] Accept/unzip never writes live CURRENT

## Conflicts with existing authority

- Header Next `sit-mvp-rack` vs body `sit-distro-gate` — this propose **aligns** the body.  
- Reject leftover-chrome / SOS-reskin / conjure-desk-this-next — **recant for this action-id** (Imagine-freeze + wol-pi are the Next). Compose millwork and Imagine-as-spec stay Reject.  
- Keep “G4 JNI named gap” — **recant**: closing tsnet AAR is in this Next. Still do not claim Play-store LTE folder-send.  
- `examples/propose-current/CURRENT-sit-vst-headscale.md` Prohibited `bake-invite-into-apk` — M9=2 + M10=1 is bake **with** gitignored inject + Headscale jail, not a key in git.  
- Live Approval APPROVED on the distro amendment: applying this is a **new** human `aether approve`.

## Before → after (header)

| Field | Live | Proposed |
|-------|------|----------|
| Objective | Play-uploadable distro remainder; following Next conjure | One sit rack: Imagine-frozen plates, typeset Plan / schema Draft, Headscale zip carry, WAKE→wol-pi |
| Baseline | distro wrap, LTE BLOCKED, conjure queued | 2026-08-25 M-series; header Next already `sit-mvp-rack`; body catch-up |
| Next | sit-mvp-rack | sit-mvp-rack *(unchanged id)* |
| This Next | LTE, Play, git | plates halt → freeze + mesh + doorbell |
| Action id | sit-distro-gate | sit-mvp-rack |
| Approval | APPROVED (old) | PENDING until you approve **this** body |

## Human decision required

- [ ] Apply proposed `CURRENT.md` body below, then `aether approve` with a **real reason**
- [ ] Reject and leave live CURRENT unchanged (header Next stays a hole)
- [ ] Revise (e.g. recant M11=2, park wol-pi)

**Do not** run `aether approve` from a model. **Do not** Imagine or assemble until this apply **and** stage-1 `output/` has your Go.

---

## Proposed CURRENT.md (paste)

```markdown
# CURRENT

**Objective:** One sit rack: Imagine-painted millwork **frozen** into the APK (human reviews PNGs + CRects before assemble); Plan = typeset published CURRENT (no md glyphs, no nested millwork window, no Changes); Draft = the same plates, editable, schema visible, TBC empty (no model auto-draft); Headscale/tsnet **carries** a contained folder zip; JOIN/SEND/WAKE are symbolic frozen PNGs; WAKE hit-rect POSTs wol-pi (S3, HOLD 15 min). Not Play production. Not Funnel. Not Imagine-as-spec. Not Camel bitmaps.
**Phase:** APPROVE
**Status:** DRAFT
**Baseline:** 2026-08-25 · M-series in `decision-tree.md` (M1=6 M2=1 M6=4 M7=1 M8=3 M9=2 M10=1 M11=2 M3=2) · human re-SELECT `sit-mvp-rack` · `sit-distro-gate` 1–3 Done (`dev/42`); LTE/Play **parked** · conjure spec `dev/43_journey-review/05_conjure-protocol/output/CONJURE.md` now **this** Next · tsnet JNI still a gap until AAR links
**Next:** sit-mvp-rack
**Approval:** PENDING
**Host:** myarch · `personal-llm-sft-v4` · STORM home `~/.mechanicall/storm` · Headscale login-server (always-on HTTPS) · wol-pi doorbell

## Product (non-negotiable identity)
- **Is:** local-first authority protocol. The **phone sit** is the lab face of that protocol.
- **This Next:** one action-id that includes **all** of: (1) **Imagine = Photoshop** — generate millwork into `dev/45_sit-mvp-rack/01_plates/output/`; human reviews PNG(s) + CRect map; **halt**; freeze into `res/drawable-nodpi/` only after Go; Compose overlays live text/hits only; (2) **Plan** = typeset fields of published CURRENT.md (Objective, Next, Keep, Reject, Limits — labels, not `**Next:**`); overflow = MORE / section plates, not a nested scroll-window; **no Changes module, no Proposal-on-PLAN**; (3) **Draft** = the same plates, interactive, schema showing, TBC well **empty** (no 7B auto-draft this MVP); writes PROPOSE only; (4) **DESK symbols** — JOIN / SEND FOLDER / WAKE as frozen PNG filmstrips, not Compose status essays, not a Material preauth well; (5) **Headscale + tsnet AAR** — JOIN `connected` only if userspace Up; mesh **carries** the contained zip; Accept copies files, never live CURRENT; (6) **AuthKey baked** — assemble injects from gitignored `~/.mechanicall/headscale-preauth` (sibling of Play keystore); treat APK as public; ephemeral + `tag:stranger` + TTL + ACL; ControlURL may be baked (not a secret); no R8-as-security; (7) **WAKE** hit-rect POSTs wol-pi; myarch S3 in working hours; HOLD 15 min then S3 unless local session; Send does not imply WAKE; recant E3b (k3s off myarch) **as part of this Next**; Funnel never; (8) rewrite BEHAVIOURS B2–B4 to match Plan/Draft plates; (9) dirty git last (human Go-git).
- **Not core:** Play production listing, Funnel, public Ollama, public wake, Imagine-mocks as FACE-SPEC, Camel Audio bitmaps, Compose millwork as SOS, model Yes, model auto-draft, website listing (P4b), child CURRENT as operator law, LTE/Play Console remainder (parked), dual-Next.

## Done (keep)
- Claim-1 CERTIFIED · casual-core paused
- sit-join-workshop · sit-vst-headscale (JNI gap **named**; closing it is this Next)
- sit-storm-factory **as factory**
- sit-testers **as face:** first-sit template engine exists; Decide = Publish not hunk pick; cream/LCD/GATE live glass (to be replaced by freeze after plate Go)
- **sit-distro-gate 1–3:** `_unzip_to` containment + adversarial tests; Chaquopy-native Decide→Publish; `targetSdk`/`compileSdk` 36 + signed AAB off-git keystore + listing pack (`dev/42` receipts)
- SOS blit **practice** (`15_binary-skin`) then revert (`16_revert-skin`) — pipeline named; millwork paint is now Imagine, not numpy PSD
- P-series / W-series / M-series in `decision-tree.md`
- Phone file `13_FIRST_DISTRIBUTION_PR_GPT5.6.md`
- W-series conjure **specified** (`CONJURE.md`) — **live as this Next**, not queued
- 2026-08-25: header Next `sit-mvp-rack`; LTE still BLOCKED if probed — **not this Next**

## Keep
- One Next; models never approve; silence ≠ permission
- GET / Bind / Send / JOIN / WAKE / LOAD / STREAM / twin-pull / storm-up / inbox-offer / inbox-accept / FILES-open are not Decide
- Phone bind ≠ this repo
- `tests/run.sh` as CI — plus Android assemble
- STORM cap 2; never A33 as a STORM serial (USB-read of `/sdcard/mechanicall/sessions/` is allowed)
- Parked sit code stays until this Next replaces the glass
- Lab drop may remain `127.0.0.1` as a **fallback**; sharing is not “online” unless tsnet is Up
- **Dev mode:** aggressive mechanicall-os work stays **under this folder** (`dev/` CONTEXT.md + `output/`; `examples/` sample CURRENTs are toys, never live Next). Subagents never `aether approve` / `next` / rewrite CURRENT. No child CURRENT as operator session authority
- FACE-SPEC: no Imagine-mocks as the spec; no painted letterbox; `resizeableActivity`; paper is the score; GATE LOAD then STREAM
- `_unzip_to` containment stays; Accept never writes live CURRENT

## Reject
- dual-concurrent-next
- leftover-**Compose**-millwork-as-sos
- Imagine-mocks as visual law / Imagine-as-spec
- storm-review-as-the-product
- Decide hunk-picker as first-sit
- model-auto-approve / model-auto-write-current / model-auto-draft-this-mvp
- EdubaWare-clone / k3s-as-Mechanicall-core
- Funnel / public Ollama / public wake / `0.0.0.0` wake
- Play **production** listing this Next
- Sharing RZCW2038KHN as a STORM serial
- Twin that writes CURRENT
- Headscale-as-folder-store / auto-install inbound app
- Camel Audio bitmap extraction
- Pretend JOIN connected without tsnet Up
- Restart drop / call sharing “online” while zip extraction is uncontained **or** tsnet is down
- Upload PR #7 as-is to Play
- Secrets in git (preauth, `.jks`, WOL_MAC, wake token)
- R8 / NDK obfuscation as AuthKey security
- Child CURRENT.md for subagents as operator law
- USB-on-mbp-edge as LTE receipt
- Nested Plan “Changes” + Proposal chips as B2 (recant B2 this Next)

## Limits
- Closed testers — not release
- No secrets in git (no `.jks` / Play JSON key / wol-pi MAC / wake token / Headscale preauth). Assemble may **inject** preauth from `~/.mechanicall/headscale-preauth`
- Treat the APK as public: ephemeral + `tag:stranger` + TTL + ACL
- Do not rewrite a bound client CURRENT from this tree
- Grouped git exclude live CURRENT unless human Go-git
- STORM is developer infrastructure, not a second product
- Website production listing parked (P4b)
- Play API-36: new apps and updates from **2026-08-31** must target API 36 (human Console)
- `MANAGE_EXTERNAL_STORAGE` / cleartext traffic are Console declarations, not silent policy passes
- **LTE sideload + Play internal** parked (M5 unanswered). Do not skip, do not fake USB-as-LTE, do not make Console this Next
- Sequential chat: one action-id at a time; ICM stages + subagents under **this** Next only (CONTEXT.md, halt at `output/`)
- Stage 1 **must** halt for human plate review. Assemble after Go is not silence
- Do not suspend myarch from this agent unless `MECHANICALL_ALLOW_SUSPEND=1` (desk-hold)
- Models deferred: Draft TBC does not call a 7B. GATE/STREAM may run **after** WAKE (conjure), never as auto-Yes

## Next allowed action
**Action id:** `sit-mvp-rack`

Workspace after apply: `dev/45_sit-mvp-rack/` (CONTEXT.md + numbered stages). Do not start `01_plates` until this CURRENT is applied.

1. **Plates (halt).** Imagine millwork (not Camel clones, not Imagine-as-spec). Write preview PNG(s) + CRect / SKIN-LAYOUT map to `01_plates/output/`. Title / LCD / GATE / DESK / tabs / paper / bind / splash. JOIN / SEND / WAKE as symbols. **Stop. Human reviews. No assemble.**
2. **Freeze + Plan/Draft** (after Go on 1). Pack `drawable-nodpi/`; overlay hits; typeset Plan; schema Draft + empty TBC; BEHAVIOURS rewrite. Sideload for look only.
3. **Mesh.** tsnet AAR; JOIN connected iff Up; gitignored AuthKey inject + Headscale jail; contained zip carry; Accept ≠ Yes.
4. **Doorbell.** WAKE → wol-pi; myarch S3 + HOLD 15 unless local session; recant E3b (k3s off myarch); Funnel never; Send does not imply WAKE.
5. Dirty git last (human Go-git).

Then **stop**.

## Approval condition
Human applies this file (Next already `sit-mvp-rack`). Then `aether approve` with a **real reason** naming plates-halt + Headscale carry + WAKE→wol-pi. Unblock of stage 2 is **your Go on `01_plates/output/`**, not another silent chat.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- play-production-this-next
- funnel-this-next
- model-auto-write-current
- model-auto-draft-this-mvp
- share-a33-across-storm
- bind-phone-to-operator-tree
- camel-png-theft
- leftover-compose-millwork-as-sos
- imagine-as-spec
- restart-uncontained-drop
- play-upload-pr7-as-is
- pretend-join-without-tsnet
- r8-as-preauth-security
- child-current-as-operator-law
- assemble-before-plate-go
```
