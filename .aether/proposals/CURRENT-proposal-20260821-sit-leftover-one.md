# CURRENT proposal — sit-leftover-one

**Not law. Not Yes.** 2026-08-21. User: REJECTED leftover split; follow-on stages are **one Next**.

Live CURRENT is still `sit-storm-factory` APPROVED. Models did **not** `aether reject`. Human applies after they reject (if that is the intent).

APPLY: human `cp` then `aether next sit-leftover-one` and `aether approve`. Model must not overwrite CURRENT.

## Why one Next

Execute FLAG/gaps were listed as four follow-on folders (`06_idle-lamps`, `07_desk-compress`, `08_tsnet-jni`, `09_storage-bind`). That is dual-concurrent-next by another name. They are **one** leftover pass on the original sit APK.

## After

Full prose: `examples/propose-current/CURRENT-sit-leftover-one.md`

Action id: `sit-leftover-one`

Work inside that Next (sequencing, not extra Nexts):

1. GATE lamps dark at idle/quiet
2. DESK strip compact — paper is the score
3. API-34 bind via SAF / app-files, not hidden appops
4. tsnet JNI factory; named gap if no gomobile
5. One storm-0 re-walk. Never Confirm. Never A33.

ICM home: `dev/40_sit-leftover-one/` (`01_implement` → `02_verify`).

## Fidelity
- [x] one Next
- [x] models never approve
- [x] no secrets
- [x] silence ≠ permission
- [x] PRODUCT: not an AI host
- [x] STORM stays factory, not a second product
- [x] leftover-as-four-nexts is Reject
