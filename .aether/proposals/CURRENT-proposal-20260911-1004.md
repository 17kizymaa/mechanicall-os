# CURRENT proposal — 2026-09-11 (hand each client a CURRENT.md)

**NOT ACTIVE. Not law. Not Yes.**  
**APPLY: human must approve then merge — model must not overwrite CURRENT without human gate.**

**Live disk today:** root `CURRENT.md` is a **proposal wrapper** (`you-decide-os-cycle` 14:48 paste). `aether current validate` **FAIL** (no Objective / Phase / Next / Keep / Reject / Limits). Git HEAD is still `first-sit-look`. Reject `proposal-wrapper-as-CURRENT` already exists. Restore or paste a SPEC-valid body; do not leave the wrapper as law.

**Why this file:** new Objective. One new Action id. Not an add-on to `you-decide-os-cycle`. Dual-Next Reject.

---

## Summary of proposed change

**Objective:** **Mechanicall: You Decide** — each potential client gets their own `CURRENT.md` in their own folder, and **they** accept it **with a reason**. This tree stays operator law.

**This Next:** `hand-client-current`

1. Operator names aliases (not emails in git) + one-sentence Objective + first action-id per client.
2. Mint one SPEC-valid `CURRENT.md` per alias (`Phase: SELECT`, `Approval: PENDING`).
3. Hand the folder. Halt. Their Yes is theirs.

Do not stamp their file. Do not put their law in mechanicall-os `CURRENT.md`. Do not invent names. Tickets stay tickets (never CURRENT). Zip / download / “looks good” ≠ Yes.

Template (not live): `.aether/proposals/CLIENT-CURRENT-TEMPLATE.md`

---

## Fidelity checklist

- [x] Still **one** Next (`hand-client-current`)
- [x] Header Next = body Action id
- [x] Models never approve; silence ≠ permission
- [x] No secrets in git (aliases only; no emails, no ticket secrets)
- [x] Client CURRENT stays PENDING until **they** stamp; operator does not `aether approve` in their folder
- [x] Dual-Next Reject — not a second live Next beside millwork / you-decide-os-cycle
- [x] Does **not** archive You Decide
- [x] Web Yes Reject (`web-yes-splits-authority`)
- [x] `inbox/tickets/` still never CURRENT
- [x] `proposal-wrapper-as-CURRENT` stays Reject
- [x] Phone bind ≠ this operator repo
- [x] SPEC-valid Phase / Status / Approval (SELECT / READY-FOR-REVIEW / PENDING on apply until human stamps)

---

## Conflicts with existing authority

- Working-tree `CURRENT.md` is not valid law (proposal pasted over CURRENT). This proposal replaces that wrapper.
- Git HEAD Next `first-sit-look` (dest millwork) is **not** this halt. Millwork stays archive / following, not deleted.
- Uncommitted You Decide door (`you-decide-os-cycle`, apex `/you-decide`) is **Done plumbing**, not a second Next. Sequential ids stay following.
- PRODUCT.md: anphuni.com Session is not core. Client CURRENT is the protocol product, not a Session seat.
- `domains/` is LAB samples, not live client law.
- Inbox Accept of a zip **skips** CURRENT (existing NOTICE). That Accept is not Mechanicall Yes.

---

## Before / after

```diff
-**Objective:** (unset / proposal wrapper / or HEAD dest millwork)
+**Objective:** **Mechanicall: You Decide** — each potential client gets their own CURRENT.md; they accept it with a reason. This tree stays operator law. Label: CLIENT CURRENT.

-**Next:** (unset)  /  first-sit-look  /  you-decide-os-cycle
+**Next:** hand-client-current

-This halt: dest millwork FLAG  /  one web-door demo
+This halt: named client packs, each SPEC-valid and PENDING, in their hands. Their reason is the stamp.
```

---

## What this is not

- Not a new public website.
- Not Generate / propose-with-model.
- Not stamping CURRENT for the client.
- Not putting three clients’ law into this repo’s `CURRENT.md`.
- Not `inbox/tickets/<id>/CURRENT.md` (tickets never CURRENT).
- Not inventing client names so the halt looks done.
- Not pasting this wrapper onto CURRENT (the paste-ready body below is the file; this preamble is not).

---

## Paste-ready CURRENT (toy — not disk)

Human copies **only** the fenced body onto `CURRENT.md` after Yes. Then `aether current validate`. Then human `aether next hand-client-current` / `aether approve "…"`.

```markdown
# CURRENT

**Objective:** **Mechanicall: You Decide** — each potential client gets their own CURRENT.md in their own folder, and they accept it with a written reason. This tree stays operator law. Label: CLIENT CURRENT.
**Phase:** SELECT
**Status:** READY-FOR-REVIEW
**Baseline:** 2026-09-11 · working CURRENT was a proposal wrapper (validate FAIL) · git HEAD `first-sit-look` · apex `/you-decide` live · tickets never CURRENT
**Next:** hand-client-current
**Approval:** PENDING
**Host:** myarch · packs live outside this git tree (or gitignored). A33 USB not this halt.

## Product (non-negotiable identity)
- **Is:** **Mechanicall: You Decide.** Local-first authority protocol. Inference proposes. Authority disposes. You decide. AI does the point-and-clicking — under a plan you can `cat` and a yes only you can give.
- **The gift:** a folder that is **theirs**, with a `CURRENT.md` they can read. Opening the file is not Yes. Zip / download / inbox Accept are not Yes. **Yes** is `aether approve "<reason>"` in **that** folder (or their own written stamp of Approval + reason). The reason is required. Silence is never permission.
- **This Next:** `hand-client-current` — name the clients (aliases), mint one SPEC-valid PENDING CURRENT each, hand the folder, halt. Do not stamp for them. Do not millwork. Do not Generate.
- **Not this halt:** dest millwork look, Play, OpenRouter, sequential public ticket ids, Session seats, putting client law in this operator file.

## Done (keep)
- Protocol: one Next, preflight, human `aether approve` with a reason, events.jsonl
- Apex door `https://anphuni.com/you-decide` · seats inbox · Kingston `inbox/tickets/` (tickets never CURRENT)
- Sit millwork archive (`first-sit-look` and later sits) — evidence, not this halt
- Sample client-shaped CURRENT: `examples/pocket-demo-client/CURRENT.md`
- Client template for this Next: `.aether/proposals/CLIENT-CURRENT-TEMPLATE.md`

## Keep
- You Decide; models never approve; silence ≠ permission
- One Next on **this** tree; one Next on **their** tree; they are not the same file
- Phone bind ≠ this operator repo
- Client CURRENT ≠ operator CURRENT
- Approval condition is a **real reason** they wrote
- Draft / proposal / this file stay NOT ACTIVE until a human Yes
- Inbox Accept copies files and **skips** live CURRENT
- Tickets (`inbox/tickets/`) never CURRENT
- Web Yes Reject
- `proposal-wrapper-as-CURRENT` stays dead
- USB ≠ LTE

## Reject
- dual-concurrent-next
- proposal-wrapper-as-CURRENT
- model-auto-write-current
- stamp-client-current-for-them
- client-law-in-operator-current
- invent-client-names
- tickets-as-current
- web-yes-splits-authority
- archive-you-decide-this-halt
- generate-this-halt / openrouter-this-halt
- millwork-this-halt
- sequential-public-ticket-ids-this-halt
- bind-phone-to-operator-tree
- domains-lab-as-live-client-law

## Limits
- A few named potential clients — operator supplies aliases + one sentence each. Models do not fill names.
- No emails, phones, ticket secrets, or personal data in git.
- Packs live **outside** this repo (preferred) or a gitignored path. Do not commit client folders.
- If they do not have `aether` yet, the file is still the contract: they write the reason; they stamp Approval; the operator does not stamp it for them. Installing aether / APK / ticket zip-back is **following** unless they already have that door.
- `aether current validate` must PASS on every pack before it leaves.
- You Decide door demo and sequential ids remain following, not a second live Next.

## Next allowed action
**Action id:** `hand-client-current`

1. Operator lists aliases + one-sentence Objective + first action-id per client (chat or a private list, not git if it has emails).
2. For each alias, copy `.aether/proposals/CLIENT-CURRENT-TEMPLATE.md` into **their** folder as `CURRENT.md`. Fill Objective / Next / Next allowed action. Leave Approval **PENDING**.
3. `aether current validate <their-folder>` — PASS required.
4. Hand the folder (copy, zip, USB). Inbox Accept if you already use outbox — still not Yes.
5. Stop. Do not run `aether approve` in their folder. Their reason is the stamp.

## Approval condition
Human copies this body onto live Domain `CURRENT.md`, then `aether current validate`, then `aether next hand-client-current` / `aether approve` with a real reason. Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-write-current
- stamp-client-current-for-them
- client-law-in-operator-current
- invent-client-names
- tickets-as-current
- web-yes-splits-authority
- archive-you-decide-this-halt
- generate-this-halt
- millwork-this-halt
```
