# Proposed CURRENT update (draft — not authority)

**Project:** mechanicall-os (operator)  
**Date:** 2026-08-20  
**Author:** agent propose only (human applies)

`APPLY:` human must approve then merge — model must not overwrite CURRENT without human gate.

## Summary

Replace live Next `people-app-phone-seat` (REJECTED) with **one** Next `mechanicall-debug-window` that **combines** desk-hop + STORM install + actual windowed Camel Space–vibes debug face + simplified mark. Not four Nexts. Not a maximised kiosk.

Replacement body: `examples/propose-current/CURRENT-mechanicall-debug-window.md`  
Face contract (not law): `dev/35_debug-window/01_propose/output/FACE-SPEC.md`  
Mark preview: `dev/35_debug-window/01_propose/output/icon-simplified.jpg`  
Window preview: `dev/35_debug-window/01_propose/output/window-mock.jpg`

## Observations

- Live CURRENT: Next `people-app-phone-seat`, Phase SELECT, Status **REJECTED**, Approval REJECTED. Preflight still ALLOW on that action-id.
- Operator 2026-08-20: combine all suggested nexts under **one** next; proceed under a law after this proposal is accepted.
- `0.8.0-vst` painted a 92% card on a dark phone and called it a window. Operator: **vst-fit sucks**; want **actual windowed mode**, Camel Space vibes, yellow & purple, compact bevels, **debugging** purpose. Icon still wrong; this update may **simplify** the mark (slab + disc).
- Desk: myarch **is** this host. Ollama listens `*:11434`, model `personal-llm-sft-v4`. Live A33 Send still **urlopen timed out** through edge reverse. Desk-hop is unproven.
- STORM-ENV: `/dev/kvm` present; **emulator package and x86_64 system image missing**. Parallel sits cannot start until install.
- PRODUCT.md: core is the filesystem protocol, not a chatbot. Seat UI is layout + rare outside-the-chat yes/no.

## Inferences

- Keeping `people-app-phone-seat` as Next repeats chrome churn under a rejected id.
- Desk-hop, STORM factory, and the window are **one product sit**, not three laws.
- “Model thinking” must not be a chat ellipsis. Camel Space’s **trance gate** (step LEDs) is the right instrument: GATE lights while Ollama streams.
- Windowed means OS-resizeable + a compact plugin frame that does not consume the display, not a screenshot of a title bar.

## Unknowns

- Whether A33 USB reverse can be made to answer, or Tailscale must be on the phone, or LAN `192.168.0.51` is enough when the sitter is on Wi‑Fi.
- Whether Samsung freeform/DeX is available on SM-A336B for true OS windows, or compact letterbox is the phone stand-in.
- Whether `sdkmanager --install emulator` is a human-attended download (licenses, kvm group).

## Proposed CURRENT change

See full replacement: `examples/propose-current/CURRENT-mechanicall-debug-window.md`

```markdown
**Objective:** One sit: a **windowed Mechanicall debug plugin** on the phone, desk traffic through **myarch** local model with a visible GATE (thinking), and **STORM** on isolated emulators. Same law: pocket folder, human Decide. Not Play Store. Not four Nexts.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-20 · people-app-phone-seat REJECTED (painted VST, desk hop unproven, no STORM AVD)
**Next:** mechanicall-debug-window
**Approval:** PENDING
**Host:** myarch · anyone can boot this desk to run `personal-llm-sft-v4`
```

## Fidelity checklist

- [x] Still **one** Next (`mechanicall-debug-window`)
- [x] Models never approve / never write CURRENT in this step
- [x] No secrets in git
- [x] Silence ≠ permission
- [x] Open / Bind / Send ≠ Decide
- [x] No dual-concurrent-next
- [x] No Play Store / Funnel / public Ollama
- [x] Phone bind ≠ mechanicall-os

## Conflicts with existing authority

- Header **Next:** `people-app-phone-seat` → `mechanicall-debug-window`
- Objective (phone seat for mate/) stays true as **lab already assembled**; live work becomes the windowed debug plugin + desk + STORM
- CURRENT still says “Not Compose this Next” in the old body; the APK **is** Compose chrome. The new body names the APK honestly as lab face, not as a second core.
- Approval stays human-only. This file is not Yes.

## Human decision required

- [ ] Apply `examples/propose-current/CURRENT-mechanicall-debug-window.md` over root `CURRENT.md`, then `aether next mechanicall-debug-window` if needed, then `aether approve "…"` with a real reason  
- [ ] Reject and leave `people-app-phone-seat` unchanged  
- [ ] Revise the action-id / FACE-SPEC, then re-propose  

**Do not** run `aether approve` / `next` from a model or agent.
