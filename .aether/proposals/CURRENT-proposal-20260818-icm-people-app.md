# Proposed CURRENT update (draft — not authority)

**Project:** mechanicall-os  
**Date:** 2026-08-18  
**Author:** human: leave the sitting; approve a new Next; use the sitting to inform a people-app

`APPLY:` human must `aether next` then merge — model must not overwrite CURRENT without human gate.

## Summary

Close `mobile-planning-demo` as the live Next. Keep the brake/pocket engine as **lab**. New Next: **`icm-people-app`** — the already-mapped ICM factory (`dev/25_icm-people-app`) whose product is a copyable workspace. Informed by the failed stranger-send: their folder, their identity, GET ≠ Yes, no Tailscale-as-me.

Replacement file: `dev/23_mobile-planning-demo/12_leave-for-people-app/output/CURRENT-icm-people-app.md`

## Observations

- Live Next is still `mobile-planning-demo` (APPROVED). Face was up on `100.90.85.68:8765`, then **stopped** this leave. Friend `~/brake-friend-sit/RECEIPT.md` still “No sitting yet.”
- Invite was never sent by this CLI. Friend refused to log in as `17kizymaa@gmail.com`. Correctly.
- Factory `25` already chose Jake / option 1 (not CURRENT-D OpenHands-as-architecture). Discovery + stage map exist. Scaffold waits **proceed to 03**.
- CURRENT Limits already say: if the sitting stays cancelled, human reject and re-SELECT — do not leave this Next as decoration.

## Inferences

- The sitting taught the gate (plan / draft / Yes / Not yet / receipt). It did not teach a people distribution path.
- People-app front door cannot be “join my tailnet as me.”
- One Next: leave 23 as lab; 25 becomes the operator Next. Do not run both.

## Unknowns

- Whether the first person to walk `01_bind` is you pretending to be a stranger, or someone else with their own folder.
- Whether a later Next adds Compose/OpenHands as a *runner* (not identity).

## Proposed CURRENT change

Before (header):

```
**Next:** mobile-planning-demo
**Approval:** APPROVED
**Phase:** IMPLEMENT
```

After (file): `CURRENT-icm-people-app.md`

```
**Next:** icm-people-app
**Approval:** PENDING
**Phase:** SELECT
**Status:** ACTIVE
```

Body: parks the sitting as lab; Keep includes LESSONS.md; Reject includes Tailscale-as-me, Funnel, POST Yes for them, CURRENT-D; Next allowed action is proceed to 03 on `dev/25_icm-people-app`.

## Conflicts with existing authority

- Replaces `mobile-planning-demo` as the only Next (this is the point).
- Does not delete lab files under `dev/23_*`.
- Does not authorize OpenHands as architecture (25 already rejected CURRENT-D).
- Does not claim a friend sat.

## Fidelity checklist

- [x] Still one Next
- [x] Models never approve
- [x] No secrets
- [x] Silence ≠ permission
- [x] No `model-auto-write-current`
- [x] Header and body match inside the replacement file
- [x] No dual-concurrent-next (sitting becomes lab)

## Human decision required

- [ ] Apply: `aether next icm-people-app` **first**, then `cp` the replacement over `CURRENT.md` (see `output/APPLY.md`)
- [ ] Reject and leave `mobile-planning-demo`
- [ ] Revise and re-propose (different action-id, or OpenHands D after all)

**Do not** run `aether approve` / `aether next` from a model as its own decision.
