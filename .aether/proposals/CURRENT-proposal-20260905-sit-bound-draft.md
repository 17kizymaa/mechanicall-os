# CURRENT proposal — 2026-09-05 sit-bound-draft

**Not law. Not Yes.** Live `CURRENT.md` stays until a **human** applies this file.  
**APPLY: human must approve then merge — model must not overwrite CURRENT without human gate.**

Grill: C-series Round 12. Operator pushed from One Next store-first toward **phone as control surface for a bound project folder**, revived millwork dests, Draft as whole-file paper + GATE Generate, CRT darkened-room composed tube, Send overlay as settings.

Live: Next `one-next-contract-and-archive-freeze` **APPROVED** (header). Body Action still `sit-module-panels`. Two-clock. This propose recants store-first **this halt** and aligns header = body = `sit-bound-draft`.

Pins (Round 13):

| Pin | Selected |
|-----|----------|
| C17b RAG | **1** folder-walk of the **bound** folder, not chroma |
| C17c OpenRouter | **1** two-pass on **desk**; **cost-cap $10/week** (not rate-limit); key never in APK/git |
| C18b diffs | **1 for now** visual live-vs-proposed fields on dest (thread 15 Draft). Unified-diff appendix + hunk millwork **following** |
| C14 `idkr` | **unnamed**. Model does not fill. |

`~/Desktop/.env` was **not** copied into this repo. Line 5 is a second OpenRouter secret, not the $10/w budget. Safe in-tree: `.env.example` + gitignored `.env`. Desk already loads Desktop `.env` via `aether_fs.load_dotenv_files` (first `sk-or-` wins). Add `OPENROUTER_WEEKLY_BUDGET_USD=10` as a KEY=value line there yourself.

---

## Observations

- Thread 15 (`dev/50_ss-closing-apk/01_ingest/output/15_RAW-_SS-CLOSING-APK-THREAD.md`): *Inference proposes. Authority disposes.* Fluency ≠ authority. Draft allows model fluency. Stamp is a recorded human decision, not crypto. Authority must stay `cat` / `grep` / `git diff`. RAG/memory: **no vector database in core**; retrieved context ≠ binding authority. *A model walks the folder. It proposes. You stamp. The project remembers.*
- Isolated dests (`sit-module-panels` / `0.19.3-glass-polish`) were Need as IsolatedDark. Human reject 2026-09-04: shittier than freeze rack. This halt **keeps dests as isolated modules** with **millwork plates** (C13 split), not IsolatedDark debug type.
- `write_schema_draft` today **replaces** `PROPOSE-CURRENT.md`. C18=1 forbids that: user prose stays; schema merges.
- `personal-llm-sft-v4` is already pocket default. Taste critical 3/3. Do not retrain this halt.
- `python/aether_llm.py` already speaks OpenRouter from `OPENROUTER_API_KEY` (env, not git).
- Logo donut filled (`ic_launcher` / `logo_mechanicall`, `#12100E`). Not sideloaded.
- Listing-bar holds. Play is a **following** Next of **this sit**, not a One Next stranger shell this halt.

## Inferences

- One Next: `sit-bound-draft`. Bound folder is the product face. Play following.
- Generate is GATE on isolated Draft millwork. Not Yes. Not GET. Not a chat app. Not GitHub CI.
- Job: prose on paper → Gate → sft-v4 few-shot + folder-walk → schema fields **merged** into PROPOSE → optional OpenRouter QC of that schema → CURRENT unchanged → Decide is still the only Yes.
- Fail-closed if desk down. Paper still works. Stranger sideload does not require your Ollama or your OpenRouter key.

## Unknowns (human pins)

- C17b / C17c / C18b as table above.
- `idkr` (C14).
- OpenRouter model slug + rate (if C17c=1): not filled from taste; you name it on apply or later.

## Proposed change (summary)

**One Next:** `sit-bound-draft`

1. Dest millwork: Draft = chassis millwork + `sit_gate` (no darkened room). CRT = darkened-room composed `sit_crt`. Send overlay = simplified-chassis **settings** panel (not terminal, not Yes, not FILES).
2. Draft dest opens whole-file `PROPOSE-CURRENT.md`. Recant ALPHA/BETA/GAMMA keys.
3. Generate = Gate hit. `personal-llm-sft-v4` + few-shot + folder-walk. Schema Objective/Next/Keep/Reject/Limits **merge** into PROPOSE. Prose stays. Visual live-vs-proposed fields on dest (C18b=1). CURRENT bytes unchanged.
4. OpenRouter QC: desk-only, **$10/week spend cap**, PROPOSE schema only, `qc skipped` if down or cap hit.
5. Bind: SAF + all-files. Operator tree refused. Sideload testers. USB ≠ LTE. Not Play this halt.
6. Rewrite B11/B12/B13 to match dest millwork (not IsolatedDark Need). `app_verify` + device look. Halt.

Toy CURRENT to paste: `examples/propose-current/CURRENT-sit-bound-draft.md`

## Fidelity checklist

- [x] Still **one** Next (`sit-bound-draft`)
- [x] Models never approve / never auto-write CURRENT
- [x] No secrets in git / APK (OpenRouter key = `~/Desktop/.env` / gitignored `.env`; `.env.example` only in tree)
- [x] Silence ≠ permission
- [x] Bind / Send / FILES / GATE / open are **not** Decide
- [x] Play production still Reject this halt; listing-bar holds
- [x] GET ≠ Yes; Generate ≠ Yes
- [x] Dual-Next Reject (Play following, not live)
- [x] Vector RAG not in core (thread 15)

## Unified diff (header / Action)

```diff
- **Next:** one-next-contract-and-archive-freeze
- **Approval:** APPROVED
- **This Next:** `sit-module-panels` — five hits…
- **Action id:** `one-next-contract-and-archive-freeze`
+ **Next:** sit-bound-draft
+ **Approval:** PENDING
+ **This Next:** `sit-bound-draft` — bound folder; millwork dests; Draft paper + GATE Generate → schema in PROPOSE
+ **Action id:** `sit-bound-draft`
```

Objective recants store-first **this halt**. One Next Play stranger shell moves to Limits / following.

---

## Honest-greenlight-gate

**What this session (the grill + this propose) can implement:** write files. Not an APK. Not a CURRENT apply. Not OpenRouter calls. Not A33 sideload.

**What `sit-bound-draft` can implement after human apply**, scored honest:

| Work | Greenlight | Why |
|------|------------|-----|
| Align header Next = body Action = `sit-bound-draft` | **HUMAN** | apply this file |
| Draft dest: millwork + `sit_gate` blit, stop IsolatedDark early-return | **GREEN** | plates exist; `onDraw` returns before `gateBmp` today — one path |
| Recant ALPHA/BETA/GAMMA as Draft chrome | **GREEN** | CRects; stop drawing those hits |
| Whole-file paper on dest (scroll + type) | **YELLOW** | EditText/IME already ate Receipt once; needs walker never-BACK |
| Merge schema into PROPOSE (prose stays) | **GREEN** | new wrapper; `write_schema_draft` replace is the bug to recant |
| GATE STREAM → sft-v4 few-shot | **GREEN** | `_ollama_stream` + `_CHAT_SYS_TEMPLATE`; few-shot is prompt |
| Folder-walk RAG (cat/grep bound CURRENT/PROPOSE/receipts, cap tokens) | **GREEN** | `_folder_names` / `_schema_lines`; **not** chroma |
| Unified-diff appendix of Field lines vs live | **following** | C18b=1 visual-for-now; `list_hunks` still exists |
| Visual live-vs-proposed fields on dest | **YELLOW** | Need this halt (C18b=1); still workshop dump in `drawDraftWorkshop` |
| OpenRouter QC two-pass on desk | **YELLOW** | `aether_llm.py` exists; need **$10/w ledger** + `qc skipped`; Desktop key not in git |
| CRT dest composed phosphor (not debug dump) | **YELLOW** | IsolatedDark + `lcdBody()` dump today |
| Send overlay as settings millwork | **YELLOW** | overlay uses `drawCrtModule` + amber dump today |
| Logo void fill | **DONE** | not on device until assemble |
| Hunk accept/reject millwork | **RED this halt** | C4d park; S5c following |
| Chroma / MODEL+RAG vector in sit | **RED** | thread 15 / NOT-IMPLEMENTED |
| OpenRouter key in APK | **RED** | secret |
| Retrain sft-v5 | **RED** | other Domain |
| Play listing / One Next stranger shell | **RED this halt** | following; listing-bar |
| P1–P6 (title unbind, site gauntlet, secrets polish, chassis-preview CRT) | **RED this halt** | parked |
| A33 look of dest millwork | **after assemble** | USB ≠ LTE; not testers |

**This session’s honest halt:** propose + tree + greenlight + `.env.example`. Implement starts only after you apply and say Go.

**Fat risk (C3=1):** three dest looks + paper + Generate + QC. If the sitting slips, cut order: OpenRouter QC → CRT composition → Send restyle. Do not cut merge-schema or GATE-on-Draft millwork — those are the bound-folder story.

### What turns YELLOW → GREEN (from disk, not taste)

**OpenRouter QC** — GREEN when:

1. Spend ledger you can `cat` (`~/.mechanicall/openrouter-week.json` or gitignored `.aether/openrouter-week.json`): USD this ISO week vs cap `OPENROUTER_WEEKLY_BUDGET_USD=10`. Over cap → no HTTP, `qc skipped`.
2. Two-pass: sft-v4 merge **then** `aether_llm` QC of the schema block only. QC may revise PROPOSE schema; never CURRENT.
3. Dest copy: `qc skipped` if no key / no desk / cap / HTTP fail. Pass-1 schema still lands.
4. Tests mock HTTP; CI has no real key. `~/Desktop/.env` never staged. `.env` gitignored.
5. Lab receipt: one Generate, CURRENT unchanged, ledger incremented, look PNG.

**CRT composed typeset** — GREEN when IsolatedMode.CRT is not a dump:

1. Darkened room + maximised `sit_crt` millwork (blit already there).
2. Phosphor is paged law with hierarchy (title / Next / body), not `LAW  {title}\\n{text}` in `lcdBody()`.
3. Dismiss → rack. SDK **and** A33 look. B11 Need matches. Not IsolatedDark-shittier.

**Send settings overlay** — GREEN when it is not a mini-terminal:

1. `drawSendOverlay` uses chassis grain / settings layout, not `drawCrtModule(sendPanel)` + amber “SEND folder” dump.
2. Hits: panel = Send, outside = dismiss. Not Yes. Not FILES. CURRENT bytes unchanged (B12).
3. Look PNG reads as settings. `sit-send-smoke` still PASS.

**Whole-file paper + IME** — GREEN when Draft dest is the file without eating the walk:

1. Scrollable `PROPOSE-CURRENT.md` prose; recant ALPHA/BETA/GAMMA / `drawDraftWorkshop` dump.
2. `sit_gate` blit + Gate hit (today `onDraw` returns before `gateBmp` on IsolatedMode.DRAFT).
3. IME_ACTION_DONE; `hideSoftInputFromWindow` on Gate / dismiss / bank change. Walker never BACK.
4. Receipt and FILES stills are those plates, not Decide+keyboard. Empty tap does not Generate.

**Visual field-diff (C18b=1)** — GREEN when live vs proposed Objective/Next/Keep/Reject/Limits are typeset on the dest (not a debug workshop string). No hunk buttons this halt.

---

APPLY: human copies the toy onto live CURRENT (or edits it), then:

```bash
aether next sit-bound-draft
aether approve "bound-folder sit; millwork dests; Draft paper + GATE Generate to PROPOSE; not Play"
```

Silence is not apply. Models do not `aether next`.
