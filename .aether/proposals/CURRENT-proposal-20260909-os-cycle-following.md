# CURRENT proposal — following after you-decide-os-cycle

**NOT ACTIVE. Not law. Not Yes.**  
Live `CURRENT.md` is unchanged. Models do not paste this. Models do not `aether approve` / `aether next`.

This file exists so success-evidence item 6 has a following proposal that stays inactive until a human Yes.

---

## What already happened (evidence, not a second Next)

- Apex door live (`7ca0d13`). Inbox on Render seats disk `/var/data/seats/you-decide`.
- One live drop pulled into `inbox/tickets/4ee13598c7855f0f84f56efbf2627d68/`.
- Operator zip on that same ticket; download ≠ Yes.
- Receipt: `dev/61_current-service-entry/06_os-cycle/output/RECEIPT.md`.

## Parked (still following)

- Propose-with-model / Generate on the ticket (output shape TBD).
- Desk Ollama when stable; OpenRouter via Desktop/.env as parked substrate.
- Sequential ids as **operator alias only** (not public enumerable).
- APK millwork / `you-decide-ritual`.
- 12–100 requests.

## Do not

- Treat this file as CURRENT.
- Select a second Action id here (dual-Next Reject).
- Recant the live door.
- Bind lab `127.0.0.1` as the public drop.
- Restart killed local `:8787` as the operating door.

Silence is never permission.
