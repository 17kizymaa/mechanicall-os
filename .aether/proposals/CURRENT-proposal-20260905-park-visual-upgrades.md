# CURRENT addendum — park visual upgrades (not apply)

**Not law.** Live `CURRENT.md` still says quieter phosphor + receipt printer **this halt**. Operator Round 18: park those. Models do not overwrite CURRENT.

## Park (following visual-upgrade Next, not `sit-dest-preview` Need)

| Park | Why |
|------|-----|
| **Phosphor colour / “quieter ink”** | Phosphor **is not a bug**. Keep `#E6C14A` hued CRT text **and** the millwork glass glow. Change colour only on an explicit later Next. |
| **Receipt printing + farm painting** | CRect isolation as a CRT-like dest was technically flawed (see below). Propose a well-local tongue later. Farm-paint plates then. |

## This halt (`sit-dest-preview`) if you apply the park

Keep: dest millwork (Draft+GATE blit, Send+FILES one dest, title dim, Decide well fill, rack GATE = ETA, one CRT dest many doors).  
CRT dest: **add composition**, keep FX + colour scheme. Not a phosphor recant. Not a receipt printer.

---

## Receipt printer — CRect proposal (following)

**Flaw in the earlier dest:** treating print as IsolatedMode like CRT (maximised module, chassis off, pack-full dest) fights the receipt **plate**. Print has to come **out of the well**, in **pack space**. `PACK_W×PACK_H` never grows. `sit_chassis` never scales (zoom-stretch Reject).

**Well-local model (proposal, not this Next):**

- Idle: RECEIPT bank, well millwork, mouth at `well.b`.
- Tongue `PackRect(mouth.l, well.b, mouth.r, well.b + feed)` clipped to `PACK_H`.
- When `tongue.b` would pass `PACK_H` (or mapped screen), **lock** the tongue rect; further scroll is `contentOffset` inside that rect.
- Dim chassis optional; do not IsolatedDark-replace the rack.
- Empty: mouth, no feed.
- New plates later: slot + tongue grain. Human PNG review.

Action id later: `sit-receipt-print` (or a visual-upgrade bundle). Not live Next.

---

APPLY park: human edits live CURRENT Keep/Limits (drop quieter-phosphor Need; drop printer Need; add composition Need; name printer as following). Then `aether next` / `approve` as you already intended.

Silence is not apply.
