# CURRENT proposal — 2026-09-05 look-closed loops

**Not law. Not Yes.** Live `CURRENT.md` stays Next `sit-dest-preview` **REJECTED** until a **human** applies this file.  
**APPLY: human must approve then merge — model must not overwrite CURRENT without human gate.**

Research: `dev/56_look-closed-loops/01_analyze/output/`.  
Handoff seed: `.continue-here.md` · `/tmp/mechanicall-handoff-first-sit-loops.md`.

Human rejects already on disk:

- 2026-09-05T15:04Z — next Next will be `first-sit-patching`, merge with what the executing Grok session did
- 2026-09-05T15:10Z — merge `sit-bound-draft` too, **want a new name altogether**

## Summary of proposed change

Do **not** continue `sit-dest-preview` as if APPROVED (preflight ALLOW on REJECTED is a protocol quirk, not permission).

Select **one** new Action id whose halt is **dest millwork look**, executed as a **look-closed loop** (PNG + FLAG, not 15/15). Keep `sit-bound-draft` Generate / OpenRouter / $10/w QC / visual field-diff as **following**. Merging Generate into the look Next repeats the failure: half the sitting becomes gate files.

Phosphor is not a bug. Receipt printer and raster CRT stay following visual upgrades.

## Name (human picks; not selected)

| id | Means | Risk |
|----|-------|------|
| `first-sit-look` | Beauty halt; Generate following | Recommended |
| `sit-look-closed` | Names the factory | Easy to confuse with a meta-Next |
| `sit-millwork-dests` | Dest leftover only | Narrow; ignores bound-folder identity |
| `first-sit-patching` | Operator’s first try | Sounds like grep-until-green |
| `sit-bound-look` | Bound folder + look, no Generate this halt | Fine if you want “bound” in the id |

## Proposed header (after human names the id)

```diff
- **Objective:** Dest millwork look on the sit before Generate/OpenRouter. …
- **Phase:** SELECT
- **Status:** REJECTED
- **Next:** sit-dest-preview
- **Approval:** REJECTED
+ **Objective:** Dest millwork look, look-closed. Halt is dest PNGs + FLAG, not 15/15. Generate/OpenRouter following.
+ **Phase:** SELECT
+ **Status:** PENDING
+ **Next:** first-sit-look
+ **Approval:** PENDING
```

Replace `first-sit-look` with whichever id you stamp.

## This Next (if applied)

1. Patch look factory **before** more dest paint: visual-reviewer Inputs → `SitRackView.kt` / nodpi; `look_verify.py` as second scorer; dest walker on storm-0 (never A33 as testers). Leave `app_verify.py` as law probes; recant B4/B11/B12 strings that fight dest Keep.
2. CRT dest composition around the tube (keep phosphor hue/glow/glitch). Truncated peek stays. No preview nav.
3. Draft dest: chassis millwork + `sit_gate` blit; recant ALPHA/BETA/GAMMA as the face. Generate on that GATE is **following**.
4. Send+FILES one dest; OEM FILES button; settings millwork not mini-terminal.
5. Title dim. Decide well millwork fill. Rack GATE = ETA.
6. SDK stills then A33 USB look. Confirm not tapped. Halt on FLAG, not on 15/15.

## Keep (carry)

- One Next; models never approve; silence ≠ permission
- Bind / Send / GATE / FILES / open are not Decide
- Phone bind ≠ this operator repo
- Host = SitRackView. Dest = isolated module millwork. Not Compose. Not LcdViewer
- Phosphor is not a bug (`#E6C14A` + millwork glow)
- USB ≠ LTE. Imagine = paint. Human PNG before freeze
- B1–B10 law behaviours

## Reject (add)

- `15/15-as-the-look`
- `launcher-png-as-sit-look`
- `seatnav-as-visual-reviewer-input` (files are deleted)
- `generate-this-halt` / `openrouter-this-halt` (still following)
- dual-concurrent-next (factory loops are **method**, not a second Next)

## Limits (add)

- Look-closed ICM stages are how this Next executes; they are not a second live Next
- `sit-bound-draft` (paper + GATE Generate + $10/w QC + visual field-diff) following
- Receipt printer / raster CRT following
- New plates still need human PNG review before freeze

## Fidelity checklist

- [x] Still **one** Next (new id; dest-preview not continued as APPROVED)
- [x] Models never approve / never auto-write CURRENT
- [x] No secrets in git / APK
- [x] Silence ≠ permission
- [x] Bind / Send / FILES / GATE / open are not Decide
- [x] Play production still Reject
- [x] Generate ≠ Yes; GET ≠ Yes
- [x] Dual-Next Reject (look factory is method; Generate following)
- [x] Vector RAG not in core

## Not this propose

- Implementing `look_verify.py` (that is `dev/56_look-closed-loops` 02/03 after you proceed)
- Sideload / testers / Funnel
- Recanting phosphor
- Copying `~/Desktop/.env`
