# Proposed CURRENT update (draft — not authority)

**Project:** mechanicall-os (operator tree)  
**Date:** 2026-08-17  
**Author:** pocket-face split (propose only)

`APPLY:` human only. Model must not overwrite this repo’s CURRENT.md.

## Summary

A new product Domain exists at `/home/anphuni/pocket-face` (`aether init` done; CURRENT still proposal-only). You asked to rebase the mobile sprint under another name and merge later.

**Do nothing to this file unless you want operator Next to stop being `mobile-planning-demo`.**

## Option A — leave law (default)

Keep **Next:** `mobile-planning-demo`. pocket-face is a sibling product; this tree still owns the demo spike until you move files.

## Option B — park the sprint here (you apply)

```markdown
**Objective:** Hold protocol + casual-core. App-projection product lives in `/home/anphuni/pocket-face`.
**Next:** casual-core-interface
**This Next:** not the Android app.
```

Then human: `aether approve "…"` and `aether next casual-core-interface` (or another id you choose).

## Fidelity

- Still one Next on *this* tree if you apply B.
- Does not create dual-concurrent-next unless you keep mobile-planning-demo **and** treat pocket-face implement as this repo’s job.
- Models never approve.

**Do not** run `aether approve` from a model.
