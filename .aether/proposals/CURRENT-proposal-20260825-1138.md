# CURRENT proposal — 2026-08-25T11:38Z

**Not law. Not Yes.** Live `CURRENT.md` stays until a **human** applies this file.  
**APPLY: human must approve then merge — model must not overwrite CURRENT without human gate.**

---

## Observations

- Live header: **Next `sit-distro-gate` APPROVED.** Objective is closed-tester Play-uploadable; Funnel Reject; dual-Next Reject.
- `dev/42` receipts: zip contain, stock-phone Publish, API-36 AAB **Done in-tree**. LTE **BLOCKED** (USB A33 ≠ LTE; this host `adb` empty). Play pack written; **upload human**. Production Reject.
- Two clocks: live CURRENT body still lists zip/Publish/API36 as actions 1–3.
- W-series grill (2026-08-24/25): W2=3 stopped the four-door recant (world-network UI website leftover). W7–W13 froze **conjure** (S3, wol-pi, sitter WAKE, HOLD 15 min, recant E3b later) and **dev mode** (`dev/` + `examples/` toys, no child CURRENT). Spec: `dev/43_journey-review/05_conjure-protocol/output/{CONJURE,DEV-MODE}.md`.
- Operator now: propose a new CURRENT; tackle Nexts **sequentially** in one chat, subagents where needed.

## Inferences

- Sequential ≠ dual-Next. One live action-id. After it closes, human `aether next` the following id.
- This chat cannot complete LTE without a cellular phone, or Play without Console login. Subagents cannot Yes, cannot fake USB-as-LTE, cannot apply CURRENT.
- Conjure is a **following** Next (`sit-conjure-desk`), not a second live Next, not Funnel.

## Unknowns

- Whether a cellular phone appears this session (gates LTE).
- Whether human will Console-upload after LTE.
- N=15 HOLD is skip-obvious; override if they say so when conjure becomes Next.

## Proposed change (summary)

1. **Keep Next `sit-distro-gate`.** Honest remainder only: LTE → human Play internal → Go-git last.  
2. **Move zip / native Publish / API-36 AAB to Done** (two-clocks fix).  
3. **Queue** `sit-conjure-desk` as the **following** action-id (human re-SELECT after this Next closes). Do not start it while `sit-distro-gate` is live.  
4. **Keep** W8/W13 dev mode: ICM `CONTEXT.md` under `dev/`; `examples/` sample CURRENTs are toys; subagents never `aether approve`; no child CURRENT as operator law.  
5. **Do not recant** Funnel, leftover-chrome-as-Next, P4b website, Play production, dual-Next.

## Fidelity checklist

- [x] Still **one** Next (`sit-distro-gate`)
- [x] Models never approve / never auto-write CURRENT
- [x] No secrets (no `.jks` / Play JSON / MAC / wake token in git)
- [x] Silence ≠ permission
- [x] Following Next is named, not live
- [x] Subagents = CONTEXT.md + `output/`, not child CURRENT

## Conflicts with existing authority

- Body actions 1–3 vs `dev/42` Done — this propose **aligns** CURRENT to receipts.  
- Queuing `sit-conjure-desk` does **not** recant `sit-distro-gate`. It is the W2=3 stop plus the frozen conjure spec as **later**.  
- E3b k3s-on-myarch recant is **inside** `sit-conjure-desk`, not this Next.  
- Live Status APPROVED on the same action-id: applying this body is an **amendment**. Human `aether approve` with a **real reason** (not `APPROVED: APPROVED`). Do not `aether next` until remainder closes.

## Before → after (header)

| Field | Live | Proposed |
|-------|------|----------|
| Objective | zip + Publish + API36 + LTE + Play + git | Closed-tester remainder (LTE → Play internal → Go-git). Following Next (not live): conjure desk. |
| Baseline | sit-testers / targetSdk 34 vs API-36 | 2026-08-25 · 1–3 Done in `dev/42` · LTE BLOCKED · Play pack ready · conjure spec frozen not live |
| Next | sit-distro-gate | sit-distro-gate *(unchanged id)* |
| This Next | six clauses including zip/Publish/API36 | **only** LTE, Play upload, Go-git |
| Done | testers as face | testers as face **plus** zip contain, native Publish, API-36 AAB |

## Human decision required

- [ ] Apply proposed `CURRENT.md` body below  
- [ ] Reject and leave live CURRENT unchanged  
- [ ] Revise (e.g. skip LTE, or make `sit-conjure-desk` live now — that is a different recant)

**Do not** run `aether approve` from a model. **Do not** start `sit-conjure-desk` until human `aether next sit-conjure-desk` after this Next closes.

---

## Proposed CURRENT.md (paste)

```markdown
# CURRENT

**Objective:** Close **honestly Play-uploadable** closed-tester distro: one LTE sideload of the signed API-36 AAB, human Play **internal/closed** upload, then Go-git. Not production. Not Funnel. After **this** Next closes, the following action-id is `sit-conjure-desk` (human re-SELECT) — not live now.
**Phase:** APPROVE
**Status:** DRAFT
**Baseline:** 2026-08-25 · `sit-distro-gate` 1–3 Done in-tree (`dev/42`) · LTE BLOCKED (USB ≠ LTE; adb empty on myarch) · Play pack ready, upload human · PR #7 open · drop :8765 down · conjure spec frozen at `dev/43_journey-review/05_conjure-protocol/output/CONJURE.md` (not law)
**Next:** sit-distro-gate
**Approval:** PENDING
**Host:** myarch · `personal-llm-sft-v4` · STORM home `~/.mechanicall/storm`

## Product (non-negotiable identity)
- **Is:** local-first authority protocol. The **phone sit** is the lab face of that protocol.
- **This Next:** (1) **one LTE sideload receipt** of the signed `0.17.1-api36` / release AAB (A33 USB via mbp-edge is **not** that receipt; lab `127.0.0.1` drop is **not**); (2) human Play **internal/closed** upload after (1) — Console is human; not production; not Funnel; (3) dirty git last (human Go-git).
- **Not core:** production Play listing, Funnel, public Ollama, SOS reskin, leftover chrome, notepad recant (C-series parked), mesh JNI as a store feature, dual-Next, model Yes, website listing (P4b), child CURRENT as operator law, k3s-as-Mechanicall-core.

## Done (keep)
- Claim-1 CERTIFIED · casual-core paused
- sit-join-workshop · sit-vst-headscale (JNI gap named)
- sit-storm-factory **as factory**
- sit-testers **as face:** first-sit template, Decide = Publish not hunk pick, current cream/LCD/GATE face, cellular-AVD off-LAN receipt, PR #7
- **sit-distro-gate 1–3:** `_unzip_to` containment + adversarial tests; Chaquopy-native Decide→Publish; `targetSdk`/`compileSdk` 36 + signed AAB off-git keystore + listing pack (`dev/42` receipts)
- SOS blit practice parked (P2b)
- P-series grill · `SHARING.md` · `/app-reviewer` · `/visual-reviewer`
- Phone file `13_FIRST_DISTRIBUTION_PR_GPT5.6.md` (GPT-5.6 distro bar)
- W-series: conjure protocol **specified not live** (S3, wol-pi doorbell, sitter WAKE, HOLD 15 min, E3b recant later)

## Keep
- One Next; models never approve; silence ≠ permission
- GET / Bind / Send / JOIN / WAKE / LOAD / STREAM / twin-pull / storm-up / inbox-offer / inbox-accept / FILES-open are not Decide
- Phone bind ≠ this repo
- `tests/run.sh` as CI — plus Android assemble
- STORM cap 2; never A33 as a STORM serial (USB-read of `/sdcard/mechanicall/sessions/` is allowed)
- Parked sit code stays
- G4 mesh JNI remains a named gap — do not list LTE folder-send as a store feature
- Lab drop binds `127.0.0.1` only; unauthenticated; last-offer slot
- **Following Next (not live):** `sit-conjure-desk` — myarch S3 in working hours; wol-pi always-on tailnet doorbell; JOIN=connected sitter may WAKE; Send does not imply WAKE; HOLD 15 min then S3 unless local session; Funnel never; recant E3b (k3s off myarch) **as part of that Next**; spec `CONJURE.md`. Human `aether next sit-conjure-desk` only after this Next closes.
- **Dev mode:** aggressive mechanicall-os work stays **under this folder** (`dev/` CONTEXT.md + `output/`; `examples/` sample CURRENTs are toys, never live Next). Subagents never `aether approve` / `next` / rewrite CURRENT. No child CURRENT as operator session authority.

## Reject
- dual-concurrent-next
- leftover-chrome-as-the-Next
- SOS-reskin-as-this-Next
- storm-review-as-the-product
- Decide hunk-picker as first-sit
- model-auto-approve / model-auto-write-current
- EdubaWare-clone / k3s-as-Mechanicall-core
- Funnel / public Ollama / public wake
- Play **production** listing this Next
- Sharing RZCW2038KHN as a STORM serial
- Twin that writes CURRENT
- Headscale-as-folder-store / auto-install inbound app
- Camel Audio bitmap extraction
- Pretend JOIN connected without tsnet Up
- Restart drop / call sharing “online” while `:8765` is down or zip extraction is uncontained
- Upload PR #7 as-is to Play
- Start `sit-conjure-desk` while `sit-distro-gate` is live
- Child CURRENT.md for subagents as operator law
- USB-on-mbp-edge as LTE receipt

## Limits
- Closed testers — not release
- No secrets in git (no `.jks` / Play JSON key / wol-pi MAC / wake token)
- Do not rewrite a bound client CURRENT from this tree
- Grouped git exclude live CURRENT unless human Go-git
- STORM is developer infrastructure, not a second product
- Website production listing parked (P4b)
- SOS/VSTGUI blit parked (P2b)
- Play API-36: new apps and updates from **2026-08-31** must target API 36 (extension to Nov 1 may be requested in Console — human)
- `MANAGE_EXTERNAL_STORAGE` / cleartext traffic are Console declarations, not silent policy passes
- Off-LAN LTE receipt **before** Console upload
- Distro bar is the nine gates in `.aether/proposals/13_FIRST_DISTRIBUTION_PR_GPT5.6.md`
- This host may have empty `adb`; LTE stays BLOCKED until a **cellular** phone exists — do not skip to Play or to conjure
- Sequential chat: one action-id at a time; ICM stages + subagents under **this** Next only (CONTEXT.md, halt at `output/`)

## Next allowed action
**Action id:** `sit-distro-gate`

1. One LTE sideload receipt of the signed API-36 artifact (not USB-on-mbp-edge, not `127.0.0.1` drop). If no cellular device: write/keep BLOCKED; do not fake it.
2. Human Play **internal** (closed if the list needs it). Not production. Not Funnel. Agent does not Console-login.
3. Dirty tree last (human Go-git).

Then **stop**. Human re-SELECT `sit-conjure-desk` if they want the desk protocol next.

## Approval condition
Human applies this file, then `aether approve` with a **real reason** (amendment: 1–3 Done; remainder LTE/Play/git; conjure queued not live). Do not `aether next` until this remainder closes.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- play-production-this-next
- funnel-this-next
- model-auto-write-current
- share-a33-across-storm
- bind-phone-to-operator-tree
- camel-png-theft
- leftover-compose-millwork-as-sos
- sos-reskin-this-next
- restart-uncontained-drop
- play-upload-pr7-as-is
- conjure-desk-this-next
- child-current-as-operator-law
```
