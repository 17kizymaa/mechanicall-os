# CURRENT

**Objective:** Phone sit is the control surface for a **bound project folder** (not this operator tree). One SitRackView chassis; millwork stays the instrument. Isolated Draft dest = chassis millwork + GATE; whole-file PROPOSE paper; Generate writes schema into that file only. Isolated CRT dest = darkened-room composed tube. Send overlay = simplified-chassis settings panel (not Yes, not FILES). Play Store of **this sit** is a following Next. Not a One Next stranger shell this halt.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-09-05 · human APPROVED `one-next-contract-and-archive-freeze` then grilled recant store-first · dest look reject 2026-09-04 (`sit-glass-polish`) · freeze `0.19.0-sit-rack` vc 25 · lab `0.19.3-glass-polish` vc 28 on A33 · sft-v4 SERVE · thread 15 ingested
**Next:** sit-bound-draft
**Approval:** PENDING
**Host:** myarch · A33 lab USB via mbp-edge (USB ≠ LTE)

## Product (non-negotiable identity)
- **Is:** local-first authority protocol. Inference proposes. Authority disposes. You decide. AI does the point-and-clicking — under a plan you can `cat` and a yes only you can give. The **phone sit** is the lab face of that protocol, not a second product.
- **This Next:** `sit-bound-draft` — bind their folder; millwork dests; Draft paper + GATE Generate → schema **merged** into `PROPOSE-CURRENT.md`; diffs they can `git diff`; CURRENT unchanged until Decide. Device look + `app_verify.py`. Not SDK-as-testers. Not Play.
- **Not core:** EdubaWare-clone, Play production this halt, Funnel, dual-Next, model Yes, MCP-as-core, LcdViewer overlay host, Compose millwork, IsolatedDark debug dests, vector RAG in core.

## Done (keep)
- Zip contain · native `yes()` · API 36 pack (not distribution)
- SitRackView host · freeze millwork plates · leftover Compose millwork deleted
- BEHAVIOURS B1–B10 · B15 · `python/app_verify.py`
- Thread 15 doctrine (copy, not SPEC)
- `personal-llm-sft-v4` pocket default; propose/taste only
- `write_schema_draft` / `list_hunks` / GATE STREAM / `_refuse_public_ollama`
- `aether_llm.py` OpenRouter from env
- Logo void filled (not yet on device)
- Listing-bar-pin: speech ≠ listing

## Keep
- One Next; models never approve; silence ≠ permission
- Bind / Send / JOIN / WAKE / LOAD / STREAM / GATE / FILES / opening the app are **not** Decide
- Phone bind ≠ this operator repo
- Freeze host = **SitRackView**. Dest = isolated **module millwork**, same host. Not Compose. Not LcdViewer.
- Walk: Bind → Plan → Draft → Decide+Why → Receipt
- Draft dest: chassis millwork + `sit_gate` at the rack CRect. No darkened room on Draft (that is CRT only).
- CRT dest: darkened-room terminal; composed `sit_crt` phosphor (not debug dump)
- Send overlay dest: simplified-chassis **settings** look. Not a terminal. Not Yes. Not FILES
- Generate = Gate on Draft dest. Type prose first. Empty tap does not fire. Fail-closed if desk down. Paper still works
- Generate writes **schema fields into PROPOSE** (Objective / Next / Keep / Reject / Limits). **User prose stays.** Wrapper must **merge**, not replace (`write_schema_draft` replace is recanted for this path)
- Folder-walk RAG: bound CURRENT + PROPOSE excerpts + receipt names into the prompt, token-capped. Retrieved text is evidence, not law. **No chroma / vector DB in core**
- Few-shot (3–8) prose→schema examples in the call. No retrain
- OpenRouter QC (desk-only, **$10/week spend cap**): second pass on the schema block; may revise PROPOSE schema only; `qc skipped` if down or cap hit; **key never in git or APK** (`~/Desktop/.env` / gitignored `.env`)
- Diffs **this halt:** visual live-vs-proposed fields on the Draft dest (thread 15 Draft: fluency vs stamp). Unified-diff appendix and hunk millwork **following**
- Personal LLM = propose/taste only. sft-v4. Never approve
- FILES = system manager. USB-on-mbp-edge is not LTE
- Imagine = paint, not spec
- ICM `dev/` = factory. This CURRENT = gate
- Behaviours (Need / Not / Evidence) — B11–B13 **rewritten** vs 0.19.3 IsolatedDark:

| id | Need | Not | Evidence |
|----|------|-----|----------|
| B1 bind-first | Unbound: FOLDER only | Auto-bind mechanicall-os | source + look |
| B2 plan-readable | PLAN millwork + STATUS glass | Isolated dest as Plan | uidump + look |
| B3–B5 | Draft edits PROPOSE only; prose stays; schema merge | Draft writes CURRENT; replace-paper | source + tests |
| B6 decide-only-yes | Two-tap + Why | First tap Yes; Send/GATE as Yes; hunk pick on Decide | source |
| B7 gate-instrument | LOAD then STREAM; Gate on Draft dest | Send labelled GATE; IsolatedDark dest without gate plate | source + look |
| B8 / B15 | Receipt is what they said; empty is empty | Fake agree | uidump + look |
| B9 window-honest | `resizeableActivity`; no painted letterbox host | 92% card | source |
| B10 bind-not-repo | Phone bind ≠ this tree | Binding mechanicall-os | tests |
| B11 crt-dest | Darkened-room composed `sit_crt` millwork dest | IsolatedDark debug type; zoom-stretch chassis | look PNG |
| B12 send-overlay | Settings / simplified-chassis overlay | Send is Yes; Send = FILES; terminal dest | uidump + CURRENT bytes |
| B13 draft-dest | Chassis millwork + GATE + whole-file paper + Generate→schema merge + visual live-vs-proposed fields | Chat bubbles; IsolatedDark; ALPHA/BETA/GAMMA keys; Draft writes CURRENT | uidump + tests |
| B14 send-folder-smoke | Overlay opens, offer/staging honest, dismiss → rack | Smoke as Play/LTE | script + look |

## Reject
- dual-concurrent-next
- leftover-compose-millwork-as-sos
- LcdViewer/SkinLayout overlay host returning as millwork
- isolated-dark-debug-as-product
- millwork-on-black-as-the-only-polish
- zoom-stretch-chassis-as-terminal
- Imagine-as-spec
- Camel PNG theft
- model-auto-approve / model-auto-write-current
- generate-writes-current
- get-as-generate
- empty-tap-as-generate
- chat-app-as-draft
- github-ci-as-phone-loop
- write-schema-draft-replace-prose (this path)
- vector-rag-in-core / chroma-as-sit-memory
- openrouter-key-in-apk
- public-ollama / Funnel
- retrain-sft-v5-this-halt
- proposal-as-CURRENT shipped in a PR
- events.jsonl stamped `by:human` as judgment
- clone-edubaware-as-core
- MCP tools named `approve` / `next`
- Play **production** listing this Next
- speech-tease-as-listing-go
- USB look sold as testers/LTE receipt
- bind-phone-to-operator-tree
- child-current-as-operator-law
- recant-sit-for-essay
- recant-icm-factory
- sdk-preview-as-evidence
- send-is-decide
- files-equals-send
- hunk-accept-is-decide

## Limits
- Closed testers / lab sideload — not release. Play of **this millwork sit** is a following Next. Listing-bar holds (UI executed + Headscale operable + **idkr unnamed**; models do not fill item 4)
- No secrets in git. OpenRouter key = `~/Desktop/.env` (already loaded) or gitignored `.env`. **Cost-cap $10/week** (`OPENROUTER_WEEKLY_BUDGET_USD`). If OpenRouter/Ollama down or cap hit: honest fail-closed / `qc skipped`; paper still works. Do not copy Desktop `.env` into the tree.
- Folder-walk token cap. Do not dump whole CURRENT into the 7B prompt
- P1 chassis-preview CRT roughness, P2 anphuni.com gauntlet, P3 Headscale/zip/Eduware-look, P4 extra local-model deploy, P5 title-unbind animation, P6 richer secret redaction — **following**, not this halt
- Hunk millwork Suggesting (S5c) — following
- Dictation — following
- Testers without desk: paper + fail-closed Generate. Lab receipt is STREAM + merge on myarch + A33 USB look
- Dirty tree: do not open as a PR that ships this file as live CURRENT
- C17b=1 folder-walk; C17c=1 two-pass + $10/w cap; C18b=1 visual field-diff for now. QC model slug via `OPENROUTER_QC_MODEL=` (operator names it).

## Next allowed action
**Action id:** `sit-bound-draft`

1. Recant IsolatedDark on Draft dest; blit millwork + `sit_gate`; Gate hit = Generate.
2. Whole-file PROPOSE paper on that dest. Merge-schema wrapper (prose stays). Few-shot + folder-walk + sft-v4. Unified-diff appendix. Fail-closed.
3. CRT dest composed typeset. Send overlay settings look.
4. `app_verify` B1–B15 as rewritten. Device look USB. USB ≠ LTE. Halt.

## Approval condition
Human copies this file onto live Domain authority (or `aether next sit-bound-draft` / approve with a real reason) after numbering or accepting C17b/C17c/C18b. Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- play-production-this-next
- funnel-this-next
- model-auto-write-current
- chroma-in-sit-core
