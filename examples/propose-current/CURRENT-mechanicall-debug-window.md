# CURRENT

**Objective:** One sit: a **windowed Mechanicall debug plugin** on the phone, desk traffic through **myarch** local model with a visible GATE (thinking), and **STORM** on isolated emulators. Same law: pocket folder, human Decide. Not Play Store. Not four Nexts.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-20 · people-app-phone-seat REJECTED (painted VST, desk hop unproven, no STORM AVD)
**Next:** mechanicall-debug-window
**Approval:** PENDING
**Host:** myarch · anyone can boot this desk to run `personal-llm-sft-v4`

## Product (non-negotiable identity)
- **Is:** local-first authority protocol.
- **This Next:** one action-id that includes **all** of: (1) **desk-hop** — boot myarch, Ollama private, GATE shows the model thinking (not chat typing dots); (2) **storm-install** — emulator + API-34 `google_apis` x86_64, isolated pockets, never share the A33; (3) **actual windowed** Camel Space–vibes face — yellow + purple, compact bevels, debugging instrument, Bind as DIR slot, Decide the only gate; (4) **simplified mark** (slab + disc).
- **Not core:** Play Store, Funnel, public Ollama, dual-concurrent-next, a maximised kiosk, a fake 92% card calling itself a window.

## Done (keep)
- Claim-1 CERTIFIED · Claim-2 parked · casual-core paused
- People-app factory + SeatMate lab in `~/people-app-stranger-sit`
- Phone APK lab exists (`0.8.0-vst` on A33) — chrome rejected as image; engine (slim chat, walk, bind-outside-pages) is reusable
- STORM-ENV research: KVM present; missing emulator package + x86_64 system image

## Keep
- One Next; models never approve; silence ≠ permission
- GET / open / Bind / Send are not the Decide
- SeatMate `confirm h-…` is a booking; CURRENT Yes is a different tap
- Phone bind ≠ this repo
- `tests/run.sh` as deterministic CI

## Reject
- dual-concurrent-next
- model-auto-approve / model-auto-write-current
- Play Store this Next
- Funnel / public Ollama (`0.0.0.0`)
- Chat-app chrome as the product (composer+bubbles as identity)
- Painted maximise / fake window
- Sharing RZCW2038KHN across STORM agents
- Splitting desk-hop / storm-install / window into extra Nexts

## Limits
- Demo seat only
- No secrets in git
- Do not rewrite bind CURRENT from this tree
- STORM cap: **2** AVDs on myarch (32 GiB, 0 swap)
- GATE is a debug instrument (step LEDs / scope). Decide stays two-tap + Why.

## Next allowed action
**Action id:** `mechanicall-debug-window`

1. **Desk-hop.** Script anyone can run on myarch: start Ollama if needed, load `personal-llm-sft-v4`, print private URLs (127.0.0.1 / LAN / Tailscale). Wire A33 (or AVD) through USB reverse. Face shows DESK idle / warming / **GATE** (model thinking via streamed generate) / quiet. Not typing dots. Prove one Send returns a short `say`.
2. **Storm-install.** `sdkmanager` `emulator` + `system-images;android-34;google_apis;x86_64`. One AVD `storm-0` (serial `emulator-5554`), isolated `/tmp/storm-env/storm-0/pocket` from `examples/pocket-demo-client`. Walker dumps uidump; never Confirm; never the A33.
3. **Window.** Actual windowed mode (`resizeableActivity`, compact plugin frame that does **not** fill the display). Camel Space compact bevels. Yellow LCD + purple GATE. Pages: Plan, Draft, Decide, Receipt. Bind = DIR slot. Simplified launcher: slab + disc only. Sideload. Opening is not Decide.

## Approval condition
Human `aether approve` with a real reason when this **one** windowed debug-plugin Next is the Next they want.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-approve-session
- model-auto-write-current
- uncapped-registration
- send-email-for-real
- host-smtp
- live-graph-oauth
- reopen-public-pipeline-register
- claim-session-is-core-product
- claim-conversation-auto-sync
- claim-personal-model-is-security-jail
