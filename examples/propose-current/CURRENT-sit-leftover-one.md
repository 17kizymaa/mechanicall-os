# CURRENT

**Objective:** Original sit APK stays **central**. One Next for execute leftover: idle/quiet GATE lamps dark, DESK compressed so paper is the score, API-34 bind without hidden appops, tsnet JNI if the factory can build else a named gap. Re-walk storm-0. Never Confirm. Never A33.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-21 · sit-storm-factory execute 10/10 glass · leftover was four follow-on folders — **one Next**
**Next:** sit-leftover-one
**Approval:** PENDING
**Host:** myarch · `personal-llm-sft-v4` · STORM home `~/.mechanicall/storm`

## Product (non-negotiable identity)
- **Is:** local-first authority protocol. The **phone sit** is the lab face of that protocol.
- **This Next:** (1) GATE idle/quiet/queued lamps **0/16 dark** per GATE-CONTRACT (not PurpleDim at rest); (2) compress DESK strip (JOIN/WAKE/offer) so PLAN/DRAFT paper is the majority of the activity; (3) API-34 bind is SAF “Choose your folder” (or app-files dir) — `MANAGE_EXTERNAL_STORAGE` appops is lab glue, not product; (4) tsnet JNI via `scripts/build-tsnet-aar.sh` — if gomobile is missing, JOIN stays not-on-net and the gap stays named; (5) one sideload + storm-walk on **emulator-5554** only + `/app-reviewer` + `/visual-reviewer`; (6) dirty tree last (human Go-git).
- **Not core:** EdubaWare, k3s-as-product, Play Store, Funnel, public Ollama, dual-Next, model Yes, bind-phone-to-this-repo, four concurrent leftover Nexts.

## Done (keep)
- Claim-1 CERTIFIED · casual-core paused
- sit-join-workshop paper + JOIN fingerprint + WAKE + hunk ICM
- sit-vst-headscale 01–04 receipts (JNI gap named)
- sit-storm-factory: STORM factory in-tree; storm-0 glass 10/10; FILES = OEM file manager; inbox depreciated local offer
- `SHARING.md` read-twin · `/app-reviewer` · `/visual-reviewer`
- Grill E-series recanted in `decision-tree.md`

## Keep
- One Next; models never approve; silence ≠ permission
- GET / Bind / Send / JOIN / WAKE / LOAD / STREAM / twin-pull / storm-up / inbox-offer / inbox-accept / FILES-open are not Decide
- Phone bind ≠ this repo
- `tests/run.sh` as CI
- STORM cap 2; pause AVDs when the 7B is hot
- Parked sit code stays
- ICM numbered folders under this action-id are **sequencing**, not extra Nexts

## Reject
- dual-concurrent-next
- leftover-as-four-nexts (idle-lamps / desk-compress / tsnet-jni / storage-bind as separate action-ids)
- model-auto-approve / model-auto-write-current
- EdubaWare-clone / k3s-as-Mechanicall-core
- Play Store / Funnel / public Ollama / public wake
- Sharing RZCW2038KHN
- Twin that writes CURRENT
- Headscale-as-folder-store / auto-install inbound app
- Pretend JOIN connected without tsnet Up

## Limits
- Demo seat only
- No secrets in git
- Do not rewrite a bound client CURRENT from this tree
- Grouped git exclude live CURRENT unless human Go-git
- STORM is developer infrastructure persisted **as code**, not as a second product
- gomobile missing → named gap, not a silent connected JOIN

## Next allowed action
**Action id:** `sit-leftover-one`

1. `GateStrip`: idle / quiet / queued → unlit (`LcdBg`), not `PurpleDim`. LOAD stays amber fill. STREAM stays purple steps.
2. Compact `DeskStrip` so the cream paper is the majority of the activity (PLAN CURRENT + DRAFT hunks visible without the strip eating the score).
3. Bind: keep “Choose your folder” SAF; do not ship lab `appops MANAGE_EXTERNAL_STORAGE` as the sitting path. Refuse operator tree. T8c: CURRENT.md still not required to bind.
4. `scripts/build-tsnet-aar.sh`. If no gomobile: RECEIPT names the gap; `tsnet_up()` stays false.
5. Sideload onto **emulator-5554** only. `storm-walk` + `app_verify` + visual. Never Confirm. Never A33.
6. Dirty tree last (human Go-git).

Internal ICM (same Next, not four Nexts): `dev/40_sit-leftover-one/01_implement` then `02_verify`.

## Approval condition
Human applies this file, then `aether next sit-leftover-one` and `aether approve` with a real reason.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- leftover-as-four-nexts
- model-auto-write-current
- clone-edubaware-as-core
- share-a33-across-storm
- bind-phone-to-operator-tree
- fake-join-without-tsnet
