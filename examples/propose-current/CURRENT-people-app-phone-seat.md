# CURRENT

**Objective:** Make a **phone seat** so people-app + SeatMate (`mate/`) can be sat on a device that is not this desk. Same law: pocket folder, human Yes. Not Play Store. Not Compose this Next.
**Phase:** SELECT
**Status:** ACTIVE
**Baseline:** 2026-08-18 · operator rejected people-app-validation (need phone seat for mate/) · bind walk already Yes
**Next:** people-app-phone-seat
**Approval:** PENDING
**Host:** myarch · phone is the seat, not a second CURRENT

## Product (non-negotiable identity)
- **Is:** local-first authority protocol.
- **This Next:** sideload **APK** chrome (human 2026-08-18: “APK will work”). Same `aether_pocket` / SeatMate gate. Bind a folder that is not mechanicall-os. URL / pocket-face is the sibling idea — keep it, do not start it as a second Next.
- **Not core:** Play Store, Compose, Funnel, operator-tree bind.

## Done (keep)
- Claim-1 CERTIFIED · Claim-2 parked · casual-core paused
- People-app factory + SeatMate lab in `~/people-app-stranger-sit`
- Operator **rejected** desktop-only validation: *actual phone seat or mate/ will not work*

## Keep
- One Next; models never approve; silence ≠ Yes
- GET / open app is not Yes
- SeatMate `confirm h-…` is a booking; CURRENT Yes is a different tap
- Phone bind ≠ this repo

## Reject
- dual-concurrent-next
- model-auto-approve / model-auto-write-current
- Play Store this Next
- Funnel / public Ollama
- Asking them to log in as the operator email
- Pretending the APK is assembled if `gradlew` is missing

## Limits
- Demo seat only
- URL face first if no SDK; APK second on a machine with SDK
- No secrets in git
- Do not rewrite bind CURRENT from this tree

## Next allowed action
**Action id:** `people-app-phone-seat`

1. On a machine **with** JDK 17 + Android SDK 34 + `gradlew`: assemble debug APK (`android/README.md`). This Nix host had **no** wrapper/APK last check.
2. Sideload. Bind a pocket folder (not mechanicall-os). Opening the app is not Yes.
3. Receipt on that pocket. URL/pocket-face stays lab for the next agent if APK chrome is not enough. Not Play Store.

## Approval condition
Human `aether approve` with a real reason when a phone seat is the Next they want.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-approve-session
- model-auto-write-current
- uncapped-registration
- send-email-for-real
- host-smtp
- live-graph-oauth
- reopen-public-pipeline-register
- claim-session-is-core-product
- claim-conversation-auto-sync
- claim-personal-model-is-security-jail
