# CURRENT

**Objective:** Original sit APK is **central**. Cut the EdubaWare/k3s platform clone. Persist **STORM** as an in-tree application component: isolated emulator review environments for chat-session subagents (never Confirm, never A33). Sit leftover (tsnet JNI, GATE, twin) stays the product; dirty git last.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-21 · sit-vst-headscale REJECTED · E-series recanted (EdubaWare cut; original app central)
**Next:** sit-storm-factory
**Approval:** PENDING
**Host:** myarch · `personal-llm-sft-v4` · STORM home `~/.mechanicall/storm`

## Product (non-negotiable identity)
- **Is:** local-first authority protocol. The **phone sit** is the lab face of that protocol.
- **This Next:** (1) STORM factory in-tree (`python/aether_storm.py`, `scripts/storm-sdk-setup.sh`, `scripts/storm-up.sh`, `/storm-review`); (2) emulator = mass-app **subagent review env**, not a sitter product; (3) original APK remains central (paper workshop, JOIN, GATE, twin); (4) **cut** EdubaWare platform / private k3s clone; (5) Headscale is **mesh only** — depreciated folder send is local `.inbox/` (gitignored), AirDrop-shaped Accept/Decline, never writes CURRENT; FILES opens the **system file manager**; (6) dirty tree only after human **Go-git**.
- **Not core:** EdubaWare, k3s-as-product, Play Store, Funnel, public Ollama, dual-Next, model Yes, bind-phone-to-this-repo.

## Done (keep)
- Claim-1 CERTIFIED · casual-core paused
- sit-join-workshop paper + JOIN fingerprint + WAKE + hunk ICM
- sit-vst-headscale 01–04 receipts (JNI gap named)
- `SHARING.md` read-twin · `/app-reviewer` · `/visual-reviewer`
- Grill E-series recanted in `decision-tree.md`

## Keep
- One Next; models never approve; silence ≠ permission
- GET / Bind / Send / JOIN / WAKE / LOAD / STREAM / twin-pull / storm-up / inbox-offer / inbox-accept / FILES-open are not Decide
- Phone bind ≠ this repo
- `tests/run.sh` as CI
- STORM cap 2; pause AVDs when the 7B is hot
- Parked sit code stays

## Reject
- dual-concurrent-next
- model-auto-approve / model-auto-write-current
- EdubaWare-clone / k3s-as-Mechanicall-core
- Play Store / Funnel / public Ollama / public wake
- Sharing RZCW2038KHN
- Twin that writes CURRENT
- Headscale-as-folder-store / auto-install inbound app

## Limits
- Demo seat only
- No secrets in git
- Do not rewrite a bound client CURRENT from this tree
- Grouped git exclude live CURRENT unless human Go-git
- STORM is developer infrastructure persisted **as code**, not as a second product

## Next allowed action
**Action id:** `sit-storm-factory`

1. Keep `python/aether_storm.py` + scripts as the factory. Create `storm-0` AVD under `~/.mechanicall/storm` (not git).
2. Swarm review of the **original APK** via that env + `/app-reviewer` + `/visual-reviewer`. Never Confirm. Never A33.
3. Do not install k3s. Do not clone EdubaWare.
4. Inbox: `python/aether_inbox.py` + `.inbox/` gitignored. Notify + Accept/Decline. Skip live CURRENT on Accept. FILES → device file manager.
5. Dirty tree last (human Go-git).

## Approval condition
Human applies this file, then `aether next sit-storm-factory` and `aether approve` with a real reason.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-write-current
- clone-edubaware-as-core
- share-a33-across-storm
- bind-phone-to-operator-tree
