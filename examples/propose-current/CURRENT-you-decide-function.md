# CURRENT

**Objective:** **Mechanicall: You Decide** — the phone sit is the product. Bound project folder (not this operator tree). Functional Draft editor. Send-folder offer. Headscale backend up. You stamp. AI proposes. Millwork look following. One Next is the law of that folder, not a second Play APK.
**Phase:** SELECT
**Status:** PENDING
**Baseline:** 2026-09-06 · live was `you-decide-sit` APPROVED · gauntlet 11/11 storm-0 · lab `0.19.9-you-decide` vc 34 · look L6 FLAG parked · `sit-vst-headscale` REJECTED (JNI gap named) · `BEHAVIOURS-ONE-NEXT.md` parked stranger-shell, not this face
**Next:** you-decide-function
**Approval:** PENDING
**Host:** myarch · A33 lab USB via mbp-edge (USB ≠ LTE) · STORM storm-0 for function gauntlet (never A33 as testers) · Headscale login-server on the operator box (not in git)

## Product (non-negotiable identity)
- **Is:** **Mechanicall: You Decide.** Local-first authority protocol. Inference proposes. Authority disposes. You decide. AI does the point-and-clicking — under a plan you can `cat` and a yes only you can give. The **phone sit** is that product’s lab face, not a second product and not an archive.
- **One Next:** one authorized action in the bound folder. Not a store title. Not a quiet-home stranger shell this halt.
- **This Next:** `you-decide-function` — three gates of one action: (1) Draft dest is a working schema editor of `PROPOSE-CURRENT.md`; (2) Send+FILES dest actually stages/offers a contained folder (not Yes); (3) Headscale backend is up (login-server + `tag:stranger` jail). tsnet userspace is the client of that backend; JOIN stays invited until the AAR is real. Factory: extend `you_decide_gauntlet.py`; first FAIL is the next patch. Halt on operable, not on look FLAG. Not Play. Not Generate. Not archive.
- **Not core / not this halt:** EdubaWare-clone, Play production, Funnel, dual-Next, model Yes, LcdViewer, Compose millwork, IsolatedDark debug dests, chroma in core, `BEHAVIOURS-ONE-NEXT` as the sit, new `applicationId`, VST-frame revival, GATE Generate.

## Done (keep)
- SitRackView host · freeze millwork plates · leftover Compose deleted
- Chassis peek + dest reader started · Draft dest chassis+gate · Send+FILES dest · title dim · Decide well fill
- BEHAVIOURS B1–B15 law probes · `app_verify` is **law**, not the look
- Look-closed research `dev/56_look-closed-loops/` · first-sit-look factory `dev/57_first-sit-look/`
- Functional gauntlet `python/you_decide_gauntlet.py` · plate-cycle look loop
- `write_schema_draft` / `stage_upload` / `send_project` / `accept_offer` in Python (Send dest not wired; tsnet AAR named gap)
- C-series: bound-folder sit; Play following; Generate/QC following

## Keep
- You Decide; models never approve; silence ≠ permission
- Bind / Send / GATE / FILES / JOIN / WAKE / open are **not** Decide
- Phone bind ≠ this operator repo
- Host = SitRackView. Dest = isolated module millwork, same view
- Walk: Bind → Plan → Draft → Decide+Why → Receipt
- Phosphor is not a bug. CRT dest = darkened-room `sit_crt`; peek truncated; no preview nav
- Draft dest: schema editor of PROPOSE (Objective / Next / Keep / Reject / Limits). Keyboard over chassis. Generate **following**
- Send+FILES one dest; OEM FILES button; Send not Yes; Send **does** stage
- Headscale is mesh, not a folder store. Offer/Accept never writes CURRENT
- Rack GATE = ETA only this halt (Generate following)
- Title kept, dimmed; U17 not a control
- Decide well millwork fill; still a well, not a dest
- Look FLAG is millwork method, not this halt. USB ≠ LTE. Imagine = paint, not spec
- Gauntlet FAIL → one patch → gauntlet again. 11/11 is not Yes

## Reject
- dual-concurrent-next
- archive-you-decide-this-halt
- ship-one-next-stranger-shell-this-halt
- ship-sit-rack-as-store-v1
- BEHAVIOURS-ONE-NEXT-as-sit-face
- leftover-compose / LcdViewer host
- isolated-dark-debug-as-product
- phosphor-as-bug
- 15/15-as-the-look
- generate-this-halt / Funnel / model-Yes
- Play production this Next
- USB look sold as testers/LTE
- bind-phone-to-operator-tree
- revive-sit-vst-headscale-bundle
- ui-and-headscale-as-beauty-next
- send-is-decide / files-equals-send
- join-connected-without-tsnet
- headscale-as-folder-store
- bake-preauth-into-git

## Limits
- Closed testers / lab sideload — not release
- No secrets in git. Preauth inject from `~/.mechanicall/headscale-preauth` (or sibling of the Play keystore). ControlURL may be baked
- `sit-bound-draft` (paper + GATE Generate + $10/w QC + visual field-diff) following
- Draft dest millwork / CRT animation / receipt printer / raster CRT — following visual
- Play / listing-bar / `idkr` unnamed — following; models do not fill
- `BEHAVIOURS-ONE-NEXT.md` parked for a later stranger-shell Next
- SPEC-v0.2 zero-daemon happy path is the **bound folder**. Headscale is operator-box infrastructure
- Quiet drop / down Headscale: Send stages locally and says so. Draft still works
- tsnet AAR may lag the backend halt; do not fake `connected`
- Doorbell / WAKE wol-pi / Funnel not this halt

## Next allowed action
**Action id:** `you-decide-function`

1. Draft dest is a working schema editor: type, persist `PROPOSE-CURRENT.md`, CURRENT unchanged, relaunch shows the type. IME over chassis. GATE ≠ Generate this halt.
2. Send+FILES dest: Send stages/offers the bound folder (contained zip + local stage; POST if drop up; mesh carry if JOIN connected). Honest status. FILES = OEM. Not Yes.
3. Headscale backend up: login-server running, ACL `tag:stranger`, ephemeral preauth, health receipt you can `cat`. Secrets never in git.
4. tsnet AAR after the backend: userspace JOIN may become `connected`. Without the AAR, JOIN stays invited.
5. Extend the functional gauntlet (typed PROPOSE survives relaunch; Send stages; Headscale health). First FAIL is the next patch. storm-0. Confirm not tapped. A33 USB look after the gauntlet is boring.

## Approval condition
Human copies this file onto live Domain authority, then `aether next you-decide-function` / `aether approve` with a real reason. Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- play-production-this-next
- funnel-this-next
- model-auto-write-current
- archive-you-decide-this-halt
- bake-preauth-into-git
- join-connected-without-tsnet
