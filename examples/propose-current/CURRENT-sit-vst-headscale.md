# CURRENT

**Objective:** One sit: genuine VST/Camel Space **frame** around a paper document workshop; stranger joins via **Headscale preauth key** (userspace in the APK); GATE shows **LOAD then STREAM**; desk is one niced 7B generate (LOAD may follow WAKE); STORM iterates on storm-0 with `/visual-reviewer`; operator **read-twin** of the pocket from mechanicall-os. Same law: their folder, human Decide. Not Play Store. Not four Nexts.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-20 · sit-join-workshop (02 paper+JOIN/WAKE/hunk ICM; tsnet JNI gap; 03 STORM not started)
**Next:** sit-vst-headscale
**Approval:** PENDING
**Host:** myarch · `personal-llm-sft-v4` (7B + LoRA) · Headscale login-server (wol-pi or always-on HTTPS)

## Product (non-negotiable identity)
- **Is:** local-first authority protocol.
- **This Next:** one action-id that includes **all** of: (1) **genuine VST frame** (real Camel Space/VST references, compact plugin, `resizeableActivity`, no painted letterbox) with **paper Suggesting inside**; (2) **Headscale** control plane + **preauth keys** in JOIN paste; userspace embed; `tag:stranger`; not OIDC; not `17kizymaa@`; (3) **GATE two-phase** — LOAD (boot/weights, timeout) then STREAM (tokens); not typing dots; (4) **one niced generate**; LOAD may run after WAKE; queue extra Sends; never auto-Yes; (5) **STORM iterate** storm-0 (suggest 3 rounds) + `/visual-reviewer`; never A33; (6) **operator read-twin** of the bound pocket over Headscale — mechanicall-os watches, does not become their law; (7) leftover sit-join: tsnet JNI, wol-pi wake, hunk ICM.
- **Grey (not Limits):** bind *location* may be the APK install dir (boring). **Many** project folders, each with a CURRENT.md, stay allowed. Do not require a pre-existing CURRENT.md to bind (that plus one pref is one-project tyranny). Operator tree still refused. See `decision-tree.md` T8c.
- **Not core:** Play Store, Funnel, public Ollama, dual-Next, painted VST, device-wide VpnService, syncthing dual-CURRENT, model Yes, bind-phone-to-this-repo.

## Done (keep)
- Claim-1 CERTIFIED · casual-core paused
- sit-join-workshop 01 research + 02 implement (paper workshop, JOIN fingerprint, WAKE script, hunk ICM)
- Verification factory + `/app-reviewer`
- Grill T1–T8b in `decision-tree.md`

## Keep
- One Next; models never approve; silence ≠ permission
- GET / open / Bind / Send / JOIN / WAKE / SAVE DRAFT / hunk-accept / LOAD / STREAM / twin-pull are not Decide
- Phone bind ≠ this repo
- `tests/run.sh` as CI
- Hunk-scoped 7B; no whole-file dump
- STORM cap 2 AVDs; this Next uses **one** (storm-0)

## Reject
- dual-concurrent-next
- model-auto-approve / model-auto-write-current
- Play Store / Funnel / public Ollama / public wake
- Chat-app chrome as identity
- Painted maximise / fake 92% window
- Sharing RZCW2038KHN
- Tailscale-as-me
- Headscale OIDC this Next
- Twin that writes CURRENT
- Imagine-mocks as visual law

## Limits
- Demo seat only
- No secrets in git (Headscale preauth, WOL_MAC, tokens)
- Do not rewrite bind CURRENT from this tree
- STORM: storm-0 iterate; cap 2 AVDs on myarch
- GATE is instrument. Decide stays two-tap + Why
- Userspace Tailscale (not VpnService)
- Grouped git exclude live CURRENT unless human Go-git

## Next allowed action
**Action id:** `sit-vst-headscale`

Brief: `decision-tree.md` new-direction locked spec. Workspace: `dev/38_sit-vst-headscale/` after apply.

1. **Research.** Genuine VST/Camel Space refs + Headscale login-server layout + FACE-SPEC v2 (frame + paper) + GATE LOAD/STREAM contract.
2. **Implement.** Frame chrome; Headscale+preauth JOIN; two-phase GATE; niced queue; read-twin; tsnet userspace.
3. **STORM iterate.** storm-0; `/app-reviewer` + `/visual-reviewer`; never Confirm; round cap 3.
4. **SHARING.md** as the twin protocol (read-only). Git-in-pocket not shipped.

## Approval condition
Human applies this file, then `aether next sit-vst-headscale` and `aether approve` with a real reason.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-approve-session
- model-auto-write-current
- hunk-write-current
- dump-whole-current-into-7b
- public-wake-url
- bake-invite-into-apk
- bind-phone-to-operator-tree
- share-a33-across-storm
