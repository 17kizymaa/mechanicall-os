# CURRENT

**Objective:** One sit: a stranger joins the tailnet **as themselves** (userspace Tailscale in the APK), wakes myarch, and works a **document workshop** — Plan is published `CURRENT.md`, Draft is Suggesting on a PROPOSE copy of that whole file, Decide publishes. Desk is a 7B+LoRA on hunk-scoped ICM. Same law: pocket folder, human Yes. Not Play Store. Not four Nexts.
**Phase:** SELECT
**Status:** DRAFT
**Baseline:** 2026-08-20 · ACTUAL-APP-VERIFICATION (mechanical 10/10 last-pass; VST chrome still a UI-fail; Draft not a workshop; Tailscale-as-me still not a send path)
**Next:** sit-join-workshop
**Approval:** PENDING
**Host:** myarch · anyone can boot this desk to run `personal-llm-sft-v4` (7B + active LoRA)

## Product (non-negotiable identity)
- **Is:** local-first authority protocol.
- **This Next:** one action-id that includes **all** of: (1) **JOIN** after bind — libtailscale/tsnet **userspace** in the APK; operator mints invite out-of-band; sitter pastes/taps it in Mechanicall; MagicDNS `myarch` / `wol-pi`; depreciated stranger ACL (wake + `:11434` only); not `17kizymaa@`; (2) **WAKE DESK** via tailnet-only HTTP on wol-pi (magic packet); not a public `/wake`; (3) **document workshop** — Plan = published whole `CURRENT.md`; Draft = Docs/Word/Overleaf/HackMD-shaped Suggesting on `PROPOSE-CURRENT.md`; hunk accept/reject writes PROPOSE only; Decide = Publish (two-tap + Why); (4) **7B ICM** — never dump whole CURRENT into the prompt; hunk-scoped; may auto-complete schema from user hunks onto PROPOSE; GATE streams thinking, not typing dots; (5) **document chrome** (paper + suggestion marks, Compose); Camel Space/VST is a **later settings reskin** from genuine plugin resources, not this Next’s identity; (6) **STORM** on isolated emulators, never share the A33.
- **Not core:** Play Store, Funnel, public Ollama, dual-concurrent-next, painted VST as the product, device-wide VpnService this Next, pocket-share/git-as-product, model Yes.

## Done (keep)
- Claim-1 CERTIFIED · Claim-2 parked · casual-core paused
- People-app factory + SeatMate lab in `~/people-app-stranger-sit`
- Phone APK lab (`0.13.0-last` / storm-0; earlier `0.8.0-vst` on A33) — engine reusable (slim chat, bind-first, SAVE DRAFT ≠ Yes); chrome rejected as VST image
- Verification factory: `dev/36_actual-app-verification/`, `python/app_verify.py`, `/app-reviewer`
- Grill (closed): `decision-tree.md` S1–S13 including continuation
- STORM-ENV: AVD `storm-0` exists; never RZCW2038KHN for walkers

## Keep
- One Next; models never approve; silence ≠ permission
- GET / open / Bind / Send / JOIN / WAKE / SAVE DRAFT / hunk-accept / schema auto-complete are not the Decide
- SeatMate `confirm h-…` is a booking; CURRENT Yes is a different tap
- Phone bind ≠ this repo
- `tests/run.sh` as deterministic CI
- Closed grill S1–S13 (except S2 recanted by S10b; S6 recanted by this apply)

## Reject
- dual-concurrent-next
- model-auto-approve / model-auto-write-current
- Play Store this Next
- Funnel / public Ollama (`0.0.0.0`)
- Chat-app chrome as the product (composer+bubbles as identity)
- Painted maximise / fake window / VST-as-identity this Next
- Sharing RZCW2038KHN across STORM agents
- Splitting join / wake / workshop / ICM into extra Nexts
- Tailscale-as-me (`17kizymaa@` as the sitter)
- Device-wide VpnService this Next
- Whole-file dump into the 7B
- Hunk accept or schema auto-complete writing live CURRENT
- Auth keys / wol-pi MAC / Tailscale API tokens in git or in the APK
- Pocket share / syncthing / zip-as-law this Next
- Auto-commit of live CURRENT (git groups exclude it)

## Limits
- Demo seat only
- No secrets in git
- Do not rewrite bind CURRENT from this tree
- STORM cap: **2** AVDs on myarch (32 GiB, 0 swap)
- GATE is a debug instrument (step LEDs / scope). Decide stays two-tap + Why.
- Userspace Tailscale only this Next (other apps stay on LTE)
- No pocket share this Next (versioning = CURRENT / PROPOSE / receipts)
- Camel Space / VST reskin is **not** this Next
- Grouped operator-git after human **Go-git**; commit ≠ approve

## Next allowed action
**Action id:** `sit-join-workshop`

Brief: `decision-tree.md` (locked spike + continuation specs). Workspace: `dev/37_sit-join-workshop/`.

1. **Research (first).** Docs Suggesting / Word Track Changes / Overleaf / HackMD → FACE-SPEC (paper Plan/Draft, JOIN/WAKE strip after bind). Prompt contract for hunk-scoped 7B+LoRA. Genuine Tailscale userspace notes. Human reviews `01_research/output/`.
2. **JOIN + WAKE.** After bind only. Paste-well feeds libtailscale/tsnet userspace. Status: not-on-net / invited / connected. MagicDNS `myarch` / `wol-pi`. wol-pi tailnet-only wake HTTP. Neither is Yes.
3. **Workshop.** Plan = published `CURRENT.md` (Objective/Next first, rest of file). Draft = Suggesting on PROPOSE copy of that whole file. Hunk accept/reject → PROPOSE only. Send inserts suggestions via hunk ICM; may auto-complete schema onto PROPOSE. Decide = Publish.
4. **STORM.** Isolated AVD pockets. Never Confirm. Never the A33.
5. **Git (only if human says Go-git).** Grouped commits: engine / android / `dev/` / tree+scripts. Exclude live `CURRENT.md`, `.aether/events.jsonl`, secrets.

## Approval condition
Human applies this file over root `CURRENT.md`, then `aether next sit-join-workshop` and `aether approve` with a real reason when **this one sit** (join + wake + document workshop + hunk ICM) is the Next they want.

Silence is never permission.

## Prohibited
- automatic-approve
- commit-secrets
- dual-concurrent-next
- model-auto-approve-session
- model-auto-write-current
- uncapped-registration
- send-email-for-real
- host-smtp
- live-graph-oauth
- reopen-public-pipeline-register
- claim-session-is-core-product
- claim-conversation-auto-sync
- claim-personal-model-is-security-jail
- hunk-write-current
- dump-whole-current-into-7b
- public-wake-url
- bake-invite-into-apk
