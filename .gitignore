# Secrets — never git. Desk already loads ~/Desktop/.env (aether_fs.load_dotenv_files).
.env
**/.env
!.env.example

# Local / generated (SPEC-v0.1: state.json is cache)
.aether/state.json
.aether/trusted
.aether/.tmp.*
.aether/.poke
.session.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor / OS
.DS_Store
*~
*.swp

# Local inbox/sparks if stored in-tree by mistake
/inbox.md

# Lab folder send/receive. Bytes gitignored. README stays. Not CURRENT.
/inbox/sitters/
/inbox/outbox/
/inbox/drop/
/.inbox/sitters/
/.inbox/outbox/
/.inbox/drop/

# Operator JOIN provision (Headscale preauth). Never git. Never CURRENT.
**/.aether/join-provision
**/.aether/join-provision.*

# ICM / STORM receipt APKs and Play bundles (S13). Source stays; binaries do not.
*.apk
*.aab

# Play upload keystore — never git. Lives in ~/.mechanicall/
*.jks
*.keystore
play-upload.properties

# Optional local noise (events/artifacts are usually committed for inspectability)
# .aether/events.jsonl

# Tauri seat
seat/node_modules/
seat/dist/
seat/src-tauri/target/

# Nix build outputs (store symlinks)
/result
/result-vm

# Local VM images / backups (never commit)
*.qcow2
*.qcow2.*
*.bak
*.bak-*
python/*.bak*

# Grok session brief cache (regenerable via aether brief)
.aether/last-grok-brief.txt

# Local instance / session noise (not product)
.aether/preflight-last
.aether/preflight.jsonl
.aether/chat.jsonl
.aether/shell.jsonl
.aether/panel.html
.aether/last-reply.md
.aether/COMMANDS.md
.aether/PANEL.md
.planning/
