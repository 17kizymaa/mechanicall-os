# Continue here — 2026-08-23 sit-testers grill closed

**Live law:** `CURRENT.md` **Next `sit-testers` APPROVED** (human applied). Face kept. SOS reskin parked (P2b).

## Do this first

1. Read live `CURRENT.md` — Next `sit-testers`.
2. `dev/41_sit-testers/` 01–03 receipts.
3. Play Console upload is **human** (no keystore in git).

Human apply (their hands):

```bash
cp examples/propose-current/CURRENT-sit-testers.md CURRENT.md
aether next sit-testers
aether approve "first-sit template, off-LAN then Play internal; keep this face"
aether current validate
```

## What was decided (do not re-grill)

| # | Pick |
|---|------|
| P1 | One CURRENT for the bundle — then P2b dropped SOS from it |
| P2 | VSTGUI blit **recanted (P2b):** leave the reskin; current face is fine; practice Done |
| P3 | First-sit: 7B writes a **full PROPOSE template**. Decide = Publish. **No hunk IN/OUT on Decide.** Stages template → revise → gate. |
| P4 | Off-LAN SDK test, then Play **internal/closed** for the **app**. Production listing parked for a **website** (P4b). |
| P5 | Cellular AVD (not myarch Wi-Fi), then one LTE sideload. A33 never STORM serial. Do not claim LTE folder-send (G4 JNI BLOCK). |
| P6 | Action-id **`sit-testers`** |

## Why the last Next failed

- SOS asked across two chats; blit practice landed then reverted. **P2b:** keep current face; reskin later (S9).
- First-time user wants a 7B **template**. Hunk picker on Decide is useless.
- Testers via Play; **no off-LAN SDK test** yet. Lab drop is `127.0.0.1`.

## Do not

- Copy Camel Audio bitmaps
- Open Play Console before off-LAN receipt **and** human CURRENT
- Production listing / Funnel / public Ollama
- Tap Confirm / Yes
- Bind mechanicall-os
- Share RZCW2038KHN into STORM
- Restart storm-1 unless they say so
- Commit (Go-git is human)

## Environment

- storm-0: leave up (`emulator-5554`) unless they kill it
- storm-1: **killed** — do not restart
- lab drop: `127.0.0.1:8765` may still be up (on-LAN only)
- APK last: `0.16.0-hunk-drop`

## After they apply CURRENT

ICM under `sit-testers` (suggested): `01_first-sit-template` → `02_off-lan` → `03_play-internal`. **No SOS blit stage.** Stop at each `output/` gate.

## Blocking

Play **internal** upload (human Console + signing key). LTE sideload if they have a phone. Go-git last. Never production. Never Confirm.
