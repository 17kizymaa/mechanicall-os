`aether_pocket.py` is a copy of `python/aether_pocket.py`.

```bash
sh scripts/sync-pocket-engine.sh
```

Do not edit the copy. Chaquopy still needs a host `aether` on PATH / AETHER_HOME for Yes/current (same `bin/aether`).
