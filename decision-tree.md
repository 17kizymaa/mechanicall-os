# Decision tree — notepad Decide + send/receive cycle (C-series)

**Premise (this grill, 2026-08-24):** User: standardise Decide input as a **normal notepad**; debug the **project cycle** (sending and receiving); grill **why this phase is in the current state**; *“I feel misunderstood; what?”* Live law is still **`sit-testers` APPROVED**. Dual-Next Reject. Models never approve. No implement until this tree closes *and* a human-applied CURRENT (unless they pick leftover-under-sit-testers on C-law).

**Interview started:** 2026-08-24  
**Interview status:** **OPEN.** C0 auto (why-this-state, from disk). C1 posed (what “notepad” is).  
**Skill:** `grill-me-store-decisions`.  
**Live law now:** header **Next:** `sit-testers` · **APPROVED**. Agents do **not** silently rewrite CURRENT.

**Prior closed tree:** 2026-08-23 testers / SOS / first-sit (P-series, P2b recant). 2026-08-20 invite + Draft workshop (S/T/E/G). 2026-08-16 mobile-planning-demo archived at bottom (D1–D14).

---

# Prior interview (closed as law-history) — tailnet invite + Draft workshop

**Premise then:** JOIN invite + Draft as proposal workshop.  
**Closed:** E7 2026-08-21. STORM factory in-tree. Play Store Reject. Camel Space parked as later reskin (**S9 — this grill reopens**).

**Prior closed tree:** 2026-08-16 mobile-planning-demo interview is archived at the bottom of this file (D1–D14). Inherited principles that still bind are copied up, not re-asked.

---

## Guiding principles

From this kickoff + live CURRENT + inherited 2026-08-16 tree (elevate further if stated mid-interview):

1. **Ask, then grill, then persist.** User: “Ask me for clarification” + `/grill-me-store-decisions`. No code until this tree closes *and* they say Go.
2. **CURRENT is law.** Next is `sit-join-workshop` (human applied + APPROVED 2026-08-20). Chat does not outrank it. Agents never rewrite CURRENT.
3. **Skip obvious questions.** Inherited D17 (2026-08-16): “avoid obvious questions… I approve any obvious questions.” If prior law + this tree already determine the pick, record as **decided (auto-approved)** and do not re-ask.
4. **Invite / Wake / Send / SAVE DRAFT / Bind / open are not Yes.** Decide is two-tap + Why. Silence ≠ permission.
5. **No Funnel / public Ollama / Play Store / secrets in git.** Same Reject + Limits as live CURRENT.
6. **Phone bind ≠ this operator tree.** Pocket is their folder.
7. **Minimize paid third parties.** Tailscale/LAN over cloud relays. No BaaS as authority store.
8. **One engine.** Chaquopy + same `python/` + `bin/aether`. Kotlin is chrome.
9. **Session is a proposal-doc editor.** Agent edits PROPOSE, not CURRENT. Bubble-chat as identity is Rejected.
10. **18 August lesson still on disk:** Tailscale-as-me is not a send path. `kamilas-tab-s9-fe` as `17kizymaa@` is not a stranger. This spike may *revise* that lesson — that is Q1, not auto-approved.
11. **HANDOFF 2026-08-19 is not law** (`dev/28_mechanicall-phone-app/06_handoff/output/HANDOFF.md`). It already named Join-network + Wake-desk. We grill whether to *adopt* it.
12. **Sitter is a stranger-as-themselves with a written-down ACL (S1).** Not `17kizymaa@`. May reach wol-pi (wake) and myarch:11434 (Ollama) only. Invite / Wake / Send still not Yes. 18 August lesson *revised*: they do join the tailnet — as them, depreciated, not as you.
13. **JOIN is in-app Tailscale (S2 recanted by S10b).** Mechanicall **is** the client (libtailscale / tsnet). No second Tailscale app required. S7 paste-well feeds the embed. Token / auth key never in git. wol-pi mint still deferred — operator Invite out-of-band, sitter pastes into Mechanicall.
14. **WAKE DESK talks to wol-pi, not to a public URL (S4).** Tailnet-only HTTP on wol-pi sends the magic packet. Wake is not Yes. This spike includes that service (MAC/token off git). Send still uses `100.90.85.68:11434` once up.
15. **Join/Wake only after bind (S3, user override).** Unbound remains FOLDER-only. Net is a sitting step, not a door in front of the pocket. Still not a fifth rail page — exact chrome for the strip-after-bind is a leaf, not a new fork.
16. **Plan + Draft are redesigned from researched vendor / AI-platform standards (S5, user override).** Not dual-pane-from-last-pass. Not “polish ChatHome.” Research is an input; CURRENT law (PROPOSE ≠ CURRENT, Decide-only Yes) still outranks vendor taste.
17. **Research set is document-workshop faces (S5b, user override).** Google Docs Suggesting, Word Track Changes, Overleaf, HackMD. Steal inline proposal + accept/reject hunks. Do **not** steal Google login, Drive-as-authority, or “accept hunk writes the published doc.” User: that shortlist question was spot-on.
18. **Draft is Suggesting on a proposal copy (S5c).** Plan = published CURRENT. Draft = PROPOSE with inline diffs vs live. Accept/reject hunks mutate PROPOSE only. Decide = Publish. Desk Send inserts suggestions, not law.
19. **The document is whole `CURRENT.md` (S5d).** Plan shows the published file (Objective/Next first). Draft is a suggested rewrite with inline hunks. CAPITALISED fields are anchors, not the only atoms. Not wells-only.
20. **S6 recanted 2026-08-20.** User: “propose this as CURRENT.md so we can execute these tasks under a single NEXT. Go!” New action-id `sit-join-workshop`. Agents still never `aether next` / `approve` / overwrite live CURRENT. Apply-ready: `examples/propose-current/CURRENT-sit-join-workshop.md`.
21. **Operator mints out-of-band; JOIN is accept (S7).** Admin Invite with *their* email. After bind: paste/tap the link in Mechanicall. No URL in git. No wol-pi mint this spike.
22. **Research first, then implement (S8).** Docs/Word/Overleaf/HackMD → FACE-SPEC, human reviews `output/`, then implement. One Next. Silence is not Go.
23. **Grill continuation (2026-08-20).** User reopened for five named branches: visual-development framework (VST UI-fail), Tailscale component, project sharing-and-versioning, 7B+LoRA ICM stability in the document workshop, **final** dirty-git consolidation of this tree. S1–S8 not recanted. Git consolidation stays last.
24. **Sit chrome is a document, not a VST (S9).** Compose stays. Paper + suggestion marks now. Camel Space / VST-inspired UI is a **later settings reskin**, using **genuine** VST/Camel Space references — not agent-invented bevels. Painted plugin is still Reject.
25. **Mechanicall is the Tailscale client, userspace only (S10–S10c).** libtailscale/tsnet in the APK. No second Tailscale app. No device-wide VpnService this spike. Paste-well feeds the embed. MagicDNS `myarch` / `wol-pi`. Other apps stay on LTE. Auth key not in git.
26. **No pocket share this spike (S12).** Versioning *is* CURRENT vs PROPOSE vs receipts. git-in-pocket / syncthing / zip parked. Operator-tree consolidation remains S13.
27. **7B+LoRA is hunk-scoped ICM; may auto-complete the schema from those hunks (S11).** Never dump whole CURRENT into the prompt. Patches + schema completions land on PROPOSE only. LoRA stays propose/taste. Auto-complete is not Yes.
28. **Grouped git, exclude live CURRENT + secrets (S13).** Execute only on **Go-git**. Commit ≠ approve. Pocket is not this tree (S12).
29. **Headscale is parked, not this Next (S14).** Preauth keys can make the *sitter* login-less (no Google/Apple). Not key-less. Tailscale.com auth keys can also skip IdP. Human interested 2026-08-20; not folded into CURRENT.
29. **Headscale is parked, not this Next (S14 interest 2026-08-20).** Preauth keys can make the *sitter* login-less (no Google/Apple). Not key-less. Tailscale.com auth keys can also skip IdP. Human asked; not recanted into CURRENT. **Unparked later by T3** (Headscale + preauth is this Next’s JOIN once CURRENT body matches the header).
30. **Git PRs are sequential and small (G-series, 2026-08-20).** User named the analyser as git-tree + 3–4 PR iterations with `/sdcard/mechanicall/` feedback from the store. S13 still requires **Go-git**. Working assumption (G1, not decided until they pick): four PRs on `feat/sit-vst-headscale`; `/sdcard/mechanicall` is a **read-only review inbox**, not a bind. Brief: `dev/38_sit-vst-headscale/00_git-pr-brief/output/EXECUTOR-BRIEF.md`.
31. **EdubaWare grill (2026-08-21).** User: halt the sit (app is good; minor client purpose) and grill **EdubaWare pivot/clone**, then **swarm implement**, then **swarm review with STORM SDK setup**, then **handle the dirty mechanicall-os tree** in this cd. Sequence is named; law is not. No swarm until this tree closes *and* a human-applied Next. PRODUCT still: not an AI hosting platform, not multi-tenant SaaS.
32. **Private containers (E1).** User: “containers privately. Go!” Self-host clone of EdubaWare *shape* with **private containers** (not his cloud, not tmux-only, not option 1 park-only). Sit stays parked as client purpose. Mechanicall stays authority protocol inside those folders.
33. **k3s/k8s (E3).** User picked 3 over recommended docker-cap-2. Real cluster ops. Next fork is *where* the cluster lives (E3b) because myarch is 32 GiB / 0 swap and still the 7B + STORM box.
34. **STORM is developer infrastructure only (E3b).** User: “2. STORM is only developer infrastructure...” Then recanted k3s-as-product.
35. **Original app is central; cut EdubaWare (E4 recant, 2026-08-21).** User: “OK original app is central, now! Cut the Eduware platform. Emulator means just a mass-app subagent review environments for this chat sessions. I just want the tools to be persistent via being an actual application component on here! Go!” E1–E3b platform clone **cut**. Persist `python/aether_storm.py` + scripts + `/storm-review`. Dirty tree last.
36. **Headscale is mesh, not a folder store (E7).** User rejected `sit-storm-factory` until uploads/downloads are named. Headscale/tsnet do not hold CURRENT. Bind is local. Twin GET is the only designed download. POST of law files is not Yes.
37. **SOS reskin left parked (P2b recant, 2026-08-23).** User: “Leave the reskin actually though cos it'll probably fail and the current one is fine now. Good practice at doing it achieved today!” `15_binary-skin` + revert is **practice Done**. S9 stands: Camel Space is a later settings reskin, not this Next. Current cream/LCD/GATE face ships.
38. **First sitting is a template, not Decide hunks (P-series).** User: a first-time user wants a template the **7B can generate**. They do **not** select which hunks to automate from one prompt. That feature was wrongly put on Decide (useless). Multi-ICM picture required.
39. **Testers via App Store now (P-series).** Easiest way to get testers. Off-the-LAN SDK test has **not** been done. Live CURRENT still Rejects Play Store — this grill is how that Reject gets recanted or replaced, not a silent listing.
40. **Ask, grill, persist, then handoff.** No code until this tree closes *and* they apply CURRENT. `/handoff` after close, not instead of the grill.
41. **Do not steal Camel Audio bitmaps.** Steal the *toolkit/pipeline* (VSTGUI-era CBitmap / filmstrip / nodpi blit). Copyright theft stays FAIL.
42. **sit-one-path proposal is not law.** `examples/propose-current/CURRENT-sit-one-path.md` parked millwork/Play Store. This grill may recant that parking.
43. **Misunderstood = progress measured as factory, not as a sitting.** User 2026-08-24. Agents shipped STORM, leftover chrome, SOS blit practice, first-sit template engine, Play-prep receipts. The person still cannot **type a decision on a notepad** and **send/receive a folder** as a cycle. Receipts in `dev/41_sit-testers/` are not that sitting.
44. **Decide input is writing.** “Normal notepad” is the named standard. Giant YES/AGAIN pads + Confirm dialog is protocol theater; walkers already must never tap Confirm, so the only text well on Decide is in the forbidden dialog.
45. **Send/receive is the product cycle, not leftover chrome.** Lab drop `127.0.0.1:8765` + “SEND FOLDER IN THE WORKS” is not two seats exchanging a project. G4 mesh JNI remains named BLOCK. Do not list LTE folder-send.
46. **Ask, grill, persist.** Same as 1/40. This C-series does not rewrite CURRENT. Implement only after the tree closes *and* they apply (or they explicitly pick leftover-under-live-Next — not recommended).

---

## Master cost map

Recurring **$/month**. Demo / lab. Most lines $0 on existing hardware.

| Line item | v1 (solo / this demo) | alpha (50 users) | growth (1,000 users) | Self-host alternative | Notes |
|-----------|----------------------:|-----------------:|---------------------:|-----------------------|-------|
| Ollama on myarch | $0 (existing box) | $0–80 always-on | $200–2,000 GPU fleet | Keep self-host | Public `:11434` rejected |
| Tailscale Personal | **$0** (already) | $0–6 Personal Plus | $30–500 / Headscale | Headscale $0 + VPS ~$5 | Invite path needs API token **off git** |
| wol-pi (always-on) | $0 (already on tailnet) | $0 | $0–5 spare Pi | Same | Holds invite mint + WoL; not in APK |
| Play listing | $0 sideload | **$25 one-time** (closed testing reopen) | $25 + 15% cut if production | Sideload / Firebase App Dist | CURRENT still Rejects until P1+human CURRENT |
| Cloud LLM | $0 | $20–200 | $500–5,000 | Ollama | Conflicts with private desk |
| EdubaWare cloud | $0 (his alpha bill) | his SaaS | n/a | **Private k3s (E3)** | User refused his cloud; picked cluster ops |
| k3s node | **$0 myarch (E3b=2)** | $5–20 VPS (not picked) | $20–200 nodes | compose cap-2 (E3 rec, not picked) | Pause STORM AVDs when cluster+7B hot |
| Auth / BaaS | $0 (don’t) | $0–25 | $25–200 | Files + Tailscale ACL | Second authority store — reject |
| Engineer time (invite + WoL + embed) | **1–3 weeks** libtailscale in APK (S10+S10b) + wol-pi HTTP | — | — | — | VPN or userspace is S10c; auth key not in git |
| Engineer time (Draft workshop) | **5–10 days** document-workshop Plan+Draft (S5+S5b) after 0.5–2 days research | — | — | — | Not a 1-day polish; hunk UI is the week |
| Lab drop (send/receive) | **$0** LAN `127.0.0.1:8765` | $0 if Headscale/tsnet carries zip | $0 mesh or it is not a product | Public relay $5–20 + incident | Quiet drop is “Staged locally”; not off-LAN |
| Decide notepad (C-series) | **1–5 days** face (C1) | $0 | $0 | N/A | Recants Confirm-dialog as the only Why well |

**Crossover:** Putting 50 strangers on the *operator* tailnet is the wrong product. Headscale or “no shared Ollama” becomes cheaper/safer around a handful of sitters. Dollar cost is still tiny vs your week.

---

## Summary of decisions

| # | Decision | Selected | Status | Cost impact |
|---|----------|----------|--------|-------------|
| S0 | Skip obvious / persist this file | inherited D17 | decided (auto) | $0 |
| S0b | Invite / Wake / Send / SAVE DRAFT ≠ Yes | live CURRENT Keep | decided (auto) | $0 |
| S0c | No Funnel, no public Ollama, no Play, no secrets in git | live CURRENT Reject/Limits | decided (auto) | $0 vs incident |
| S0d | Phone bind ≠ mechanicall-os | B10 + Keep | decided (auto) | $0 |
| S0e | This grill does not rewrite CURRENT and does not implement | kickoff “ask for clarification” | decided (auto) | $0; implement only after Go |
| **S1** | **Who sits + what “depreciated stranger” means** | **1 — stranger-as-themselves, limited ACL** (matches rec) | decided | 3–10 days if mint is real; hours if JOIN only opens Tailscale |
| **S2** | **How the phone *accepts*** | **1 recanted by S10b** — was JOIN-opens-Tailscale-app | recanted | see S10b |
| S2b | Send host once connected | **amended:** MagicDNS `myarch:11434` via embed; USB reverse operator lab | decided (auto) | $0 |
| **S3** | **Where JOIN + WAKE live on the face** | **2 — only after bind** (override; rec was 1 unbound strip) | decided | 0.5–1.5 days; B1 stays visually pure |
| **S4** | **Boot myarch** | **1 — WAKE DESK via wol-pi** (matches rec) | decided | 1–3 days wol-pi HTTP + APK; no public `/wake` |
| **S5** | **Draft workshop** | **Other — redesign Plan+Draft from researched vendor/AI-platform standards** (override; rec was dual-pane) | decided | research 0.5–2 days + redesign 3–8 days; not a 1-day polish |
| **S5b** | **Which vendors/platforms are the standard** | **2 — document-workshop faces** (override; rec was 1 plan-vs-apply) | decided | +markdown/hunk UI (week); $0 licenses |
| **S5c** | **What a hunk accept writes** | **1 — Draft is Suggesting on a proposal copy** (matches rec) | decided | hunk UI on PROPOSE; Decide still the only CURRENT write |
| **S5d** | **What *is* the document** | **1 — whole CURRENT.md as markdown** (matches rec) | decided | 6–10 days markdown Suggesting; not wells-only |
| **S6** | **Law / which Next** | **recanted → 2 — one new id `sit-join-workshop`** (was 1 lab-under-verification; rec was 2) | recanted | process; human apply + next + approve |
| **S7** | **Who mints / carries the invite URL JOIN opens** | **1 — operator admin Invite; sitter pastes/taps in app** (matches rec) | decided | hours of JOIN well + probe |
| **S8** | **After Go, first stage** | **1 — research first, then implement** (matches rec) | decided | 0.5–2 days research gate, then 1–2 weeks build |
| S0f | ACL `tag:stranger` + wol-pi MAC/token off git + STORM never A33 | Tailscale admin / Limits | decided (auto) | $0 |
| S0g | Git consolidation is last; S2 was recanted by S10b (exception) | user named “final” git | decided (auto) | $0 |
| **S9** | **Visual-development fork (VST UI-fail)** | **1 now + later Camel Space reskin from genuine VST resources** (Other; rec was 1) | decided | 3–7d document chrome now; reskin parked as settings |
| **S10** | **Tailscale component** | **3 — embed libtailscale / tsnet in the APK now** (override; rec was 1 probe+MagicDNS) | decided | 1–3 weeks + VPN permission; recants S2 unless S10b says otherwise |
| **S10b** | **Does S10 recant S2?** | **1 — recant S2; Mechanicall is the Tailscale client** (matches rec *if* S10 deliberate) | decided | 1–3 weeks; paste-well feeds embed |
| **S10c** | **VpnService vs userspace embed** | **1 — userspace / app-local tunnel** (matches rec) | decided | 1–3 weeks embed; no device VPN dialog |
| **S12** | **Pocket sharing-and-versioning** | **1 — no share this spike** (matches rec) | decided | $0; protocol is the versioning |
| **S11** | **7B + LoRA ICM in the workshop** | **Other — 1 + auto-complete entire schema from user hunks** (on PROPOSE) | decided | 2–5d prompt/patch; still $0 GPU |
| **S13** | **Dirty git consolidation** | **1 — grouped commits, exclude CURRENT + secrets** (matches rec) | decided | 30–90 min after Go-git; commit ≠ Yes |
| **S14** | **Headscale for login-less join?** | **parked — interested, not this Next** | parked | $0 software + always-on login-server; not key-less |
| **S15** | **How paper Suggesting is built** | declined; working assumption 1+3 Compose hunks + FACE-SPEC factory | parked | not law until picked |
| **S16** | **Which userspace Tailscale library** | — | parked (subsumed by T3 if Headscale unparks) | official lib vs gomobile |
| **T0** | New direction is grill-then-PROPOSE; compact after | user this turn | decided (auto) | $0 |
| **T1** | **Where the new direction lives vs live Next** | **1 — one new action-id for the whole bundle** (matches rec) | decided | human PROPOSE after compact; I do not next/approve |
| **T2** | **Unpark VST visual upgrade** | **1 — genuine instrument frame, paper workshop inside** (matches rec) | decided | 5–10d; B9 still forbids painted letterbox |
| **T3** | **Unpark Headscale architecture** | **1 — Headscale + preauth keys** (matches rec) | decided | $0 software + always-on HTTPS login-server |
| **T4** | **GATE boot + model response** | **1 — two-phase LOAD then STREAM** (matches rec) | decided | 1–3d; not typing dots |
| **T5** | **Background model while desktop in use** | **1 — one niced generate; LOAD after WAKE** (matches rec) | decided | 1–2d queue; never auto-Yes |
| **T6** | **STORM reviews + iterations** | **1 — iterate on storm-0** (matches rec) | decided | round cap; never A33 |
| **T7** | **Visual-focused design reviewer** | **1 — `/visual-reviewer` in STORM loop** (matches rec) | decided | 0.5–1d skill; never Confirm |
| **T8** | **Sharing protocol** | **Other — want a communication feature connected to mechanicall-os workspace** | decided (needs T8b) | not git-as-product |
| **T8b** | **What “connected to mechanicall-os” is** | **1 — operator read-twin** (stands; “boring is a good strategy”) | decided | 2–4d Headscale pull; not a second CURRENT |
| **T8c** | **Bind follows twin — installation file** | **Other — 4 (APK install dir) as location; NOT one-CURRENT lock. Grey-area.** | parked (grey) | many Domains under the install; operator tree still refused |
| **G0** | Git-tree facts for the executor | auto from porcelain + preflight + tests | decided (auto) | $0; 60 dirty paths; tests green |
| **G1** | **3–4 PR slice + `/sdcard/mechanicall/` role** | — | parked (E-series last: dirty tree) | 30–90 min grouping after Go-git; not Yes |
| **E0** | Live law + PRODUCT + STORM SDK + dirty tree facts | auto from CURRENT + PRODUCT + STORM-ENV | decided (auto) | $0 |
| **E1** | **What “EdubaWare pivot/clone” is in this repo** | **Other — 3 + private containers** (not cloud, not tmux, not park-only) | decided | $0 infra; RAM on myarch |
| **E2** | **Where the new direction lives vs REJECTED Next** | **1 — one new action-id** (auto; dual-Next Reject) | decided (auto) | process; human apply |
| **E3** | **Where private containers run / how many** | **3 — k3s/k8s** (override; rec was 1 docker cap 2) | decided | +week ops; RAM/VPS |
| **E3b** | **k3s on myarch vs VPS** | **2 — k3s on myarch** + STORM is lab-only | decided | $0 extra; pause AVDs when cluster+7B hot |
| **E4** | **What “swarm review with STORM SDK” is now** | **Other — original app central; cut EdubaWare; persist STORM factory** | decided | in-tree component; AVD not git |
| **E7** | **Headscale folder upload/download** | **Other — depreciated local inbox send + notify/Accept; mesh only** | decided | gitignored `.inbox/`; never CURRENT |
| **G2** | **SOS look vs grey pastiche** | **Keep yellow/purple/cream; SOS texture/depth/luminance via bitmap skin (not Camel PNG)** | decided | 0.15.2-luminance demo sideload; not Play Store |
| **E8** | **FILES button** | **system file manager on bound dir** (cut in-app overlay) | decided | $0; Intent VIEW folder |
| P0 | Code facts for this grill | auto (SOS revert, Decide hunks, Play Reject, no off-LAN) | decided (auto) | $0 |
| **P1** | **Law-shape of SOS + template ICM + testers** | **1 — one CURRENT for the whole bundle** then **P2b dropped SOS from the bundle** | decided | 1–2 weeks + $25 if closed testing |
| **P2** | **SOS toolkit** | **1 — VSTGUI-grammar blit** then **P2b recant: leave reskin** | recanted | practice Done; $0 more |
| **P2b** | **Ship the blit this Next?** | **Leave reskin; current face is fine** | decided | saves 1–2 weeks; testers see 0.16.x chrome |
| **P3** | **First-sit Draft** | **1 — 7B full PROPOSE template; Decide is Publish** (Other parsed as “1”) | decided | 3–8 days; $0 GPU |
| **P4** | **Tester path** | **1 — off-LAN then Play internal/closed for the app** (Other: “1 for the app, 2 for the website later”) | decided | $25 Play; hours–2d off-LAN |
| **P4b** | **Website production listing** | **parked** — liked 2 for a website, not this Next | parked | later CURRENT |
| **P5** | **Off-LAN definition** | **auto: cellular AVD first, then one LTE device; A33 never STORM serial** | decided (auto) | hours–2d; G4 mesh not a store claim |
| **P6** | **Action-id** | **`sit-testers`** (matches rec) | decided | $0 process; human apply |
| **C0** | **Why this phase is in this state** | **auto from disk + prior trees** — factory receipts, not a finishable sitting | decided (auto) | $0 to name; weeks already spent |
| **C1** | **What “Decide = normal notepad” is** | — | **open** | 1–5d face; $0 SaaS |

---

## Per-decision sections

### P0 — Code facts this grill will not re-ask (auto)

**Question (exactly as posed):** *(not posed — explored the tree)*

**Options presented:** none.

**Selected:** Record as fact.

**Rationale / recommendation:** Skill: if the codebase answers it, do not ask.

Facts:

1. **SOS UI failed as architecture, then the fix was reverted.** Camel Space SOS Test is a **bitmap-skinned VSTGUI editor** (`CBitmap` of the whole panel; knobs are filmstrips; hit-tests are `CRect`s). 0.15.1–0.15.2 painted millwork live in Compose. `15_binary-skin` named the VSTGUI pipeline (`drawable-nodpi/sit_skin.png` blit + overlay hits) and explicitly **did not extract Camel PNGs**. `16_revert-skin` restored cream Host Compose millwork. Live glass is the revert, not SOS.
2. **Libraries:** original authors used **VSTGUI 2.x**, not Compose `drawBehind`. JUCE `BinaryData` is Alchemy-era, not Camel Space 2005. Neither VSTGUI nor JUCE is in the APK.
3. **Drafting:** `DecideModule.kt` lists IN/OUT hunks + alt wordings. `ChatHome.kt` is still field chips + SEND. S11 said hunk-scoped ICM may auto-complete schema on PROPOSE. User now: first-time wants a **template the 7B generates**; they do **not** pick hunks from one prompt; Decide hunk picker is useless.
4. **Store:** live CURRENT **Rejects Play Store**. `STORE-GATE.md` G2 millwork FLAG, G4 mesh BLOCK. No off-the-LAN SDK test in receipts. storm-1 was killed. Lab drop is `127.0.0.1:8765` (on-LAN).
5. **Law:** `sit-one-path` proposal parks millwork and Play Store. Not applied. Dual-Next still Reject.

**Cost tradeoff:**

- **v1:** $0 to record. Re-asking “did we copy VSTGUI?” wastes the grill.
- **alpha / growth:** listing a Compose pastiche costs tester bounce, not $25.
- **SaaS vs self-host:** N/A.
- **Engineer-time:** 0.
- **Crossover:** none.

**Dependencies:** S9, S11, S0c, G2, E7, STORE-GATE. Unlocks P1.

**Status:** `decided` (auto)

---

### P1 — Law-shape of SOS + first-sit template + testers

**Question (exactly as posed):**

> You named three failures as one rage: SOS face never used the original toolkit, first-time Draft is not a 7B template (hunk pick landed on Decide), and you need testers via the App Store without even an off-the-LAN SDK test. Live CURRENT **Rejects Play Store**, **one Next**, and parked Camel Space as a later reskin. How should this grill become law?

**Options presented:**

1. **One new CURRENT action-id for the whole bundle (recommended).** Recant S9 (SOS face *is* this Next, VSTGUI-grammar blit, not Camel PNGs). Recant Decide-as-hunk-picker (first-sit = 7B emits a CURRENT *template*; multi-ICM; Decide stays Publish). Recant Play Reject **only as closed testing** after a named **off-the-LAN SDK test**. Human applies after this tree closes. Dual-Next still Reject.
2. **Sequential Nexts.** UI CURRENT, then draft CURRENT, then store CURRENT. One at a time after each human next. Testers wait.
3. **Play closed testing only.** Ship the current face/draft to testers this week. UI and ICM later.
4. **Sideload / off-LAN SDK only.** Keep Play Reject. Testers via APK link. SOS + template still this grill but not a listing.

**Selected:** **1 — One CURRENT for the whole bundle** (matches rec). Not sequential. Not Play-only. Not sideload-only.

**Rationale / recommendation:**

**Recommend option 1.** (User picked it.)

Why: you said testers *now* **and** the UI/draft are why testers would bounce. Option 3 ships the thing you are angry at. Option 2 is honest sequencing but contradicts “now” and burns three human CURRENT applies. Option 4 keeps CURRENT Play Reject and still does not get Play testers. Option 1 is one Next; closed testing is not Funnel and not a production listing; off-LAN SDK is the missing gate so “App Store” is not a LAN lie.

I will not open Play Console, copy Camel bitmaps, or overwrite CURRENT from this grill.

**Cost tradeoff:**

- **v1 (solo):** $0 listing until human CURRENT. Engineer **2–4 weeks** (SOS blit 1–2w + template ICM 3–8d + Play closed testing + off-LAN test 1–3d). Play **$25 one-time** if you pick a listing track.
- **alpha (50 testers):** Play closed testing $25 + 0% cut (internal/closed). Firebase App Distribution $0. Sideload $0 and does not scale past friends.
- **growth (1,000):** production listing $25 + 15% if you later lift closed→prod. Self-host APK $0 + support cost.
- **SaaS vs self-host:** Play is the distribution SaaS. Sideload/Firebase are the $0 alternatives. Crossover: ~10 testers who are not on your LAN — sideload dies; closed testing wins.
- **Crossover:** If the face is still Compose pastiche, Play spend is wasted (bounce). If testers are three friends on Wi-Fi, option 4 is cheaper by $25 and a week of Console.

**Dependencies:** P0 facts; live CURRENT Play Reject / dual-Next; S9; S11. Unlocks P2 (toolkit), P3 (template ICM), P4 (tester track).

**Status:** `decided`

---

### P2 — What “copy the original authors’ libraries” means for SOS

**Question (exactly as posed):**

> Camel Space SOS Test was built in **VSTGUI 2.x**: one panel `CBitmap` in the DLL, knobs as filmstrips, hits as `CRect`s. We never linked that toolkit. We painted millwork in Compose, then `15_binary-skin` named the blit pipeline, then `16_revert-skin` undid it. You cannot drop Win32 VSTGUI into a Chaquopy APK. What do we steal so the sit *looks* like SOS Test without stealing Camel Audio PNGs?

**Options presented:**

1. **VSTGUI-grammar blit (recommended).** Steal the *pipeline*, not the C++: whole-panel PNG in `res/drawable-nodpi/` (CBitmap), overlay hit-targets (CRect), knob/GATE filmstrips later. No Camel PNGs. This is what 15_binary-skin already wrote and 16 reverted.
2. **Embed JUCE / iPlug2 / a real VST editor toolkit on Android.** Native months. Not VSTGUI 2.x. High chance of a second pastiche.
3. **Try harder at live Compose millwork** (shaders, more lips, SOS luminance). Already failed across two chats.
4. **Use extracted Camel Space / CamelPhat bitmaps.** Looks right, is copyright theft, FACE-SPEC V7 FAIL.

**Selected:** **1 — VSTGUI-grammar blit** (matches rec). Not JUCE. Not Compose millwork. Not Camel PNGs.

**Rationale / recommendation:** **Recommend option 1.** (User picked it.) Option 3 is the failure mode you named. Option 4 is theft. Option 2 is the “we copied a library” fantasy that still will not be VSTGUI 2.x and will delay testers. Option 1 *is* how SOS-quality GUIs live in binaries.

**Cost tradeoff:**

- **v1:** $0 licenses. Engineer **1–2 weeks** to re-land blit + match SOS Test grammar (LCD well, GATE pits, overlapping plates) against `camel-space-sos.jpg`.
- **alpha / growth:** skin is a few MB in the APK. No SaaS.
- **SaaS vs self-host:** N/A. Photoshop/SkinMan vs a Python renderer we already have (`scripts/render-sit-skin.py`).
- **Crossover:** If blit still “looks nothing like it” after one honest pass vs the SOS photo, then a hired pixel artist (not JUCE) is the next fork — not Compose millwork again.

**Dependencies:** P1 option 1. Unlocks P3 (template sits *inside* the SOS frame).

**Status:** `recanted` by P2b

---

### P2b — Leave the reskin (recant P2 as this Next)

**Question (exactly as posed):** *(user, not a multiple-choice)*

> Leave the reskin actually though cos it'll probably fail and the current one is fine now. Good practice at doing it achieved today!

**Options presented:** none (override).

**Selected:** **Park SOS/VSTGUI blit.** Keep live cream/LCD/GATE face. Treat `15_binary-skin` (pipeline named, skin landed, then `16_revert-skin`) as **good practice Done**, not a ship task. S9 later-settings reskin stands. Testers sit the current UI.

**Rationale / recommendation:** Honour the recant. Re-landing blit under `sit-testers` would spend the tester Next on a face they now call fine, and they expect it to fail. Template + off-LAN + Play internal remain.

**Cost tradeoff:**

- **v1:** **saves 1–2 engineer weeks.** $0 licenses either way.
- **alpha / growth:** testers see the current face; bounce risk is Draft/store, not millwork.
- **SaaS vs self-host:** N/A.
- **Crossover:** If testers bounce on looks, unpark P2 as a **later** CURRENT (S9), not this Next.

**Dependencies:** P2. Narrows P1 bundle. Unlocks a slimmer `sit-testers` PROPOSE.

**Status:** `decided`

---

### P3 — First-time Draft is a 7B template, not Decide hunks

**Question (exactly as posed):**

> A first-time user just wants a **template the 7B can generate**. They do not select which hunks to automate from one prompt. That picker was implemented on **Decide** (you called it useless). Multi-ICM: first sit ≠ later revise ≠ Publish. What is the first sitting?

**Options presented:**

1. **First-sit template (recommended).** Empty/short pocket: one prompt → 7B writes a **full PROPOSE CURRENT template** (Objective through Next, Keep/Reject stubs). Plan shows law (maybe none). Draft shows the template as paper, not field-chip roulette. Decide = Publish the template (two-tap + Why). **No hunk IN/OUT on Decide.** Later sittings: typed revise on Draft / optional one-field SEND. Multi-ICM = stages `template` → `revise` → `gate`, local stage still owns the write.
2. **Every prompt regenerates the whole CURRENT template.** Simple. Destroys a workshop. Easy to clobber Keep.
3. **Keep Decide hunks; add Generate template on Bind/Plan only.** Fastest code. Leaves the useless picker on Decide.
4. **On-device ICM folders as the UX** (`01_template/`, `02_revise/`). Honest ICM. Heavy chrome. Testers bounce.

**Selected:** **1 — First-sit template** (user Other = `1.` — parsed as option 1, not a new fourth path). Not every-prompt regenerate. Not keep Decide hunks. Not ICM folders as the phone UX.

**Rationale / recommendation:** **Recommend option 1.** (User signalled 1.) Option 3 keeps the thing you hate. Option 2 is a blender. Option 4 is the meta-agent skill as a phone OS — not a first sit. Option 1 matches “template the 7B can generate” and keeps Decide as Yes only.

**Cost tradeoff:**

- **v1:** $0 GPU (myarch 7B). Engineer **3–8 days** (prompt, parser for a full template, hide Decide hunk list, first-sit vs revise branch).
- **alpha / growth:** still self-host Ollama or testers bring no desk (template button quiet / GATE quiet is honest).
- **SaaS vs self-host:** Cloud LLM $20–200/mo at alpha if you later leave myarch. Conflicts with private desk.
- **Crossover:** If 7B cannot emit a usable template in one generate, first-sit must ship a **static markdown template** the human edits — not more hunk UI.

**Dependencies:** P1, P2. Unlocks P4 (what testers actually tap).

**Status:** `decided`

---

### P4 — Testers: Play track, and the missing off-LAN SDK test

**Question (exactly as posed):**

> You need testers **now**. App Store is the easiest list. We have **not** done an off-the-LAN SDK test (lab drop is `127.0.0.1`; storm-1 is dead; JOIN is NOT-ON-NET). Live CURRENT still **Rejects Play Store**. P1 already chose one bundle that recants that Reject *only as closed testing after a named off-LAN test*. Which tester path?

**Options presented:**

1. **Off-LAN SDK test, then Play internal/closed testing (recommended).** Gate: one APK on a device **not** on myarch Wi-Fi (cellular AVD or LTE phone that is not RZCW2038KHN-as-STORM-share). Then Play **internal testing** (≤100) or **closed testing** (email list). Not production. Not Funnel. $25 developer account. Recant CURRENT Play Reject only that far.
2. **Play production listing this Next.** Full review. STORE-GATE G2/G4 still FLAG/BLOCK. Highest bounce + policy risk.
3. **Firebase App Distribution / APK link only.** $0. No Play testers. Fast.
4. **Off-LAN test only; Play stays Reject.** Honest vs live law. No Console.

**Selected:** **1 — Off-LAN SDK test, then Play internal/closed for the app.** User Other: *“I kinda like 1 for the app and 2 for the website, but we'll just do 1 for now!”* Production listing parked as **P4b** (website, later CURRENT). Not Firebase-only. Not Play-stays-Reject.

**Cost tradeoff:**

- **v1:** $25 one-time Play Console. Off-LAN test: **hours–2 days**. No 15% cut on internal/closed.
- **alpha (50):** internal testing is enough. Privacy policy + data safety form if closed/open.
- **growth (1,000):** closed or open testing, then a **later** CURRENT for production. 15% only if production.
- **SaaS vs self-host:** Play vs Firebase ($0) vs sideload ($0). Crossover ~10 off-LAN testers.
- **Crossover:** If off-LAN test fails because mesh JNI is BLOCK, **do not** list “send a project over LTE” as a store feature — list bind/plan/template/decide only.

**Dependencies:** P1 option 1. Unlocks P5 (auto) and P6 (action-id).

**Status:** `decided`

---

### P4b — Website production listing (parked)

**Question:** not posed as its own fork.

**Selected:** **Parked.** User likes production listing **for a website**, not this app Next.

**Cost tradeoff:** $0 now. Hosting later is a different CURRENT. Not Funnel.

**Status:** `parked`

---

### P5 — Off-LAN SDK test (auto from P4)

**Question (exactly as posed):** *(not posed — P4 option 1 already named the gate; skip-obvious)*

**Options presented (would have been):** cellular AVD / LTE phone / both / remote SDK.

**Selected:** **Cellular AVD first (not myarch Wi-Fi), then one LTE sideload.** Serial `RZCW2038KHN` may be a *person’s* tester phone; it is **never** a STORM review serial. Lab drop `127.0.0.1` is **not** the off-LAN receipt. Store listing must **not** claim LTE folder-send until JNI exists (G4 BLOCK).

**Rationale / recommendation:** Auto-approved from P4. Re-asking “what is off-LAN” after they picked option 1 is noise.

**Cost tradeoff:** hours–2 days. $0 extra SaaS.

**Dependencies:** P4. Unlocks Play internal *after* the receipt exists.

**Status:** `decided` (auto)

---

### P6 — Action-id for the human-applied CURRENT

**Question (exactly as posed):**

> One Next will hold SOS blit + first-sit 7B template + off-LAN test + Play internal/closed. Dual-Next stays Reject. What is the **action-id** you will apply?

**Options presented:**

1. **`sit-testers` (recommended).** Short. Names why Play is in the Next. SOS + template are how testers don’t bounce.
2. **`sit-sos-template-testers`.** Honest bundle in the id. Ugly.
3. **`sit-one-path`** (already on disk, parks millwork and Play). Would need a rewrite of that file, not a reuse as-is.
4. **`sit-storm-factory` leftover.** Keep live Next and stuff this in. That is the unsuccessful progress mode.

**Selected:** **1 — `sit-testers`** (matches rec). Not sit-sos-template-testers. Not sit-one-path as-is. Not leftover storm-factory.

**Rationale / recommendation:** **Recommend option 1 (`sit-testers`).** (User picked it.) Option 4 is the failure. Option 3’s file currently parks the work you just unparked. Option 2 is a commit-message as an id.

**Cost tradeoff:** $0. Process only. Human `cp` + `aether next` + `aether approve`.

**Dependencies:** P1–P5. Unlocks PROPOSE CURRENT + `/handoff`.

**Status:** `decided`

---

### C0 — Why this app-development phase is in the current state (auto)

**Question (exactly as posed):**

> /grill-me-store-decisions on why this app development phase is in the current state. Go! (I feel misunderstood; what?)

**Options presented:** none (explored the tree instead of asking “are we stuck?”).

**Selected:** Record as diagnosis. The misunderstood thing is **what counted as progress**.

**Rationale / recommendation:** Skill: if the codebase answers it, do not ask. Facts on 2026-08-24:

1. **Live law** `CURRENT.md` Next **`sit-testers` APPROVED**. Objective is testers + first-sit 7B template + off-LAN then Play internal. Face kept. SOS parked (P2b). That bundle is *distribution of a sitting that is not finishable*.
2. **Decide is not a notepad.** `DecideModule.kt` is two giant pads (YES / NOT YET → AGAIN) then a `Dialog` whose only `OutlinedTextField` is Why, closed by a button labelled **Confirm**. Walkers must never tap Confirm. Agents must never `aether approve`. The writing surface for a decision therefore lives in the control humans forbade agents to use, and testers only reach after two arming taps. That is protocol theater, not Notepad.
3. **Plan already has paper; Draft is still ChatHome.** `PlanModule.kt` shows CURRENT.md + Changes chips + a Proposal well. `ChatHome.kt` is chips + composer + SEND (first-sit template copy). BEHAVIOURS B3/B4 still fail as a workshop (`app_verify` / `/app-reviewer` last closed FAIL on B3, B4, B7). So “standardise input as notepad” is a recant of *mixed modes*, not a polish of Decide pads.
4. **Send/receive is a lab toy, not a cycle.** `python/aether_inbox.py` `send_project` zips the pocket to `drop_hosts()` defaulting LAN (`127.0.0.1:8765`). Quiet drop → “Staged locally.” `poll_incoming` pulls an offer; Accept copies files and **skips live CURRENT.md**. Face: `SEND FOLDER` opens a preview panel (“Project in the works”). JOIN is still not tsnet-up. G4 mesh JNI is a named BLOCK. Off-LAN cellular AVD receipt exists; **LTE folder-send is not claimed**. Two seats exchanging a project is the named debug, and it is not done.
5. **Factory ate the Next.** `sit-storm-factory` succeeded as factory (STORM, FILES=OEM, depreciated inbox, glass) and failed as progress (user: unsuccessful progress-wise). Then SOS blit (`15_binary-skin`) then revert (`16_revert-skin`). Then first-sit template engine + hunk picker removal. Then Play-internal *prep* with no keystore and no Console login. storm-0 died at 10h `max_runtime`. adb empty. Each of those is a receipt. None of them is bind → plan → type a decision → send → the other seat receives.
6. **What was heard vs what was meant.** Agents heard: testers, Play, template ICM, keep the face, never Confirm. Agents did: store process, prompt stages, chrome, STORM. User meant: a **person** sits this face, **writes** like a notepad, and **the project actually moves**. Feeling misunderstood is that substitution.

**Cost tradeoff:**

- **v1:** $0 to name. Already spent **weeks** of engineer-time on factory/chrome/Play-prep vs **1–5 days** notepad + **2–7 days** a send path that is not `127.0.0.1`-only.
- **alpha (50):** Play internal $25 is wasted if testers hit Confirm-theater and SEND FOLDER that stages locally.
- **growth (1,000):** a broken cycle does not become a product by listing.
- **SaaS vs self-host:** drop is self-host. Public relay would be $5–20/mo and contradicts Funnel/public Reject.
- **Engineer-time:** C0 is 0. C1–C-law is the week that was postponed.
- **Crossover:** none — recording this is cheaper than another STORM round.

**Dependencies:** P-series closed; E7 inbox; G4 BLOCK; B6 two-tap+Why; live `sit-testers`. Unlocks C1 (notepad), then C2 (what “working send/receive” is), then C-law (one CURRENT vs leftover-under-sit-testers).

**Status:** `decided` (auto)

---

### C1 — What “standardise the decide input mode (normal notepad)” means

**Question (exactly as posed):**

> You said standardise Decide as a **normal notepad**. On disk, Decide is two giant pads (YES / NOT YET) that arm to AGAIN, then a dialog whose only text well is Why, submitted by **Confirm**. Plan already has a Proposal well; Draft is still ChatHome chips+composer. Walkers never tap Confirm. What is the notepad?

**Options presented:**

1. **Decide page is the notepad (recommended).** Full-page editable paper on DECIDE: they type Why (and can read/edit the proposal text) *on the page*. YES / NOT YET stay **small** two-tap actions on that paper so B6 still holds. The Confirm **dialog goes away**. One writing widget family as Plan’s Proposal well (OutlinedTextField / paper), not a second mode.
2. **Keep pads + dialog; only restyle the Why field** to look like Notepad (font, cream paper). Input *mode* stays dialog-after-arm. Cheapest. Still theater.
3. **Recant two-tap.** Decide is Notepad.exe: the document *is* the decision; one explicit Publish after they typed a non-empty Why. First tap can be Yes if Why is already on the page. Conflicts with current B6 “first tap is not Yes” unless we rewrite BEHAVIOURS with a human CURRENT.
4. **Hand off to the OEM notepad / FILES.** Decide opens the system editor on `PROPOSE-CURRENT.md` (or a Why file). Mechanicall only has Publish after they return. Honest files. Ugly sitting.

**Recommended:** **option 1.** Option 2 is the misunderstanding again (chrome). Option 3 is a real protocol change — only if you say the two-tap was the bug. Option 4 is FILES (already E8) pretending to be Decide.

**Selected:** —

**Rationale / recommendation:** Recommend **1**. “Normal notepad” is the *input mode*: you sit and write. The Why well must be on the page *before* arming, or testers never write and agents cannot demo Decide without tapping Confirm. Two-tap can remain so silence still is not permission. Standardise = same paper widget as Plan, not a new millwork family.

**Cost tradeoff:**

- **v1:** 1 = **1–3 engineer days** (DecideModule rewrite, app_verify B6 still two-tap+Why-required, walker still never Confirm because Confirm is gone). 2 = hours and no behaviour change. 3 = **2–5 days** + BEHAVIOURS rewrite + human CURRENT. 4 = hours Intent + loss of in-app sitting.
- **alpha / growth:** $0 SaaS. Bounce cost if testers cannot find where to type.
- **SaaS vs self-host:** N/A.
- **Crossover:** 1→3 only if you hate two-tap itself, not the dialog. 1→4 if Compose text fields keep eating IME (already a lab lesson). Never 2 if the rage is “not a notepad.”

**Dependencies:** C0 diagnosis; B6; P3 (Decide is Publish, not hunk pick — not recanted by 1). Unlocks C2 (send/receive definition), then C-law (does this recant `sit-testers` remainder / Play).

**Status:** `open`

---

### S0 — Obvious law (auto-approved, not re-asked)

**Question (exactly as posed):** *(not posed — skip-obvious)*

> Do we re-open Play Store, Funnel, model-approve, dual-Next, bind-this-repo, or “is Invite a Yes”?

**Options presented:** none this turn (would have been yes/no noise).

**Selected:** Keep live CURRENT: not Play, not Funnel, not public Ollama, not secrets in git, not dual-Next, not model Yes, bind ≠ this tree. Invite/Wake/Send/SAVE DRAFT/open/Bind are **not** Decide. This turn is grill-only.

**Rationale / recommendation:** Inherited principle 17 + CURRENT Reject/Limits. Re-asking would violate “Ask me for clarification” by wasting the first question on settled law.

**Cost tradeoff:**

- **v1:** $0. Re-asking costs a turn of attention.
- **alpha / growth:** Funnel or public `:11434` is a stolen-model incident, not a line item.
- **SaaS vs self-host:** N/A.
- **Engineer-time:** 0.
- **Crossover:** none.

**Dependencies:** live CURRENT; 2026-08-16 tree D8/D11/D7.

**Status:** `decided` (auto-approved)

---

### S1 — Who sits, and what is a “depreciated stranger”?

**Question (exactly as posed):**

> The 18 August sitting failed because Tailscale-as-me is not a send path, and the people-app lesson was: a stranger should not have to join your tailnet as you. You now want an in-app action so the **app user can accept an invitation to the tailnet**, then **boot myarch** and **send requests**, with a **depreciated stranger role**. Who is sitting in the app, and what does that role mean?

**Options presented:**

1. **Stranger-as-themselves, limited ACL (recommended).** They accept an invite under *their* identity — not `17kizymaa@`. Tailscale ACL writes them down: may reach wol-pi (wake) and myarch:11434 (Ollama), nothing else. “Depreciated stranger” = privilege written down, not operator. Invite / Wake / Send still not Yes. Matches the 2026-08-19 HANDOFF shape.
2. **Operator-only; stranger role retired.** The app user is you. Off-LAN A33 joins the tailnet so *you* can boot myarch and Send. “Depreciated stranger” means we **deprecate** the people-app fantasy of a sitter who never joins the private net. One operator device. Still not logging in as a friend.
3. **Me-as-stranger lab first.** You hold a second Tailscale identity (throwaway, not operator email) on a second device or login to prove the invite path. Same ACL as 1. Real friend later.
4. **No join.** Keep 18 August as law: strangers never join. Desk hop stays USB reverse / LAN. Invite chrome is docs. This **rejects** the spike you just named.

**Selected:** **1 — Stranger-as-themselves, limited ACL.** User picked the recommended option. Not operator-only. Not me-as-stranger lab. Not “no join.”

**Rationale / recommendation:**

**Recommend option 1.** Option 3 if no friend is in the room this week.

Why: you put “app user,” “accept an invitation,” and “depreciated stranger role” in one sentence. That is not “I join as myself.” Option 2 is the cheap reading (A33 already hops via USB reverse on LAN; Tailscale-as-you already exists) and it **erases** the word stranger. Option 4 contradicts the spike. Option 1 is the 19 August HANDOFF: Join as them, then Wake, then Send. “Depreciated” is read as **written-down ACL**, not as a typo for “deprecated” — unless you pick 2, which *is* that typo.

Code cannot answer this: `ChatHome.kt` hardcodes desk URLs `127.0.0.1` / LAN `192.168.0.51` / Tailscale `100.90.85.68`. There is **no** Join action in the APK. The 18 Aug receipt and the 19 Aug HANDOFF disagree about whether a stranger should ever be on the tailnet. Only you can pick.

**Cost tradeoff:**

- **v1 (solo):** $0 Tailscale. Option 1 = wol-pi must hold a Tailscale API token (not in git) and mint an invite/auth path + ACL tag = **3–10 engineer days**. Option 2 = hours if Tailscale app is already on the phone; deep-link / “open Tailscale” chrome. Option 3 = same build as 1 plus one extra identity (free). Option 4 = $0 and Send from LTE still prints `Desk quiet.`
- **alpha (50):** Operator tailnet + 50 tagged strangers is operationally a product. Tailscale Personal Plus $0–6 or Headscale ~$5 VPS. Do not put 50 people on your personal tailnet without intending to.
- **growth (1,000):** Wrong topology. At that scale the people-app lesson (no join) is cheaper than a shared Ollama tailnet. Crossover around a handful of sitters: leave 1, stop sharing the desk model.
- **SaaS vs self-host:** Tailscale Personal $0 vs Headscale $0+VPS. Public Ollama $0 and an incident. No BaaS.
- **Engineer-time:** 1/3 blocked on “how they accept” (S2) — Android cannot `tailscale up --authkey` inside Mechanicall without Tailscale app or embedding libtailscale. 2 can ship a status lamp + Wake without minting invites.
- **Crossover:** 1→2 if the only phone that matters this week is yours. 1→3 until a real stranger exists. Never 4 if boot-myarch-from-LTE is the point.

**Dependencies:**

- Depends on: live CURRENT (desk-hop clause already names boot myarch); 18 Aug LESSONS.md; 19 Aug HANDOFF.md; B4/B7 (Send needs a live desk).
- Unlocks: S2 (accept mechanism), S3 (where Join lives), S4 (WoL vs assume up), S6 (stay under `ACTUAL-APP-VERIFICATION` vs human re-SELECT — stranger-as-themselves is a product door; operator-only is already desk-hop).

**Status:** `decided`

**Post-select note:** “Depreciated” is **written-down privilege**, not a typo for “deprecated the stranger.” 18 August is revised, not reversed: they still must not sit as `17kizymaa@`. They *do* join, tagged. Next fork is how Mechanicall accepts without the APK pretending it is Tailscale.

---

### S2 — How does the app user *accept* the invitation on the phone?

**Question (exactly as posed):**

> S1 locked stranger-as-themselves with a limited ACL. Tailscale will not let a random APK invite people with no operator secret. The 2026-08-19 HANDOFF said: until wol-pi holds a Tailscale API token (not in git) and mints a path, a button labelled Invite is decoration. For this **quick spike**, how does Mechanicall let them accept?

**Options presented:**

1. **JOIN opens an invite URL / Tailscale app (recommended for a quick spike).** Operator (or later wol-pi) mints an invite. Mechanicall shows Join-network status: not-on-net / invited / connected. The actual accept happens in **Tailscale’s app** (or the invite link in a browser). Mechanicall then probes wol-pi + myarch. Honest. Days, not weeks. Invite is still not Yes.
2. **wol-pi mints an auth key; Mechanicall requests it.** wol-pi holds the Tailscale API token off-git. App sends *their* email; box returns an invite/auth path. User still needs Tailscale app (or option 3) to consume the key. This is the durable door. Not “quick.”
3. **Embed libtailscale in the APK.** Mechanicall *is* the Tailscale client. VPN permission, fat APK, not a quick spike. Only pick if you refuse a second app on the phone.
4. **QR of invite URL or pre-auth key.** Operator shows a QR from the desk; stranger scans in Mechanicall; that opens Tailscale (same as 1 with a sitting-room prop). Lab-friendly. Still not a secret in git.
5. **Paste key only.** They already installed Tailscale; Mechanicall is a well + “open Tailscale.” Weakest “action in the app.”

**Selected:** **1 — JOIN opens invite URL / Tailscale app.** User picked the recommended option. Not wol-pi mint this spike. Not libtailscale. Not QR. Not paste-only.

**Rationale / recommendation:**

**Recommend option 1** for this spike. Keep **2** as the durable follow-on, not this week’s chrome.

Why: you said *quick spike* and *an action that allows the app user to accept*. The action can be **Join** that hands them the minted invite and then **watches the net** — that is an action, not a homework assignment at `login.tailscale.com` *inside Mechanicall’s story*. Option 2 is what HANDOFF called the honest gap; it needs a tiny always-on service + token hygiene. Option 3 is a VPN product. Option 4 is 1 with a QR. Option 5 is decoration with extra typing.

**Cost tradeoff:**

- **v1:** $0 Tailscale. 1 = 1–3 engineer days (chrome + reachability probe + ACL already on tailnet). 2 = +3–7 days wol-pi service, token in a file the APK never sees. 3 = 1–3 weeks embed + VPN UX. 4 = +0.5 day scan. 5 = hours and a lying Join button.
- **alpha (50):** 1 still means *you* mint invites (admin click or a later 2). 2 is the only way 50 invites are not your weekend. 3 is an MDM-shaped product.
- **growth (1,000):** None of these belong on the operator tailnet. Headscale or stop sharing Ollama. Dollar delta still << the support cost of 1,000 tagged strangers.
- **SaaS vs self-host:** Tailscale API on wol-pi is still self-host. Do not put the token in Play-distributed APK (that is a secret in a zip).
- **Engineer-time:** 1 is the spike. 2 is the factory. 3 is a different app.
- **Crossover:** 1→2 when you are tired of admin-click invites. 1→3 only if the sitter will not install Tailscale. Never 5 as the only door.

**Dependencies:**

- Depends on: S1=1 (they join as themselves).
- Unlocks: S3 (where Join lives), S4 (Wake only when connected), S6 (1 can sit under desk-hop verification; 2/3 look like a new Next).

**Status:** `decided`

**Post-select note:** Durable mint (S2 option 2) stays an open *later* branch, not a silent extra Next. JOIN ≠ Yes. Connected ≠ permission to write CURRENT.

---

### S2b — Where Send goes once they are connected (auto-approved)

**Question (exactly as posed):** *(not posed — skip-obvious given S1+S2)*

> Once Join=connected, which Ollama URL does Send use?

**Options presented:** none this turn.

**Selected:** **Tailscale myarch `http://100.90.85.68:11434` when Join=connected.** USB reverse `127.0.0.1:11434` remains operator lab (A33 on cable). LAN `192.168.0.51` is desk-wifi, not a stranger on LTE. Public bind and Funnel stay refused. `_refuse_public_ollama` unchanged.

**Rationale / recommendation:** S1 already limited them to myarch:11434. A stranger will not have `adb reverse`. Re-asking would be noise.

**Cost tradeoff:**

- **v1:** $0. Probe the one Tailscale URL; GATE quiet if down.
- **alpha / growth:** still one desk. Do not add a URL picker as a product.
- **SaaS vs self-host:** N/A.
- **Engineer-time:** hours to prefer Tailscale host when connected; stop listing 127.0.0.1 as if the sitter had USB.
- **Crossover:** none this spike.

**Dependencies:** S1=1, S2=1. Unlocks S4 (Wake is what makes that URL live).

**Status:** `decided` (auto-approved)

---

### S4 — How does the phone **boot myarch** once they are on the tailnet?

**Question (exactly as posed):**

> You said accept-invite is **to enable the boot myarch and send requests**. JOIN (S2) gets them on the net. Send (S2b) talks to `100.90.85.68:11434`. myarch **sleeps**. wol-pi is already on the tailnet and can Wake-on-LAN. What is the boot action in Mechanicall for this spike?

**Options presented:**

1. **WAKE DESK via wol-pi (recommended).** Only enabled when Join=connected. Mechanicall calls a tiny tailnet-only endpoint on wol-pi that sends a magic packet to myarch’s MAC. Status: sleeping / waking / up. Probe `:11434`. Wake is not Yes. If already up, status is up and Send works.
2. **Assume myarch is already up.** JOIN only unlocks Send. Boot stays `scripts/boot-desk.sh` on the machine. Phone never WoL. Cheaper; you said “boot myarch” in the same sentence as the invite.
3. **JOIN + probe only this spike; WoL is a follow-on.** Quickest honest slice. Button labelled Wake is decoration until wol-pi HTTP exists — same lie HANDOFF warned about for Invite. Only pick if you want Join-and-Send *this week* and Wake later.
4. **SSH from the phone to myarch to start Ollama.** Requires myarch already up enough for SSH. That is not wake-from-sleep.

**Selected:** **1 — WAKE DESK via wol-pi.** User picked the recommended option. Not assume-up. Not defer WoL. Not SSH.

**Rationale / recommendation:**

**Recommend option 1.** wol-pi is already the always-on box. CURRENT’s desk-hop clause is “anyone can boot this desk.” A stranger on LTE cannot run `boot-desk.sh` on myarch. Option 2 leaves them connected to a sleeping box and Send prints `Desk quiet.` Option 3 is the real “quick” if you will not stand up a 30-line HTTP on wol-pi this spike — say so. Option 4 is not boot.

Honest gap: the WoL HTTP on wol-pi is **not** in tree yet, same class as the invite mint you just deferred. Wake needs a small service + MAC + token/ACL, **not** in git. If you pick 1, this spike includes that service, not only APK chrome.

**Cost tradeoff:**

- **v1:** $0 hardware (wol-pi exists). 1 = 1–3 days wol-pi HTTP + APK button + probe. 2 = $0 extra, Send fails when the desk sleeps. 3 = 0 days WoL, a documented follow-on. 4 = hours and still no sleep-wake.
- **alpha (50):** one wol-pi still wakes one desk. 50 sitters hammering WoL is fine; 50 desks is a different product.
- **growth (1,000):** not a WoL fleet. Stop.
- **SaaS vs self-host:** a public wake URL is Funnel-shaped and **rejected**. Tailnet-only HTTP on wol-pi is $0.
- **Engineer-time:** 1 is the meaning of “enable boot.” 3 is the meaning of “quick.” They fight; you pick.
- **Crossover:** 1→3 only if wol-pi SSH this week is blocked. Never a public `/wake`.

**Dependencies:**

- Depends on: S1=1, S2=1, S2b (Send URL).
- Unlocks: S3 (Join + Wake as two controls, not a fifth planning page), S6 (1 adds a wol-pi service — may want human re-SELECT; 3 stays under `ACTUAL-APP-VERIFICATION` more easily).

**Status:** `decided`

**Post-select note:** wol-pi HTTP is in scope for this spike. Secrets (Tailscale token if any, MAC) stay off git and off the APK. Public wake URL remains Reject. GATE stays quiet until `:11434` answers.

---

### S3 — Where do JOIN and WAKE live, and can they fire before BIND?

**Question (exactly as posed):**

> Bind-first (B1): unbound, **FOLDER** is the only *planning* module. HANDOFF: Join + Wake are two extra controls, **not** the main story — Plan / Draft / Decide / Receipt stay the product. You now have **both** JOIN (S2) and WAKE (S4). Where do they sit, and may a stranger accept the invite **before** they bind a folder?

**Options presented:**

1. **Network strip beside DIR; Join allowed unbound (recommended).** Compact DESK strip: Join status + Wake. Not a fifth rail page. Unbound still shows FOLDER as the only module, plus the strip so they can accept the invite before picking a project. Wake enabled only when connected. Gray-outside still closes *modules*, not the strip.
2. **Only after bind.** Unbound is FOLDER only — no Join. They must pick a pocket before they can accept the invite. Visually pure B1; makes the net a sitting step, not a door.
3. **FILES-style overlay** from a DESK chip. Same dimmer + gray-outside-closes rule as FILES. Strip hidden until they open DESK.
4. **Fifth rail page NETWORK.** Plan / Draft / Decide / Receipt / Net. Rejects “not a fifth planning page” unless you insist the net *is* a module.
5. **Inside Draft only.** Join/Wake appear as Send’s prerequisites. Hidden if they never open Draft. Makes the desk feel like chat infrastructure.

**Selected:** **2 — Only after bind.** User override of recommendation (was 1 — strip + Join unbound). Unbound is FOLDER only — no Join. They must pick a pocket before they can accept the invite.

**Rationale / recommendation:**

**Recommend option 1.**

Why: accepting a tailnet invite is **not** binding a CURRENT. A stranger on LTE should be able to Join, then Wake, then Bind their folder, then Send. Option 2 couples two different verbs and fails the first-mile story (HANDOFF: Invite is the first mile). Option 3 hides the lamp. Option 4 becomes a verb museum. Option 5 makes Draft the network panel and fights B4 (Draft is a proposal workshop, not a modem).

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 0.5–1.5 days chrome on the existing DIR row. 2 = slightly less chrome, worse first-run. 3 = reuse FILES dialog (hours) but an extra tap. 4 = +1 day a page you already rejected as identity. 5 = pollutes ChatHome.
- **alpha / growth:** none of these are SaaS. Support cost is “where is Join,” not dollars.
- **SaaS vs self-host:** N/A.
- **Engineer-time:** 1 is the smallest honest surface for two controls.
- **Crossover:** 1→3 if the strip crowds the LCD. Never 4 while four modules are the walk.

**Dependencies:**

- Depends on: S2=1, S4=1, B1 bind-first.
- Unlocks: S5 (Draft stays a workshop, not a modem), S6 (chrome vs new Next).

**Status:** `decided`

**Post-select note:** First mile is **bind, then Join, then Wake, then Send**. HANDOFF’s “invite before the sitting” is declined. Bind-first stays the product door. JOIN/WAKE still must not become a fifth planning page — after bind they live as a compact strip beside DIR (the non-page half of rec 1). If that strip placement is wrong, say so on S3b; otherwise treat as leaf.

---

### S5 — What is still missing so Draft is actually a proposal-editing workshop?

**Question (exactly as posed):**

> Last-pass already: **PLAN** has editable CAPITALISED wells + SAVE DRAFT (writes `PROPOSE-CURRENT.md`, never CURRENT). **DRAFT** shows LIVE vs DRAFT rows, field chips that fill the composer, one last desk say, SEND (no bubbles, no `…`). B4 can PASS the scorer while you still hate the module. You said Draft should **actually** be a proposal editing workshop. What is the workshop?

**Options presented:**

1. **Dual-pane Draft: LIVE locked | PROPOSE editable (recommended).** Left: live CURRENT fields, read-only. Right: the proposal wells, editable, SAVE DRAFT. SEND talks to the desk and **patches the right pane in place** (not “open Plan to read it,” not a chat log). Plan keeps its editor (B3). Same file: `PROPOSE-CURRENT.md`.
2. **Draft becomes the only editor; Plan is live read-only again.** One workshop surface. Undoes last-pass’s “edit on PLAN” as the place people type.
3. **Full markdown PROPOSE document on Draft.** Observations / Inferences / Unknowns / Proposed CURRENT change — a real proposal doc, not only AUTHORITY_FIELDS wells. Heavier; matches 2026-08-16 “session is a proposal-doc editor” more literally.
4. **Last-pass + apply-into-Plan.** Draft stays a comparison + composer; Send writes fields and Plan wells refresh. Weakest “workshop on Draft.”
5. **Bring bubbles back as a third mode.** Rejected identity (chat-app chrome).

**Selected:** **Other — “Just let us redesign the plan-draft modules based on standards from researching vendors/AI platforms!”** User override of recommendation (was 1 — dual-pane Draft). Not 2 (Draft-only editor). Not 3 (full markdown picked a priori). Not 4 (last-pass+apply). Not 5 (bubbles).

**Rationale / recommendation:**

**Recommend option 1.**

Why: the gap is not “can anyone edit a field somewhere” (Plan already can). The gap is **Draft does not let them work the proposal**. Rows are labels; the composer is a chat; desk say does not land in wells. Dual-pane is the workshop table: what is law vs what is proposed, both visible, only the proposed side types, desk paints that side, Decide is still the only Yes. Option 2 throws away B3’s home. Option 3 is a later document editor (worth a follow-on). Option 4 leaves Draft as a spectator. Option 5 is Reject.

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 1–2 days Compose + make `draft_chat` return fields into the right pane (engine already returns `fields` + `schema_draft`). 2 = same plus ripping Plan wells. 3 = 2–4 days markdown editor + parse round-trip. 4 = hours. 5 = −identity.
- **alpha / growth:** still one file. No SaaS. Support cost is “which pane is law.”
- **SaaS vs self-host:** N/A.
- **Engineer-time:** 1 is the spike clause you named. 3 is a second spike.
- **Crossover:** 1→3 when CAPITALISED fields are too small for a real CURRENT proposal (long Objective/Keep). Never 5.

**Dependencies:**

- Depends on: B3/B4 last-pass; inherited D10 (proposal-doc editor); S3 (Draft is not the modem).
- Unlocks: S6 (scope vs CURRENT Next); implement Go.

**Status:** `decided`

**Post-select note:** Both **Plan and Draft** are in the redesign (user said “plan-draft modules”). Vendor standards inform chrome and interaction; they do **not** get to make the model Yes, write CURRENT, or restore chat-as-identity. S5b must name the research set or this decision is an unbounded mood.

---

### S5b — Which vendors / AI platforms are the research set?

**Question (exactly as posed):**

> You overrode the dual-pane guess: redesign Plan + Draft from **standards from researching vendors/AI platforms**. Which shortlist is the standard? (Research will still refuse chat-as-identity, model-Yes, and Funnel — CURRENT outranks vendor taste.)

**Options presented:**

1. **Plan-vs-apply faces (recommended).** Cursor Plan/Ask, Claude Code plan-mode, Linear cycles / “proposed change,” Notion AI draft-vs-publish, GitHub PR (files changed + approve). Shared pattern: a proposed artifact, a live/base artifact, a human apply. Closest to PROPOSE vs CURRENT.
2. **Document-workshop faces.** Google Docs Suggesting, Word Track Changes, Overleaf, HackMD. Shared pattern: inline proposal, accept/reject per hunk. Heavier editor.
3. **Canvas / artifact faces.** ChatGPT canvas, Claude artifacts, Cursor composer pane. Shared pattern: side doc the model edits. High risk of sliding back into chat identity — only pick if we steal the *doc*, not the *bubbles*.
4. **Agent-runner faces.** Devin, OpenHands, Grok swarm, Codex. Wrong sitter. Those are operator/CI. SeatMate already said not this.
5. **You name 2–4 products.** If the shortlist in your head is specific, pick this and type the names next.

**Selected:** **2 — Document-workshop faces.** User override of recommendation (was 1 — plan-vs-apply). User continue: “That last question was spot-on!” Shortlist: Google Docs Suggesting, Word Track Changes, Overleaf, HackMD. Not Cursor/Linear/PR as the primary set. Not canvas. Not Devin/OpenHands.

**Rationale / recommendation:**

**Recommend option 1.** Steal the *shape*: proposed plan vs live plan vs human apply. Do not steal Cursor’s chat stack or GitHub’s login. Option 2 if you want hunk-level Yes (conflicts with Decide as the only Yes — we’d accept hunks into PROPOSE, not CURRENT). Option 3 is how last-pass happened (artifact that is still a chat). Option 4 is the 18 Aug OpenHands temptation. Option 5 if you already know the three apps you trust.

**Cost tradeoff:**

- **v1:** $0 licenses if we only *read* public UX. 1 = 0.5–1 day research + 3–5 days redesign. 2 = +markdown/hunk UI (week). 3 = faster visually, identity risk. 4 = wasted research.
- **alpha / growth:** copying a vendor too closely can look like their product (support confusion), not a dollar line.
- **SaaS vs self-host:** research must not import their cloud as the engine. Chaquopy + files stay.
- **Engineer-time:** unbounded if the set is “all AI platforms.” That is why this question exists.
- **Crossover:** 1→2 when CAPITALISED fields cannot hold a real proposal. Never 4.

**Dependencies:**

- Depends on: S5 Other.
- Unlocks: research stage folder; S6 (this is no longer a “quick” Draft polish — law question gets sharper).

**Status:** `decided`

**Post-select note:** Rec 1 warned hunk-level Yes conflicts with Decide-only Yes. They picked the hunk faces anyway — so **S5c is now the load-bearing fork**, not a leaf. Research reads Docs/Word/Overleaf/HackMD for *interaction*, then maps accept/reject onto PROPOSE unless they explicitly blow up Decide.

---

### S5c — In Docs Suggesting, accept writes the document. What does a hunk accept write here?

**Question (exactly as posed):**

> You picked **document-workshop faces** (Google Docs Suggesting, Word Track Changes, Overleaf, HackMD) and said that shortlist was spot-on. In those products, **accepting a suggestion publishes it into the doc**. Here, CURRENT is the published doc and **only Decide** writes it. How do we steal Suggesting without stealing apply-as-you-go?

**Options presented:**

1. **Draft is Suggesting on a proposal copy (recommended).** Plan = published CURRENT (read, or last-pass wells that still save as draft). Draft = PROPOSE with inline diffs vs live. Accept/reject hunks mutate **PROPOSE only**. Decide = Publish (two-tap + Why). Desk Send inserts suggestions, not law. This is Word if the review copy is not the published file.
2. **Plan is the document in Suggesting mode.** They look at live CURRENT with suggestion marks; every edit is a suggestion whose bytes live in PROPOSE until Decide. One surface. Risk: it *looks* like they are editing law.
3. **Visual diffs only — no per-hunk accept.** Steal the look (red/green vs live). SAVE DRAFT writes the whole proposal. Fastest. Weakest “workshop” vs Docs.
4. **Accept hunk writes CURRENT.** Illegal. Same as model-Yes with extra taps. Do not pick.

**Selected:** **1 — Draft is Suggesting on a proposal copy.** User picked the recommended option. Not Plan-as-Suggesting. Not visual-only. Not hunk-writes-CURRENT.

**Rationale / recommendation:**

**Recommend option 1.**

Why: Docs Suggesting works because there is one doc. Mechanicall has two files on purpose. The honest steal is: **PROPOSE is the review copy**, CURRENT is published, Decide is Publish. Option 2 is more “spot-on Docs” visually and more likely to fail a STORM (“they thought the yellow text was law”). Option 3 is last-pass with prettier paint. Option 4 is prohibited.

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 5–8 days hunk UI + `write_schema_draft` / PROPOSE patcher (engine already writes PROPOSE; hunks are chrome + a small Python apply-to-propose). 2 = same plus a lying Plan. 3 = 1–2 days paint. 4 = $0 and a law incident (repair >> build).
- **alpha (50):** 1 scales as “review copy vs published.” 2 scales as support tickets. 4 does not scale — it is a protocol bug.
- **growth (1,000):** still files. No Google Docs API, no Drive. Dollar delta vs real Docs: $0 vs $0–12 Drive + a second CURRENT.
- **SaaS vs self-host:** do not import Google Suggesting API. Reimplement marks on local markdown.
- **Engineer-time:** hunk accept is the week you just bought with S5b. Option 3 declines that week.
- **Crossover:** 1→3 only if hunk UI misses this calendar. Never 4. 1→2 only if they will not open two modules.

**Dependencies:**

- Depends on: S5 Other, S5b=2, S0b (Decide-only Yes).
- Unlocks: S5d (what the document *is*: whole CURRENT.md vs fields vs PROPOSE fenced block), S6 (this is a redesign Next, not a verification polish).

**Status:** `decided`

**Post-select note:** Docs semantics mapped: **PROPOSE is the review copy**, CURRENT is published, Decide is Publish. Hunk accept is not Yes. Desk suggestions land in Draft, never in CURRENT. Next fork is what bytes the review copy *is*.

---

### S5d — What is the document the workshop edits?

**Question (exactly as posed):**

> S5b+S5c locked a **document** workshop: Draft is Suggesting on a proposal copy; hunks write PROPOSE; Decide publishes. Last-pass only edits **AUTHORITY_FIELDS** (Objective, Next, Keep…). A real CURRENT.md is a whole markdown file. What is “the document”?

**Options presented:**

1. **Whole `CURRENT.md` as markdown (recommended for the faces you picked).** Plan shows the published file (Objective/Next first, rest scrollable). Draft shows PROPOSE as a suggested rewrite of that file with inline hunks. Accept/reject is per section/hunk. CAPITALISED fields are anchors, not the only editable atoms. Matches Docs/Overleaf.
2. **AUTHORITY_FIELDS only.** Each field is a hunk (accept this Objective, reject that Next). Fast. Not really a document workshop — it is last-pass wells with Docs paint.
3. **Only the fenced “Proposed CURRENT change” block** inside `PROPOSE-CURRENT.md`. The rest of the proposal (Observations / Inferences / Unknowns) is read or lightly edited. Smaller surface; closer to `aether` propose-current shape.
4. **Two layers.** Fields are first-class hunks on the first screen; “the rest of the file” expands into markdown Suggesting. Spike can ship fields first without lying that the whole doc is done.

**Selected:** **1 — Whole `CURRENT.md` as markdown.** User picked the recommended option. Not fields-only. Not fenced-block-only. Not two-layer ship-slice.

**Rationale / recommendation:**

**Recommend option 1**, with **4** as the honest ship-slice if the whole-file editor blows the calendar.

Why: you overrode plan-vs-apply *and* said the document-workshop shortlist was spot-on. Wells-only (2) wastes that. Option 3 is the protocol nerd’s copy (the fenced block) and hides the proposal’s argument. Option 1 is Overleaf-on-CURRENT. Option 4 is how a spike does 1 without drowning a first sitting.

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 6–10 days markdown Suggesting (diff, hunks, IME, a small phone). 2 = 2–3 days on existing wells. 3 = 3–5 days on one fence. 4 = 3–4 days fields + a stub expander, then 1 later.
- **alpha (50):** 1 needs “their CURRENT is a real doc.” 2 needs “their CURRENT is a form.” People-app pitch is a plan they can *read* — that argues 1.
- **growth (1,000):** still files. A markdown editor is not Google Docs $12. Do not embed a Docs iframe.
- **SaaS vs self-host:** no Docs API. Local markdown + `difflib` / a tiny Python hunk apply.
- **Engineer-time:** 1 is the week+ you bought. 4 is the spike that still points at 1.
- **Crossover:** 1→4 this calendar if IME+hunks on A33 fight. Never 2 after S5b=2 unless you recant the shortlist.

**Dependencies:**

- Depends on: S5c=1 (review copy is PROPOSE).
- Unlocks: S6 (whole-file workshop + wol-pi + stranger join is not `ACTUAL-APP-VERIFICATION` polish).

**Status:** `decided`

**Post-select note:** Option 4 (fields first, file later) was the calendar hedge; they declined it. This spike’s Draft is an Overleaf-shaped review copy of the **whole** bound CURRENT, not a form. IME + hunks on A33 is the engineering risk, not a new product fork.

---

### S6 — Does this stay under live Next `ACTUAL-APP-VERIFICATION`, or do you re-SELECT?

**Question (exactly as posed):**

> Live CURRENT **Next** is `ACTUAL-APP-VERIFICATION` (APPROVED). Body Action id still says `mechanicall-debug-window`. Dual-concurrent-next is prohibited. This grill locked: stranger-as-themselves JOIN, WAKE via wol-pi HTTP, and a **whole-file document workshop**. That is not a verification polish. Where does the work live so we do not lie about law?

**Options presented:**

1. **Stay under `ACTUAL-APP-VERIFICATION` as lab.** No CURRENT rewrite. After this tree + your Go, implement as “desk-hop + B4 actually.” Fastest. Stretches the Next (verification factory vs new door + redesign).
2. **You apply a PROPOSE, then `aether next` one new action-id** that covers **both** clauses (join+wake **and** document workshop). One Next, honest name. I will **not** run `next` / `approve`. Cleanest. **Recommended.**
3. **Two Nexts** — invite/wake first, workshop second. Dual-concurrent-next / splitting. Prohibited unless you recant that Reject.
4. **Grill-only freeze.** Tree is the spec. No implement, no PROPOSE file, until a later Go on a later Next. Safest; fights “quick spike.”

**Selected:** **1 — Stay under `ACTUAL-APP-VERIFICATION` as lab.** User override of recommendation (was 2 — apply PROPOSE then `aether next` one new id). Not two Nexts. Not grill-only freeze.

**Rationale / recommendation:**

**Recommend option 2.**

Why: S1 already made a product door the people-app left. S4 adds a wol-pi service. S5d adds a week of markdown Suggesting. Calling that `ACTUAL-APP-VERIFICATION` is the same class of lie as body Action id `mechanicall-debug-window` while the header says something else. Option 1 is what we did when you said “quick spike” at kickoff — the tree is no longer quick. Option 3 is Reject. Option 4 if you are done for today and want the spec only.

I still will not rewrite CURRENT. If you pick 2, I can draft `examples/propose-current/…` for **you** to apply.

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 0 process days, muddy receipts. 2 = 10–20 minutes of your `aether next` + `approve`. 3 = process fight + dual work. 4 = $0 build.
- **alpha / growth:** N/A. This is operator law, not SaaS.
- **SaaS vs self-host:** N/A.
- **Engineer-time:** the build is the same under 1 or 2. 2 saves repair time when the next agent reads CURRENT.
- **Crossover:** 2→1 only if you explicitly want to keep this Next’s APPROVED clock and accept the stretch. 2→4 if you are closing the sitting.

**Dependencies:**

- Depends on: S1=1, S4=1, S5d=1, live CURRENT, dual-Next Reject.
- Unlocks: close this interview; optional PROPOSE draft; implement only after Go.

**Status:** `decided`

**Post-select note:** Header/body Action-id drift remains (`ACTUAL-APP-VERIFICATION` vs `mechanicall-debug-window`). They accepted the stretch: join+wake+whole-file workshop counts as desk-hop + B4 “actually.” Receipts must still say lab, not “CURRENT changed.” I do not rewrite CURRENT to match.

---

### S3b — After bind, JOIN+WAKE chrome (auto-approved leaf)

**Question (exactly as posed):** *(not posed — skip-obvious given S3=2 + HANDOFF)*

> Exact placement of the two controls after bind?

**Selected:** Compact **DESK strip beside DIR** (Join status + Wake). Not a fifth rail page. Not inside Draft. Not FILES overlay unless the strip crowds the LCD later.

**Rationale / recommendation:** S3 declined unbound Join but did not decline the strip. Fifth page was already rejected. Draft is a document workshop (S5c), not a modem.

**Cost tradeoff:** v1 $0; 0.5 day chrome. Alpha/growth N/A. Engineer-time: leaf.

**Dependencies:** S3=2, S4=1, S5c=1.

**Status:** `decided` (auto-approved)

---

### S7 — Who mints or carries the invite URL that JOIN opens?

**Question (exactly as posed):**

> S2 locked **JOIN opens an invite URL / Tailscale app** and **deferred wol-pi mint**. S6 locked implement-as-lab under this Next. Tailscale still will not let the APK mint users. Who provides the link the sitter accepts?

**Options presented:**

1. **Operator invites out-of-band; sitter pastes or taps the link in Mechanicall (recommended).** Operator still clicks Invite at Tailscale admin with **their** email (not `17kizymaa@`). Mechanicall after bind: well + JOIN opens that URL (or Tailscale). Status lamp + probe. Honest. Same mint as 18 Aug; the *accept* is now in the app.
2. **JOIN only opens the Tailscale app.** No URL well. They must already have the invite in email and complete it in Tailscale. Mechanicall just probes connected/not. Weakest “action that allows accept.”
3. **Invite URL baked into the APK or a git file.** A secret-shaped door. Do not pick.
4. **wol-pi mint anyway this spike.** Contradicts S2. Only pick if you recant “JOIN opens Tailscale, mint later.”

**Selected:** **1 — Operator invites out-of-band; sitter pastes or taps the link in Mechanicall.** User picked the recommended option. Not Tailscale-app-only. Not baked URL. Not wol-pi mint this spike.

**Rationale / recommendation:**

**Recommend option 1.**

Why: you wanted an **action that allows the app user to accept**. That is Join opening *their* invite, not a homework assignment they do entirely outside Mechanicall (2), not a token in git (3), not the week of mint you already deferred (4). Operator still does the admin click — HANDOFF called that decoration if Join isn’t in the app; with 1, the accept is in the app and the mint stays human.

**Cost tradeoff:**

- **v1:** $0. 1 = hours (well + intent to URL + probe). 2 = minutes and a weaker verb. 3 = $0 and a leaked invite. 4 = 3–7 days you already parked.
- **alpha (50):** 1 does not scale (your weekend). That is why S2 parked mint. 50 sitters is S2 option 2 later, not this lab.
- **growth (1,000):** not this tailnet.
- **SaaS vs self-host:** N/A. No token in APK.
- **Engineer-time:** 1 is the JOIN chrome. 4 is a second project.
- **Crossover:** 1→4 when you are tired of admin-click. Never 3.

**Dependencies:**

- Depends on: S2=1, S1=1, S3=2 (after bind), S6=1 (lab).
- Unlocks: close interview if no remaining non-obvious forks; implement only after Go.

**Status:** `decided`

**Post-select note:** 18 Aug mint path stays human. The spike is the **accept** verb + status, not a Tailscale API. `kamilas-tab` as `17kizymaa@` remains not a stranger.

---

### S8 — After you say Go, what runs first?

**Question (exactly as posed):**

> Tree is almost closed. S5 demanded vendor research. S6 said implement as lab under this Next after Go. S1–S7 locked join+wake **and** whole-file Suggesting. What is the first stage when you say Go? (Still one Next. I still do not implement this turn.)

**Options presented:**

1. **Research first, then implement (recommended).** Numbered ICM folder: `01_research/` of Docs Suggesting / Word Track Changes / Overleaf / HackMD → FACE-SPEC for Plan=published, Draft=PROPOSE Suggesting (whole CURRENT.md) + JOIN/WAKE strip. You review `output/`. Then `02_implement/`. Matches S5 and meta-agent gates.
2. **JOIN+WAKE first, workshop second — same Next.** Prove stranger-on-LTE: bind → paste invite → Tailscale → Wake wol-pi → Send. Workshop redesign after that receipt. Faster sitting; research waits.
3. **Workshop first, net second — same Next.** USB desk-hop already exists for the operator. Document workshop is the hated module. Stranger door waits.
4. **No research folder; just build both.** Fights S5. Fastest pixels, weakest standard.

**Selected:** **1 — Research first, then implement.** User picked the recommended option. Not JOIN-first. Not workshop-first. Not skip-research.

**Rationale / recommendation:**

**Recommend option 1.**

Why: you overrode dual-pane *because* you wanted standards, and you said the vendor-shortlist question was spot-on. Skipping research now would throw S5 away. Option 2 is the real “quick spike” for clause 1 if the sitting is this week — say so. Option 3 matches “Draft is the problem” if the net can wait. Option 4 is vibe chrome.

**Cost tradeoff:**

- **v1:** $0. 1 = 0.5–2 days research + human gate, then 1–2 weeks build (wol-pi HTTP + JOIN + markdown Suggesting). 2 = 2–4 days net, workshop later. 3 = 6–10 days workshop, net later. 4 = same build time, worse FACE-SPEC.
- **alpha / growth:** N/A.
- **SaaS vs self-host:** research is reading public UX, not buying Docs.
- **Engineer-time:** 1 front-loads the week you already bought. 2/3 split the calendar inside one Next (legal under S6).
- **Crossover:** 1→2 if a stranger is in the room before research lands. 1→3 if you will not sit a stranger this week.

**Dependencies:**

- Depends on: S5, S5b=2, S6=1, S7=1, S4=1.
- Unlocks: **close this interview.** Next human line is Go / proceed to 01_research / or edit the tree.

**Status:** `decided`

**Post-select note:** Interview **closed**. Next human line is **Go** (create `dev/37_tailnet-invite-draft-workshop/` and run `01_research/`) or an edit to this tree. Silence is not Go.

---

## Locked spike spec (closed 2026-08-20)

Not CURRENT. Not Yes. Lab under live Next `ACTUAL-APP-VERIFICATION`.

**Walk (after bind):** FOLDER → JOIN (paste/tap their invite) → Tailscale accept → WAKE DESK (wol-pi magic packet) → Plan (published CURRENT.md) / Draft (PROPOSE Suggesting on the whole file) → Send (desk inserts suggestions) → Decide (Publish).

**Net**

- Stranger-as-themselves, limited ACL (wol-pi + `100.90.85.68:11434` only). Not `17kizymaa@`.
- JOIN after bind only. Compact DESK strip beside DIR. Not a fifth page. Not inside Draft.
- Operator Invite at Tailscale admin, out of band. Sitter pastes/taps the link in Mechanicall. No mint API this spike. No URL in git.
- WAKE = tailnet-only HTTP on wol-pi. No public `/wake`. Secrets off git / off APK.
- Send host when connected: Tailscale myarch. USB reverse is operator lab.

**Workshop**

- Research set: Google Docs Suggesting, Word Track Changes, Overleaf, HackMD.
- Plan = published CURRENT.md (Objective/Next first, rest of file).
- Draft = Suggesting on PROPOSE copy of that whole file. Hunk accept/reject writes PROPOSE only.
- Decide = Publish (two-tap + Why). Desk Send inserts suggestions, not law.
- CAPITALISED fields are anchors, not the only atoms.

**After Go:** `01_research/` → FACE-SPEC in `output/` → human review → `02_implement/`.

---

### S9 — What is the visual-development fork? (avoid / refine the VST UI-fail)

**Question (exactly as posed):**

> The painted VST / Camel Space plugin already **failed as UI** (CURRENT baseline: people-app-phone-seat REJECTED as painted VST; last-pass still fills the activity with yellow LCD + grey bevels). S5b locked a **document-workshop** (Docs Suggesting / Word / Overleaf / HackMD). You asked to continue with a **new framework for visual development** — avoid or refine the current basic-looking VST. Is that a **look**, a **toolkit**, or a **way agents build chrome**?

**Options presented:**

1. **Document chrome, keep Compose (recommended).** Avoid VST-as-identity. Plan/Draft look like paper + suggestion marks. GATE can stay a strip. `resizeableActivity` stays (honest window = OEM, not painted 92%). CURRENT’s “Camel Space vibes” is lab-stretch, same class as S6.
2. **Refine Camel Space — keep the instrument.** Yellow LCD + purple GATE + compact bevels, but not a fake plugin window and not a “basic VST screenshot.” Document workshop sits *inside* the instrument. Avoid the fail, do not avoid the palette.
3. **New UI toolkit** for the workshop pane (WebView + ProseMirror/CodeMirror, or a markdown editor library, or Flutter). Compose remains a shell or goes away. Real “framework” change. Weeks.
4. **Visual-dev process, same stack.** Keep Compose. The fail was agents inventing VST from vibes. Factory: FACE-SPEC + HTML/paper mock + no bevels unless the spec says so. “Framework” = how we develop visually, not a new runtime.
5. **Generate the face from a visual model** (Imagine / v0 / Galileo). Fast pixels, uncontrolled identity, fights “law not stack.”

**Selected:** **Other — option 1 *but* later a VST-inspired Camel Space reskin as a settings project, from genuine resources.** User: “1 *but* I want to actually find and use the genuine resources to make a VST-inspired Space Camel UI later (reskin - more of a settings project for later).” Not 2 now. Not a new toolkit. Not vibe-Imagine. Not process-only (though S8 research still produces mocks).

**Rationale / recommendation:**

**Recommend option 1**, with **4** as the factory that produces it.

Why: S5b+S5d already picked Overleaf-on-CURRENT. A grey plugin with yellow LCD is the opposite of Suggesting-on-paper. CURRENT still names Camel Space because that was the debug-plugin Next — S6 already stretched that Next; stretching the *look* to match the workshop is the honest follow. Option 2 is right only if the product is still “debug VST” and the workshop is a pane. You said the VST **UI-failed**. Option 3 is the toolkit reading of “framework”; pick it if Compose cannot do hunk marks. Option 4 is mandatory *process* even if you pick 1 — S8 research should output mocks, not another bevel. Option 5 is how we got the VST.

Code already true: `SeatTheme.kt` `PluginWindow` fills the activity; painted 360×560 letterbox was reverted; `resizeableActivity=true`. B9 forbids fake traffic-light VST.

**Cost tradeoff:**

- **v1:** $0 vendors. 1 = 3–7 days restyle + markdown Suggesting chrome (overlaps S5d’s week). 2 = 2–4 days polish on existing palette. 3 = 1–3 weeks new stack + Chaquopy bridge risk. 4 = 1–2 days factory, then 1 or 2. 5 = hours of images + a week of “not quite.”
- **alpha (50):** a document face is mum-class; a VST face is operator-class. 50 sitters will not learn a plugin. Dollar delta $0.
- **growth (1,000):** toolkit 3 looks like an editor product. Do not take that crossover from a lab sit.
- **SaaS vs self-host:** no Figma subscription required. HTML mocks in `output/` are $0. v0/Galileo is a vendor ($0–20/mo) and a second taste.
- **Engineer-time:** 1+4 is the spike. 3 is a rewrite. 2 keeps the fail’s costume.
- **Crossover:** 1→3 only if hunk marks are unusable in Compose Text. 1→2 only if you recant “VST UI-fail.” Never 5 as identity.

**Dependencies:**

- Depends on: S5b=2, S5d=1, S8=1, CURRENT window clause, B9.
- Unlocks: S11 (7B context: a whole-file paper editor must not dump the file into the prompt); S10 (Join strip on document chrome vs VST LCD); S8 research FACE-SPEC what to steal visually.

**Status:** `decided`

**Post-select note:** Two clocks. **Now:** document chrome (paper, hunks, Compose). **Later, not this spike:** settings theme “Camel Space / VST-inspired” sourced from real plugin/Camel Space materials, not from `SeatPalette` vibes. That later project must not restore painted letterbox (B9). S8 research FACE-SPEC is paper/Suggesting, with a one-pager of genuine VST references parked for the reskin.

---

### S10 — What is the Tailscale *component* (not just a hardcoded 100.x)?

**Question (exactly as posed):**

> S2/S7 already locked: Mechanicall does **not** embed Tailscale; JOIN opens the Tailscale app / pasted invite; mint stays your admin click. `ChatHome.kt` still hardcodes `127.0.0.1` / `192.168.0.51` / `100.90.85.68`. You asked to cover **Tailscale component considerations**. What is the component, this spike?

**Options presented:**

1. **Reachability probe + MagicDNS (recommended).** No libtailscale. JOIN still opens Tailscale. Status = can we reach `wol-pi` and `myarch:11434` (MagicDNS names, not frozen 100.x). ACL `tag:stranger` stays operator-admin. Matches S2/S7; fixes the brittle IP.
2. **Keep hardcoded 100.x this spike.** Fastest. Probe those literals. Breaks when Tailscale IPs rotate.
3. **Embed libtailscale / tsnet in the APK now.** Mechanicall *is* the VPN. Recants S2. Weeks. Only if you refuse a second app.
4. **wol-pi Tailscale Serve** for the wake HTTP (HTTPS on the tailnet). Still not Funnel. Extra moving part; nicer than a raw port on wol-pi.
5. **Headscale** so strangers are not on your personal tailnet. Right around 50 sitters; wrong this lab week.

**Selected:** **3 — Embed libtailscale / tsnet in the APK now.** User override of recommendation (was 1 — probe + MagicDNS). Not hardcoded 100.x. Not Serve-only. Not Headscale this week.

**Rationale / recommendation:**

**Recommend option 1.**

Why: “component” should mean **one named job** — desk reachability — not a second Tailscale client. MagicDNS is the genuine Tailscale resource (same spirit as S9’s genuine VST later): use the real name system instead of painting IPs. Option 2 is how the last-pass face already lies. Option 3 was declined at S2. Option 4 is a wol-pi hardening you can add inside S4 without changing the phone. Option 5 is the 50-user crossover already in the cost map.

**Cost tradeoff:**

- **v1:** $0 Tailscale Personal. 1 = hours (resolve MagicDNS + probe; fallback to last-known 100.x if DNS quiet). 2 = 0 hours and a future outage. 3 = 1–3 weeks + VPN permission. 4 = hours on wol-pi (`tailscale serve`). 5 = a weekend of Headscale + a VPS ~$5/mo.
- **alpha (50):** 1 still your personal tailnet. Crossover to 5 (Headscale) or stop sharing Ollama. 4 scales fine for one wake box.
- **growth (1,000):** not this tailnet. Dollar delta vs Funnel: Funnel is $0 and a public wake — **rejected**.
- **SaaS vs self-host:** Tailscale Personal $0 vs Headscale $0+VPS. No new SaaS in the APK.
- **Engineer-time:** 1 is the component. 3 is a different app.
- **Crossover:** 1→4 when raw HTTP on wol-pi is annoying. 1→5 at more than a handful of tagged strangers. Never 3 unless S2 is recanted.

**Dependencies:**

- Depends on: S2=1, S4=1, S7=1, S0f ACL.
- Unlocks: S11 (stable workshop still needs a live desk URL); S12 (sharing a pocket ≠ sharing the tailnet).

**Status:** `decided`

**Post-select note:** S2 said Mechanicall is **not** the VPN. S10=3 says it **is**. That is a recant unless S10b keeps both (two clients — worse). VPN permission, APK size, and an auth-key path come back; S7’s “paste URL into Tailscale app” may die.

---

### S10b — S2 said JOIN opens the Tailscale app. S10 embeds Tailscale in Mechanicall. Which door is law?

**Question (exactly as posed):**

> S2 (recommended, you accepted): JOIN opens an invite URL / **Tailscale’s app**; Mechanicall only probes. S10 (you overrode): **embed libtailscale / tsnet in the APK now.** Those two cannot both be the only door. Do you recant S2?

**Options presented:**

1. **Recant S2. Mechanicall *is* the Tailscale client (recommended if you meant S10=3).** JOIN = in-app accept (auth key or invite consumed by libtailscale). No second Tailscale app required. S7’s paste-well feeds the embed, not an external app. MagicDNS still for names. Weeks. VPN permission.
2. **Keep both.** Embed plus “open Tailscale app” fallback. Two VPN clients. Support nightmare. Only if you cannot ship embed on some devices.
3. **Misspoke — recant S10=3, keep S2.** Back to probe + (if you still want names) MagicDNS. Hours, not weeks.
4. **tsnet on wol-pi only, not in the APK.** Wake HTTP via tsnet on the Pi. Phone still uses Tailscale app (S2). This is S10 option 4’s cousin, not “in the APK.”

**Selected:** **1 — Recant S2. Mechanicall is the Tailscale client.** User picked the recommended-if-deliberate option. Not keep-both. Not recant-S10. Not tsnet-on-wol-pi-only.

**Rationale / recommendation:**

**Recommend option 1** *if* S10=3 was deliberate; **option 3** if you tapped embed meaning “take Tailscale seriously as a component.”

Why: two doors is how 18 Aug failed (Tailscale-as-me plus a friend who never got in). An embedded client is a real component: one accept, one status, one ACL identity. It also blows the “quick spike” calendar and needs an auth path (S2 deferred wol-pi mint — embed often wants an auth key). Option 4 is the cheap “component” that does not recant S2.

**Cost tradeoff:**

- **v1:** 1 = 1–3 engineer weeks + libtailscale/tsnet maintenance + VPN UX. Auth key must still not live in git (wol-pi or paste). 2 = 1 plus Tailscale-app QA. 3 = hours. 4 = hours on wol-pi.
- **alpha (50):** embed-in-APK is how you stop saying “also install Tailscale.” Still your tailnet. Play review later will ask why you need VPN.
- **growth (1,000):** VPN-in-app is an MDM product. Do not take that crossover from lab.
- **SaaS vs self-host:** libtailscale is still Tailscale’s control plane ($0 Personal). Headscale later.
- **Engineer-time:** 1 is the largest net fork in this continuation. 3 is a free undo.
- **Crossover:** 1→3 this turn if the calendar is still “research then implement” under S8 and you will not eat three weeks of VPN. 1→4 if the component you wanted was wol-pi, not the phone.

**Dependencies:**

- Depends on: S2, S7, S10=3.
- Unlocks: S7 amendment (paste into embed vs open URL); S11 (desk URL via MagicDNS inside the tunnel); S8 calendar (research FACE-SPEC must include VPN join chrome).

**Status:** `decided`

**Post-select note:** S2 is **recanted**, not dual-doored. JOIN after bind pastes the operator-minted invite/auth into libtailscale. Tailscale’s consumer app is not required. S2b hardcoded 100.x is also stale: names via MagicDNS inside the embed (auto, skip-obvious). Auth material still never in git.

---

### S2b-amended — Send host once the embed is up (auto-approved)

**Question (exactly as posed):** *(not posed — skip-obvious given S10b)*

> Keep frozen `100.90.85.68` now that Mechanicall is a Tailscale client?

**Selected:** **MagicDNS `myarch:11434` and `wol-pi` for wake.** USB reverse `127.0.0.1` remains operator lab only. LAN IP is desk-wifi, not a stranger. Public bind still refused.

**Cost tradeoff:** v1 $0. Engineer-time: hours inside the embed. Crossover: none.

**Status:** `decided` (auto-approved)

---

### S10c — Device-wide VPN, or only Mechanicall’s sockets?

**Question (exactly as posed):**

> S10b locked: the APK **is** the Tailscale client. Official Tailscale Android uses a **VpnService** (the whole phone joins the tailnet). tsnet/libtailscale can instead run **userspace** so only Mechanicall’s sockets (wake + Ollama) go through the tunnel. A stranger’s other apps then stay on LTE. Which embed?

**Options presented:**

1. **Userspace / app-local tunnel (recommended).** Mechanicall reaches wol-pi + myarch. Other apps do not ride your tailnet. No “this app wants to set up a VPN” system sheet — or a smaller one. Right for a document workshop and a depreciated stranger.
2. **VpnService — device-wide.** Genuine Tailscale-on-Android shape. Phone is on the tailnet. ACL `tag:stranger` is the fence for *every* app, not just Mechanicall. Heavier consent. Kid/sitter may refuse the VPN dialog.
3. **VpnService + the same ACL fence (2 with S1 taken as sufficient).** Device-wide, and you accept that Maps/Chrome also hit the tailnet unless ACL denies.
4. **Userspace now, VpnService as later settings** — same two-clock as S9’s Camel Space reskin.

**Selected:** **1 — Userspace / app-local tunnel.** User picked the recommended option. Not VpnService. Not ACL-as-only-fence-for-the-whole-phone. Not later-settings VPN.

**Rationale / recommendation:**

**Recommend option 1.**

Why: S1 already wrote the stranger down to wol-pi + `:11434`. Putting *Chrome* on that tailnet is extra blast radius for no workshop gain. Userspace is enough for Send and Wake. Option 2 is the “genuine Tailscale resource” (mirrors S9 later-reskin thinking) but it is a product door a mum-class sitter may bounce on. Option 4 if you want genuine VPN later without blocking this lab.

**Cost tradeoff:**

- **v1:** $0. 1 = still 1–3 weeks embed, easier Play/sideload story (no always-on VPN). 2 = same weeks + VPN UX + ACL must be perfect. 3 = 2. 4 = 1 now + a later settings project.
- **alpha (50):** device-wide VPN in a sideload APK is support (“why did my banking app die”). Userspace is quieter.
- **growth (1,000):** VpnService-in-app is MDM. Do not take that from lab.
- **SaaS vs self-host:** N/A. Still Tailscale Personal.
- **Engineer-time:** 1 and 2 are the same library, different Android permission surface. 1 is less QA against the rest of the phone.
- **Crossover:** 1→2 only if userspace cannot resolve MagicDNS or cannot open `:11434`. 1→4 if you want a settings toggle later.

**Dependencies:**

- Depends on: S10b=1, S1 ACL, S4 Wake, S2b-amended MagicDNS.
- Unlocks: S12 (sharing a pocket still ≠ sharing the tailnet); S11 (desk path is the tunnel).

**Status:** `decided`

**Post-select note:** Tailscale-as-component is closed for this continuation: embed + recant S2 + userspace. Device-wide VPN parked (unlike Camel Space, they did **not** ask to park it as later settings). Reopen S10c only if userspace cannot hit MagicDNS/` :11434`.

---

### S12 — How is a *bound project* shared and versioned?

**Question (exactly as posed):**

> PRODUCT: Mechanicall is **not a git/VCS product**. CORE_PRINCIPLES: what matters is `git diff`-able; ARCHITECTURE: git is the sync, no extra collab primitives. The sitter’s pocket is **their** folder (S0d), already versioned as CURRENT vs PROPOSE vs `events.jsonl`. You asked to cover **project sharing-and-versioning ideas**. (Operator-tree dirty git is **S13**, last.) What happens to the bound pocket?

**Options presented:**

1. **No share this spike (recommended for lab).** One phone, one folder. Versioning *is* the protocol (CURRENT / PROPOSE / receipts). Backup is adb or whatever they already do. No syncthing, no auto-git, no zip-as-law.
2. **git in the pocket, Mechanicall never auto-commits.** Bind may `git init` or attach an existing repo. Share = *they* add a remote (GitHub/Codeberg/local). Commits are a human verb, not Yes, not Send. Genuine resource: git. Matches CORE without becoming a VCS product.
3. **syncthing (or folder sync) phone ↔ myarch as backup.** Two writable copies is a second CURRENT unless one side is read-only. Easy lie.
4. **Operator twin only** (`aether_pocket_serve` / adb pull). Desk holds a copy for the operator. Sitter never “shares.” Fine as lab backup; not sharing.
5. **Share-sheet zip / export.** A copy. Not law. Dangerous if they keep editing both.

**Selected:** **1 — No share this spike.** User picked the recommended lab option. Not git-in-pocket now. Not syncthing. Not operator-twin-as-sharing. Not zip.

**Rationale / recommendation:**

**Recommend option 1** this lab, **2** as the parked genuine resource (same two-clock as Camel Space settings).

Why: a document workshop already *is* versioning (published vs suggestions). Adding git UI now fights S8 research-first and S6 lab. Option 2 is what CORE already promised the day this stops being one phone. Option 3/5 create dual authority. Option 4 is an operator convenience, not a sitter feature — say so if you want it as a leaf, not as “sharing.”

**Cost tradeoff:**

- **v1:** $0. 1 = 0 extra days. 2 = 1–3 days git status/log chrome + refuse auto-commit CURRENT. 3 = syncthing ops + split-brain repair. 4 = already almost have scripts. 5 = hours and a lying duplicate.
- **alpha (50):** 2 is how 50 pockets sync without a BaaS ($0 git hosting they choose). 3 is 50 syncthing pairs (support). 5 is 50 zips (not a product).
- **growth (1,000):** git remotes they own vs you becoming GitHub. Dollar delta: $0 vs a hosted repo product.
- **SaaS vs self-host:** no Drive, no Dropbox-as-CURRENT. Git they host or a forge they already pay.
- **Engineer-time:** 1 keeps the calendar. 2 is a later settings/project like Camel Space.
- **Crossover:** 1→2 when two humans edit one pocket. Never 3 with two writers.

**Dependencies:**

- Depends on: S0d bind≠repo, S5c PROPOSE vs CURRENT, PRODUCT/CORE tension, S13 later.
- Unlocks: S11 (model must not “version” by rewriting CURRENT); S13 (operator tree ≠ pocket share).

**Status:** `decided`

**Post-select note:** git-in-pocket stays a parked genuine resource (like Camel Space settings), not this FACE-SPEC. Dual copies are still a law bug if someone adds them later.

---

### S11 — How does a 7B with an active LoRA stay stable in the document workshop?

**Question (exactly as posed):**

> S5d locked **whole `CURRENT.md`** as the document. Desk Send **inserts suggestions** (S5c), not law. The desk model is **`personal-llm-sft-v4`** — a **7B with an active LoRA** — already on a brutal slim ICM (`CHAT_USER_TURNS=3`, 280/240 chars, stages show-plan|propose|gate, fields only, local stage owns PROPOSE write). Dumping the whole file into that model will make it wander. How does ICM keep the workshop stable?

**Options presented:**

1. **Hunk-scoped ICM (recommended).** Never send whole CURRENT. Prompt = tiny sys + stage `propose` + **the one section/hunk in focus** + live vs proposed for that hunk + last 3 user turns. Model returns a **patch for that hunk** (not a rewritten file, not chat). LoRA remains propose/taste only. Python applies the patch to PROPOSE. GATE still streams. Matches ICM paper: load only the layer you need.
2. **Whole-file in the prompt anyway.** 7B+LoRA hallucinates Keep/Reject, “helpfully” rewrites law-shaped prose. Unstable. Don’t.
3. **Dumb section retrieve, no vectors.** Split CURRENT on headings; retrieve the named section. Still no embeddings (CORE: no vector DB in v0). Cousin of 1 with a file index.
4. **Human-only workshop this spike; desk off.** Most stable 7B. Fights “desk inserts suggestions.” Only if you will not Send in Draft.
5. **Bigger or cloud model for the workshop.** Recants local `personal-llm-sft-v4` / private desk. Conflicts with CURRENT Host.

**Selected:** **Other — option 1, and it can auto-complete the entire schema from the user’s hunks too.** User: “1 (can auto-complete the entire schema from users hunks, too).” Not whole-file prompt. Not desk-off. Not cloud. Not retrieve-only.

**Rationale / recommendation:**

**Recommend option 1.** Option 3 if “hunk” needs a heading map.

Why: the ICM paper and this repo’s slim chat already solved “7B gets lost in the middle.” The workshop must **not** undo that by pasting the novel. A LoRA trained to propose will over-write if you hand it the whole CURRENT. Option 2 is the fail. Option 4 is honest if the 7B cannot patch; you already wanted Send to insert suggestions. Option 5 is a different product.

**Cost tradeoff:**

- **v1:** $0 GPU (same box). 1 = 2–5 days prompt/stage + patch parser + tests that a dumped-file path is refused. 2 = $0 and a broken workshop. 3 = +0.5 day heading index. 4 = −desk days, +weaker Draft. 5 = $20–200/mo API or a bigger GPU.
- **alpha (50):** 1 stays one 7B. 5 is the bill. 2 is 50 confused sitters.
- **growth (1,000):** still one local model per desk, not a fleet, if 1 holds. Crossover to 5 only if 7B cannot emit a hunk at all after LoRA.
- **SaaS vs self-host:** 1/3/4 = $0 Ollama. 5 = cloud and a corpus leak.
- **Engineer-time:** 1 is the stability work. It is not optional if S5d stands.
- **Crossover:** 1→3 when sections are long. 1→4 if tests show the LoRA cannot emit patches. Never 2. Never 5 on this Host line.

**Dependencies:**

- Depends on: S5c, S5d, existing `draft_chat` slim ICM, ARCHITECTURE (LoRA = propose only), S8 research (FACE-SPEC must name the prompt contract).
- Unlocks: S13 (last); close continuation.

**Status:** `decided`

**Post-select note:** “Entire schema” = AUTHORITY_FIELDS (and the rest of the proposal schema), filled as **suggestions on PROPOSE** from the hunks the human actually touched — not a silent rewrite of live CURRENT.md, not a whole-file dump into the 7B. One Send may propose many fields; Decide still publishes.

---

### S13 — Final dirty git consolidation of this local mechanical tree

**Question (exactly as posed):**

> You named this **final**. `git status` on mechanicall-os: **modified** `CURRENT.md`, `DECISIONS.md`, `.context.md`, `.aether/events.jsonl`, `docs/LAB-STATUS.md`, `tests/run.sh`; **untracked** `android/`, `python/aether_pocket*.py`, `python/app_verify.py`, `decision-tree.md`, `dev/23`–`36`, scripts, tests, examples, research. Live CURRENT is law — a commit is not `aether approve`. How do we consolidate **this operator tree** (not the sitter pocket — S12=none)?

**Options presented:**

1. **Grouped commits, exclude live CURRENT + secrets (recommended).** Separate commits: pocket engine+tests; android/; `dev/` receipts; `decision-tree.md`+scripts. Human confirms the list. `CURRENT.md` / `.aether/events.jsonl` stay dirty **or** you commit them yourself as *your* law edits — I do not treat commit as Yes.
2. **One wip commit of everything except CURRENT + secrets.** Honest dump, messy history.
3. **gitignore the lab; keep android/dev untracked on master.** Tree stays dirty by policy.
4. **Grill-only file list; you run git later.** I write a MANIFEST of groups. No `git add` / `commit` until a later explicit Go-git.
5. **Commit CURRENT too, one ball of dirt.** Illegal-shaped: looks like the model shipped law. Do not pick.

**Selected:** **1 — Grouped commits, exclude live CURRENT + secrets.** User picked the recommended option. Not one wip ball. Not gitignore-the-lab. Not manifest-only. Not commit-CURRENT.

**Rationale / recommendation:**

**Recommend option 1** as the spec, **execute only after you say Go-git** (silence is not a commit). Option 4 if you want the list now and the commands later.

Why: the tree is months of lab. One ball hides `CURRENT.md` in 10k lines of APK. CURRENT in a model-authored commit reads as approve. Option 3 loses the engine. Option 5 is the bug.

**Cost tradeoff:**

- **v1:** $0. 1 = 30–90 minutes grouping + your review. 2 = 10 minutes and a bad bisect forever. 3 = $0 and no backup of android. 4 = 15 minutes of manifest. 5 = $0 and a law incident.
- **alpha / growth:** N/A. This is operator hygiene.
- **SaaS vs self-host:** git they already have. No GitHub required (local commits).
- **Engineer-time:** the cost is review attention, not dollars.
- **Crossover:** 1→2 only if you will rewrite history later. 1→4 if this turn must not touch the index. Never 5.

**Dependencies:**

- Depends on: S12 (pocket ≠ this tree), Limits no secrets, S0e no CURRENT rewrite, user “final.”
- Unlocks: **close continuation.** Then either Go-research (S8) or Go-git (S13) as separate human lines.

**Status:** `decided`

**Post-select note:** Continuation **closed**. Proposed groups (not run): (A) `python/aether_pocket*.py` + `app_verify.py` + tests; (B) `android/`; (C) `dev/23`–`36` receipts; (D) `decision-tree.md` + scripts + examples/research that are lab. Exclude: `CURRENT.md`, `.aether/events.jsonl` unless **you** commit them; any token/MAC. I do not `git commit` until **Go-git**.

---

### S14 — Would Headscale make JOIN login-less?

**Question (exactly as posed):**

> APPROVED (Quick ask: would using headscale make this login-less? interested)

**Options presented:** *(not a pick-one grill — parked interest)*

1. **Headscale + preauth key** — sitter never opens Google/Apple/GitHub. Paste key into Mechanicall/tsnet. Operator still mints the key.
2. **Tailscale.com auth key** — same sitter experience (no IdP) on the existing tailnet. Still Tailscale Inc coordination.
3. **Headscale + OIDC** — more login, not less.
4. **Stay on Tailscale.com invite URL** — current S7; usually dumps the sitter into an IdP.

**Selected:** **parked — interested, not this Next.** No recant of S7/S10. No CURRENT rewrite.

**Rationale / recommendation:**

**Recommend staying on this Next’s paste-well + userspace embed.** If the pain is “stranger must create a Tailscale Inc account,” prefer **Tailscale.com auth keys** first (same tailnet, no new control plane). Choose **Headscale** when the pain is “I don’t want strangers on Tailscale Inc’s coordination / my Google-linked tailnet at all.”

Headscale does **not** make join: key-less, operator-less, identity-less, or host-less. It can make it **IdP-less** for the sitter.

**Cost tradeoff:**

- **v1:** Headscale $0 software. Needs an always-on **login-server HTTPS URL** the phone can reach on LTE (wol-pi or ~$5 VPS). Operator time: a weekend to stand up + ACL. Tailscale.com auth keys: $0 extra, hours.
- **alpha (50):** Headscale or stop sharing Ollama (already in the cost map). Tailscale Personal Plus $0–6 still puts 50 people on their IdP if you use invites.
- **growth (1,000):** operator tailnet is the wrong product either way.
- **SaaS vs self-host:** Headscale is the self-host control plane. Delta vs Tailscale.com: $0–5/mo + your ops vs $0 and their IdP.
- **Engineer-time:** embed path is the same (tsnet `--login-server` + `--authkey`). Control plane is the extra week.
- **Crossover:** Tailscale.com auth keys → Headscale when you refuse their coordination. Never OIDC if the goal is login-less.

**Dependencies:** S7, S10–S10c, S1 ACL. Unlocks: later Next or a Limits line; not `sit-join-workshop` split.

**Status:** `parked`

---

## Continuation locked spec (closed 2026-08-20)

Adds to the earlier spike spec. Still not CURRENT. Still not Yes.

**Visual (S9):** Document chrome now (paper + Suggesting, Compose). Camel Space / VST-inspired **later settings reskin** from genuine VST resources. No painted plugin.

**Tailscale (S10–S10c):** Recant S2. APK **is** the client (libtailscale/tsnet), **userspace** only. Paste invite into Mechanicall. MagicDNS `myarch` / `wol-pi`. No Tailscale app required. No device-wide VPN. Auth not in git.

**Share (S12):** No pocket share this spike. Versioning = CURRENT / PROPOSE / receipts.

**7B+LoRA (S11):** Hunk-scoped ICM. Never dump whole CURRENT. May auto-complete **schema** from user hunks onto **PROPOSE**. Auto-complete ≠ Yes.

**Git (S13):** Grouped commits after **Go-git**. Exclude live CURRENT + secrets.

**After Go:** still S8 — `01_research/` FACE-SPEC (paper/Suggesting + userspace join + hunk prompt contract), then implement.

---

### S15 — How is paper Suggesting *built* so we do not paint another VST?

**Question (exactly as posed):**

> Live CURRENT `sit-join-workshop` is **APPROVED**. S9 already locked: document chrome, keep Compose, Camel Space/VST later from genuine resources. `01_research/output/FACE-SPEC.md` already specifies inline suggestion marks on paper. You asked to continue the grill with a **new framework for visual development** (avoid/refine the basic VST UI-fail). The look is law. What is the **build framework** for those marks?

**Options presented:**

1. **Compose hunk renderer + FACE-SPEC as factory (recommended).** `AnnotatedString` / paragraph blocks + accept/reject on the focused hunk. Agents may not invent bevels: they implement FACE-SPEC and optional HTML paper mocks already in `01_research/output/`. One toolkit (Compose). Matches S9.
2. **WebView workshop pane** (ProseMirror or CodeMirror). Compose is the shell only. Better genuine Docs/Overleaf behaviour. Recants “keep Compose” for the document itself. Heavier IME/back-stack.
3. **Visual factory without a new runtime:** HTML/paper mocks + screenshot golden tests (`uiDump` / Compose preview) before Kotlin. Still option 1’s renderer; the *framework* is the mock→spec→pixel gate that VST skipped.
4. **JUCE / genuine VST chrome now.** You parked this as later settings. Do not pick unless you recant S9’s two-clock.

**Selected:** **Declined this turn.** Parked. **Working assumption (not law until they pick):** option 1 + process wrap 3 — Compose hunk renderer, FACE-SPEC/mocks as the factory, no WebView, no JUCE now.

**Rationale / recommendation:**

**Recommend option 1**, with **3** as the mandatory process wrap (same two-part as S9: look now, factory so agents cannot vibe a plugin).

Why: the VST fail was not “we used Compose.” It was agents painting an instrument with no FACE-SPEC. Option 2 is the real document editor and costs a second runtime inside Chaquopy. Option 4 is the parked reskin. Option 3 without 1 is process with nothing to ship.

**Cost tradeoff:**

- **v1:** $0. 1 = 4–8 days hunk UI on existing Compose. 2 = 1–2 extra weeks WebView + JS editor + IME. 3 = +0.5–1 day mocks/goldens on top of 1. 4 = a settings project you already deferred.
- **alpha / growth:** still one APK. WebView editors become “why doesn’t selection work like Docs.”
- **SaaS vs self-host:** no Figma required. Goldens live in `dev/37_sit-join-workshop/`.
- **Engineer-time:** 1+3 is the visual framework. 2 is a toolkit recant.
- **Crossover:** 1→2 only if Compose IME cannot do inline accept/reject on A33. Never 4 this Next.

**Dependencies:**

- Depends on: S9, FACE-SPEC, CURRENT visual clause, B9.
- Unlocks: S17 (hunk shape the renderer must display); `02_implement` chrome.

**Status:** `parked` (user declined the question)

**Post-select note:** Do not implement a WebView editor or JUCE chrome off a declined picker. FACE-SPEC remains Compose paper until S15 is answered or they say proceed to 02 (then the working assumption applies).

---

### S16 — Which userspace Tailscale *library* is the component?

**Question (exactly as posed):**

> CURRENT already locked: Mechanicall **is** the Tailscale client, **userspace** only (no VpnService, no second Tailscale app). You asked to cover Tailscale component considerations. S10b decline in one session does not recant that. Which binary do we actually embed?

**Options presented:**

1. **Official Tailscale userspace / libtailscale Android bits (recommended).** Genuine resource, same spirit as “genuine VST later.” MagicDNS + tsnet-class API Tailscale ships. Auth key / invite still pasted, never in git.
2. **Homegrown gomobile `tsnet` bind.** More control, more you-maintain-Go-on-Android. Easy to become a second Tailscale.
3. **Spike both in `01_research` notes, pick what resolves `myarch:11434` on a phone.** Honest if we have not yet run userspace on A33.
4. **Defer embed; JOIN opens Tailscale app after all.** Recants CURRENT. Only if userspace cannot ship this Next.

**Selected:** — *(awaiting answer)*

**Rationale / recommendation:**

**Recommend option 1**, **3** if nobody has yet proven userspace MagicDNS on Android in this lab.

Why: “component” now means one library, not a philosophy. Official bits are the Tailscale analogue of genuine VST manuals. Option 2 is how we re-invent the VPN. Option 4 is a CURRENT recant — human `aether` only.

**Cost tradeoff:**

- **v1:** $0 Tailscale Personal. 1 = 1–3 weeks wrapping their userspace API + paste-well. 2 = +1–2 weeks Go bind. 3 = +1–2 days spike. 4 = hours and a law fight.
- **alpha (50):** official lib tracks Tailscale protocol; homegrown drifts.
- **growth:** still Personal vs Headscale (S14 parked).
- **SaaS vs self-host:** control plane stays Tailscale.com until S14.
- **Engineer-time:** 1 is the component. 3 is the cheap proof.
- **Crossover:** 1→3 this week if the A33 cannot import the AAR. 1→4 only after a failed spike, with a human CURRENT recant.

**Dependencies:**

- Depends on: CURRENT JOIN clause, S10–S10c, S7 paste-well.
- Unlocks: `02_implement` JOIN; S17 still independent.

**Status:** `parked` (new direction T1–T8 supersedes this leaf until T3)

---

### T0 — New-direction grill then compact (auto)

**Question:** *(not posed — skip-obvious)*

> Play Store, Funnel, model-Yes, dual-Next, secrets in git?

**Selected:** Still refused. This grill produces a PROPOSE file later; agents do not apply CURRENT. Compact after the tree for these forks.

**Cost tradeoff:** $0. **Status:** `decided` (auto)

---

### T1 — Where does this new direction live?

**Question (exactly as posed):**

> You want to **unpark VST visual upgrade** and **Headscale architecture**, plus GATE **boot+response** time, **background model calls while the desktop is in use**, **STORM reviews + iterations**, a **visual-focused design reviewer**, and a **sharing-protocol discussion**. Live Next is `sit-join-workshop` (APPROVED; 02 implemented, 03 STORM not started; CURRENT still says VST reskin and Headscale are *not* this Next). Dual-concurrent-next is Reject. After this grill you will compact the session. Where does the bundle live?

**Options presented:**

1. **One new action-id covering the whole bundle (recommended).** Includes: genuine VST/Camel Space chrome (not painted fake window), Headscale (or auth-key IdP-less — T3), GATE = Ollama boot load + streamed reply, background desk jobs that never auto-Yes, STORM review/iterate loops, a visual design reviewer (skill/workflow), sharing-protocol *discussion* that may reopen S12. Remaining sit-join gaps (tsnet JNI, 03 STORM) fold in. Human applies a PROPOSE after compact. I do not `aether next`.
2. **Finish `sit-join-workshop` 03 first, then a second Next for this bundle.** Cleaner receipts. Two sittings. Dual-Next still forbidden *at once*.
3. **Stretch `sit-join-workshop`** to include VST+Headscale+boot GATE+background+reviewer+sharing. Recants CURRENT Limits (“VST reskin is not this Next”, “no pocket share this Next”, S14 parked). Fastest; same lie-class as the old verification stretch.
4. **Grill-only; no Next.** Capture T2–T8 for the compact. No PROPOSE until a later session.

**Selected:** **1 — One new action-id covering the whole bundle.** User picked the recommended option. Not finish-03-first. Not stretch sit-join. Not grill-only.

**Rationale / recommendation:**

**Recommend option 1.** Option 2 if 03 STORM on storm-0 is the only thing you will miss after compact.

Why: you named a *direction*, not a leaf. CURRENT Limits explicitly park VST and sharing; unparking them in-place is a silent CURRENT rewrite. One new id keeps one-Next discipline. Option 3 is what you already recanted at S6→apply. Option 4 if compact is tonight and law can wait.

**Cost tradeoff:**

- **v1:** $0 infra. 1 = 10–20 min of your `next`+`approve` after PROPOSE + 2–4 weeks build (VST genuine chrome + Headscale or auth keys + GATE boot + STORM loop + reviewer). 2 = +1–2 days 03 then same weeks. 3 = 0 process, muddy Limits. 4 = 0 build.
- **alpha / growth:** N/A (operator law).
- **SaaS vs self-host:** Headscale later (T3) is the dollar line, not T1.
- **Engineer-time:** the bundle is weeks either way; T1 only picks the *label*.
- **Crossover:** 1→2 if storm-0 receipts are the compact’s only leftover. 1→4 if you will not apply CURRENT this week.

**Dependencies:** live CURRENT, dual-Next Reject, S9 later-reskin, S12 no-share, S14 parked Headscale.
**Unlocks:** T2 VST, T3 Headscale, T4 GATE boot, T5 background desk, T6 STORM loop, T7 visual reviewer, T8 sharing.

**Status:** `decided`

**Post-select note:** Working name for the PROPOSE (not law until they apply): `sit-vst-headscale`. Remaining sit-join gaps (tsnet JNI, 03 STORM) fold into that id. After this grill: write PROPOSE, compact, human `aether next` + `approve`.

---

### T2 — Unpark VST: what is the upgrade?

**Question (exactly as posed):**

> S9 locked **paper Suggesting now**, Camel Space/VST as a **later settings reskin from genuine resources**. CURRENT Limits: “Camel Space / VST reskin is **not** this Next.” You want to **unpark the VST visual upgrade**. Painted 92% plugin was a UI-fail (B9). What gets unparked?

**Options presented:**

1. **Genuine instrument frame, paper workshop inside (recommended).** Compact plugin chrome sourced from **real** Camel Space / VST manuals (bevels, LCD for Next/GATE only). `resizeableActivity` stays; **no** painted letterbox. Plan/Draft stay the document (Suggesting). VST is the *rack*, not the *score*.
2. **Identity swap: the sit *is* a VST.** Document workshop is one plugin pane among knobs. Higher risk of verb museum / fake window.
3. **Settings theme only.** Paper default; VST skin toggle. Unparks S9’s later clock without changing the default sit.
4. **GATE/LCD strip only.** Rest stays paper. Smallest unpark.

**Selected:** **1 — Genuine instrument frame, paper workshop inside.** User picked the recommended option. Not identity swap. Not settings-only. Not strip-only.

**Rationale / recommendation:**

**Recommend option 1.**

Why: you asked to unpark the *upgrade*, not to restore the fail. Genuine resources were the S9 condition. Paper Suggesting (S5b) still needs a document. Option 2 fights B9 and the workshop. Option 3 is honest if “unpark” means “allow the skin.” Option 4 is a strip, not an upgrade.

**Cost tradeoff:**

- **v1:** $0 licenses if we only *read* Camel Space/VST public chrome. 1 = 5–10 days restyle + keep workshop. 2 = +week of pane chrome. 3 = 2–4 days theme. 4 = 1 day.
- **alpha:** mum-class sitters may bounce on a synth; 1 keeps the document readable.
- **SaaS vs self-host:** no Figma required; FACE-SPEC + screenshots in `output/`.
- **Engineer-time:** 1 is the visual half of the new Next. T7 reviewer scores it.
- **Crossover:** 1→3 if genuine assets cannot be sourced this Next. Never painted 360×560.

**Dependencies:** T1=1, S9, B9, FACE-SPEC paper.
**Unlocks:** T7 visual reviewer; FACE-SPEC v2.

**Status:** `decided`

**Post-select note:** S9 “later reskin” is unparked as the *frame*. Workshop remains Suggesting on PROPOSE. Painted 92% card stays Reject.

---

### T3 — Unpark Headscale: what architecture?

**Question (exactly as posed):**

> S14 parked Headscale: preauth keys can make the **sitter** skip Google/Apple; not key-less. You asked to **unpark Headscale architecture**. JOIN paste-well already exists. Userspace embed is CURRENT. What control plane?

**Options presented:**

1. **Headscale + preauth keys (recommended if “architecture” means you own coordination).** `login-server` = your Headscale (wol-pi or a small always-on box with HTTPS). JOIN pastes a preauth key into tsnet/userspace. No IdP. ACL `tag:stranger` on Headscale. Not OIDC. Key never in git.
2. **Tailscale.com auth keys only.** IdP-less sitter, keep Tailscale Inc coordination. Cheaper. Does **not** unpark Headscale.
3. **Headscale + OIDC.** More login, not less. Don’t if the goal was login-less.
4. **Keep S7 invite URLs on Tailscale.com.** Do not unpark.

**Selected:** **1 — Headscale + preauth keys.** User picked the recommended option. Not Tailscale.com keys-only. Not OIDC. Not invite URLs.

**Rationale / recommendation:**

**Recommend option 1** because you named *Headscale architecture*, not “skip Google.” Option 2 if the only pain is IdP.

**Cost tradeoff:**

- **v1:** Headscale $0 software + always-on HTTPS login-server (wol-pi or ~$5 VPS). 1 = +3–7 days ops + JOIN `login-server` flag. 2 = hours on existing tailnet. 3 = IdP setup. 4 = $0 and 18 Aug door.
- **alpha (50):** 1 is why you unpark. 2 puts 50 people on your Google-linked tailnet.
- **growth:** neither is a 1,000-sitter product.
- **SaaS vs self-host:** 1 is self-host control plane. 2 is $0 Tailscale Personal.
- **Engineer-time:** embed path is the same; control plane is the extra week.
- **Crossover:** 1→2 if you will not run a login-server this Next.

**Dependencies:** T1=1, S14, S10 userspace, S7 paste.
**Unlocks:** T4 GATE (desk must be reachable); JOIN FACE-SPEC.

**Status:** `decided`

**Post-select note:** S14 unparked. JOIN paste is a **preauth key**; tsnet `--login-server` points at Headscale. wol-pi or a VPS hosts it. Key never in git. Userspace still (S10c).

---

### T4 — GATE shows boot load + model response. How?

**Question (exactly as posed):**

> Today GATE is idle/warm/gate/quiet with 16 LEDs during **streamed generate**. You want timeout/boot logic whenever someone uses the local model: the **gate bar should represent boot loading time + model responses**. `personal-llm-sft-v4` may still be paging into VRAM. What is the bar?

**Options presented:**

1. **Two-phase GATE (recommended).** Phase LOAD: Ollama up + model in memory (timeout, e.g. 60s) — bar fills on load. Phase STREAM: existing step LEDs on tokens. Distinct colours or labels (LOAD vs GATE). Quiet after. Not typing dots. Not Yes. WAKE desk (magic packet) is *before* LOAD if myarch was asleep.
2. **One mixed 0–100 bar.** Load and tokens share a single fill. Harder to tell “still booting” from “still thinking.”
3. **BOOT lamp + GATE strip as two widgets.** More chrome, clearer.
4. **Keep current GATE; only add numeric timeouts.** Smallest change; does not “represent boot.”

**Selected:** **1 — Two-phase GATE (LOAD then STREAM).** User picked the recommended option. Not mixed 0–100. Not two widgets. Not timeouts-only.

**Rationale / recommendation:**

**Recommend option 1.** LOAD vs STREAM is what a 7B on a sleeping desk actually does. Mixing them (2) is how people think the model hung. WAKE remains the strip button; LOAD is GATE after the box is up.

**Cost tradeoff:**

- **v1:** $0. 1 = 1–3 days engine (`write_gate` states `load`/`gate`) + strip. 2 = 1 day, worse UX. 3 = +0.5 day widgets. 4 = hours.
- **alpha:** timeouts matter when 50 sitters wake one GPU.
- **SaaS vs self-host:** still Ollama.
- **Engineer-time:** 1 is T5’s prerequisite (background jobs need a LOAD queue).
- **Crossover:** 1→3 if the strip cannot show two phases. Never `…` on Send.

**Dependencies:** T1, existing GATE, WAKE S4.
**Unlocks:** T5 background calls.

**Status:** `decided`

**Post-select note:** `write_gate` gains `load` then `gate`. WAKE is still the magic-packet strip. LOAD starts when the box is up. Timeouts are engine, not Yes.

---

### T5 — Background model calls while the desktop is in use

**Question (exactly as posed):**

> myarch is **this** desktop. Phone sits will call `personal-llm-sft-v4` while you may be using the machine. You asked for **background model calls whilst-desktop-in-use logic**. Background must never auto-Yes. What is the rule?

**Options presented:**

1. **One generate at a time, niced; LOAD may run in background after WAKE (recommended).** Queue extra Sends. Desktop stays interactive. GATE shows queue/LOAD/STREAM. Never writes CURRENT from a background job.
2. **Idle-detect: only run when the local user is idle.** Phone waits. Kind to the desktop; mean to the sitter.
3. **Allow full concurrent hitch.** Simplest; desktop stutters.
4. **Dedicated inference hardware / second GPU.** Not this lab (32 GiB, 0 swap).

**Selected:** **1 — One generate at a time, niced; LOAD may run in background after WAKE.** User picked the recommended option. Not idle-detect. Not concurrent hitch. Not a second GPU.

**Rationale / recommendation:**

**Recommend option 1.** Matches one desk, one 7B, STORM cap 2 AVDs. Option 2 is a later nicety. Option 3 fights “desktop in use.” Option 4 is a purchase.

**Cost tradeoff:**

- **v1:** $0. 1 = 1–2 days queue in pocket/desk-hop. 2 = +idle probe (xprintidle / loginctl). 3 = 0 days, hitch. 4 = $200–2,000 GPU.
- **alpha:** one box cannot serve 50 concurrent generates — queue is the truth.
- **SaaS vs self-host:** still Ollama.
- **Engineer-time:** 1 is small if LOAD is already T4.
- **Crossover:** 1→4 when you buy a second GPU. 1→2 if you sit at the desk during client calls.

**Dependencies:** T4 two-phase GATE, Host myarch.
**Unlocks:** T6 STORM (must not fight the same GPU unbounded).

**Status:** `decided`

**Post-select note:** Background LOAD is not a Yes. Queue extra Sends. Desktop stays the operator’s.

---

### T6 — STORM reviews + iterations

**Question (exactly as posed):**

> CURRENT already names STORM on isolated emulators, never A33, cap **2** AVDs. `03` of sit-join was not started. You want **STORM reviews + iterations**. Walkers never Confirm. What is the loop?

**Options presented:**

1. **Iterate on storm-0 (recommended).** Walk uidump → `app_verify` + T7 visual reviewer → patch FACE → repeat until pass or a round cap (e.g. 3). Never Confirm. Never RZCW2038KHN. One AVD so the GPU queue (T5) survives.
2. **One STORM receipt then stop.** Fast compact; no iterate.
3. **Two isolated AVDs in parallel** (storm-0, storm-1). Cap 2. Different pockets. Heavier RAM (32 GiB, 0 swap).
4. **Share the A33.** Rejected.

**Selected:** **1 — Iterate on storm-0.** User picked the recommended option. Not one-shot. Not two AVDs. Not A33.

**Rationale / recommendation:**

**Recommend option 1.** “Reviews + iterations” is a loop. Two AVDs (3) hitch the same box you just niced. Option 2 is not iteration.

**Cost tradeoff:**

- **v1:** $0. 1 = 0.5–2 days per round × cap. 2 = one afternoon. 3 = +8 GiB RAM risk. 4 = law bug.
- **alpha:** STORM is lab, not 50 users.
- **SaaS vs self-host:** uidump+Python, no Appium.
- **Engineer-time:** 1 is T7’s live subject.
- **Crossover:** 1→3 only if 32 GiB is idle. Never A33.

**Dependencies:** T1, T5, STORM cap 2, existing storm-0.
**Unlocks:** T7 visual reviewer as a STORM panel.

**Status:** `decided`

**Post-select note:** Round cap belongs in the PROPOSE (suggest 3). Walkers never Confirm. T7 is a panel in each round.

---

### T7 — Visual-focused design reviewer

**Question (exactly as posed):**

> You asked for a **visual-focused design reviewer**. `/app-reviewer` already scores BEHAVIOURS (bind, workshop, Decide, GATE). T2 wants a genuine VST *frame* around a paper workshop. Who reviews the look, and how?

**Options presented:**

1. **New skill `/visual-reviewer` called from the STORM loop (recommended).** Scores FACE-SPEC v2: genuine bevels vs painted fake window, paper readable, GATE LOAD vs STREAM, no chat-identity. Never Confirm. Never Yes. Screenshots + source.
2. **Extend `/app-reviewer`** with a visual pillar. One factory, less sprawl.
3. **Human-only visual.** Agents do not score look. Slowest iterate.
4. **Generate mocks with Imagine and treat them as the spec.** Fast; how we got the VST fail.

**Selected:** **1 — New skill `/visual-reviewer` called from the STORM loop.** User picked the recommended option. Not extend-only. Not human-only. Not Imagine-as-spec.

**Rationale / recommendation:**

**Recommend option 1** so visual cannot be “upgraded” into a behaviour PASS. Option 2 if you hate a second skill.

**Cost tradeoff:**

- **v1:** $0. 1 = 0.5–1 day skill + STORM hook. 2 = hours of BEHAVIOURS.md. 3 = your time each round. 4 = $0 images + a fail.
- **alpha:** not a 50-user cost.
- **SaaS vs self-host:** no Figma.
- **Engineer-time:** 1 is the factory; T2 is the pixels.
- **Crossover:** 1→2 if the skill file would duplicate app-reviewer. Never 4 as authority.

**Dependencies:** T2, T6, existing app-reviewer.
**Unlocks:** T8 sharing (last named).

**Status:** `decided`

**Post-select note:** app-reviewer stays protocol/behaviour. visual-reviewer scores T2 chrome. Neither taps Confirm.

---

### T8 — Sharing protocol (discussion)

**Question (exactly as posed):**

> S12 locked **no pocket share** this spike; versioning is CURRENT / PROPOSE / receipts. PRODUCT: not a VCS product. CORE: git-diff-able. You asked for **some sort of discussion on the sharing protocol**. What does this Next *do* with sharing?

**Options presented:**

1. **Discussion artifact only (recommended).** A stage writes `SHARING.md` (who holds the folder, git later, no Drive-as-CURRENT, no syncthing dual-write). **Do not ship share.** S12 stays for the sit. Unparks the *conversation*.
2. **git in the pocket, never auto-commit.** Unpark S12 option 2 as shipped chrome.
3. **Operator twin only** (adb / pocket-serve). Lab backup, not sitter share.
4. **Keep S12 silent** — no discussion file. Weakest “discussion.”
5. **syncthing phone ↔ myarch.** Two writable copies. Easy law bug.

**Selected:** **Other — “IDK - I just want the standard to offer some sort of communication feature where its somehow connected to my mechanicall-os workspace!”** Not 2 (git shipped). Not 5. T8b names the connection.

**Rationale / recommendation:**

**Recommend option 1.** You said *discussion*, not “ship git.” Option 2 is a later Next. Option 5 is the dual-CURRENT fail.

**Cost tradeoff:**

- **v1:** 1 = 0.5–1 day research/discuss stage. 2 = 1–3 days git chrome. 3 = hours. 4 = $0. 5 = split-brain repair.
- **alpha:** 2 is how 50 pockets sync without BaaS.
- **SaaS vs self-host:** no Drive. Git they own, later.
- **Engineer-time:** 1 fits compact. 2 does not have to ship this week.
- **Crossover:** 1→2 when two humans edit one pocket. Never 5 with two writers.

**Dependencies:** T1, S12, PRODUCT/CORE.
**Unlocks:** close this direction grill; PROPOSE `sit-vst-headscale`.

**Status:** `decided`

**Post-select note:** “Connected to mechanicall-os” must not mean bind-the-phone-to-this-repo (B10 / S0d). T8b is the shape of the wire.

---

### T8b — What is the communication feature to *this* workspace?

**Question (exactly as posed):**

> You don’t want to pick git/syncthing/silent. You want a **communication feature somehow connected to your mechanicall-os workspace**. Phone bind ≠ this repo (Keep). What is the wire?

**Options presented:**

1. **Operator read-twin (recommended).** mechanicall-os can *pull/read* the bound pocket (CURRENT, PROPOSE, receipts, events) over Headscale. Operator watches the sit. Writes stay on the phone until Decide. Not a second CURRENT. Extends `aether_pocket_serve` / adb twin as the *standard*.
2. **Inbox on the operator tree.** Phone appends a note into `mechanicall-os/inbox.md` (or `.aether/`). Easy to treat as law by mistake.
3. **Bind this repo.** Illegal (B10).
4. **Discussion-only SHARING.md** that names option 1 as the future standard, no wire this Next.
5. **Realtime chat between phone and operator Grok session.** Chat-identity; dual authority.

**Selected:** **1 — Operator read-twin.** User picked the recommended option. Not inbox-as-law. Not bind-this-repo. Not discussion-only. Not Grok chat.

**Rationale / recommendation:**

**Recommend option 1.** “Connected to my workspace” = you can *see* their folder from here, not *be* their folder. Option 4 if compact must not grow a daemon.

**Cost tradeoff:**

- **v1:** $0. 1 = 2–4 days tailnet twin read (Headscale already T3). 2 = hours and a law hazard. 3 = $0 and a refuse. 4 = 0.5 day doc. 5 = a second chat product.
- **alpha:** 1 is one operator, many pockets (read). 5 is a hosted Session (PRODUCT: not core).
- **SaaS vs self-host:** twin over Headscale $0. No BaaS.
- **Engineer-time:** 1 is the share *standard* without being a VCS product.
- **Crossover:** 1→4 this calendar if twin isn’t ready. Never 3.

**Dependencies:** T8 Other, T3 Headscale, S0d, existing pocket-serve.
**Unlocks:** close grill; write PROPOSE for compact.

**Status:** `decided`

**Post-select note:** Communication = operator **reads** the sit over Headscale. Writes stay on the phone until Decide. mechanicall-os is the watch window, not the bound law.

**2026-08-20 amendment:** User: “Read-twin works; boring is a good strategy! Bind should follow.” Twin **stands**. Bind is the next boring rule: only Mechanicall-installed folders, not mkdir-any-path (today `bind_folder` creates empty dirs). Operator tree still refused (`PRODUCT.md`+`bin/aether`+`CORE_PRINCIPLES.md`+`AGENTS.md`).

---

### T8c — Bind follows twin: what is the Mechanicall *installation file*?

**Question (exactly as posed):**

> Read-twin stays (boring is the strategy). Bind should follow. Earlier: bind should only work under the **mechanicall installation file**. Today bind will `mkdir` any path except the operator tree. Twin reads CURRENT/PROPOSE/receipts — so bind needs something to read. What file makes a folder bindable?

**Options presented:**

1. **`CURRENT.md` already there (recommended).** Bind refuses empty/random dirs. Domain exists; twin has law to pull. First-run can *seed* a template CURRENT (not Yes) then bind. Operator tree still refused.
2. **A marker that is not law** (`.mechanicall` or `INSTALL.md`). Install ≠ CURRENT. Bind if marker exists; CURRENT may still be missing (“no plan yet”).
3. **The four operator markers.** That is this git tree (or a clone). B10 / Keep refuse. Don’t.
4. **Only under the APK’s Android install dir** (`Android/data/...`). Boring location; still need a Domain file inside.
5. **Keep mkdir-any-folder.** Bind does not follow twin.

**Selected:** **Other — “4. But wait if we are limiting the projects to one CURRENT.md then wtf?! Let's just note this branch in ideas as a user limitations/potentials grey-area.”** Not 1 (CURRENT.md required). Not 3 (operator markers). Not 5 (any mkdir).

**Rationale / recommendation:**

**Recommend option 1.** Twin is boring because it reads the same files the sit already is. Bind follows by only opening folders that already *are* a Domain. Seeding a template is install, not Yes. Option 2 if you want “installed but no plan yet.” Option 3 is the operator tree. Option 4 can stack with 1.

**Cost tradeoff:**

- **v1:** $0. 1 = hours to refuse empty bind + optional seed template. 2 = same + a marker file. 3 = $0 and a Yes-on-operator-CURRENT bug. 4 = SAF/path lock. 5 = status quo.
- **alpha:** 1 means every sitter has a real CURRENT before JOIN. Twin always has a document.
- **SaaS vs self-host:** files, not a store.
- **Engineer-time:** 1 is the bind follow-through.
- **Crossover:** 1→2 when you want bind-before-plan. Never 3.

**Dependencies:** T8b, S0d, `bind_folder` today mkdir.
**Unlocks:** update PROPOSE bind clause; compact.

**Status:** `parked` (grey-area — ideas, not CURRENT Limits)

**Post-select note:** Boring bind *location* = under the Mechanicall APK install dir (Android/data/… or a named Mechanicall home). **Many** project folders, each with their own CURRENT.md, are allowed. Twin still reads whichever pocket is bound. Operator tree still refused. Do **not** ship “must already have CURRENT.md or bind fails” as law — that plus one-prefs-pocket would be one-project tyranny. Reopen T8c when the sit has more than one Domain on the phone.

---

## Grey-area — bind location vs many CURRENT.md (ideas / potentials)

**Not law. Not Decide.** User 2026-08-20.

| Limitation | Potential |
|------------|-----------|
| One `pocket_one` pref today | Many folders under the APK install dir; DIR picks which CURRENT |
| Twin reads one bound path | Twin can list install-dir children later |
| Require CURRENT.md to bind | Blocks “empty afternoon folder”; seed template instead |
| Bind operator tree | Their Yes would apply mechanicall-os Next — stay refused |
| mkdir anywhere | Fat-finger /sdcard junk; install-dir is the boring fence |

Revisit after compact if a second project must sit on the same phone.

---

## New-direction locked spec (T1–T8b, closed 2026-08-20)

Not CURRENT until human applies `examples/propose-current/CURRENT-sit-vst-headscale.md`.

| Piece | Lock |
|-------|------|
| Law | One new id `sit-vst-headscale`. Do not stretch `sit-join-workshop`. |
| VST | Genuine instrument **frame**; paper Suggesting **inside**. No painted letterbox. |
| Headscale | Self-hosted login-server + preauth keys in JOIN paste. Userspace still. No OIDC. |
| GATE | LOAD (boot/weights) then STREAM (tokens). Timeouts. Not `…`. |
| Desktop | One niced generate; LOAD may follow WAKE. Never auto-Yes. |
| STORM | Iterate storm-0; never A33; round cap in PROPOSE (suggest 3). |
| Visual | `/visual-reviewer` in that loop. app-reviewer stays behaviours. |
| Share | Operator read-twin (stands). Bind location grey-area: APK install dir, many CURRENT.md allowed. Not bind-this-repo. |

Fold leftover sit-join (tsnet JNI, STORM 03) into this id.

---

### G0 — Git-tree facts (auto, not re-asked)

**Question (exactly as posed):** *(not posed — explore the tree instead)*

> What is dirty, what is already tested, what must never be in a PR, and which Next is live?

**Options presented:** none (codebase answers).

**Selected:** Record facts for the executor; do not commit.

**Facts (2026-08-20 analyser turn):**

- Header **Next:** `sit-vst-headscale`. Phase APPROVE. Status APPROVED. Human next+approve at 21:41Z. `aether preflight sit-vst-headscale` ALLOW.
- Body **Action id** still `sit-join-workshop`. Agents do not rewrite CURRENT. Human applies remaining PROPOSE body when they want the fields to match.
- `git status --porcelain`: **60** paths. `master` = `7471ad1` tracking `origin/master`. Open GitHub PRs: none. Historical PRs #1–#6 were small; #2–#4 reused one feature branch.
- Pocket tests green: `test_aether_pocket` 39, `test_app_verify` 4 (mechanical 10/10), `test_aether_client` 13, `test_aether_pocket_serve` 6.
- `android/app/build` = 237M, gitignored. `android/app/src` ≈ 300K. `local.properties` gitignored.
- `dev/37` RECEIPT: JOIN fingerprint + wol-pi wake + hunk ICM exist; **tsnet JNI does not**; STORM 03 not started.
- S13 still: grouped commits, exclude CURRENT + secrets, **only after Go-git**.
- No adb devices this turn; `/sdcard/mechanicall/` could not be pulled.

**Rationale / recommendation:** Skip-obvious. Re-asking “is CURRENT law?” wastes the G1 question.

**Cost tradeoff:**

- **v1:** $0.
- **alpha / growth:** N/A (operator hygiene).
- **SaaS vs self-host:** git they already have.
- **Engineer-time:** 0; this is a receipt.
- **Crossover:** none.

**Dependencies:** S13, T1, live CURRENT header pin, cold-start header-vs-body rule.
**Unlocks:** G1 (how to slice PRs).

**Status:** `decided` (auto)

---

### G1 — How do we slice 3–4 PRs, and what is `/sdcard/mechanicall/`?

**Question (exactly as posed):**

> Live Next is `sit-vst-headscale` (APPROVED; preflight ALLOW). The dirty tree is **60** paths. Historical GitHub PRs #1–#6 were small and reviewable. S13 locked grouped commits after **Go-git**. You asked for **3–4 PR iterations** with extra debugging/alignment from `/sdcard/mechanicall/` (available from the store). How do we slice the next PRs, and what is that folder?

**Options presented:**

1. **Four sequential PRs on `feat/sit-vst-headscale`; `/sdcard/mechanicall` is a read-only review inbox (recommended).** PR1 pocket engine+tests (already green). PR2 `android/` source only (gitignore hides 237M build). PR3 ICM 38 + leftover 37 + FACE-SPEC v2 after human *proceed to 01*. PR4 STORM+twin+GATE/Headscale **after** at least one inbox drop. Park `dev/23`–`36` PNG archaeology as a later archive PR. Do **not** bind `/sdcard/mechanicall` (existing sessions tree — `examples/pocket-demo-client/README.md`). Twin still reads the bound pocket (`/sdcard/mechanicall-pocket` or APK install-dir child). Exclude CURRENT, events, secrets. No commit until **Go-git**. Halt after each PR. Cap 4; do not open a fifth under this action-id.
2. **One PR of everything except CURRENT+secrets.** Recants S13 grouping. Review-hostile. Easy to smuggle `app/build` or CURRENT.
3. **Skip consolidation; PR1 is only sit-vst FACE-SPEC docs.** Engine stays untracked; CI on master still does not run pocket tests.
4. **Bind `/sdcard/mechanicall` as the twin source / one Domain.** Recants the pocket-demo README and T8c “sessions tree is not the bind.” Easy to treat operator sessions as sitter law.

**Selected:** *(open — waiting)* Working assumption in `dev/38_sit-vst-headscale/00_git-pr-brief/output/EXECUTOR-BRIEF.md` is **option 1**. Not executed.

**Rationale / recommendation:**

**Recommend option 1.**

Why: you named the role (git-tree analyser → executor) and the loop (3–4 PRs + sdcard feedback). Option 2 is the ball-of-dirt S13 already refused. Option 3 leaves the only green tests off origin. Option 4 is a bind bug: `/sdcard/mechanicall` is the old sessions tree; the demo pocket is `/sdcard/mechanicall-pocket`. Inbox ≠ bind. Twin (T8b) still reads the *bound* folder, not the sessions dump.

**Cost tradeoff:**

- **v1:** $0 GitHub (already). 1 = 30–90 min grouping + 4 review halts + whatever the inbox names. 2 = 10 min and a bad bisect. 3 = 0 engine backup. 4 = $0 and a law incident.
- **alpha / growth:** N/A.
- **SaaS vs self-host:** git + adb pull. No BaaS. No Play.
- **Engineer-time:** PR1 is hours (files already exist). PR2 is hours if gitignore holds. PR3–PR4 are the actual sit-vst weeks (VST frame + Headscale + GATE), not the grouping.
- **Crossover:** 1→3 only if you will not say Go-git this week. Never 2. Never 4.

**Dependencies:** G0, S13, T6 STORM cap, T8b read-twin, pocket-demo “do not bind `/sdcard/mechanicall`”.
**Unlocks:** Go-git PR1; then inbox; then PR2–PR4. Research `01_research` is a separate “proceed to 01” line.

**Status:** `open`

**Post-select note:** Executor brief already names option 1 so a later agent is not blocked. They still wait for this pick **and** Go-git. **E-series parks G1 until dirty-tree last.**

---

### E0 — Facts from the tree (auto, not re-asked)

**Question (exactly as posed):** *(not posed — explore instead)*

> What is live law, what is EdubaWare, what is STORM SDK, what is dirty?

**Selected:** Record; do not recant PRODUCT in this leaf.

**Facts (2026-08-21):**

- CURRENT: Next `sit-vst-headscale` **REJECTED**. Dual-Next Reject. Models never approve.
- Park PROPOSE exists: `examples/propose-current/CURRENT-park-sit-edubaware.md` — not applied.
- PRODUCT: Mechanicall is local-first **authority protocol**. Not an AI hosting platform. Not multi-tenant SaaS. Not “another chatbot.”
- EdubaWare (Jake Van Clief / Eduba, alpha ~2026-08-06): ICM folders in **shared containers**, live multiplayer chat, client workspaces. Public pitch is **cloud alpha** (VIP; he covers model cost). ICM *method* is already this repo’s meta-agent (arXiv:2603.16021). GitHub: RinDig/Interpretable-Context-Methodology.
- STORM SDK (`dev/34_one-goal-vst/03_storm-research/output/STORM-ENV.md`): myarch has cmdline-tools/adb/android-34; **missing emulator package + system-images;android-34;google_apis;x86_64**; **no AVDs**. Cap 2 AVDs (32 GiB, 0 swap). Never A33. Never Confirm.
- Dirty tree: last by S13 + user sequence. Commit ≠ Yes. Never live CURRENT.

**Status:** `decided` (auto)

---

### E1 — What is “EdubaWare pivot/clone” *in this repo*?

**Question (exactly as posed):**

> You halted the phone sit (app is good; minor client purpose) and asked to grill **EdubaWare pivot/clone**, then swarm-implement, then swarm-review with STORM SDK, then the dirty mechanicall-os tree. Live Next `sit-vst-headscale` is **REJECTED**. PRODUCT says Mechanicall is not an AI host / not multi-tenant SaaS. EdubaWare’s public product is **cloud alpha** on ICM folders. What should this repo *become* (or stop being)?

**Options presented:**

1. **Look-only / park.** Operator uses EdubaWare (or Clief Notes) for multiplayer ICM. This repo stays the authority protocol. Sit stays parked. Matches `park-sit-edubaware`. No clone. Swarm implement has nothing new to build except a park receipt.
2. **Hosted clone.** Recant PRODUCT: Mechanicall *becomes* a multiplayer workbench (containers, live chats, client spaces) like EdubaWare. Weeks–months. Dual-identity risk.
3. **Self-host clone of the *shape* (recommended if you insist on building).** Local-first workbench: shared ICM folder + CURRENT/aether + multiplayer *on that folder* (not a SaaS). No Eduba cloud. No “agent seats for sale.” Steal interaction (map, container-or-tmux, live files), keep Decide-only Yes.
4. **ICM-repo only.** Use RinDig’s open ICM tree as the method; do not clone EdubaWare product. Already mostly true.
5. **Stretch REJECTED `sit-vst-headscale`** to include an EdubaWare clone. Dual/stretch. Reject.

**Selected:** **Other — option 3 with private containers.** User: “containers privately. Go!” Not 1 (park-only). Not 2 (hosted clone / his cloud). Not 4 (method-only). Not 5 (stretch sit-vst). Not tmux-as-enough.

**Rationale / recommendation:**

**Recommend option 3 if “pivot/clone then swarm implement” means a build; option 1 if “I like their product” means use theirs.** They picked the build **and** named the isolation: **private containers**. That is EdubaWare’s workbench *shape* (one workspace per box) without his cloud. PRODUCT still holds: not a SaaS host. “Go!” on E1 is the pick, not aether approve.

**Cost tradeoff:**

- **v1:** 1 = $0 + EdubaWare alpha (his model bill). 3 = $0 infra + 1–3 weeks local workbench. 2 = $5–50/mo VPS+GPU or his cloud. 4 = $0. 5 = muddy Limits.
- **alpha (50):** 2 is a product. 3 still one operator box. 1 is their SaaS.
- **growth (1,000):** none of this is Mechanicall-core as written.
- **SaaS vs self-host:** EdubaWare public = hosted. 3 is self-host. Delta = your ops vs his alpha.
- **Engineer-time:** 1 = hours. 3 = 1–3 weeks. 2 = 4–12 weeks. Swarm does not shrink 2 into a day.
- **Crossover:** 1→3 when you refuse to wait on his alpha. 3→2 only if you recant PRODUCT. Never 5.

**Dependencies:** E0, PRODUCT, live REJECTED Next, dual-Next Reject.
**Unlocks:** E2 (one new Next vs park-only), then swarm implement shape, STORM SDK, dirty tree last.

**Status:** `decided`

**Post-select note:** Containers are **private** (per workspace, not a shared public host). EdubaWare remains the stolen *interaction*, not the control plane. Sit APK parked, not deleted.

---

### E2 — Where does this live vs REJECTED `sit-vst-headscale`? (auto)

**Question (exactly as posed):** *(not posed — skip-obvious)*

> Dual-Next is Reject. Live Next is REJECTED. You said Go on a new build. One new action-id, park-only, or stretch sit-vst?

**Options presented:** (would have been)

1. **One new action-id for private-container workbench + later swarm/STORM/git as that Next’s stages** (recommended).
2. Park-only (`park-sit-edubaware`) — recants E1 Go.
3. Stretch `sit-vst-headscale` — dual/stretch.

**Selected:** **1 — one new action-id** (auto-approved). Working name (not law): `icm-private-containers`. Human applies PROPOSE after this grill; agents do not `aether next`.

**Rationale / recommendation:** T1 already locked one-id-for-the-bundle. Same here.

**Cost tradeoff:**

- **v1:** $0 process. 1 = 10 min of your next+approve after PROPOSE + the E1 weeks. 2 = halt. 3 = muddy Limits.
- **alpha / growth / SaaS:** none.
- **Engineer-time:** process only.
- **Crossover:** never 3.

**Dependencies:** E1=private containers, T1, dual-Next Reject.
**Unlocks:** E3 topology.

**Status:** `decided` (auto)

---

### E3 — Where do the private containers run, and how many at once?

**Question (exactly as posed):**

> E1 locked **private containers** (not Eduba cloud). myarch is 32 GiB, **0 swap**, already the 7B desk. STORM already caps **2** AVDs. A container per client workspace is the EdubaWare steal. Where do they run, and what is the live cap?

**Options presented:**

1. **Docker (or podman) on myarch; one container per workspace; cap 2 live** (recommended). Same RAM honesty as STORM. Extra workspaces queued. Desk 7B still niced; containers share the box, not a second GPU.
2. **wol-pi** always-on, weaker CPU. Kind to the desktop; mean to the workbench.
3. **k3s / k8s** on myarch or a VPS. Real product ops; not this lab week.
4. **One shared container** for all clients. Not private.
5. **Dedicated second machine / GPU.** Purchase. Not this lab.

**Selected:** **3 — k3s / k8s.** User override of recommendation (was 1 — docker cap 2). Not wol-pi. Not one shared container. Not a second GPU purchase (5).

**Rationale / recommendation:**

**Recommend option 1.** They picked **3**: real product ops, private workspaces as cluster tenants. Honest: this is the alpha crossover I named, not the lab week. Isolation can still be one namespace/pod per workspace. CURRENT/aether stay in the folder the pod mounts — k8s is not a second authority store.

**Cost tradeoff:**

- **v1:** $0. 1 = 1–3 days compose/files + cap. 2 = Pi RAM/CPU pain. 3 = +week k8s. 4 = $0 and a privacy lie. 5 = $200–2,000.
- **alpha (50):** 1 is still a lab; you queue. 3 or 5 is the crossover.
- **growth:** not this box.
- **SaaS vs self-host:** still your metal. No Eduba bill.
- **Engineer-time:** 1 is the implement swarm’s first factory. 3 adds cluster YAML, ingress, storage — swarm implement is heavier.
- **Crossover:** 1→5 when you buy a box. 1→3 when you hate compose (they took this now). Never 4 if E1 is private.

**Dependencies:** E1 private containers, T5 niced generate, T6 STORM cap 2.
**Unlocks:** E3b (myarch vs VPS), then E4 STORM SDK (same box only if cluster is not eating it).

**Status:** `decided`

**Post-select note:** Option 3 said “on myarch **or** a VPS.” That split is E3b. Swarm + STORM SDK on the *same* 32 GiB box as k3s + 7B is the failure mode.

---

### E3b — Where does the k3s cluster live?

**Question (exactly as posed):**

> You picked **k3s/k8s** (E3=3) for private containers. myarch already runs `personal-llm-sft-v4` (32 GiB, 0 swap) and is the STORM host (cap 2 AVDs, emulator image **not installed yet**). Option 3 allowed myarch **or** a VPS. Where is the cluster?

**Options presented:**

1. **VPS (or spare always-on box) for k3s; myarch keeps 7B + STORM** (recommended). Private still: Tailscale/Headscale to the node, no public Funnel, no 0.0.0.0. ~$5–20/mo. Desktop stays yours.
2. **k3s on myarch anyway.** $0 extra. 7B + k3s + STORM fight. Cap live workspaces to 1, STORM to 0 or 1 while cluster is up.
3. **k3s on wol-pi.** Always-on, weak. Possible for *control plane* only; worker elsewhere.
4. **Split: k3s server on VPS, worker on myarch.** More moving parts; GPU jobs can stay home.
5. **Recant E3 back to docker cap 2 on myarch.** Lab week honesty.

**Selected:** **2 — k3s on myarch.** User: “2. STORM is only developer infrastructure...” Not VPS (1). Not wol-pi (3). Not split (4). Not recant to docker (5).

**Rationale / recommendation:**

**Recommend option 1** (keep the desk). They picked **2** and recast STORM: AVDs are **lab**, not a reason to buy a VPS. k3s+7B on myarch is the live product path. STORM cap goes to **0 while cluster+7B are hot**; storm-0 only when the operator is reviewing. Never A33. Never public kube-apiserver.

**Cost tradeoff:**

- **v1:** 1 = $5–20/mo + a weekend of k3s. 2 = $0 and hitch. 3 = $0 Pi, slow. 4 = $5–20 + extra YAML. 5 = $0 and recants 3.
- **alpha (50):** 1/4 scale; 2 does not.
- **growth:** real nodes, not myarch.
- **SaaS vs self-host:** VPS is still self-host k3s, not EdubaWare. They chose $0 metal.
- **Engineer-time:** 1 = +2–5 days vs docker. 2 = +2–5 days *and* you debug OOM when you forget to stop AVDs.
- **Crossover:** 1→2 only if you will not pay $5 and will pause STORM (they took this). 1→5 if cluster was a mood. Never public kube-apiserver.

**Dependencies:** E3=3, T5, T6, no Funnel.
**Unlocks:** E4 — STORM SDK is factory, not product.

**Status:** `decided`

**Post-select note:** T6 “iterate storm-0” stays lab law for the parked sit. It is not a sitter-facing SLA.

---

### E4 — What is “swarm review with STORM SDK” now?

**Question (exactly as posed):**

> You asked for swarm implement, then **swarm review with the STORM SDK setup**, then the dirty tree. You just locked: **k3s on myarch**, and **STORM is only developer infrastructure**. The new product is private k3s workspaces, not the APK. `STORM-ENV.md`: emulator package + API-34 x86_64 image are **missing**; no AVDs. Never A33. What does that review step *do*?

**Options presented:**

1. **Install STORM SDK this Next as factory only** (recommended). `sdkmanager` emulator + `system-images;android-34;google_apis;x86_64`; at most one AVD, and **only when k3s+7B are quiet**. Swarm review of k3s = kubectl/receipts/`tests/run.sh`. AVD is extra for the parked sit. Never Confirm. Never A33.
2. **No AVD this Next.** Swarm review is k3s-only (pods, logs, aether tests). Install emulator later if the sit unparks.
3. **Rename STORM** to mean k3s test namespaces / jobs. Drop Android emulator from this direction.
4. **Two AVDs + k3s + 7B at once** — cap 2 as product. OOM. Don’t.
5. **Source-only forever** (like sit-vst 03 round 1). Fast compact; no glass.

**Selected:** **Other — original app central; cut EdubaWare platform; persist STORM as an application component.** User: “OK original app is central, now! Cut the Eduware platform. Emulator means just a mass-app subagent review environments for this chat sessions. I just want the tools to be persistent via being an actual application component on here! Go!” Recants E1–E3b as *product*. Not k3s review (2/3). Not two AVDs+k3s+7B (4).

**Rationale / recommendation:**

Factory already in-tree this turn: `python/aether_storm.py`, `scripts/storm-sdk-setup.sh`, `scripts/storm-up.sh`, `.grok/skills/storm-review/SKILL.md`. SDK on disk **already has** emulator + API-34 x86_64 image (STORM-ENV.md was stale). AVD home `~/.mechanicall/storm` is not git. “Go!” is the pick + factory, not `aether approve`. PROPOSE `sit-storm-factory` waits for human apply. Dirty tree still last.

**Cost tradeoff:**

- **v1:** $0. Images already local. AVD create is minutes. Boot is RAM.
- **alpha:** still lab.
- **SaaS vs self-host:** local SDK.
- **Engineer-time:** factory landed this turn; swarm glass after AVD boot + applied Next.
- **Crossover:** never A33. Never EdubaWare-as-core.

**Dependencies:** user recant, T6 never A33, original sit.
**Unlocks:** human apply `sit-storm-factory`; then storm-0 boot; Go-git last.

**Status:** `decided`

---

### E7 — How are Headscale folder uploads/downloads handled?

**Question (exactly as posed):**

> Final consideration needed: how are the headscaled folder uploads/downloads handled?

**Code already true (so we do not invent EdubaWare disk):**

- Headscale is a **login-server**. It coordinates WireGuard. It does **not** store folders, CURRENT, or PROPOSE.
- Tailscale.com **Taildrop / Send** is not a Headscale product feature we can steal as law.
- Bound pocket is **local** on the phone (B10). JOIN pastes a preauth key; tsnet state is `filesDir/tsnet/`, never the Domain folder.
- Designed download: **GET-only twin** (`python/aether_twin.py` / future tsnet `Listen :7078`) of named law files into `~/mechanicall-twin/<sitter>/`. GET is not Yes. POST is 405.
- JNI tsnet is **not** in the APK yet — so even that download is not live. Lab fallback is adb pull (not STORM, not A33-as-product).
- S12: no syncthing dual-write, no git-in-pocket this sit. `aether_pocket_serve.py` Yes POST is the **brake face**, not twin.

**Options presented:**

1. **Mesh + read-twin only** (recommended). No Headscale upload. Operator **downloads** CURRENT/PROPOSE/receipts/events/gate/tailnet-fp via GET over the tailnet. Sitter **uploads** nothing to Headscale. Seed a folder with Bind (local) or a template copy — that is install, not Yes. Matches T8b + SHARING.md.
2. **Seed download once.** After JOIN, phone may **GET a template pocket** from operator (PROPOSE-shaped zip or file list) into the bind path if empty. Never overwrite live CURRENT. Still no Headscale object store.
3. **Propose-only PUT.** Twin grows POST `/propose` that writes `PROPOSE-CURRENT.md` only. Still 405 on `/current`. Easy to mistake for Yes.
4. **Bidirectional rsync/syncthing over Headscale.** Two writers. Dual-CURRENT. S12 Reject.
5. **EdubaWare-style upload folder into a container.** Recanted (E4). Don’t.

**Selected:** **Other — depreciated opt-in folder send to mechanicall-os `.inbox/` (gitignored) + operator can send folders back (new application, etc.) with user notify/Accept.** Not 1-only. Not 4 syncthing. Not 5 EdubaWare. User: “6. I want to give the users a depreciated option to upload the folder to my mechanicall-os repo (locally; .gitignored). It also allows me to send folders back full of a new application, etc. (this notifies the user). Protocols for handling this should follow standards of similar procedures of other AI platforms with this action.”

**Rationale / recommendation:**

**Recommend option 1** as the *default* (twin GET). They added a **depreciated** extra path: local inbox, not Headscale-as-Drive. Steal AirDrop/Taildrop/ChatGPT-download: offer → notify → Accept/Decline. Accept skips live CURRENT. A new-application folder is still an offer, not auto-install Yes. Default twin GET remains.

**Cost tradeoff:**

- **v1:** 1 = $0 + tsnet Listen (JNI leftover). 2 = +0.5–2 days seed GET. 3 = +1 day and a Yes-shaped footgun. 4 = split-brain repair. 5 = recant.
- **alpha (50):** 1 is one operator watching many GET endpoints. 4 is 50 dual-CURRENT incidents.
- **growth:** not a Drive.
- **SaaS vs self-host:** no BaaS. Headscale stays coordination.
- **Engineer-time:** 1 is already specified; blocked on tsnet JNI. 2 is extra. Never bake pockets into the APK.
- **Crossover:** 1→2 if first-run empty folder is too harsh (T8c grey). Never 4. Never 5.

**Dependencies:** T8b, S12, SHARING.md, E4 recant, tsnet JNI gap, B10.
**Unlocks:** amend PROPOSE `sit-storm-factory` with an explicit Headscale-bytes clause; then human re-apply.

**Status:** `decided`

**Post-select note:** `python/aether_inbox.py` + `.inbox/README.md` + gitignore. JNI still must carry bytes later. FILES overlay cut → device file manager (E8).

---

### E8 — FILES button (auto from E7 p.s.)

**Question (exactly as posed):** *(stated, not asked)*

> p.s. the files button should also get cut to just opening the devices actual file manager on the directory

**Selected:** Cut in-app FILES overlay. FILES opens the system file manager on the bound path. DIR stays Bind.

**Status:** `decided`

---

## Open branches not yet asked

| # | Branch | Why it waits | Cost note |
|---|--------|----------------|-----------|
| **C1** | What “Decide = normal notepad” is | **posed now** | 1–5d face; $0 SaaS |
| **C2** | What “working send/receive” is (lab drop vs mesh vs two-AVD vs LTE) | waits C1 | 2–7d; G4 JNI still BLOCK for mesh |
| **C-law** | One new CURRENT vs leftover under `sit-testers` | waits C1+C2 | process; dual-Next still Reject |
| **C3** | Recant B6 two-tap? only if C1=3 | waits C1 | BEHAVIOURS + human CURRENT |
| P-series | testers / SOS / template | **closed 2026-08-23** | live Next still `sit-testers` until human recants |
| E5 | Dirty tree / Go-git | last, as named | S13; never CURRENT |
| G1 | 3–4 PR slice | parked until E5 | $0 |
| T8c | Bind location vs many CURRENT.md | grey-area | $0 |

---

## Code already true (so we will not re-ask)

- Draft tab is `ChatHome.kt`: CAPITALISED field chips, LIVE vs DRAFT rows, one last desk say, SEND always (not `…`). Desk chat writes PROPOSE only when **local** stage is `propose`; `parsed_stage` cannot promote a question into a write (`python/aether_pocket.py` `draft_chat`).
- Plan tab is `PlanModule.kt`: human-editable wells + `SAVE DRAFT · not Yes` → `writeSchemaDraft` → `PROPOSE-CURRENT.md`. Never CURRENT.
- Desk hosts hardcoded: `127.0.0.1:11434`, `192.168.0.51:11434`, `100.90.85.68:11434`.
- Bind-first: unbound → FOLDER only (`SeatNav.kt`).
- No Join / Invite / Wake chrome in the APK. wol-pi is a peer; WoL is not wired. **Amendment 2026-08-20:** `dev/37` 02_implement landed JOIN paste/fingerprint + WAKE POST + paper Suggesting hunks (`0.14.0-sit`). tsnet JNI still absent. STORM 03 still not started. Mechanical app_verify can be 10/10 while VST frame and Headscale are unbuilt (this Next).
- B4 in `dev/36_actual-app-verification/01_contract/output/BEHAVIOURS.md` already demands “Draft is a plan workshop.” Mechanical scorer can PASS while a human still hates the workshop. That gap is S5.

---


---

# Archive — closed 2026-08-16 mobile-planning-demo interview

The following tree is **closed**. It is kept so S0 can inherit skip-obvious, Pocket Domain, Chaquopy, and “session is a proposal-doc editor” without re-asking. New work is the interview **above** the archive rule.

# Decision tree — mobile planning projection (phone demo spike)

**Premise:** Add an executable **spike** for a **phone projection** of Mechanicall OS: a demo you can use on your phone, live, to plan with/for a client, talking to a **remote Ollama** you serve, with a UI that honestly captures the **CLI/authority actions** — without pretending the phone is a new product core.

**Interview started:** 2026-08-16  
**Interview status:** **closed** 2026-08-16 — no remaining non-obvious forks. Spike spec: `dev/23_mobile-planning-demo/output/SPIKE.md`. Propose-current (human apply): `examples/propose-current/PROPOSE-mobile-planning-demo.md`.  
**Skill:** `grill-me-store-decisions`  
**Live law at start:** `CURRENT.md` Next = `casual-core-interface` · Phase APPROVE · Status APPROVED · Limits = design/docs this Next; heavy implement only after human approve of the contract. Non-goals on this Next include multi-surface suite expansion.

**Live law now (after D1):** `CURRENT.md` **Next field** = `mobile-planning-demo` · Phase APPROVE · Status APPROVED · two human `APPROVED` events 2026-08-16. **Body drift:** “This Next”, Action id, Brief, Limits, and Approval condition still describe `casual-core-interface`. No `next_selected` event in `.aether/events.jsonl` for the new id. Agents do **not** silently rewrite CURRENT to match.

---

## Guiding principles

Captured from the user at kickoff (elevate further if stated mid-interview):

1. **Demo build only — not a product.** Sideload APK on *your* phone for a live client sitting. Not Play Store. Not Mechanicall-core. Not a multi-user suite. D9=3 (whole-tree context) is allowed **only** while this remains a demo; if it ever stops being a demo, D9 reopens to 2.
2. **Works on my phone.** One operator device. Not a multi-user SaaS.
3. **Live client planning.** The session is real planning work, not a slide deck.
4. **Remote Ollama.** Phone consumes a model you serve (host already has B1 / `ollama-personal` / `/peer-serve`).
5. **UI must capture CLI actions.** The face has to *feel* like `aether` authority verbs, not a generic chat wrapper.
6. **Executable on a 4–8 week native calendar.** “Now” means we can *start* after this grill; first client-ready sitting is weeks, not a weekend. Still not a six-month suite or a second protocol.
7. **Law not stack** (from live CURRENT / PRODUCT). Phone is a *projection*. Filesystem remains truth. Models never approve. Silence ≠ permission.
8. **Minimize paid third parties.** Local-first; Tailscale/LAN over cloud relays; no new SaaS as authority store.
9. **This Next is `mobile-planning-demo`.** Human re-SELECTED and accepted (2026-08-16). Casual-core is paused, not cancelled. Do not treat the stale Action-id block as a second Next.
10. **Client is the planner.** They tap and say Yes. UI is mum-class (gate + plan + agent), not an operator verb museum. The phone is the workshop table.
11. **Client law lives outside this repo.** Their CURRENT is not `mechanicall-os/CURRENT.md`. The operator Next (`mobile-planning-demo`) stays on the product tree. The sitting binds a client path.
12. **Host device is the operator’s phone.** You hold the workshop. “Best setup” still means one filesystem of truth + remote Ollama — not a second copy of CURRENT.
13. **Pocket Domain.** Client CURRENT lives on the phone’s storage. `aether` runs on the phone. Ollama stays on myarch. Do not mirror a second CURRENT on the desk.
14. **Native app projection, 4–8 weeks.** The client-facing face is a real Android (assumed) app. That must remain a *projection* of the same files/`aether` law unless D12 explicitly chooses a Kotlin clone.
15. **One engine: embedded CPython + shipped `aether`.** Chaquopy (or equivalent) runs the same `python/` + `bin/aether`. Kotlin is chrome. No second parser of CURRENT.
16. **The session is a proposal-doc editor.** “Chat” is an interactive document page. The agent edits a **proposal of CURRENT** (not CURRENT). Live CURRENT + receipts + human Yes stay visible. Bubble-chat as the main face is rejected. Interaction polish is **after this sprint**.
17. **Skip obvious questions.** User 2026-08-16: “avoid obvious questions… I approve any obvious questions.” If prior law + this tree already determine the pick, record the recommendation as **decided (auto-approved)** and do not re-ask.

---

## Master cost map

Recurring **$/month**. This spike is a **single-operator demo**. Most lines are $0 if we stay on existing hardware.

| Line item | v1 (solo / this demo) | alpha (50 users) | growth (1,000 users) | Self-host alternative | Notes |
|-----------|----------------------:|-----------------:|---------------------:|-----------------------|-------|
| Ollama inference | $0 (existing desktop GPU/CPU) | $0–80 (one always-on box) | $200–2,000 (GPU fleet or API) | Keep self-host | Do **not** send client plans to cloud APIs unless chosen later |
| Phone ↔ host network | **$0 Tailscale** (D8 auto) | $0–6 Tailscale Personal Plus | $30–500 Tailscale / headscale | Headscale $0 + VPS ~$5 | Public `:11434` rejected; LAN-only fails pocket-away-from-desk |
| App store listing | $0 if PWA / sideload | $25 one-time Google Play | $25 + 15–30% if paid | Sideload / F-Droid / PWA | Demo should not wait on Play review |
| Hosted web (anphuni.com Session) | $0 unused | lab only | not core | N/A | PRODUCT: Session is **not** Mechanicall core |
| Cloud LLM (OpenRouter / xAI) | $0 if Ollama-only | $20–200 | $500–5,000 | Ollama | Conflicts with “serve my remote Ollama” unless fallback |
| Auth / BaaS (Firebase etc.) | $0 (don’t) | $0–25 | $25–200 | Files + Tailscale ACL | Would become a second authority store — reject |
| Domain / TLS if public URL | $0–12 | $12 | $12 | Tailscale Serve / MagicDNS | Prefer private |
| Engineer time (this spike) | **4–8 weeks** (native app) | — | — | — | D5=3 user override; sideload first, Play optional $25 |
| Phone backup of client Domain | $0 (syncthing / `adb backup` / git remote you already own) | $0–5 | $0–20 | Never Drive-as-authority | Only copy is on the phone until you choose a *backup*, not a second law |

**Crossover:** SaaS chat wrappers get cheaper than self-host GPU around ~200 always-on users. Irrelevant to a one-phone demo. The cost that actually matters now is **your week**, not dollars.

---

## Summary of decisions

| # | Decision | Selected | Status | Cost impact |
|---|----------|----------|--------|-------------|
| 1 | Where the spike lives vs CURRENT Next | **3 — human re-SELECT `mobile-planning-demo`** (override; rec was 1) | decided | $0 infra; casual-core paused; CURRENT body still stale |
| 2 | Audience / demo moment | **1 — you + client as planner** (matches rec’s “if they participate” branch) | decided | +2–4 days mum-class gate UX vs operator demo |
| 3 | Bound Domain (whose CURRENT is on screen) | **4 — client project outside this repo** (override of rec 3; “on the host phone, best setup”) | decided | $0 if folder exists; privacy/insurance cost, not SaaS |
| 6 | Topology: files / aether / Ollama / UI | **2 — Pocket Domain** (override; rec was 1) | decided | +3–10 days aether-on-phone; $0 infra |
| 5 | Delivery vehicle on the phone | **3 — native app, 4–8 weeks** (override; rec was 2) | decided | 4–8 weeks your time; $0 infra; sideload |
| 12 | Native engine: projection vs second law | **2 — Chaquopy / embedded CPython + same aether** | decided | Heavier APK; $0 vendors; no dual-maintenance tax |
| 4 | Verb subset on the face | **2 — five + receipts**, plus proposal-doc editor session (not bubble chat) | decided | +0.5–1 day receipts; editor is the 4–8 week UX core |
| 10 | Look / session shape | **Proposal-doc editor page** (agent edits PROPOSE; refine after sprint) | decided | Not dual-pane Grok-cosplay; not a verb keypad |
| 7 | What Yes writes | **1 — apply visible PROPOSE into CURRENT + `aether approve`** | decided | One tap; agent never writes CURRENT |
| 8 | Network | **Tailscale → myarch:11434** (auto-approved) | decided | $0; B1 already proved this path |
| 11 | After the sprint | **Keep as this Next’s sideload lab app** (auto-approved) | decided | Not throwaway; not casual-core product claim |
| 13 | OS / UI toolkit | **Android + Kotlin/Compose chrome + Chaquopy** (auto-approved) | decided | Flutter+Chaquopy is the awkward pairing |
| 14 | Who types in PROPOSE | **Client and agent may edit; only human Yes applies** (auto-approved) | decided | Follows D2=1 + D7=1 |
| 9 | What Ollama may read | **3 — whole pocket tree every turn** (override; rec was 2; **demo-only**) | decided | Simpler demo; reopen to 2 if this ever ships |

---

## Per-decision sections

### D1 — Where does this spike live so we can execute it without lying about CURRENT?

**Question (exactly as posed):**

> CURRENT Next is `casual-core-interface` (APPROVED, design/docs first). Casual-core non-goals on this Next include multi-surface suite expansion. You asked to add a **spike we can execute now**. Where does that spike live, legally?

**Options presented:**

1. **Lab spike only.** Park the work under `dev/NN_mobile-planning-projection/` (plus this `decision-tree.md`). CURRENT Next stays `casual-core-interface`. We do **not** rewrite law. After the grill, a human may approve a *propose-current* appendix or just run the spike as lab. Fastest honest “execute now.”
2. **Casual-core projection clause.** The missing `docs/CASUAL-CORE-INTERFACE.md` (claimed accepted in CURRENT, file not in tree after 2026-08-11 tree-org) gains an explicit “phone projection” slice: same core (agent + Domain + Yes/Not yet), one extra face. Spike implements that slice only after you approve the contract. Slower; keeps product story coherent.
3. **Human re-SELECT Next** to something like `mobile-planning-demo`. This becomes the only Next. I will not run `aether next` / `approve`. You would. Cleanest for “this is what we are doing,” heaviest process, and it *pauses* casual-core.
4. **Client pitch document only.** No phone app. A written proposal you walk through. Zero build. Does **not** capture CLI actions on a phone.

**Selected:** **3 — Human re-SELECT Next to `mobile-planning-demo`.** User: “re-selected. accepted.” Live `CURRENT.md` **Next:** field is now `mobile-planning-demo`. Two human APPROVED events logged 2026-08-16T08:23Z. **User override** of recommendation (was 1 + appendix of 2).

**Rationale / recommendation:**

**Recommend option 1**, with a one-page appendix shaped like option 2.

Why: you said *demo*, *my phone*, *execute now*. Live CURRENT forbids treating a new surface as this Next’s product work. We already have a projection pattern (`aether panel` → `.aether/PANEL.md` + `panel.html`) and a working B1 Ollama path. A lab spike can reuse those without claiming “Mechanicall is now a mobile app.” Option 2 is the right *product* home later; option 3 is correct only if you want to stop casual-core this week; option 4 fails your “UI must capture CLI actions” principle.

**Cost tradeoff:**

- **v1 (solo):** $0. Option 1 = ~hours–days of your time after the grill. Option 2 = extra 1–2 days writing the contract before any phone pixels. Option 3 = process time + you pause the approved Next. Option 4 = $0 and no spike.
- **alpha (50):** If we accidentally pick 3 and ship “the Android app is the product,” we inherit store, support, and dual-authority costs ($0–50/mo + weeks). Option 1 keeps that door closed.
- **growth (1,000):** Native app + hosted backend would be the first real dollar line ($500+/mo or a GPU fleet). Do not unlock that from a demo decision.
- **SaaS vs self-host:** Irrelevant at v1 if Ollama stays on your desk. Delta vs OpenRouter: $0 vs ~$20–200/mo the moment the live client session leaks to a cloud model.
- **Engineer-time:** 1 = 0.5–2 weeks build after decisions. 2 = +3–5 days contract. 3 = +1 day process, same build. 4 = 0 build weeks.
- **Crossover:** Switch from 1→2 when the casual-core contract is the thing you want the client to see *as* the product. Switch 1→3 only if casual-core is blocked and the phone demo is the only work that matters this week.

**Dependencies:**

- Depends on: live CURRENT (Next, Limits, Reject).
- Unlocks: D2 audience/demo moment; D3 which Domain the phone binds; D4 verb subset vs full CLI; D5 delivery vehicle (PWA vs native); D6 Ollama path; D7 write vs propose authority on device.

**Status:** `decided`

**Post-select note:** Recommendation was 1. Human overrode to 3 and set `**Next:** mobile-planning-demo`. CURRENT body was **not** rewritten (Action id still `casual-core-interface`). Events have approve but no `next_selected` row. That is recorded, not auto-fixed. Casual-core remains Keep/Done inventory, not this Next.

---

### D2 — Who sits in the live session, and what must they walk away believing?

**Question (exactly as posed):**

> Next is now `mobile-planning-demo`. Who is in the room (or on the phone), and after ten minutes what must they believe that a chat screenshot cannot prove?

**Options presented:**

1. **You + the client as planner.** They tap / speak / say Yes. They must believe *they* can stay in control of an agent while a real plan writes itself. Phone is the workshop table.
2. **You + a peer watching; you plan for a client.** The peer must believe Mechanicall is a planning *protocol* (Next, refuse, human Yes), not a chatbot. The client Domain is the content. You drive.
3. **You solo, rehearsal.** No second human. The phone must be good enough that a later client session will not embarrass you. Spike is a dress rehearsal.
4. **You + outsider glance** (friend, investor, “look at this”). They must believe a product exists. Theater. Lowest fidelity allowed; weakest proof of planning utility.

**Selected:** **1 — You + the client as planner.** User: `1`. Matches the recommendation’s “if they participate” branch (default rec was 2).

**Rationale / recommendation:**

**Recommend option 2**, unless the “someone else” *is* the client — then **1**.

Your kickoff was “show someone else the planning utility” *and* “plan things with it, live for the client.” That parses as: audience ≠ work-object. A peer seeing a real client Domain get a Next, a preflight refuse, and a human Yes is the unique demo. Option 1 is correct only if they participate. Option 3 wastes the “show someone” clause. Option 4 fails “capture the CLI actions” — a pretty chat is enough for theater.

**Cost tradeoff:**

- **v1 (solo):** $0. 2 needs one extra human and a clean client Domain folder (hours). 1 needs larger Yes/Not-yet targets and fewer operator verbs (1–2 extra UI days). 3 is cheapest build, no social proof. 4 can be a static panel.html.
- **alpha (50):** Shipping 1-as-product means mum-class gate UX (casual-core again) — weeks. Shipping 2-as-product stays operator-grade. Shipping 4-as-product is a landing page, not Mechanicall.
- **growth (1,000):** 1 implies support, accounts, and a hosted face ($500+/mo or a fleet). 2 can stay self-host + Tailscale forever.
- **SaaS vs self-host:** Audience 4 tempts anphuni.com Session (already rejected as core). 1/2 stay on your Ollama. Dollar delta at v1: $0 vs opening a hosted-seat line.
- **Engineer-time:** 2 ≈ baseline spike. 1 ≈ +2–4 days touch/readability. 3 ≈ −1 day polish. 4 ≈ −3 days (static).
- **Crossover:** Switch 2→1 the moment the someone else must *use* the gate, not watch it. Switch any→4 only if the meeting is a glance, not a plan.

**Dependencies:**

- Depends on: D1 (this is now the Next, so the demo *is* the work).
- Unlocks: D3 which Domain; D4 verb subset (1 needs fewer verbs); D7 authority on device; D10 look; D9 privacy (1 exposes more to the client’s eyes).

**Status:** `decided`

**Post-select note:** Client participates. Spike UI must be large-gate, short copy, no operator chrome. Unlocks D3 as a *safety* question: their Yes must not apply to root `mobile-planning-demo`.

---

### D3 — Whose CURRENT is on the phone when they say Yes?

**Question (exactly as posed):**

> The client is the planner. If the phone is bound to this repo’s CURRENT, they will see Next `mobile-planning-demo` and can approve *our* work. Whose folder is law on the device?

**Options presented:**

1. **Named client Domain in this tree** (e.g. `domains/<client>/` or a new folder we create). Phone binds there. Their CURRENT is their plan. Root CURRENT stays the operator law for building the spike. `domains/` is already labelled LAB.
2. **Root `mechanicall-os` CURRENT.** They plan *this product*. Rejected for D2=1 unless the client is actually helping design Mechanicall.
3. **Disposable workshop Domain.** A fresh folder (`examples/workshop-YYYYMMDD/` or similar) with a real CURRENT, sanitized or invented project. First sitting is protocol-real, corpus-safe. Can graduate to 1 or 4 later.
4. **Client project outside this repo.** A path on your disk that is *their* work (existing job folder). Phone binds there. Cleanest “this is your plan” if those files already exist.

**Selected:** **4 — Client project outside this repo.** User: `4 - on the host phone (mine; best setup)...` Override of recommendation (was 3 first sitting / 4 if files already exist). Amendment: the sitting is hosted on the operator’s phone; they want the best topology for that (asked next as D6, not assumed).

**Rationale / recommendation:**

**Recommend option 3** for the first live sitting, **4** if you already have their project on disk, **1** if you are starting their Mechanicall home today.

Why: D2=1 makes option 2 a law bug — their Yes would actualise `mobile-planning-demo` on the product tree. Root Keep (“no child Domain as session authority”) means *this operator session* does not grow a sub-CURRENT theatre; it does **not** mean the phone cannot bind `aether current /path/to/client`. That path form already exists. Option 3 is the cheapest way to prove the loop without leaking a real client corpus to a demo Ollama session. Switch to 4 when the meeting is billable work, not a first showing.

**Cost tradeoff:**

- **v1 (solo):** $0. 3 = ~30–60 minutes to seed a workshop CURRENT. 1 = same plus a name and some real scope. 4 = $0 if the folder exists; +hours if you must copy/sanitize. 2 = $0 and a mistaken approve that pollutes product law (expensive in *repair time*, not dollars).
- **alpha (50):** 1-as-default implies many Domain folders on one host (still $0) plus backup/privacy process. 4-as-default implies you hold client disks (insurance/trust cost, not SaaS). 2-as-default does not scale — one CURRENT cannot be 50 clients.
- **growth (1,000):** Multi-tenant CURRENT on one phone is a hosted product ($500+/mo or a fleet). Do not unlock from this spike. Stay one-folder-per-sitting.
- **SaaS vs self-host:** 4 tempts “put their repo in the cloud so the phone can see it.” Delta vs local bind: $0 vs GitHub/Drive as a second authority store. Reject the cloud copy unless they already live there.
- **Engineer-time:** Binding is a path argument (`aether … <path>`) — hours, not weeks. The cost is *content and privacy*, not UI.
- **Crossover:** 3→4 when the sitting is real work. 3→1 when they want a lasting Mechanicall home. Never 2 while D2=1.

**Dependencies:**

- Depends on: D1 (operator Next is the spike), D2 (client taps Yes).
- Unlocks: D9 privacy (3 is safest); D4 verbs (same loop either way); D6/D8 (where that folder is served from); D7 (whose files the phone may write).

**Status:** `decided`

**Post-select note:** Do not bind the phone to `mechanicall-os`. Client Yes writes their tree. “On the host phone” is **not** yet “CURRENT bytes live in `/sdcard`.” That is D6. Recommendation 3 was declined in favour of real client work — privacy (D9) gets heavier.

---

### D6 — Best setup on your phone: where do files, aether, and Ollama sit?

**Question (exactly as posed):**

> You chose a client project outside this repo, hosted on *your* phone, and asked for the best setup. Remote Ollama still stands. Where do the client’s CURRENT bytes live, where does `aether` run, and where does the model run?

**Options presented:**

1. **Thin phone (recommended default).** Client folder lives on **myarch** (or another disk you already use for that job) — *outside* `mechanicall-os`. `aether` runs on myarch against that path. Ollama runs on myarch. Phone is a mum-class projection (PWA/panel face) + Yes/Not yet. One tree. Matches B1. “Host phone” = you hold the UI they touch.
2. **Pocket Domain.** Client CURRENT + files live **on the phone’s storage**. `aether` must run on the phone (Termux or an embedded port). Ollama stays on myarch (remote). Best if you leave the desk. Costs: aether-on-Android, backup, and a fat finger next to the only copy of their plan.
3. **Phone-only.** Files + aether + a small on-device model. Breaks “remote Ollama” unless you revoke that principle. Weakest planning model; simplest offline.
4. **SSH viewport.** Phone is Termux/JuiceSSH into myarch. Files never leave the desk. Looks like a CLI. Conflicts with D2=1 (client as planner, mum-class). Fine as *your* escape hatch, not their face.
5. **Mirrored copy.** Phone holds a working copy; desktop is “real.” Two CURRENTs. Reject unless you enjoy merge fights mid-meeting.

**Selected:** **2 — Pocket Domain.** User: `2!` Override of recommendation (was 1 / thin phone). CURRENT + files on phone storage; `aether` on-device; Ollama remote on myarch. Not a desktop mirror.

**Rationale / recommendation:**

**Recommend option 1.** That is the “best setup” for D2=1 + D3=4 + remote Ollama + existing B1.

Why: Mechanicall’s product is one folder of truth. myarch already serves Ollama and runs `aether`. The phone already works as a second device on that path. Putting CURRENT on the phone (option 2) is the right *pocket* setup later, but it is a second port of the protocol and a week of Termux/Android, not a spike you execute after this grill. Option 3 drops your stated model. Option 4 fails the client-as-planner face. Option 5 is a dual-authority bug.

If the meeting is *away from the desk* and the phone is the only machine in the room, say so — then the honest pick is **2**, not 1 with a prayer.

**Cost tradeoff:**

- **v1 (solo):** $0 infra for 1 (reuse B1 + panel projection). 2 = 3–10 extra engineer-days (Termux aether or embed) + phone backup habit. 3 = $0 money, worse model, +on-device RAM. 4 = $0, no new UI. 5 = $0 and a mid-session split-brain (repair time >> build time).
- **alpha (50):** 1 stays one host + many folders ($0–80 electricity). 2 means 50 phones are sources of truth (support nightmare, still $0 SaaS). 3 does not scale quality. 5 becomes a sync product ($0 or a Dropbox line — reject as authority store).
- **growth (1,000):** 1 + Tailscale is still self-host. 2-as-product is a mobile OS ($25 Play + weeks + support). Crossover: only move files onto the phone when the sitting *cannot* see myarch.
- **SaaS vs self-host:** 1/2/4 stay $0. Temptation on 2 is “just put the folder in Drive.” That is a second CURRENT. Delta: $0 vs $0–12 Drive + a lying receipt.
- **Engineer-time:** 1 = days (projection UI). 2 = +1 week minimum. 3 = +days model ops. 4 = hours (no mum UI). 5 = never done, only regretted.
- **Crossover:** 1→2 when you regularly plan off-LAN and cannot Tailscale to the desk. Never 1→5.

**Dependencies:**

- Depends on: D1 (spike is the Next), D2 (client taps), D3 (external client path), principle 4 (remote Ollama), principle 12 (you hold the phone).
- Unlocks: D5 delivery vehicle (1 → PWA/projection; 2 → Termux or native; 4 → no new app); D7 write path; D8 network; D9 where the corpus sits; D4 verbs (same five either way).

**Status:** `decided`

**Post-select note:** User overrode “best setup = thin phone.” The sitting can leave the desk. Spike now includes an on-phone `aether` (or an honest subset that still writes the same files). D5 is blocking. D9: the only copy of their plan is in your pocket until a *backup* exists — backup ≠ second CURRENT.

---

### D5 — How does aether + the mum face run on the phone?

**Question (exactly as posed):**

> Pocket Domain means `aether` must touch files on the phone. The client must see a mum-class face, not a raw shell. How do we run that without a second implementation of law?

**Options presented:**

1. **Termux + real `aether` + local panel/HTML.** Install POSIX `aether` in Termux, bind the client folder on phone storage, `aether panel` or a tiny localhost page. Fastest honest spike. Face is “dev” unless we theme it hard.
2. **Termux `aether` + Android WebView wrapper.** Same law as 1 (one shell `aether`). Wrapper is the mum face (big Yes/Not yet, plan, chat). Termux stays the engine. Extra 1–3 days. Recommended for D2=1.
3. **Native port.** Reimplement preflight/approve/CURRENT in Kotlin (or embed Python via Chaquopy). Second law. 4–8 weeks. Reject for a spike.
4. **Chrome PWA only.** No on-device `aether`. Browser cannot faithfully write the Domain folder. Fails Pocket Domain + “capture CLI actions.”
5. **Termux raw CLI only.** `aether shell` in a terminal. $0 extra UI. Client-as-planner will not tap this.

**Selected:** **3 — Native app, 4–8 weeks.** User: `App projection is... 3! Let's do this for 4-8 weeks...` Override of recommendation (was 2; 3 was labelled “reject for a spike”). They named it **app projection** — vehicle is native; whether the *engine* is still `aether` is D12, not assumed to be a Kotlin clone.

**Rationale / recommendation:**

**Recommend option 2.** One `bin/aether`. One folder. A face the client can tap.

Why: D6=2 forbids “phone is only a browser.” D2=1 forbids “phone is only a terminal.” Option 1 is acceptable if “execute this weekend” beats polish — we can wrap later. Option 3 is the habitat overfit casual-core just escaped. Option 4 cannot be the only copy of CURRENT. Option 5 is your escape hatch, not their workshop.

**Cost tradeoff:**

- **v1 (solo):** $0. 1 = 1–3 days if Termux + `aether` already almost runs. 2 = +1–3 days WebView/TWA. 3 = 4–8 weeks + a second bug surface. 4 = days of UI that cannot keep law. 5 = hours.
- **alpha (50):** 2-as-product still means “install Termux + wrapper” — not shippable to 50 clients. 3 is what 50 users would need ($25 Play + support). Stay honest: this spike is *your* phone.
- **growth (1,000):** Only 3 looks like an app store product. Do not take that crossover from a demo sitting.
- **SaaS vs self-host:** All five $0 if Ollama stays on myarch. Wrapper must not route chat through a cloud BaaS.
- **Engineer-time:** 2 is the minimum mum-class pocket. 1 is the minimum *lawful* pocket. 3 is a rewrite.
- **Crossover:** 1→2 before the first client sitting if they will hold the phone. 2→3 only if you later productize Android and still generate CURRENT via the same file schema (not a new protocol).

**Dependencies:**

- Depends on: D6=2 (aether on phone), D2=1 (mum face), D3=4 (folder outside this repo, on device).
- Unlocks: D4 verb buttons on that face; D7 which of those buttons write; D8 how Termux reaches myarch:11434; D10 layout.

**Status:** `decided`

**Post-select note:** Calendar is 4–8 weeks. Principle 6 amended. D2=1 + D6=2 + D5=3 = mum-class native face over a pocket Domain. The dangerous reading of option 3 (“reimplement preflight in Kotlin”) is **not** locked. User said *projection*. D12 must close that.

---

### D12 — Native for 4–8 weeks: projection of aether, or a second law?

**Question (exactly as posed):**

> You chose a native app projection and a 4–8 week build. Option 3’s label also said “reimplement preflight/approve/CURRENT in Kotlin.” Does the app *call* the same `aether`, or does Kotlin become a second protocol?

**Options presented:**

1. **UI projection + exec `bin/aether`.** Compose/Flutter (or similar) is chrome only. Approve / preflight / next / current are subprocess (or bundled POSIX) of the same scripts as desktop. One law. Packaging is the hard 4–8 weeks.
2. **Embedded CPython (Chaquopy / similar).** APK ships the same `python/` + `bin/aether`. One law. Heavier APK; fewer “shell on Android” fights.
3. **Kotlin (or Dart) protocol clone.** Re-parse CURRENT, re-code preflight/approve/events/next. Same *filenames*, second *implementation*. Full 4–8 weeks of dual-bug risk. Only pick this if you want the app to survive without shipping POSIX aether.
4. **On-device daemon.** Termux or a tiny service runs real `aether`; the app is HTTP/localhost chrome. Same as 1 with a clearer process split. Extra moving part.

**Selected:** **2 — Embedded CPython (Chaquopy / similar).** User: `2`. Matches recommendation. APK ships the same `python/` + `bin/aether`. Kotlin/Compose (or similar) is chrome only.

**Rationale / recommendation:**

**Recommend option 2**, with **1** as fallback if Chaquopy is ugly on your device.

Why: you said *app projection*. In this repo that word already means “face of the files,” not “new authority.” PRODUCT and casual-core both refuse a second stack. A Kotlin clone will drift from `bin/aether` the first time SPEC-v0.2 gains a field — and the client Yes would depend on which parser they hit. 4–8 weeks is enough time to make a *face* honest; it is not enough time to make two laws stay twins. Option 4 is fine if you already live in Termux. Option 3 is the one I still recommend you refuse.

**Cost tradeoff:**

- **v1 (solo):** $0 vendors. 1/2/4 = 4–8 weeks of UI + bind + Ollama client + packaging. 3 = same calendar *plus* a permanent dual-maintenance tax (every `aether` change twice).
- **alpha (50):** 1/2 still sideload-your-phone. 3-as-product looks “more Android-native” and becomes the only copy of law 50 people trust — then a desktop `aether` disagree is a support incident ($0 SaaS, high repair).
- **growth (1,000):** Clone (3) is what an app-store product tempts. Crossover only if desktop CLI is abandoned. You have not abandoned it.
- **SaaS vs self-host:** Irrelevant if the engine is files. Clone tempts Room/SQLite “for speed” — that is a second store. Delta: $0 vs a lying receipt.
- **Engineer-time:** 2 ≈ 4–8 weeks (your stated budget) with one protocol. 3 ≈ 4–8 weeks now + 20–40% forever. 1 ≈ same as 2 if process-exec works; more if Android kills subprocesses.
- **Crossover:** Stay 2/1 until `aether` cannot run on-device at all. Then 4. Never 3 while desktop `aether` is still the operator tool.

**Dependencies:**

- Depends on: D5=3 (native calendar), D6=2 (files on phone), principle 7 (law not stack), user’s word “projection.”
- Unlocks: D4 verb surface (buttons call aether vs call Kotlin); D7 write path; D8 network to myarch Ollama; D10 look; language/toolkit (Compose vs Flutter) as a *later* leaf, not law.

**Status:** `decided`

**Post-select note:** Law stays `bin/aether`. A contract test (app approve vs CLI approve → same events.jsonl shape) belongs in the 4–8 week plan. Toolkit (Compose vs Flutter) is still a leaf.

---

### D4 — Which CLI actions does the client actually see?

**Question (exactly as posed):**

> The app calls real `aether`. The client is the planner. `AETHER_VERBS` is a long list. Which verbs are on the face, and which stay operator-only?

**Options presented:**

1. **Authority five + chat only.** Always-on CURRENT (objective / Next / phase). Buttons: preflight, approve, reject, next. Chat to remote Ollama (peer/propose). No seed/garden/distill/panel/shell. Smallest honest Mechanicall.
2. **Five + visible receipts.** Same as 1, plus read-only `brief` / last events / `drift`. They can *see* the trail that proves it is not a chatbot. No extra writers.
3. **Full `AETHER_VERBS` keypad.** Every CLI action as a button. Captures the CLI literally. Overwhelms a client. Extra week. Reject for D2=1.
4. **Panel split.** Main face = 2. **Advanced** (hold / operator PIN) = distill, seed, event, artifact, garden, … You can operate mid-sitting without handing them the museum.

**Selected:** **2 — Five + visible receipts**, with a user amendment that replaces “chat as chatbot.” User (prior turn): `2...` + chat like a session, agent edits a proposal of CURRENT on a page, refine after sprint. User (this turn): the session is **mainly an editor** — an interactive doc editor session/page where the agent edits the proposal document.

**Rationale / recommendation:**

**Recommend option 2.**

Why: PRODUCT’s five questions *are* the planning utility you want to show. Receipts (`events`, brief, drift) are how they believe a Yes happened. Option 1 is almost enough but a chat-only screen still looks like a wrapper. Option 3 fails mum-class. Option 4 is the right *operator* escape if you already know you will need `seed` mid-meeting — pick 4 if you are also the operator in the same sitting (you are). Slight lean: **2 now, 4 if you refuse to leave the app for your own tools.**

Explored instead of asking a longer list: `bin/aether` verbs and `aether_panel.py` already split “main board = human gates” vs “Advanced… full power list.” Option 4 is that pattern on a phone.

**Cost tradeoff:**

- **v1 (solo):** $0. 1 = baseline UI weeks. 2 = +0.5–1 day of read-only surfaces (panel already projects events). 3 = +1 week of buttons nobody should tap. 4 = +1–2 days for a hidden Advanced pane.
- **alpha (50):** 3-as-product is an admin console. 1/2 stay casual-core-shaped. 4 is how you support them without giving them `deinit`.
- **growth (1,000):** Same split. Do not ship 3.
- **SaaS vs self-host:** Irrelevant. Extra verbs do not add vendors.
- **Engineer-time:** 2 fits inside 4–8 weeks. 3 eats the UX budget. 4 is cheap if we steal panel’s Advanced list rather than invent one.
- **Crossover:** 2→4 the first time you need `seed` or `event` while they hold the phone. Never 2→3.

**Dependencies:**

- Depends on: D2=1 (mum), D12=2 (buttons *are* aether), existing verb list and panel split.
- Unlocks: D7 (which of those buttons may write); D10 look (five + receipts vs keypad); D11 whether Advanced later becomes casual-core.

**Status:** `decided`

**Post-select note:** Main face is not a message bubble list. It is a live proposal document (same role as `examples/propose-current/PROPOSE-TEMPLATE.md`). Agent may write/edit that file only. Authority five + receipts remain on the face so Yes still looks like Mechanicall, not Google Docs. D10 closed by this amendment. D7 must still lock what Yes does to CURRENT.

---

### D10 — Look / session shape (closed with D4)

**Question (exactly as posed):**

> Panel dual-pane vs chat-first vs verb keypad — superseded by the user’s editor-session amendment.

**Options presented (original open list):**

1. Panel dual-pane (plan + chat + human gate)
2. Chat-first bubbles
3. Verb keypad
4. **(User amendment) Interactive proposal-doc editor session/page** — agent edits PROPOSE; CURRENT/receipts/gates remain visible; refine after sprint

**Selected:** **4 — proposal-doc editor session.** Not 2 (bubbles). Not 3 (keypad). Not a Grok-left/CURRENT-right habitat clone.

**Rationale / recommendation:** This is what they asked for. Repo already has the artifact: propose-current is a file, not a chat transcript. The 4–8 week sprint ships a working editor + gates; pixel choreography later.

**Cost tradeoff:**

- **v1:** $0. Editor-page is the UX budget of the 4–8 weeks (not an add-on). Bubble UI would waste that budget on the wrong metaphor.
- **alpha / growth:** A doc-shaped plan is easier to export/`cat` than a chat transcript. Chat-as-product would fight PRODUCT (“chat is ephemeral unless it lands as CURRENT + events”).
- **SaaS vs self-host:** Do not put the editor on Notion/Google Docs. That is a second authority store. Delta: $0 vs $0–12 and a lying receipt.
- **Engineer-time:** Editor + aether apply path is the sprint. Refine-after means we do not spend weeks on OT/CRDT.
- **Crossover:** Add richer chat *around* the doc only after the first sitting if they miss a transcript.

**Dependencies:** Depends on D4=2. Unlocks D7 (Yes vs apply vs editor-save).

**Status:** `decided`

---

### D7 — What does Yes do, if the session is an editor?

**Question (exactly as posed):**

> The agent edits a proposal document. The client is the planner. Saving the editor must not become law. When they tap Yes, what writes, and what stays a draft?

**Options presented:**

1. **Yes = apply the visible proposal into pocket CURRENT + `aether approve`.** One human tap actualises the draft and records APPROVED. Agent never writes CURRENT. `next` stays a separate control if they re-SELECT. Cleanest mum-class. **Recommended for this sprint.**
2. **Two taps.** Yes / Accept only marks the proposal accepted (or writes a receipt). A second **Apply** (or `aether next`) changes CURRENT. More protocol-faithful. Easier to refuse mid-apply. Heavier for a client.
3. **Editor save is law.** Autosave/agent keystrokes write CURRENT. Reject. That is model-as-authority.
4. **Sprint rail only.** Lock: agent writes PROPOSE only; human Yes is the only CURRENT writer; exact apply-vs-approve-vs-next split refined after sprint. App still needs *a* Yes this sprint — treat as 1 unless you pick 2.

**Selected:** **1 — Yes applies the visible proposal into pocket CURRENT + `aether approve`.** User: `1...` plus “avoid obvious questions… I approve any obvious questions.”

**Rationale / recommendation:**

**Recommend option 1**, with option 4’s rail (polish later).

Why: D2=1 requires their Yes to *do something they can see* on the live CURRENT page. Option 3 is prohibited by PRODUCT. Option 2 is correct Mechanicall on a desktop; on a phone with a client it feels like a trick. The propose-current template already lists Apply / Reject / Revise — Yes maps to Apply+approve; Not yet maps to Reject. `aether next` can wait on Advanced or a second sitting.

**Cost tradeoff:**

- **v1:** $0. 1 = one apply path (days, inside 4–8 weeks). 2 = two-step UX (+1–2 days + more explanation). 3 = $0 and a law bug. 4 without a default = an app with a dead button.
- **alpha (50):** 1 is casual-core-shaped. 2 is operator-shaped. 3 is a recall incident (plan changed while they blinked).
- **growth:** Applying a proposal is still files — no SaaS. A cloud “suggest edits” API would be the dollar line ($20–200/mo). Stay on your Ollama.
- **SaaS vs self-host:** Editor must write a local PROPOSE file, then Yes shells into `aether`. Not a Firebase draft.
- **Engineer-time:** 1 fits the sprint. 2 spends polish they asked to defer. 3 looks faster and is forbidden.
- **Crossover:** 1→2 if the first sitting shows they want to accept a draft without changing Next yet.

**Dependencies:**

- Depends on: D2=1, D4=2, D10 editor, D12=2 (`aether approve` / apply via Chaquopy), `examples/propose-current/PROPOSE-TEMPLATE.md`.
- Unlocks: D8 (Ollama must reach the editor); D9 (proposal + CURRENT are the corpus on the phone); implementable spike spec.

**Status:** `decided`

**Post-select note:** `next` is not on the main Yes. Not yet = reject / leave CURRENT. Editor save stays PROPOSE.

---

### D8 — Network (auto-approved)

**Question:** LAN vs Tailscale vs public URL for phone → myarch Ollama.

**Options:** (1) LAN only (2) Tailscale (3) public bind of :11434

**Selected:** **2 — Tailscale** to myarch:11434. Auto-approved: pocket Domain + leave-the-desk + existing B1 + PERSONAL-LLM-PEER-REPL forbids public bind. LAN-only dies off-wifi.

**Rationale:** Determined by D6=2 + principle 4 + principle 8.

**Cost tradeoff:** v1 $0 Tailscale personal. Alpha $0–6. Growth headscale or paid tailnet. Public URL is $0 and a stolen-model/stolen-corpus incident. Engineer-time: reuse B1 checklist, hours not weeks.

**Dependencies:** D6, B1 UAT. Unlocks editor-agent while away from desk.

**Status:** `decided` (auto-approved)

---

### D11 — Persistence (auto-approved)

**Question:** Throwaway vs keep vs fold into casual-core.

**Options:** (1) throwaway (2) keep as lab/sideload face of this Next (3) claim as casual-core product

**Selected:** **2 — keep as `mobile-planning-demo` sideload lab app.** Not throwaway (4–8 weeks). Not a product claim (principle 1; casual-core paused).

**Rationale:** D1+D5 already spent the calendar. Folding into casual-core is a later human Next.

**Cost tradeoff:** $0. Throwaway wastes 4–8 weeks. Productizing now reopens the suite overfit CURRENT rejected.

**Status:** `decided` (auto-approved)

---

### D13 — OS / chrome toolkit (auto-approved)

**Question:** iOS? Flutter? Compose?

**Selected:** **Android + Kotlin/Compose chrome + Chaquopy engine.** iOS not implied. Flutter+Chaquopy is the hard pairing. Sideload only (no Play this sprint).

**Rationale:** D5=3 + D12=2 determine the stack. Asking would be obvious.

**Cost tradeoff:** $0. Play $25 later if you want it. Engineer-time inside the 4–8 weeks.

**Status:** `decided` (auto-approved)

---

### D14 — Who may type in the proposal (auto-approved)

**Selected:** **Client and agent may both edit PROPOSE.** Only a human Yes applies it. Agent keystrokes never write CURRENT.

**Rationale:** D2=1 (they are the planner) + D7=1 + D10 editor. A read-only doc they cannot fix mid-sitting is theater.

**Cost tradeoff:** $0. Shared editor is already the sprint UX. CRDT not required (one phone, one sitting).

**Status:** `decided` (auto-approved)

---

### D9 — What may the remote Ollama read?

**Question (exactly as posed):**

> The pocket Domain may hold a real client corpus. Ollama on myarch will see whatever we send. What is in the model context each editor turn?

**Options presented:**

1. **CURRENT + PROPOSE + a short events tail only.** Smallest honest planning context. Safest. May miss project facts.
2. **Those + files the human attaches or @mentions** in the editor session. Model stays hungry unless they feed it. **Recommended.**
3. **Whole pocket tree every turn.** Simplest build. Fattest. Easiest to leak a contract they did not mean to show the model (the model is on *your* desk, but the sitting is theirs).

**Selected:** **3 — Whole pocket tree every turn.** User: `3. App projection is only a demo build!` Override of recommendation (was 2). Bound to principle 1: demo APK only.

**Rationale / recommendation:**

**Recommend option 2.** This is the one fork that is *not* obvious: quality of the live plan vs what leaves the phone toward myarch. 1 is safer and may stall the sitting. 3 is how you accidentally ingest their entire folder into GPU memory and logs. 2 matches “human Yes / human attach” — they choose what the model may know.

**Cost tradeoff:**

- **v1:** $0. 1 = cheapest tokens/RAM. 2 = +attach UX (1–2 days). 3 = less UX, more RAM, worse leak.
- **alpha:** 3-as-default is how 50 clients’ PDFs end up in one host’s Ollama logs. 2 scales as a habit.
- **growth:** Full-tree ingest is a GPU bill ($200–2,000) and a retention policy. 2 stays cheap.
- **SaaS vs self-host:** Still your Ollama. The cost is *trust*, not a vendor. Cloud LLM would add $20–200/mo and is already rejected.
- **Engineer-time:** 2 is inside the sprint. 3 looks faster and is the wrong default.
- **Crossover:** 2→1 if the first client is secretive. 2→3 never as default; optional “send this folder” is just 2 with a bulk attach.

**Dependencies:** D6=2, D8=Tailscale, D7=1, principle 4. Unlocks spike spec (context packer).

**Status:** `decided`

**Post-select note:** Full-tree ingest is a **demo shortcut**, not a product default. Ollama stays on myarch (not cloud). If the sitting’s pocket folder holds more than they intend the model to see, shrink the folder — do not add an attach UI this sprint. Crossover: reopen D9→2 the moment this is more than a demo build.

---

## Open branches not yet asked

Cost notes are order-of-magnitude engineer-time, not vendor invoices.

| Branch | Why it exists | Cost note |
|--------|---------------|-----------|
| **D4 Verb subset** | Five + receipts + editor session — decided | Editor is the UX core |
| **D5 Delivery vehicle** | Native app 4–8 weeks — decided | Your calendar, not a vendor |
| **D12 Engine** | Chaquopy + same aether — decided | Heavier APK, one law |
| **D6 Topology** | Pocket Domain — decided | +3–10 days on-phone aether |
| **D7 Yes** | Apply PROPOSE + approve — decided | One tap |
| **D8 Network** | Tailscale — decided (auto) | $0; B1 path |
| **D9 Model context** | Whole pocket tree — decided (demo-only override) | Reopen if this stops being a demo |

**Interview closed.** No further grill questions. Execute from `dev/23_mobile-planning-demo/output/SPIKE.md`.
| **D11 Persistence** | Keep as this Next’s lab app — decided (auto) | Not product, not trash |
| **D13 Toolkit** | Android Compose + Chaquopy — decided (auto) | Determined by D12 |
| **D14 Editor writers** | Client + agent edit PROPOSE — decided (auto) | Yes is human-only |

---

## Codebase facts already resolved (do not re-ask)

Explored instead of interviewing:

- CLI verbs (single source `bin/aether` `AETHER_VERBS`): `init onboard try panel shell app deinit status distill watch repair poke trust current preflight approve reject next demo brief drift probe event artifact seed spark session graph garden rival help version verbs`.
- Existing projection: `python/aether_panel.py` writes `.aether/PANEL.md` + `panel.html`; buttons for preflight / approve / reject; models never approve.
- Remote Ollama already specified: `docs/PERSONAL-LLM-PEER-REPL.md`, `nix/modules/ollama-personal.nix`, B1 second-device UAT (mbp-edge → myarch:11434 PASS).
- Android is **LAB** (`docs/LAB-STATUS.md`). Session on anphuni.com is **not core** (`PRODUCT.md`).
- `docs/CASUAL-CORE-INTERFACE.md` is named in CURRENT Done/Brief but **is not in the tree** (tree-org 2026-08-11). The contract is a hole, not a file we can silently extend.
- Prior multi-surface / `suite-android-gap-brief` was parked when Next moved to casual-core.
- Propose-only artifact already exists: `examples/propose-current/PROPOSE-TEMPLATE.md` (draft, not authority; human Apply / Reject / Revise; models must not `aether approve`).

---

## Interview log

- 2026-08-16 — opened tree; posed D1.
- 2026-08-16 — D1 decided: option 3 (user override). CURRENT Next field = `mobile-planning-demo`; body still casual-core; posed D2.
- 2026-08-16 — D2 decided: option 1 (client as planner). Posed D3 (bound Domain).
- 2026-08-16 — D3 decided: option 4 (client project outside repo) + “on the host phone (mine; best setup)”. Posed D6 topology.
- 2026-08-16 — D6 decided: option 2 Pocket Domain (user override). Posed D5 (how aether runs on the phone).
- 2026-08-16 — D5 decided: option 3 native app 4–8 weeks (user override; named “app projection”). Principles 1/6 amended. Posed D12 (projection vs Kotlin clone).
- 2026-08-16 — D12 decided: option 2 Chaquopy + same aether. Posed D4 (verb subset).
- 2026-08-16 — D4 decided: option 2 + editor-session amendment (not bubble chat). D10 decided: proposal-doc editor; refine after sprint. Posed D7 (what Yes writes).
- 2026-08-16 — D7 decided: option 1. Principle 17: skip/auto-approve obvious. Auto-closed D8 Tailscale, D11 keep-lab, D13 Android/Compose, D14 both-edit-PROPOSE. Posed D9 (model context) as the remaining non-obvious fork.
- 2026-08-16 — D9 decided: option 3 whole-tree (override; demo-only). Principle 1 strengthened. Interview closed. Spike + propose-current written.
