# Summary — 09 brake send

URL face is sendable (`send-brake.sh`). Receipt lands in the pocket. APK chrome matches the brake; this machine cannot assemble the APK.

Review `RECEIPT.md` and `../output/BRAKE.md`. Send a URL, or build the APK on a machine with an SDK.
