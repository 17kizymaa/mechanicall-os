# Summary — 12 leave for people-app

Sitting face is down. Friend never tapped. A fidelity proposal names `icm-people-app` as the next operator Next and keeps the sitting as lab.

CURRENT is unchanged until you run the commands in `APPLY.md`. Do not ask the agent to `aether next`.
