# Cons of the development journey — what the same six weeks cost

**When:** 2026-08-24  
**Tone:** matching honesty. Not a dunk. Not inflated. Does not recant the pros.  
**Not law:** operator Next stays `sit-distro-gate`. This file does not re-SELECT, approve, or implement zip / Play / Go-git.

Counts: `COUNTS.md` in this folder. Prior classify: `dev/26_understand-what-built/02_classify/output/` (Aug 18). Live Limits: root `CURRENT.md`.

---

## The picture that is still unpaid

A person has a folder of work. They do not learn `aether`. They see the plan. They say Yes or Not yet. Tomorrow they open a receipt.

That picture was already designed on 18 Aug (`dev/25` six-stage walk; `worth.md` one-sentence test). It has **not been collected**. The six weeks after v0.1 did collect a protocol, a diary, a sit face, a STORM factory, and a Play-internal pack. They did not collect a stranger’s (or a friend’s) Yes that is not yours.

The lucky kid shares a Yes. The lost kid shares a monorepo. The journey’s cons are how often the second kid won the calendar.

---

## Why the same five layers can hurt

The layers that protect identity also tax attention. That is not a reason to abandon them. It is the cost of using them without a filing Yes.

| Layer | What it protects | How it hurt this journey |
|-------|------------------|--------------------------|
| **0 Identity** | Who we are (`AGENTS.md`, principles) | Identity is long. Agents wake into a 40-line conclusion contract + CURRENT + SPEC + PRODUCT. The operator became the person who remembers which file wins. |
| **1 Routing** | One Next (`CURRENT.md`) | Routing worked *as a brake* and failed *as a calendar*. 43 `NEXT_SELECTED` in six weeks. Law survived. One walk did not. |
| **2 Stage contract** | What *this* folder may do | 33 numbered `dev/` dirs, gaps 07/10–14/16/19–22, duplicate `15` and `18`. Inside `39`, stage numbers collided again (`13_camel-sos-check` and `13_run-test`; two `15_*`). Contracts multiplied faster than INDEX. |
| **3 Factory** | How we always write | `docs/LAB-STATUS.md` still dated 2026-08-09. `.context.md` still titled awareness-agent, last distill 2026-08-04, `file_count: 477`. Layer 3 catalogs lagged Layer 4 capture. |
| **4 Working** | `output/` you can `cat` | Capture-sacred filled the disk. `dev/39` alone: 119 png + 110 xml. Dirty tree is declared as step 6 of the live Next — which means unshippable is the default until a human Go-git. |

ICM gives the plan a shape. Mechanicall gives Yes a gate. Together they are stronger than either slogan — **if someone files**. Filing never got a Yes. `26` ghost-fix (Aug 18) is still propose-only. Sixteen factories (`27`–`42`) arrived on top of an unapplied classify.

---

## Cons that are already true on disk

### 1. Filing never got a Yes

`CORE_PRINCIPLES.md`: capture is sacred; structure is deferred; filing is proposed, then human-approved.

You captured. You almost never filed. Evidence:

- Ghosts `19`, `22`, `11`, `14` still cited; folders gone (`ghost-fix.md`).
- Proposed `dev/INDEX.md` never applied (`INDEX.md` still “draft”).
- LAB-STATUS date 2026-08-09, typo `reciept`, live rows for `23`/`25`/`26` never added — and those “live” rows are now stale anyway (`23` is not the Next).
- `.context.md` human header still `/home/awareness-agent`.

This is the principle working *as capture* and failing *as a share*. Friends cannot drink a diary. Agents will load the wrong door.

### 2. Lost is many doors, not a weak mind

`lost-and-lucky.md` already named it. The trail since then made it louder.

Protocol-first → package-alpha → server-ops defer → multi-surface suite → park → Hyprland → casual-core pause → people-app → phone seat → sit-join → VST/Headscale → STORM factory → leftover → testers → distro-gate.

Each door had a reason. The cons is **attention**. One Next is a lock on the file. It is not a lock on the operator’s week. 102 `APPROVED` and 43 `NEXT_SELECTED` in `DECISIONS.md` is a working brake **and** a restless calendar.

### 3. The gate exists; the reasons sometimes do not

24 lines are `APPROVED: APPROVED`. 9 are `REJECTED: REJECTED`.

The product is supposed to be an inspectable Yes. A reasonless Yes is a receipt that proves a button, not a judgment. That is doctrine rot in the log that is meant to be the conversation-with-authority.

The good rejects are famous (overfit, “I follow the laws myself too much,” hunk-picker, leftover chrome). The empty ones train agents that Approve is a vibe.

### 4. Instance law and the factory diary disagree

Live `CURRENT.md` still lists zip containment, stock-phone Publish, and API 36 as **this Next’s** actions 1–3.

`dev/42_sit-distro-gate/CONTEXT.md` marks those three **Done**, LTE **BLOCKED**, Play pack written, upload human.

That split is legal under Limits (“grouped git exclude live CURRENT unless human Go-git”). It is still a cons: a cold agent who reads CURRENT will re-do zip; a cold agent who reads `42` will skip to LTE. Two sources of “where we are.” Filesystem-as-truth does not survive two clocks.

`06_production-grade/output/PROBLEM.md` is the same split in the operator: “production-grade APK in git” vs Reject production listing. The brake wrote a page. The want did not die. Dual-Next is Reject because it kept trying to happen.

### 5. Dirty tree is the default, not an accident

This sitting: 35 porcelain lines (3 modified, 32 untracked) on `feat/sit-testers`, 17 commits not in `master`. Live Next action (6) is “dirty git last (human Go-git).”

Two `REPO_CLEAN` events (10 Aug, 11 Aug) reset to `origin/master` and parked WIP on dated branches / off-repo archives. Capture survived. The working tree regularly became unshippable. That is honest. It is also why a friend cannot clone HEAD and meet the sit you are looking at.

### 6. The face can lie when the lab is down

CURRENT Reject: restart drop / call sharing “online” while `:8765` is down or zip is uncontained. Drop is still **down** after zip landed in-tree (`01_zip-contain/output/RECEIPT.md`: do not start until a human says drop-up).

Lab drop binds `127.0.0.1`, unauthenticated, last-offer slot. G4 mesh JNI is a named gap. Pretend JOIN without tsnet Up is Reject.

The cons is product-shaped: a sit with JOIN / Send / inbox **looks** like a network product. Off-LAN LTE is still BLOCKED (`04_lte-receipt/output/BLOCKED.md`: adb empty; A33 USB ≠ LTE). USB-on-mbp-edge is the receipt you keep almost taking.

### 7. Chrome outran bind

`DECISIONS.md` 20 Aug already said it: people-app `25` designed bind → plan → propose → gate → do → receipt. Phone stages `27`–`35` then spent days projecting operator chrome onto a demo pocket. If the child’s folder is not bound, later stages are empty theatre.

The 16 Aug sitting cancelled. `worth.md`: the only metric that matters is a person who is not you saying Yes on their own plan. Still uncollected. Testers-as-face is Done-as-keep. Testers-as-testers is not: GPT-5.6 distro bar NO on PR #7 as-is; Play Console still human; LTE still blocked.

### 8. There is still no friend path

`docs/SINGLE-APP-DISTRIBUTION.md` (3 Aug): casual CLI bet **failed**; seat UX **incomplete**. FIRST-PROJECT is still clone + chmod. `seat/` in this checkout was README without source (ghost-fix). Pocket still LAB. Agent-agnostic cold-start is for developers.

If they have to learn `aether`, the share failed. Six weeks later, the share still teaches `aether` — or asks them to sideload a lab APK whose Decide→Publish on a **stock** phone is the live Next, not a finished gift.

### 9. Cooperative authority is not a jail — including against you

`docs/ALPHA-LIMITATIONS.md`: preflight cannot stop an agent that edits CURRENT or runs `approve` without a human. No authenticated operator identity.

`DECISIONS.md` 10 Aug `HUMAN_ESCALATE`: agent edited CURRENT Next per human order because `aether next` blocked without prior approve. That is the operator using the hole the alpha admits. The protocol is cooperative. A high-energy human can make the file true by force. Peers cannot tell that Yes from a model Yes except by reading the escalate line.

Silence is never permission. A spoken “just write CURRENT” is permission — and it is how overfitting comes back wearing a human voice.

### 10. STORM and ICM tax can become a second job

Reject: `storm-review-as-the-product`. Limits: STORM is developer infrastructure, cap 2, never A33.

Disk: `dev/39` is a screenshot factory. `/app-reviewer` and `/visual-reviewer` are real. Meta-agent requires a >40-line conclusion on every turn. Operator already named CURRENT sprawl as overwhelm (4 Aug, protocol-first comments).

The cons is not “you reviewed too much.” It is that **looking** acquired more surface area than **one friend finishing**. The anxious friend in the group chat was supposed to be fired. The diary hired them as INDEX, LAB-STATUS, conclusions, and STORM.

### 11. Distribution is gated — and the calendar does not care

Play API-36: new apps and updates from **2026-08-31** must target 36. Baseline on CURRENT: targetSdk was 34 vs that gate. `03_api36-aab` now claims Done in-tree. LTE before Console is still blocked. Keystore off git (good). Binaries in the PR (want vs law, `PROBLEM.md`).

Honesty here looks like delay. Delay vs a store clock is the cons. Recanting PR #7 as-is was right. It still means closed testers are not testers until a human does LTE + Console.

### 12. `aether` outgrew the sketch and the sidecar did not keep up

v0.1 success criteria (≤220 lines, one screen) retired — correctly. Live CLI is 2065 lines, single POSIX file. That is honest size doctrine.

The cons: `.context.md` still describes awareness-agent v0.1. `SPEC-v0.1.md` remains in the README contract line beside v0.2. New agents load a mixed era. You retired the myth in SPEC and left it in the sidecar.

---

## What this journey is especially bad at (lean in)

- Filing after capture (structure deferred forever)
- One non-dev friend finishing in an hour
- Keeping `CURRENT.md` and `dev/NN/CONTEXT.md` on the same clock
- Writing a **reason** into Yes / Not yet (rubber-stamp rows)
- Stopping the **operator’s next door**, not only the model’s next tool
- Distinguishing a lab face from a shareable gift
- Letting INDEX / LAB-STATUS / `.context.md` die while output/ lives

That last line is the inverse of the product pitch. Chat is supposed to die; the folder remains. Here the **folder’s catalogs** died and the **session output** remained. Capture without filing is a second kind of chat.

---

## What is not a con (so honesty stays clean)

| Fake con | Truth |
|----------|--------|
| “The tree looks junior, so you cannot develop.” | Protocol, denials, conflict order, negative tests — architect-grade. Filing is the missing skill, not syntax. (`lost-and-lucky.md`) |
| “Recanting surfaces wasted the weeks.” | Recant is the method. Session, Hyprland, leftover chrome, hunk-picker, Play-as-is were the journey working. |
| “Dirty git this Next is sloppy.” | Action (6) **is** dirty last. The con is making that the usual state of every Next, not this declared step. |
| “Peer review slowed you down.” | GPT-5.6 / Opus / PR rounds changed protocol. The con is review **surface area**, not review **existence**. |
| “You should have shipped Play production.” | Live Reject. `PROBLEM.md` already holds that line. |

---

## The cost in one sentence

**You built a machine that can stop — and then kept walking so fast that filing, one friend, and instance law never caught up.**

The brake is real. The unpaid bill is the same one `worth.md` named: a Yes that is not yours, on a folder that is not this repo, opened tomorrow without you. Closed-tester Play is a different share. It is allowed as live Next. It is not a substitute for that bill unless a tester who is not you can say Not yet and the sit lives with it.

---

## Checkpoint

Strike a sentence if it overclaims. Keep this file if the feeling is right.

This stage **halts**. No `03_persist-pros`, no friend-walk scaffold, no zip, no drop-up, no Console.

Constructive picks (silence is not Yes):

- **Edit `CONS.md` in place** — cheapest correction.
- **Persist the pros** — say proceed to `03_persist-pros` (dated page; still not CURRENT).
- **File the Aug 18 ghost-fix** — that is a different factory (`26` stage 03); not this Next.
- **Execute live Next** — `sit-distro-gate` LTE is BLOCKED until a cellular sideload; zip/publish/API36 receipts already sit under `dev/42`. Human Go-git last.

The cons do not unlock a second Next. They name why the first one is still hard.
