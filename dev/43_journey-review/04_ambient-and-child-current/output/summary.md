# Summary — 04_ambient-and-child-current

Research/discuss only. W1=park R. W2=stop four-door build. Live Next `sit-distro-gate`.

## Headlines

- Funnel = public expose. Shutdown logic = **never start it** + refuse `0.0.0.0` + QUIET desk. Already in `wake_desk` / `_refuse_public_ollama` / GATE-CONTRACT.
- Ambient requests = wol-pi doorbell (always-on, tailnet) → WAKE → LOAD → STREAM → QUIET. myarch **should sleep**. E3b k3s-on-myarch fights that (later recant, not this Next).
- Child `CURRENT.md` for subagents **is** dual-Next if it carries Next/Approve. Legal children: CONTEXT.md, proposals, other **folders**’ CURRENT, ICM stages inside one action-id.
- `aether … [path]` already gives one Next per folder. Operator tree must not grow sub-CURRENT theatre.

Halt. Answer W7 and W8.
