# Workflow failures — how this repo actually breaks

**When:** 2026-08-24  
**Not law. Not Yes. Not a recant of `sit-distro-gate`.**  
**Sister:** `../02_journey-cons/output/CONS.md` (what the weeks cost). This file is **how the work moves** when it fails.  
**Trigger:** operator asked for more process research, then four product doors in one sentence (world network · Draft/Plan UI · non-pastiche finish · marketing website).

---

## The pattern (one sentence)

**A blocked physical step is replaced by a new face, a new grill, or a new Next — and chat tries to outrank CURRENT.**

That is the workflow failure. ICM folders, `aether`, FACE-SPEC, and STORM are not the bug. The bug is **starting the next door before the current door’s remaining clause is either done or honestly still BLOCKED.**

---

## Mechanisms (facts)

### M1 — Wishlist stacking before the blocked clause

Live Next `sit-distro-gate` remaining: LTE sideload (BLOCKED — empty `adb` on this host; USB A33 ≠ LTE) → human Play internal → dirty git last. Zip / native Publish / API 36 already have `dev/42` receipts.

This turn’s ask is four new products **in parallel with that remainder**, joined by **&**. Dual-concurrent-next is Reject. This is the same motion as `06_production-grade/output/PROBLEM.md` (production-grade APK in git vs production Reject) while LTE is still BLOCKED.

### M2 — Grill opened, not answered, new grill started

Root `decision-tree.md` **R-series is OPEN** (R1–R3: what “production-grade APK in git” means). C-series notepad is PARKED unanswered. S15 (how paper Suggesting is built) was **declined**. P4b website listing is **parked**.

Opening a W-series without parking or answering R is the process eating itself. The tree is supposed to be the memory. An unanswered grill is a second CURRENT.

### M3 — Recant-by-chat instead of propose-current

Law already covers the four doors:

| Ask | Live law |
|-----|----------|
| Anyone in the world networks with me | Reject Funnel / public wake; JOIN is not Yes; pretend JOIN without tsnet Up Reject; G4 mesh JNI **named gap**; drop `127.0.0.1` unauthenticated; closed testers is the **legal** “other people” this Next |
| Explore Draft + Plan UI redesign | BEHAVIOURS B2–B5 already define Plan/Draft; FACE-SPEC v2 already specifies PLAN published / DRAFT Suggesting / DECIDE Publish; **leftover-chrome-as-the-Next Reject** |
| Finish intended non-pastiche UI | G2: keep cream/paper; SOS texture later **not Camel PNG**; P2b SOS blit **parked**; `16_revert-skin` restored cream millwork; live glass is the revert |
| Redesign website to market it | **P4b parked** (liked production listing *for a website*, not this app Next); 2026-08-04 Session demoted to lab; website sit **stopped after ~2 minutes, zero OpenRouter calls** |

Chat cannot recant those. `.aether/proposals/` + human apply can. `HUMAN_ESCALATE` 10 Aug (agent wrote CURRENT because `aether next` blocked) is the hole this pattern uses.

### M4 — Surface replacement when a person bounces

From `research/2026-08-19-codebase-wants-deep-research.md`: Dashboard → TUI → chat → website → playground → Android IDEs → mum Geobook → phone → Play → APK → people-app. Each rebuild follows a person leaving or a face that felt like ritual.

The want that does not change (their mouth, 27 Jul–19 Aug): a face a non-developer will sit in, CURRENT on screen, Yes outside the chat, no CLI zoo. Replacing the **face** when the **gate** was the failure (2-minute website, cancelled sitting, no stranger Yes) is the workflow loop.

Marketing website now, while Play internal is not uploaded and LTE is BLOCKED, is that loop with a landing page.

### M5 — Two clocks (factory Done vs instance law)

CURRENT body still lists zip / stock Publish / API 36 as actions 1–3. `dev/42/CONTEXT.md` marks them **Done**. Cold agents re-do zip or skip LTE depending on which file they read. Limits allow grouped git exclude of live CURRENT until Go-git — which **creates** the two clocks. Process fix is a **propose file**, not a second Next.

### M6 — Yes without a reason; Next without a lock

`COUNTS.md`: 102 `APPROVED`, 43 `NEXT_SELECTED`, 24× `APPROVED: APPROVED`, 9× `REJECTED: REJECTED`. One Next locked the file and did not lock the calendar. Rubber-stamp rows train the next session that Approve is a vibe.

### M7 — Capture-sacred, filing never Yes

Ghost-fix 18 Aug still propose-only. LAB-STATUS 2026-08-09. `.context.md` awareness-agent 2026-08-04. `dev/39` 119 png + 110 xml. Catalogs die; `output/` lives. That is a second kind of chat.

### M8 — Looking instead of the physical receipt

STORM / `/visual-reviewer` / 40-line conclusions are legal factory. They become the job when LTE needs a phone on cellular. Reject: `storm-review-as-the-product`. This sitting’s empty `adb` is the honest stop. More chrome is the dishonest go.

---

## What already specifies Draft / Plan / the look

You do not need a new research program to know the intended sit.

**Walk (BEHAVIOURS):** bind → show-plan → propose → gate → do-after-Yes → receipt.

- **PLAN** = published `CURRENT.md` + Changes + Proposal diffs. Not a protocol museum.
- **DRAFT** = workshop on PROPOSE (live vs proposed). Desk proposes. Not chat-as-product.
- **DECIDE** = two-tap + Why; copy “Publish the proposal.” Not hunk IN/OUT.
- **RECEIPT** = what they already said. Empty is honest.

**FACE-SPEC v2** (`dev/38_sit-vst-headscale/01_research/output/FACE-SPEC.md`): rack around cream paper; LCD = Next + GATE only; JOIN/WAKE are strip verbs not Yes; no painted letterbox; no Camel bitmaps; no Imagine-as-spec.

**Gap vs “finish non-pastiche”:** live glass is cream millwork **after** SOS blit revert (`16_revert-skin`). P2b parked. S15 (Compose hunk renderer vs other) **declined**. Finishing the look is a **later** Next (leftover / SOS), not this one.

---

## What “anyone in the world networks with me” can legally mean

| Meaning | Status |
|---------|--------|
| Closed testers (Play internal/closed email list) | **This Next** after LTE + Console |
| Stranger JOIN on Headscale userspace (them, not you) | Named; JNI/tsnet gap; not Yes; not Funnel |
| Public Funnel / public Ollama / public wake | **Reject** |
| Lab drop as “online” | **Reject** while `:8765` down or zip uncontained; bind `127.0.0.1` |
| LTE folder-send as a store feature | **Do not list** (G4) |
| Website Session as the network | Lab, demoted; 2-minute bounce |

“Anyone in the world” without a recant of Funnel is **closed testers + honest JOIN states**. That is sit-distro-gate, not a new mesh product.

---

## What “redesign the website to market it” is

P4b: parked on purpose. User already said they like production listing **for a website later**. 4 Aug protocol-first demoted Session from primary. The failure mode is teaching the protocol for two minutes, then leaving.

Marketing a sit that is not Play-internal yet sells the 4 Aug lab as if it were the store. Hosting cost is cheap. **Bounce cost** is the 2-minute receipt repeating.

---

## The workflow that actually builds the app

Successful motion in this tree (when it worked):

1. One action-id in CURRENT.  
2. Propose file if law must change; **human** applies.  
3. Halt at `output/`. Real reason on Yes / Not yet.  
4. Close or park an open grill before opening another.  
5. Physical receipts (LTE, off-LAN) are not replaceable by STORM or a landing page.  
6. Dirty git **last**.

Failed motion (this sitting’s risk):

1. Four doors with **&**.  
2. Recant parked forks in chat.  
3. Leave R-series open.  
4. Skip LTE because `adb` is empty and start UI/website instead.  
5. Ask an agent how to build everything.

---

## How to move forward (process recommendation — not CURRENT)

**Do not recant `sit-distro-gate` for this bundle.** The bundle is at least **three** later Nexts (network-as-Funnel-or-JOIN, sit chrome leftover/SOS, website P4b). Dual-Next is Reject. `&` is the failure.

This sitting, on this host (`adb` empty):

1. Park or answer **R-series** (binaries not in git).  
2. Optionally **propose** CURRENT so actions 1–3 read Done (two clocks) — human applies.  
3. LTE when a cellular phone exists; else leave BLOCKED.  
4. Console is human after (3).  
5. Go-git last.  
6. **After** that Next closes: pick **one** of UI leftover / JOIN honesty / P4b website as the next action-id — via propose-current, not chat.

The app that helps people sit with you is already specified (BEHAVIOURS + FACE-SPEC). The missing collection is a **tester who is not you** on a signed artifact off LAN — which is still this Next.

---

## Checkpoint

This file does not implement UI, website, Funnel, leftover, or SOS.  
Grill W-series is in root `decision-tree.md`. Frontier questions are there. Silence is not recant.
