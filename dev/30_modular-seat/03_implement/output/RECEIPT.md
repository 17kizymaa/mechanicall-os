# Implement — modular Mechanicall seat

**When:** 2026-08-19  
**Next:** `people-app-phone-seat` · SELECT / REJECTED  
Specialists: engine, face, aether-client. Model did not approve / next / reject.

## Rooms

Splash (logo) · Home · Plan · Draft (Fields | Chat) · Decide (two-tap + Why?) · Receipt · Bind · Client (allowlisted aether)

Hamburger top-right: rooms + PROJECT file shortcuts.

## Law kept

- `yes()` / `not_yet()` only from Decide Confirm
- `client_run` refuses approve / reject / next
- `write_schema_draft` and `draft_chat` write PROPOSE only
- `open_is_yes` is false
- operator tree refused
