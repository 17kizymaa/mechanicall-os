# Sprint brief for Opus 5 — Mechanicall protocol completion

## Authorization
Human **ACCEPTED** you as sprint lead collaborator. Host agent implements your waves. You do not approve CURRENT.

## Repo
`/mnt/kingston-nixos-sync/opt/mechanicall-os` · branch often `feat/domain-shell-panel-tui` · one-true CLI `./aether` (POSIX sh).

## Product lock
- Core = CURRENT.md + aether preflight + human approve (local filesystem authority protocol).
- Session on anphuni.com = capped hosted lab ≤5 seats — not core.
- Personal LLM = propose only.
- No CURRENT v2 schema unless human later says so (thin-gate preferred).
- Operator works in **Grok Build TUI** — Protocol Lab must account for external TUI not auto-preflighting.

## Scaffolding already present
- `./aether` with current, current validate, preflight, approve, reject, events, panel, shell agent
- PRODUCT.md boundary map, AGENTS CURRENT-first, CORE/ARCH/NOT-IMPLEMENTED peer absorb
- docs/PROTOCOL-TEST-SURFACE.md, OUTLOOK-RESEARCH-BOUNDARY.md
- Site: /protocol literacy page; Session lab privacy split; CURRENT-format agent on seats
- Peer GPT-5.6 CONDITIONAL response absorbed; stale cli.py critique corrected
- Tests: `sh tests/run.sh`

## Recent session decisions (honour)
1. Protocol-first APPROVED with comments (Grok seat, CURRENT overload hold, no new CURRENT type yet).
2. Client One idle on website; do not depend on them this sprint.
3. Email-draft Domain transfer scrapped; Outlook research only.
4. Publish honesty: max-5 is multi-seat lab language, not “not multi-user.”
5. Prefer completing protocol demo + Grok observability over new product surfaces.

## Gaps to close toward “completion”
- Lifecycle: root CURRENT stuck APPROVE/APPROVED with same Next — need thin re-SELECT Next.
- Protocol demo script: refuse + allow + approve path one command.
- Grok interference observability (SessionStart / doc / optional hook).
- Protocol Lab: design for Grok-primary + optional web; action+bind+probe without boring ceremony.
- Remaining peer exit items if any incomplete (release notes draft text if public, etc.).
- Ensure tests cover `current validate`.

## Ask
Produce ordered waves to iterate the **protocol idea to completion from scaffolding**. Wave 0 = what host agent should do **now** in this session (concrete files/commands). Keep scope protocol-complete alpha, not club-cortex.
