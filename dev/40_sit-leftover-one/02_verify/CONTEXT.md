# 02_verify — one re-walk

**Not Yes.** Same Next `sit-leftover-one`. After 01_implement. Never Confirm. Never A33.

## Inputs
- `../01_implement/output/RECEIPT.md`
- `python/app_verify.py`
- `scripts/storm-walk.sh`

## Process
1. Preflight `sit-leftover-one`.
2. assembleDebug + `adb -s emulator-5554 install -r` (refuse RZCW2038KHN).
3. storm-walk unbound then bound demo pocket (not mechanicall-os).
4. app_verify source + uidump. Visual vs FACE-SPEC / GATE-CONTRACT (idle lamps dark; paper majority).

## Outputs
- VERIFICATION.md → output/
- VISUAL.md → output/
- storm-0 uidump/png under output/storm-0/
