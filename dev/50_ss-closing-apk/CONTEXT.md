# ss-closing-apk — factory (Layer 1)

**Not operator law.** Operator Next is still `listing-bar-pin` until human `aether next ss-closing-apk`.  
**Pocket:** `/home/anphuni/ss-closing-apk` (renamed from `sit-ui-sprint`). Symlink `./pocket`.  
**Thread (copy, not SPEC):** `01_ingest/output/15_RAW-_SS-CLOSING-APK-THREAD.md`  
**APK:** `0.19.0-sit-rack` vc 25 on A33 USB (USB ≠ LTE). Archive `~/.mechanicall/apk-archive/mechanicall-0.19.0-sit-rack-vc25.apk`.

## Pipeline

1. `01_ingest/` — pull phone thread + APK version. Done this sitting.
2. `02_handoff/` — pocket propose CURRENT (`refine-sit-taste`) + operator propose (`ss-closing-apk`). Halt for human `aether next`.

## One Next

Operator: `ss-closing-apk` (proposed, not live).  
Pocket: `refine-sit-taste` (proposed, not live). Freeze `freeze-sit-rack` stays pocket law until they apply.

Following (not live): `closed-tester-listing` · `anphuni-web-pitch` · `stale-md-reinstantiate` · `sit-headscale-operable` still later.

Play **production** listing is still Reject on operator CURRENT. Dual-Next Reject. Models never approve.
