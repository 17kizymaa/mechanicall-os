# 26 — Understand what we built (tree inventory)

**Layer 1 routing.** First stage of a possible ICM directory refactor.  
**Not a second operator Next.** Root `CURRENT.md` stays `mobile-planning-demo`.

This folder is a **factory for understanding**. It does not move trees. It does not rewrite CURRENT. It does not scaffold the people-app (`dev/25_*`) and it does not resume the pocket sitting (`dev/23_*`).

## Pipeline

1. `01_inventory/` — what exists, what shipped, what is lab, what is gone (**done**)
2. `02_classify/` — SHIPPED / SUPPORT / LAB / ARCHIVE compact map (**this stage**)
3. *(not created)* `03_propose-tree/` — propose-only new layout (human applies)
4. *(not created)* `04_verify-labels/` — LAB-STATUS + DOC-AUTHORITY still match disk

Stop after each `output/` until the human says proceed.

## Non-goals

- Do not `aether approve` / `next` / `reject`.
- Do not move, rename, or delete directories.
- Do not claim this is the operator Next.
- Do not proceed `25` scaffolding unless the human says **proceed to 03** there.
