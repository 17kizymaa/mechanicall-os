# Summary — 02_classify

Every root path and every `dev/` folder now has a tag. Core stays at repo root. `dev/` stays a diary. Live: `23` (operator Next), `25` (people-app factory), `26` (this factory). Ghosts: `19`, `22`, `11`, `14` — fix pointers, do not recreate.

Review `classify.md`, `INDEX.md`, `ghost-fix.md`.  
Say **proceed to 03** to write a propose-only move/label manifest. Still no `git mv` until you apply.
