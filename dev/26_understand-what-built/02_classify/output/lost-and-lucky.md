# Lost and lucky — identity, hypothesis, who to share with

**When:** 2026-08-18  
**Identity offered:** *I am a lost and lucky kid trying to share mechanicall-os with others.*  
**Not law:** operator Next stays `mobile-planning-demo`. This file does not re-SELECT.  
**Not this file:** no Play Store, no CURRENT rewrite, no friend invited yet.

---

## 1. The identity (Layer 0, proposed)

Keep it. It is more accurate than “protocol engineer,” and it is a better door for friends.

| Phrase | What it should mean in this repo |
|--------|----------------------------------|
| **Lost** | Too many live doors. Attention spilled across a parked sitting, a people-app factory, a classify factory, a paused casual-core, an incomplete seat. Lost is not “cannot think.” Lost is “no one Next for *sharing*.” |
| **Lucky** | You found filesystem authority + ICM *before* you shipped a SaaS you could not unwind. The diary is messy because you kept the work. That is luck with scars. |
| **Kid** | Permission to show a friend a receipt, not a platform. Permission to finish one walk. |
| **Trying to share** | The product feeling: *you decide; the agent clicks; you can read the plan.* Share that loop. Do not share `mechanicall-os` as a git tree. |

A one-line L0 card you can paste into a later `25` reference (not applied here):

```text
I am a lost and lucky kid.
I share a folder and a Yes, not a platform.
If they have to learn aether, I failed the share.
```

---

## 2. Hypothesis (not yours — mine)

**Your assumption:** the workspace proves a lack of dev fundamentals.

**My hypothesis:** you do **not** lack development fundamentals. You lack **filing** and **first-audience** fundamentals. The tree looks junior because you obeyed your own locked principle too well.

From `CORE_PRINCIPLES.md`:

> Capture is sacred; structure is deferred.  
> Getting a thought into the filesystem must cost zero decisions… Filing and sorting happen later — proposed, then human-approved — never at the moment of entry.

You captured for months. Filing never got a Yes. Ghosts (`dev/19`, `dev/22`), duplicate `15` / `18`, stale `.context.md`, `seat/` README without source, `domains/` README without samples, `23` missing week folders, LAB-STATUS still dated 2026-08-09 — that is **deferred structure**, not inability to write software.

### Evidence you *have* fundamentals

- Conflict order (`docs/DOC-AUTHORITY.md`) — most repos never write this.
- A real protocol: parseable CURRENT, preflight 0/3, approve/reject, events, `aether demo`.
- Tests that refuse theatre (`tests/negative.sh`, shellcheck, `aether current validate`).
- Honesty docs as first-class: `NOT-IMPLEMENTED.md`, `docs/ALPHA-LIMITATIONS.md`, PRODUCT boundary table.
- “Models never approve” held across ~49 commits and peer sprints.
- ICM used correctly in anger: CONTEXT → `output/` → halt → “proceed to 02.”
- When law split, you ran a propose factory (`24`) instead of silently rewriting CURRENT.

That is architect-grade. Peers could audit it. That is not “cannot develop.”

### Evidence that *looks* like no fundamentals (and what it actually is)

| Disk fact | Easy story | Better story |
|-----------|------------|--------------|
| Duplicate numbers, missing 07/10–14/16/19–22 | Doesn’t know how to name folders | Capture outran filing; prior compact (`22`) vanished |
| LAB-STATUS cites missing `19` | Sloppy docs | Catalog not updated after the folder was absorbed into PR #5 |
| FIRST-PROJECT starts `git clone` + `chmod` | Can’t design onboarding | Technical literacy path exists; **friend path does not** |
| Sitting cancelled, Next still `mobile-planning-demo` | Can’t close a loop | Lost: law still points at a door you are not walking |
| `seat/` contract vs empty tree | Vapourware | Honest incomplete — the failure is inviting friends to a front door that is not here |
| Uncommitted pocket + ICM factories | No git hygiene | Same capture-sacred habit, now in the working tree |

So: **strong protocol fundamentals, weak “one friend can finish in an hour” fundamentals.** Those are different skills. The second is the one sharing requires.

---

## 3. Are non-dev friends the audience?

### Yes — as the *dream* audience (1–3 people who trust you)

PRODUCT and `25` discovery already named them: a person with a **folder of work** who will not learn `aether` and will not SSH to your LAN. The casual CLI bet is documented as **failed** (`docs/SINGLE-APP-DISTRIBUTION.md`). If Mechanicall cannot be shown to a friend without a terminal ceremony, you are sharing a lab, not a gift.

Good friend-shaped folders: a zine, a gig, a charity AGM, a family trip, “what do we do about mum’s paperwork.” One Next. One Yes. One receipt tomorrow.

### No — as the audience of *this repository*

Do not send friends to `mechanicall-os`.

| Why not | Disk reason |
|---------|-------------|
| The tree is the operator diary | `dev/` is 16 numbered scars + 3 live factories |
| First-project is a developer ritual | `git clone`, `aether demo`, CURRENT schema |
| Cooperative, not a jail | If they never honour CURRENT, they see ChatGPT with extra files |
| Linux-primary alpha | `docs/ALPHA-LIMITATIONS.md` |
| Incomplete casual face | Seat source absent here; phone sitting cancelled |
| You are still lost across doors | A friend will feel *your* overwhelm, not the pitch |

**Public / Play Store / “anyone”:** not this year, and not this Next. CURRENT already forbids productizing the demo.

### The precise audience

| Audience | Verdict |
|----------|---------|
| One friend with a folder they care about, in a room with you | **Yes — first share** |
| Three friends who already trust you | **Yes — after one receipt exists** |
| Non-dev strangers from the internet | **Not yet** — they need a face you have not finished |
| Developers who want a protocol | **Already served** — this repo, FIRST-PROJECT, `aether demo` |
| “Make me an app that just does it” | **No** — they want silence to be permission |

If a friend needs the agent to go without Yes, Mechanicall will feel like homework. That friend is the wrong friend for v0.

---

## 4. Use the framework to evolve the framework

Do not add a product. Add a **walk**, then change Layer 3 from what bounced.

### The share loop (ICM, one friend)

```text
00_identity   you are the lost-and-lucky kid (this file)
01_pick       one friend, one folder that is not mechanicall-os
02_show       PLAN.md in words they already use
03_propose    you (or the agent) draft only
04_gate       they say Yes / Not yet  — you never click Yes for them
05_do         only if Yes
06_receipt    they open it tomorrow without you
07_bounce     what confused them → rewrite L3, not L0
```

`01–06` are already mapped in `dev/25_icm-people-app`. **`07_bounce` is the evolution step.** ICM improves ICM when a non-dev’s confusion becomes a contract edit, not a new framework.

### What to change when they bounce (Layer 3, not a rewrite of aether)

| If they say… | Evolve this, not the CLI |
|--------------|--------------------------|
| “What is aether?” | You failed the share. PLAN.md and the face must never say the binary name. |
| “Which folder?” | `01_bind` copy + refuse-operator. Pick *with* them. Never this repo. |
| “Did it already do it?” | Silence ≠ Yes is the whole gift. Make GATE.md a big Yes / Not yet, no other buttons. |
| “I’ll look later” (they don’t) | Receipt must be a page they already open — Messages, a printed sheet, a photo — not `dev/`. |
| “Just use ChatGPT” | Wrong friend, or the plan was not *their* folder. Do not add features. Pick again. |

### What *not* to evolve

- Do not re-root mechanicall-os as `01_bind/` (identity C — classified as a fight with PRODUCT).
- Do not teach friends `AETHER_VERBS`.
- Do not finish the Kingston kiosk to “unlock” sharing. One table, one laptop, one folder is enough.
- Do not keep `mobile-planning-demo` as decoration while you share a different story. Resume that face for a sitting, or you reject it. Lost is many doors.

### How this factory helps the share

| Already on disk | Use for friends |
|-----------------|-----------------|
| `25` stage map | The walk they take |
| `26` classify + INDEX | *Your* honesty so you do not demo ghosts |
| `23` pocket face | Same law, if the friend is in the room and the sitting is real |
| `aether` + CURRENT | The Yes, hidden behind GATE.md |
| `pros.md` north star | The sentence you say out loud |

---

## 5. A share you could finish

**Name:** one friend, one folder, one morning receipt.

**You bring:** the six-stage workspace (after `25` scaffold) *or* a paper stand-in: Plan / Draft / Yes / Receipt.

**You refuse:** GitHub, `aether help`, this repo, Play Store, “let me just approve for you.”

**Done when:** they can tell someone else what the plan was and whether they said Yes — without you in the room.

That is the framework evolving the framework: the next `CONTEXT.md` you write is shorter because a friend already tripped on the long one.

---

## 6. Checkpoint

Edit the identity line if “kid” is not how you want to be seen.  
Edit the audience table if your actual friend is a developer — then FIRST-PROJECT is enough and `25` can wait.

Constructive picks (silence is not Yes):

- **Scaffold the walk** — `proceed to 03` on `dev/25_icm-people-app`
- **Clear your own diary first** — `proceed to 03` on `dev/26_*` (INDEX + ghosts) so you share from an honest desk
- **Name the friend and folder** in a short note (Layer 4) before any scaffold — cheapest test of the identity
- **Resume or reject** `mobile-planning-demo` so lost has one fewer door

The lucky kid shares a Yes. The lost kid shares a monorepo. You already know which one you meant.
