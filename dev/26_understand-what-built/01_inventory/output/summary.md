# Summary — 01_inventory

Mechanicall OS is a **local-first authority protocol**: `CURRENT.md` + POSIX `aether` (v0.2, ~2065 lines) + human Yes + `.aether/events.jsonl`. That is the product. Everything else is optional interface, lab, or archive.

The `dev/` tree is 16 numbered folders with **missing 07, 10–14, 16, 19–22** and **duplicate 15 and 18**. LAB-STATUS still cites `dev/19_*` and `.context.md` cites `dev/22_tree-org-*`; both are gone from disk.

Live operator Next is still **`mobile-planning-demo`**. `25_icm-people-app` is a factory parked after mapping (say **proceed to 03** there to scaffold). This folder is understand-only.

Review `inventory.md`. Do **not** move directories until you pick a next stage (or reject this factory).
