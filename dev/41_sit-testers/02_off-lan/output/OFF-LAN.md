# OFF-LAN

**Not Yes.** Never A33-as-STORM. No USB phone on the bus this sitting.

## Cellular AVD (done)

storm-0 `emulator-5554`: `svc wifi disable`. Remaining agent: `MOBILE[HSPA] CONNECTED extra: epc.tmobile.com`. WIFI extra gone. App still launches; first-sit template does not need myarch Wi-Fi SSID.

Honest: emulator NAT is still `10.0.2.2` on the **host**. That is not a stranger’s LTE. It **is** “not AndroidWifi / not myarch LAN SSID.”

## Desk quiet path (done)

Empty pocket + `_desk_alive` false → static template on PROPOSE. Off-LAN testers without mesh/7B still get a plan to Decide. GATE quiet is honest. JOIN stays NOT-ON-NET.

## LTE sideload (named, not faked)

No second serial. No RZCW2038KHN. USB list: no phone. **Human** sideloads `0.17.0-testers.apk` onto an LTE device that is not a STORM serial.

Lab drop `127.0.0.1:8765` is **on-LAN** and is not this receipt.
