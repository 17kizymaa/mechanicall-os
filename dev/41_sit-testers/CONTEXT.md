# 41 — sit-testers

**Layer 1.** Live Next `sit-testers` APPROVED. Keep current face. First-sit 7B/static template on PROPOSE. Decide is Publish (no hunk picker). Off-LAN then Play internal. Never Confirm. Never A33-as-STORM. Never production listing. Go-git last.

## Pipeline

1. `01_first-sit-template/` — engine + Draft/Decide face. Tests.
2. `02_off-lan/` — storm-0 + desk-quiet/static path; LTE named if no second device.
3. `03_play-internal/` — signed APK/AAB + Console steps. Human uploads. Not production.

User said **Execute E2E** — run 01→03 this sitting; halt only on Play Console (human account).
