# RECEIPT — 01 first-sit template

**Not Yes.** Confirm not tapped. CURRENT.md on `sittest` was not created.

## Engine
`draft_chat` on an empty pocket with desk quiet → stage `template`, PROPOSE written, CURRENT absent. Tests: `test_first_sit_static_template_does_not_write_current`, `test_first_sit_prompt_asks_full_template`. 63 pocket/inbox tests OK. `app_verify` 10/10. B6 now requires no Decide hunk picker.

## Face
APK **0.17.0-testers**. Draft: “First sit. One prompt makes a template.” Composer **What is this folder for** / **Send template**. Decide: **Publish the proposal. Not hunk pick.** YES / NOT YET. No IN/OUT. Confirm not shown.

## Glass walker
`adb input text` into Compose often empty (IME). SEND FOLDER vs Send template name collision. Standardised: tap `content-desc=Send template`; host `draft_chat` is the template proof. Pushed PROPOSE onto `/sdcard/sittest` for PLAN/DECIDE dumps. PLAN showed shop fields. Decide Publish only.
