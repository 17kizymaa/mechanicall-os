## Inputs
- Layer 0: CURRENT header Next `ACTUAL-APP-VERIFICATION`
- Layer 3: people-app pitch + walk, FACE-SPEC, Limits (phone bind ≠ this repo; Decide two-tap + Why)
- Layer 4: operator: draft is not a module; cannot edit the plan by hand; sprint painting stops

## Process
Write the behaviour contract the reviewer scores. Each behaviour: id, need, not, evidence kind (source / uidump / both). Opening is not Yes.

## Outputs
- BEHAVIOURS.md -> output/
