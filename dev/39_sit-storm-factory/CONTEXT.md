# 39 — sit-storm-factory

**Layer 1.** Live Next `sit-storm-factory` APPROVED (human 2026-08-21T17:25:37Z). Original sit central. EdubaWare cut. STORM factory + depreciated inbox. FILES = device manager. Models never approve. Walkers never Confirm. Go-git is a separate human line.

## Pipeline

1. Factory already in tree (`python/aether_storm.py`, inbox, FILES).
2. This folder: receipts for continue (inbox face + source STORM review).
3. Dirty tree: wait for **Go-git**.
4. `05_execute/` — sideload sit APK onto **emulator-5554** only; storm-walk + `/app-reviewer` + `/visual-reviewer`. Never Confirm. Never A33. See `05_execute/output/EXECUTE.md`.
5. Leftover FLAGs are **not** extra Nexts. `12_icm-plan/` then **`13_run-test/`** (storm-1 on 5556; storm-0 left up; bind→plan→draft→decide, never Confirm). Play Store stays Reject.
6. `15_hunk-inbox/` — Plan Proposal diffs, Decide multi-hunk, SEND IN THE WORKS, lab drop send/receive. Never Confirm. Never A33. Leave storm-0.
