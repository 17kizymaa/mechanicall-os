# HANDOFF — compact for execute

**Not Yes.** Live Next `sit-storm-factory` APPROVED. Models did not approve. Walkers did not Confirm. Serial `RZCW2038KHN` refused.

## Law

- Original sit APK is central. EdubaWare/k3s cut.
- STORM = persistent review factory (`python/aether_storm.py`). Cap 2. Pause AVDs if 7B is hot.
- Headscale = mesh only. Folder send = depreciated `.inbox/` + pocket `.aether/offer` (Accept/Decline, skip CURRENT).
- FILES = device file manager.
- Go-git: grouped commits, **exclude** live `CURRENT.md`, `.aether/events.jsonl`, secrets, APKs, `local.properties`.

## This wrap

| Item | State |
|------|--------|
| storm-0 AVD | created under `~/.mechanicall/storm` (not git) |
| `scripts/storm-up.sh` | **running** — adb `emulator-5554` (google API-34 x86_64) |
| storm-walk | started MainActivity; **package not installed** — no uidump/png |
| `app_verify` | **10/10** source; A33 probe exit 3 |
| Inbox face | ACCEPT/DECLINE + SEND FOLDER; not Yes |
| tsnet JNI | still a named gap |

## Execute stage (next human/agent after compact)

1. Build/sideload the sit APK onto **emulator-5554** only (`scripts/storm-walk.sh` after install). Never A33. Never Confirm.
2. Optional: `/app-reviewer` + `/visual-reviewer` + `/storm-review` on uidump in `dev/39_sit-storm-factory/output/storm-0/`.
3. Do not bake preauth keys. Do not clone EdubaWare. Do not write CURRENT from inbox Accept.
4. Further git only if more execute artifacts appear; live CURRENT still human-owned.

## Go-git (this wrap)

Grouped on `master` (live CURRENT **not** committed):

1. `f8bbe7e` feat(pocket): sit engine
2. `fbff54e` feat(scripts): STORM factory
3. `d6c68b1` feat(android): sit face
4. `5600792` docs(sit): SHARING / grill / skills
5. `6ab94fa` docs(dev): 37–39 receipts
6. `4a6e334` chore(lab): archive 23–36

Left dirty on purpose: `CURRENT.md`, `.aether/events.jsonl`, `.context.md`, `DECISIONS.md`.

## Honest

Opening the emulator is not Decide. Empty storm-0 out dir is honest until the APK is on that serial.

## Execute ran (2026-08-21)

Sideload **Success** on `emulator-5554` (`0.15.0-vst`). Glass dumps in `output/storm-0/`. `app_verify` 10/10 source+glass. FILES opened OEM manager. Confirm not tapped. A33 refused. Details: `05_execute/output/EXECUTE.md`. PASS is not human Yes.
