# RECEIPT — 11_ux-polish

**Not Yes.** Live Next `sit-storm-factory` APPROVED. Confirm not tapped. A33 not used. Play Store not started.

Proposal (not law): `examples/propose-current/CURRENT-sit-ux-draft.md`

## Swarm evals (08 glass + source)

| Role | File | Score |
|------|------|--------|
| Accessibility | `evals/ACCESS.md` | struggle **4/5** on 08 chip museum |
| Real-use sitter | `evals/REAL-USE.md` | stuck on JOIN well + chips |
| Draft researcher | `evals/DRAFT-RESEARCH.md` | protocol museum, not Suggesting |

Those evals describe **before**. This pass is the first face answer.

## Shipped on emulator-5554

| Piece | What |
|-------|------|
| Draft | OBJECTIVE + NEXT on the paper; MORE n for the rest; LIVE vs purple SUGGESTING; ACCEPT/REJECT hunk; `Change Objective` + SEND |
| Decide | ~90ms cream fill, YES gold on dark plate (no gold-on-gold), AGAIN ink on cream, second tap while armed |
| JOIN | paste well **gone**. `join_accept("")` uses `MECHANICALL_JOIN_KEY` or gitignored `.aether/join-provision`. Secret still never in `tailnet.json`. JOIN is tappable without a well |
| Icon | LCD + GATE pits inside an ellipse (`scripts/render-sit-logo.py`) |

Glass: `output/storm-0/{home,draft,decide,decide-arming}.png`  
APK: `output/mechanicall-0.15.0-vst-ux.apk`

`app_verify` **10/10**. A33 `--serial` exit 3. Join tests: empty provision, env key, secret not stored.

## Named leftover

- tsnet JNI — JOIN will not lie **connected** without userspace
- A33 USB on mbp-edge still needs `doas sh /tmp/sideload-sit.sh`
- PLAN is still a CURRENT dump above the field editors (ACCESS FLAG)
- WAKE can look gold while JOIN is NOT-ON-NET (lab probe)

CURRENT not rewritten. No Go-git.
