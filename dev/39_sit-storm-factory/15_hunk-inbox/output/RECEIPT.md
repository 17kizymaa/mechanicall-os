# RECEIPT — 15_hunk-inbox

**Not Decide. Not Yes.** Live Next still `sit-storm-factory`. Operator CURRENT 3411 bytes, unchanged.

## Shipped on the original sit APK (`0.16.0-hunk-drop`)

1. **PLAN** = CURRENT.md (law) + Changes (list) + **Proposal** (diff highlight + editor → `apply_hunk` / PROPOSE only).
2. **DECIDE** = selectable hunks (IN/OUT) + alternative wordings for the same requested change. Yes still two-tap + Why. Walkers did not Confirm.
3. **SEND FOLDER** = **IN THE WORKS** panel (folder name, file count, PUT ON THE WIRE). Not Yes.
4. **Lab drop** `python/aether_drop.py` bind `127.0.0.1:8765`. Phone POST/GET via `10.0.2.2`. Accept still skips live CURRENT.md.

## Demo today

Rae on storm-1 sent `sitrae`. Bob on storm-0 accepted. `shop-notes.md` landed. Bob’s CURRENT `wait-offer` stayed. Operator CURRENT stayed.

## Tests

`python3 -m unittest tests.test_aether_pocket tests.test_aether_inbox` — 60+ tests, including three alts, included-only Yes, drop send/receive, poll-after-accept.

`python3 python/app_verify.py` — 10/10.

## Still named

- tsnet JNI / gomobile (G4 mesh BLOCK). JOIN stays NOT-ON-NET.
- Play Store Reject until a human CURRENT lifts it.
- Go-git is a separate human line.
