# STORE-GATE — Play Store is not this sit

**Not Yes.** Live CURRENT still **Rejects** Play Store. This file is the human’s own bar, not a listing.

Publish the sit APK on a store **only when all four are true**. Until then: sideload.

| Gate | Need | Now |
|------|------|-----|
| G1 patches | Plan = CURRENT + change list; Draft KEEP/UNDO; 7B ICM from its replies; JOIN well gone | **This pass** on storm-0. A33 still needs `doas` sideload. |
| G2 UI vs SOS Test | Direct comparison (COMPARISON.md) + last-minute quality | Written. Millwork still FLAG vs SOS lighting. |
| G3 quality | `/app-reviewer` 10/10 and no FAIL-closed visual | Source 10/10. PLAN dump of `**stars**` still FLAG. |
| G4 client projects | A sitter can **send**, **manage**, **receive** a folder | **Partial.** FILES + bind = manage. SEND FOLDER stages a local copy. Accept/Decline copies except live CURRENT. **Mesh carry (phone ↔ desk over Headscale) is blocked on tsnet JNI.** Local inbox is not “a client on LTE sent me a project.” |

**Do not** open Play Console from this tree. Funnel stays Reject. After G1–G4, the human applies a **new** CURRENT that lifts Play Store — models will not.

## Client send / manage / receive (honest)

- **Manage:** bind a folder; FILES opens the OEM manager on that directory. Works on storm-0.
- **Send:** `SEND FOLDER` → `.aether/upload-stage` (depreciated local offer). Not Yes.
- **Receive:** DESK OFFER ACCEPT/DECLINE. Accept skips `CURRENT.md`.
- **Not yet:** bytes over the tailnet from a stranger’s phone. `build-tsnet-aar.sh` still exits 2 without gomobile. JOIN will not lie connected.

G4 is **BLOCK** for a store listing until JNI or an equivalent honest path exists.
