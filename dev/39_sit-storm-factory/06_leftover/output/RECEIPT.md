# RECEIPT — 06_leftover (one pass)

**Not Yes. Not a second Next.** Live Next `sit-storm-factory` APPROVED. `aether preflight sit-leftover-one` REFUSED. User: implement leftover as **one** continuation. Confirm not tapped. A33 not used.

## Done this pass

1. **GATE lamps** — idle/quiet/queued unlit is `LcdBg` (0/16 dark). STREAM unlit track stays `PurpleDim`. LOAD/WARM amber fill. Glass: GATE QUIET shows empty bevel boxes, not purple fill. `SeatTheme.kt` GateStrip.
2. **DESK compact** — JOIN / WAKE / SEND FOLDER on one row; preauth is a 22.dp well. PLAN shows Keep+Reject; DRAFT shows LIVE + hunks + SEND. Paper is the majority.
3. **Bind sitting path** — “Choose your folder” is the primary pad (system picker). Typed “Bind this folder” is support. Copy: all-files access is lab, not product. Glass DIR overlay. `BindModule.kt`.
4. **tsnet** — `scripts/build-tsnet-aar.sh` exit 2: **named gap: gomobile not on PATH**. JOIN stays NOT-ON-NET. Not faked.

## Verify

- `python3 python/app_verify.py` **10/10** (B7 now requires idle `LcdBg`)
- `--uidump-dir 06_leftover/output/storm-0/verify` **10/10**
- `--serial RZCW2038KHN` exit 3
- Sideload Success on `emulator-5554` only

## Honest

gomobile still missing. NEXT chip on DRAFT still clips. PASS is not human Yes. Live CURRENT not rewritten. No Go-git.
