# VISUAL — sit-storm-factory execute (storm-0 glass)

**Not Yes.** `/visual-reviewer` on storm-0 `emulator-5554` after sideload of `0.15.0-vst`. Never A33. Confirm not tapped.

Contracts: `dev/38_sit-vst-headscale/01_research/output/FACE-SPEC.md`, `GATE-CONTRACT.md`, `SeatTheme.kt`, `SeatNav.kt`.

Glass PNGs: `home.png` (unbound) · `after-relaunch.png` (PLAN) · `draft.png` · `decide.png` · `receipt.png` · `files.png` (OEM).

| id | verdict | evidence |
|----|---------|----------|
| V1 rack | PASS (FLAG paper room) | Title MECHANICALL + bound name + DIR/FILES; LCD; GATE strip; paper modules inside PluginWindow bevel. DESK strip is tall — PLAN CURRENT and Draft NEXT chip clip. Iterate chrome density, not a yellow LCD slab. |
| V2 host | PASS | Activity fills 1080×2340. `fillMaxSize` + `resizeableActivity`. No painted 360×560 card, no traffic-light WinDots, no “92% window”. |
| V3 paper | PASS | Cream/ink plan document. Draft LIVE cream vs purple suggestion; ACCEPT/REJECT HUNK. Not chat bubbles. |
| V4 LCD | PASS | Bound: `name-the-outcome · QUIET` (Next + GATE phase). Unbound: “Name your folder. One project.” Does not dump Objective/events/chat. |
| V5 gate | PASS (FLAG idle lamps) | Labels GATE IDLE / GATE QUIET distinct from SEND. SEND stays SEND. No typing dots. **FLAG:** idle/quiet still paints 16 `PurpleDim` boxes; GATE-CONTRACT wants 0/16 dark. LOAD amber vs STREAM purple remain in source (`GateStrip` `load`/`gate`). FAIL-closed (`…` / Send labelled GATE) does not fire. |
| V6 desk | PASS | JOIN NOT-ON-NET / WAKE SLEEPING are strip verbs. SEND FOLDER labelled depreciated not Yes. FILES is not Decide. YES pad on Decide is first-arm, not Confirm. |
| V7 genuine | PASS | FILES opened the **device Files app** (`sdk_gphone64_x86_64 > mechanicall-pocket`) with CURRENT.md / PROPOSE / RECEIPT. Not a fake plugin explorer. Rack cites FACE-SPEC grammar (title/LCD/GATE/paper), not an Imagine mock as law. |

FAIL closed: none (V2 letterbox, V5 `…`, V3 bubbles all hold).

FLAG iterate (not this Next unless human asks):

1. Idle/quiet GATE lamps → unlit (`LcdBg`), not purple dim.
2. Compress DESK strip so the score is the majority of the activity.

PASS is not human Yes.
