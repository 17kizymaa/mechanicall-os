# TRIAGE — 2–3h client demo (SE lead)

**Not Yes.** Live Next `sit-storm-factory` APPROVED. Analysis target = **this lab SDK / sideload APK**, not Play Store.

See `SDK.md`. Analysts: Camel Space depth (FLAG) · logo (debug-window mark not carried).

## What “SDK version” means here

`0.15.0-vst` code 16 · compile/target **34** · STORM `~/.android-sdk-mechanicall` · storm-0 API-34 `google_apis;x86_64` · review serial **emulator-5554**. Compose BOM 2024.06 · Chaquopy 3.11 · JDK 17.

Not: Play listing, targetSdk 35, A33, a second Next.

## Analyst verdicts (this APK)

**Depth (Camel Space SOS Test vs PluginWindow):** FLAG. Structure of the rack is right (OEM host, cream paper, no letterbox). Millwork is flat: chained `.border(1.dp lite).border(2.dp dark)` **collapses** to one hairline; LCD is a filled Box not a cutout; GATE lamps are 8.dp schematic cells. Cheap patches (same `SeatTheme.kt`): nested bevel that actually stacks; LCD well; GATE pits. No knobs, no letterbox, no DAW cosplay.

**Logo:** `logo_mechanicall.png` = `ic_launcher.png` = **debug-window slab+disc** on gunmetal. Sit chrome is cream rack. Splash 96.dp still shows the cartoon. Carry-over = miniaturize PluginWindow (title / gold LCD / cream / 1+2 stroke). Demo: replace those two files only.

**Decide:** arm is 2s **lerp/crossfade** YES↔AGAIN (`DecidePad` `lerp` + stacked Text alpha). Boring is accurate. Instrument steal: pad as a well that **fills** (GATE-like), not a dissolve.

## Spend (recommend)

| Slot | Do | Don’t |
|------|----|--------|
| Now (same Next, leftover chrome) | Nested bevel + LCD well + GATE pits; rack glyph PNG; Decide well-fill instead of crossfade; sideload storm-0 | Play Store, Funnel, targetSdk bump |
| Follow-up Next (needs **your** apply) | `sit-draft-dictation` — DRAFT workshop + hold-to-talk into focused field. Config not release. | Dual-Next while storm-factory is still Next |

Play Store is **Reject** in live CURRENT. Sideload is the demo path. I will not start a store listing without you rewriting CURRENT.

Proposal (not law): `examples/propose-current/CURRENT-sit-draft-dictation.md`

## Ask

Approve the **now** spend before I patch Compose/PNG. DRAFT/dictation waits until you `aether next` that id.
