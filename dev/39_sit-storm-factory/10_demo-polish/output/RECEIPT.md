# RECEIPT — 10_demo-polish (sideload demo, same Next)

**Not Yes.** Live Next `sit-storm-factory` APPROVED. Preflight ALLOW. Confirm not tapped. A33 refused. Play Store not started.

## Hiccup (addressed, not re-litigated)

Default `java` on PATH is **26.0.1**. Gradle fails with `What went wrong: 26.0.1`. Assemble uses `JAVA_HOME=$HOME/.jdk/temurin-17` + `ANDROID_HOME=$HOME/.android-sdk-mechanicall` as in `scripts/storm-sdk-setup.sh`.

## Shipped on emulator-5554

| Piece | What |
|-------|------|
| Millwork | Nested lips with gaps; LCD well; GATE pits (`SeatTheme.kt`) |
| Decide | 16-step GATE fill, relay YES→AGAIN (`DecideModule.kt`). Two-tap + Why unchanged |
| Logo | `scripts/render-sit-logo.py` → drawable + xxxhdpi. Cream full-bleed |
| OEM splash | `Theme.Mechanicall.Sit` cream, not DeviceDefault black + white circle |
| APK | `android/app/build/outputs/apk/debug/app-debug.apk` · **0.15.0-vst** code 16 · ~42 MiB |

Glass: `output/storm-0/{home,decide,decide-arming,decide-armed,after-splash}.png`

`app_verify` **10/10** source. `--serial RZCW2038KHN` exit 3.

## Named leftover (not this pass, not a second Next)

- tsnet JNI / gomobile — JOIN stays `NOT-ON-NET`
- Dictation / DRAFT workshop depth — proposal `examples/propose-current/CURRENT-sit-draft-dictation.md`
- Play Store — **Reject** in live CURRENT

## Sideload for the client sit (you, not the walker)

Copy the debug APK. Do not ask this agent to `adb` the A33 (`RZCW2038KHN`).
