# 25 — ICM people-app factory (Jake / option 1)

**Layer 1 routing.** Van Clief ICM (arXiv:2603.16021). One agent. Folders are the architecture.

**Law:** root `CURRENT.md` **Next** = `icm-people-app`. Approval is **PENDING** (human re-SELECT + body; no `aether approve` event). Do **not** rewrite operator CURRENT. Do not run approve/next/reject. OpenHands is not the architecture.

**Not this folder:** Play Store listing, swarm UI, cloning RinDig, `compose up`, POST `/yes` on the pocket face.

## Pipeline

1. `01_discovery/` — who it is for; what “useful” means; Jake vs Mechanicall vs demo
2. `02_mapping/` — stage map of the *people-app workspace* (the product of this factory)
3. `03_scaffolding/` — write that workspace tree (empty `output/` except `.gitkeep`)
4. `04_questionnaire/` — one-pass `setup` questions (factory, not a run)
5. `05_validation/` — convention check; halt

Stop after each `output/` until the human says proceed.

## Non-goals

- Do not `aether approve` / `next` / `reject`.
- Do not apply CURRENT-D (Compose/OpenHands as identity).
- Do not claim this Next is already a shipped people product.
- First walk: `~/people-app-stranger-sit` — Next `seatmate-identity`; IDENTITY.md written; stack later.
