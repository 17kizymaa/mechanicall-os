# 34 — One goal (all pending work)

**Layer 1.** Operator REJECTED a split of next-stages. All of it is **one goal** under live Next `people-app-phone-seat` (Status REJECTED; preflight ALLOW). Model does not approve / next / reject / rewrite CURRENT.

## The one goal

1. Bind is **outside** the four plugin pages (Plan, Chat, Decide, Receipt)
2. Persist the folder; Bind is a slot, not a walk tab
3. Desk traffic goes **through this myarch** (localhost → LAN → Tailscale failover + USB reverse)
4. Law copy stays on **Decide**; elsewhere stay discreet
5. Decide buttons fill the window
6. 00s VST chrome: windowed, does not maximise
7. Delete glass Client socket; PROJECT lists the folder
8. Research STORM parallel test envs (SDK) — factory note, not a second Next

Opening these files is not a decision.
