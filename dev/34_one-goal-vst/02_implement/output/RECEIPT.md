# Implement — 0.8.0-vst

**Not CURRENT.** Confirm not tapped.

- Four plugin pages: Plan, Chat, Decide, Receipt. Bind is DIR (title-bar slot + overlay). Path persists in SharedPreferences.
- Desk hosts: 127.0.0.1 → 192.168.0.51 → 100.90.85.68, 8s each. `scripts/desk-through-a33.sh` USB reverse via edge.
- Copy: decision language on Decide only. Gate bubble is `Use Decide.`
- Decide: two pads fill the window (APPROVE / REJECT). Why still required on Confirm.
- Chrome: windowed 00s VST on a dark host; does not maximise.
- Client socket removed from FaceBridge / aether_bridge. PROJECT lists pocket files; `read_shortcut` stays inside the folder.
- Tests: 27 pocket + 13 client OK.
