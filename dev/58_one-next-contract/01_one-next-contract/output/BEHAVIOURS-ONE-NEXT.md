# BEHAVIOURS-ONE-NEXT — Mechanicall: One Next

**Not CURRENT. Not Decide. Not the sit rack.** Contract for the stranger / store face. Sit B1–B15 stay `dev/36_actual-app-verification/01_contract/output/BEHAVIOURS.md` and apply only to archived **You Decide**.

Pitch: *Turn a messy goal into one clear next step. AI may suggest; nothing is active until you publish; home shows only that Next; “I did it” records what happened.*

Principle: **suggest ≠ commit ≠ outcome**. One Next per project. Models never approve. Open ≠ Yes. Silence ≠ Yes.

Walk (Maya): outcome → suggested plan (not active) → publish+why → quiet home (one Next only) → I did it → receipt → next proposal (suggestion again).

Fail closed on **N1, N5, N6**. A round that fails those is FAIL, even if N2–N4 / N7–N10 pass.

| id | Need | Not | Evidence |
|----|------|-----|----------|
| **N1** first-screen-outcome | First screen asks what they are trying to move forward. No bind, no all-files, no permissions wall as the product. | Unbound sit FOLDER; `MANAGE_EXTERNAL_STORAGE` rationale as home; millwork rack as first paint | uidump + look PNG |
| N2 suggested-not-active | Suggested Objective / One Next / Keep / Avoid / Limits is labeled **not active**. Copy must say it is a suggestion. | Suggestion painted as the live plan; auto-publish | uidump + source |
| N3 fields-editable-offline | Human can edit those fields. Offline / no-AI path still works (empty suggestion or last local draft). | Chat bubbles as the only editor; generation required to proceed | source + tests |
| N4 not-yet-leaves-plan | **Not yet** leaves the active published plan (if any) untouched. No CURRENT/active bytes change. | Not yet writes the suggestion through; silent apply | tests |
| **N5** publish-needs-why | Publish requires a human reason. Open / reload / edit / Bind / Send / FILES / GET are not Yes. | First tap is Yes; empty Why; Confirm-shaped chrome | source + tests |
| **N6** home-only-one-next | After publish, home shows **only** the authorized One Next (plus the three actions: I did it / Not this / look). No CRT dump, no lamps, no bank row as the face. | Sit millwork home; JOIN/WAKE/SEND; Objective dump as home | uidump + look |
| N7 i-did-it-receipt | **I did it** asks what happened, then writes a receipt. Empty receipt is honest before the first done. | Fake agree on open; receipt as CRT page | source + look |
| N8 next-is-suggestion-again | After a receipt, the proposed next Next is a **suggestion** (N2 again). Use / Not yet. Not auto-active. | Auto-advance live Next; dual live Nexts | tests |
| N9 export-and-wipe | Export Markdown (schema + receipt). Wipe / delete project is possible. App-private store; SAF only for export/import. | Bound operator tree; all-files as storage | source + tests |
| N10 no-lab-chrome | No `MANAGE_EXTERNAL_STORAGE`. No JOIN / WAKE / SEND lamps. No GATE SEQ as the product. New `applicationId`. Sit package stays lab. | Shipping `com.mechanicall.pocket.demo` as Play v1 | manifest + uidump |

## Maya UAT (human; &lt; 3 minutes to one chosen Next)

1. Goal: get the app to five friends next week without more features.
2. Suggested plan (not active): one Next *Sideload to Alex tomorrow and watch them open it once.*
3. Publish with why.
4. Quiet home shows that sentence only.
5. I did it → e.g. stuck on permissions → receipt.
6. Proposed next (drop all-files) stays a suggestion until Use.

Walkers **never** tap Confirm / Publish. Models **never** `aether approve`. Never bind `mechanicall-os`. Never A33 as STORM serial.

## Scoring (following — not this stage)

- Kernel: unit tests on propose / not-yet / publish / I-did-it / propose-next (`tests/`, CI).
- Glass: STORM walk + uidump against N1/N6/N10. `look_verify.py` is sit millwork; do not reuse as One Next halt.
- Fail closed: N1, N5, N6.

## Sit analog (do not mix)

| Sit | One Next |
|-----|----------|
| B1 bind-first | N1 outcome first (recants bind as the face) |
| B5 propose-not-current | N2 + N4 + N8 |
| B6 decide-only-yes | N5 |
| B8 / B15 receipt | N7 |
| B10 bind-not-repo | N9 wipe + refuse operator tree |
| B7 / lamps / millwork | N10 cut |
