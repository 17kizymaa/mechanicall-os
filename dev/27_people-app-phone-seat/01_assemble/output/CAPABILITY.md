# Capability — 2026-08-18

| Host | Fact |
|------|------|
| myarch (assemble) | Temurin 17.0.20 at `~/.jdk/temurin-17` (system default was JDK 26) |
| myarch SDK | `~/.android-sdk-mechanicall` platforms;android-34 + build-tools;34.0.0 |
| myarch wrapper | `android/gradlew` + `gradle-wrapper.jar` added this turn |
| mbp-edge `100.70.86.90` | Alpine; **no** Java / **no** SDK; ADB host only |
| A33 | `RZCW2038KHN` `SM-A336B` Android 14 · `device` on USB `usb:3-2` |
| Pocket on phone | `/sdcard/mechanicall-pocket` already present (REJECTED demo sitting) — not overwritten |
| Prior package | `com.mechanicall.pocket.face` already installed (different id) |

Tools live **outside the repo**. `android/local.properties` is gitignored.
