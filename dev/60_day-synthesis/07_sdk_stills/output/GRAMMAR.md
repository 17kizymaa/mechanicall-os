# Two grammars on one sit

**Not Yes. Not CURRENT.** Storm-0 stills: `INDEX.md`. Live Next `you-decide-ritual` still says recycle millwork + ritual walk.

The archive is not a failed skin of the walk. It is a **second grammar**. The active style (commitment object) is another. SDK stills show them stacked, not merged.

## Grammar R — rack / millwork (archive)

FACE-SPEC: VST *editor* hosted by the OEM. Title plaque, phosphor hole, three lamps, four banks, well modules, gate row. Cream metal, screws, coffee. Identity = **instrument**.

SDK: splash is 100% R (`STATUS`, `GATE SEQ`, gears). After bind, chassis + banks + lamps are still R even when copy is ritual.

## Grammar C — commitment / You Decide (active this sprint)

Day-20 loop: folder → NOT ACTIVE → Why → Yes → quiet One Next → I did it → receipt ↔ send. Mum stamps; he runs. Identity = **decision**.

SDK: plan peek, draft paper, YES plaque, Why field. Quiet One Next never photographed (no Yes).

## Where they already merge

Decide **YES** on a millwork plaque is the successful sentence: R supplies the till plate, C supplies the stamp. Draft paper over chassis is close (C document, R frame).

## Where they fight

| Leak | R says | C says |
|------|--------|--------|
| Splash | STATUS / GATE SEQ / gears | Name a folder / nothing changes until you decide |
| JOIN/WAKE lamps | desk verbs, peers of Send | leftover-lab, not first sit |
| Four banks always | plugin pages | after Yes, home is one Next |
| Phosphor on paper | amber is the instrument voice | receipt/send are paper |
| Send copy twice | FOLDER millwork | packing slip |

M9 FLAG (“workshop language”) is this collision scored as a bug in R. If R is a grammar we *keep*, M9 is the wrong scorer, not the wrong chassis.

## Discuss first (G1)

Do not start with splash, lamps, or cream pills. Those hang off this:

**When they conflict, which grammar owns first sit?**

| id | Meaning |
|----|---------|
| **G1=1** | R is the face. Ritual is copy on a plugin. Stop household-painting the keys. |
| **G1=2** | C is the face. R is archive to dim/cover until first sit reads as a house. |
| **G1=3** | Two modes (already F1=2): C default, R = lab rack. Artistic work = stop R leaking into default. |
| **G1=4** | Dual on purpose: R = instrument she sits at; C = what it *does*. Keep millwork. Fix *leaks* (splash STATUS, lamps as Yes-peers, double send), not the chassis. |

**Selected: G1=2\*** (2026-09-08). Not the menu’s G1=2.

Human pin: commitment **owns first sit**, **but millwork is augmented, not covered/dimmed**. The chassis is **not the PNG**; it is the **concept** (instrument frame), moving forward from the archive.

That recants:
- menu G1=2 “dim/cover until it reads as a house”
- menu G1=4 “keep these plates, fix leaks only” as the whole story
- treating `sit_chassis.png` as the chassis

It keeps:
- Grammar C = what first sit *means*
- Grammar R = craft we grow so the instrument can say C
- SitRackView host; no second toolkit
- Archive as specimen, not law

## Remaining ambiguity (G2) — yes, one real one

“Augment the concept, not the PNG” still splits:

| id | Meaning |
|----|---------|
| **G2=1** | Same pack / CRects / host. **New millwork plates** allowed (metal, cream, screws, phosphor hole, banks, gate). Archive pixels are a reference, not frozen. **Recommended** |
| **G2=2** | Archive pixels stay; we only add modules/overlays on top (true recycle-the-bitmap) |
| **G2=3** | Concept may **re-lay** the instrument (lamp count, bank count, well shape) as long as it is still millwork, not Material/Compose |

**Selected: G2=1+3** (2026-09-08). New millwork plates **and** re-lay allowed. Not G2=2 (freeze pixels). Still millwork, not Material/Compose. SitRackView host.

**Farm-paint recanted** (human, after P2): do not rectangle-fill the specimen. Chassis = **layers** (`CHASSIS-LAYERS.md`). Field is millwork case, not oak.

Audit: `../08_grammar_audit/output/AUDIT.md`.
