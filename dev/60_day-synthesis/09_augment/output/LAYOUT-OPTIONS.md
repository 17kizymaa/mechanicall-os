# Layout options — C rooms on millwork (G3=1 preview)

**Not Yes. Not CURRENT. Not Imagine-as-spec.**  
Pins: G1=2\* · G2=1+3 · **G3=1** (generator preview so Imagine can paint) · **F3=3** (chassis Send LED + Decide ritual). Farm-paint recanted. Chassis ≠ freeze PNG.

This file is the grill surface for **L** (which plate stack `render-sit-skin.py` draws). Reply `L=…`. Silence is not a pin.

---

## Steal ethos (looked up — you asked)

Three **steal** layers already on disk. They are not clones. Session 05c *is* `05_preview_framework/output/ELEVATION.md` (DECISIONS 2026-09-08T08:48: “comparative analysis… imma go for that as 05c”). Compaction Turn 21–22: “each module… comparative product research” → that file.

### 1. Pipeline (VST shop) — *how the face is made*

| Steal | Don’t |
|-------|--------|
| Photoshop-frozen overlapping plates → one `CBitmap` → `CRect` hits → pack in the binary (`drawable-nodpi/`, no density scale) | Camel Audio / CamelPhat / Step FX **bitmaps** (`camel-png-theft`) |
| Host (DAW / Android OEM) only owns the window; the editor **blits** | Live Compose rectangles + tiled metal shader as identity |
| `scripts/render-sit-skin.py` as inspectable PSD; Imagine as **Photoshop**, not spec | Imagine mock as law; JUCE/VSTGUI C++ in the APK |

Sources: `decision-tree.md` **M0b** / **P2**; `dev/39_sit-storm-factory/15_binary-skin/output/BINARY-PIPELINE.md`; FACE-SPEC “Steal: Camel Space / generic 2000s plugin *editor*”.

### 2. Interaction (You Decide rooms) — *what a tap means*

| Room | Steal | Don’t |
|------|--------|--------|
| Bind | Files / Gander / Obsidian vault — name **this** house | Explorer museum |
| Plan | iA Writer / till **ticket** | Chat; CRT as the document |
| Draft | GitHub Suggesting — **not merged** | WYSIWYG bar; ALPHA keys |
| Decide | Monzo/Wise sheet + BIG Launcher — Why then fat Yes | Confirm; biometric as Yes |
| Quiet Next | Duolingo Start / gym GO — one action | Lamp dashboard; streaks |
| I did it | Till “paid” — labour, not a second Yes | Confetti |
| Receipt | Thermal strip you can forward | CRT page |
| Send | Share sheet / zip-as-bag — files only | Send as Yes |
| GATE | Download bar — honest wait | Door into the plan |

Sources: `01_ingest/output/19_PRUNE-YOU-DECIDE-APK-SPRINT.md` (“steal *interaction grammar*, not CRT plates”); `05_preview_framework/output/ELEVATION.md`.

**Recant on 19’s last sentence:** “Rebuild the face as a till… **no millwork plates**” is **not** live. **G1=2\*** kept millwork as craft. Till is the *grammar C speaks*; millwork is the *instrument that says it*.

### 3. Millwork grammar (instrument) — *how metal looks*

| Steal | Don’t |
|-------|--------|
| Named plates; faceplate is a **layer**; panel is the product; bench is host chrome | Photograph of a synth **on a desk** as the UI |
| Lighting and metal **function**; layout breathes | Screaming bolts, plastic 3D, oak furniture |
| Rack **around** paper (FACE-SPEC) | Yellow LCD as the whole sit |

Source: `09_augment/output/CHASSIS-LAYERS.md` “Comparative grammar (steal, don’t clone)”.

**Ethos in one line:** steal **pipeline + interaction + plate grammar**. Never steal **pixels, pets, Confirm, or the bench**.

---

## Already explicit (do not re-invent rooms)

C actions **already have** a plate / hit / peek. Generator + Imagine must **speak these**, not a new product.

| C room | Plate / hit (WALK) | Peek (PEEK-REDAFT) | SitCRects (pack px) | AUDIT |
|--------|--------------------|--------------------|---------------------|-------|
| Boot | `SPLASH` | Instrument power-on → bind | title + splash | AUGMENT copy |
| Door | `BIND` | House name / “Name a folder.” | `bindPad` | KEEP pad |
| Goal | `PLAN` | “This is the plan.” Ticket keys | `well` rows + banks | KEEP keys |
| Draft | `DRAFT` | **NOT ACTIVE** millwork plate | well → writer dest | AUGMENT plate |
| Stamp | `DECIDE` | WHY + **YES** plaque | `well` + Why | KEEP plaque |
| Quiet home | `SitPlate.NEXT` | One Next; two millwork feet | `didIt` / `notYetFeet` | AUGMENT feet |
| I did it | `SitHit.DidIt` | Labour → receipt | well row 0 | KEEP law |
| Receipt | `RECEIPT` | Empty is empty; graphite | well | AUGMENT ink |
| Bag | Send lamp + dest | One lamp; files only, not Yes | `send` 435,870–652,1110 + `sendPanel` | AUGMENT ink |
| Wait | Gate row | ETA / wait lights | `gate` 75,1864–1005,2107 | AUGMENT word |
| Leftover | JOIN / WAKE / CRT dest | Not first sit | `join` / `wake` / `isolatedCrt` | PARK verbs |

Banks **KEEP**: PLAN DRAFT DECIDE RECEIPT. No 5th NEXT bank. Title plaque **KEEP**, not a control. Phosphor **KEEP**.

**F3=3 gloss (this round):** moodboard **Send LED stays on the chassis** (anytime offer). **Also** a Decide-ritual addition (not Yes). One dest. Accept ≠ Yes.

---

## The actual gap (why L exists)

Three **geometries** disagree. Rooms are named; **holes** are not.

```
Freeze / SitCRects (live hits)
  plaque → phosphor hole → 3 lamps (JOIN/SEND/WAKE) → 4 banks → well → gate

FACE-SPEC / render-sit-skin.py as written
  title → lcd → gate → desk → tabs → paper (score fills the rest)

19 prune “till” (interaction, not a pack)
  ticket / open order / charge plate / Start block / strip / bag
```

G3=1 retargets the **generator** at one of these so Imagine has a labeled preview. Imagine paints metal into that preview. You stamp. Then nodpi. Imagine ≠ spec.

---

## L — Which stack does the generator draw?

### L=1 — Recycle freeze **slots** (same CRects, new millwork)

Generator paints overlapping plates **into SitCRects**: plaque, phosphor, lamp row, banks, well, gate. Moodboard = hole diagram, not pixels.

- Send LED stays in the lamp row (F3=3).
- JOIN/WAKE **holes** become non-verb millwork (screws / meters / blanks) — AUDIT relay, not dimmed icons.
- Well swaps content per `SitPlate` (ticket / NOT ACTIVE / YES / two feet / strip).
- Imagine gets a labeled wireframe at 1080×2138 with those boxes.

**Today-shaped.** Hits already law. Closest to “inspiration not PNG.”

### L=2 — FACE-SPEC generator **as written**

`title / lcd / gate / desk / tabs / paper` from `SKIN-LAYOUT.md`. C lives on the cream **paper** inset. Desk strip is leftover JOIN/WAKE in FACE-SPEC — would have to become Send + blanks.

**Fights live SitCRects** and the moodboard lamp row. Honest rack-around-paper. Re-lay cost this sitting.

### L=3 — Till **as the pack** (19 visual)

New plate stack: ticket / charge / Start / strip / bag as the geometry. Banks may shrink. Lamp museum gone except Send.

**Steals interaction as layout.** Risk: recant millwork into a household till (the 19 last-sentence fail G1=2\* already killed). Use L=3 only if you want the *holes* to be till, not just the copy.

### L=4 — One **full CBitmap per plate** (FRAMEWORK swap)

Generator emits `sit_plan` / `sit_draft` / `sit_decide` / `sit_next` / `sit_receipt` / `sit_splash` as complete panels. Imagine paints each. SitRackView swaps resource, one blit.

**Most VST.** Most Imagine passes. Hits still integer, but every plate is a new bake.

---

## Recommend

**L=1.** Generator retargets to **SitCRects**. Imagine paints cream/gold/purple overlapping plates **in those holes**. ELEVATION till grammar is **copy and elevate inside the well**, not a new pack. F3 Send LED keeps its freeze slot.

L=2 only if you want to throw the moodboard lamp row. L=3 only if you recant “instrument” toward “till furniture.” L=4 if today is allowed to be several stamped previews, not one chassis.

---

## S — How Send lands on **Decide** (F3=3 shape)

Chassis LED is settled. Decide addition is not.

| id | Decide addition | Risk |
|----|-----------------|------|
| **S=1** | One millwork **packing-slip pit** on the Decide well (offer folder, ≠ Yes). Same dest as the lamp. **No second LED.** | Copy must not read as Yes |
| **S=2** | After Yes only (quiet Next). Chassis LED still anytime. | You said Decide *part of the ritual* — this misses the stamp room |
| **S=3** | Second Send LED on the Decide plaque | Two lamps; Send≈Yes; stills already FLAG double copy |

**Recommend S=1.** One lamp (moodboard). Two hits (lamp + Decide pit). One dest. Not Yes.

---

## Pipeline after L (and S)

```
render-sit-skin.py  →  output/preview/skin-wire.png + SKIN-LAYOUT.md
Imagine (paint)     →  output/preview/skin-imagine.png     # not spec
human stamp         →  drawable-nodpi/  (aapt2 = Resource Hacker)
SitRackView blit    →  G3b later (one bake vs keep overlays)
```

Do not farm-paint the freeze JPEG. Do not run Imagine until **L** is pinned.

---

## Halt

Reply `L=1` (or 2/3/4) and `S=1` (or 2/3). Then the generator can emit a preview Imagine can paint.
