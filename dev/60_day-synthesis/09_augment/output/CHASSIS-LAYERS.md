# Chassis layers — show the instrument

**Not Yes. Not CURRENT.** Recants **farm-paint** (Pillow glyph boxes / cream floods on the specimen PNG). G1=2\* · G2=1+3. SitRackView host.

**Update:** the freeze PNG is **inspiration**, not the chassis. See `CHASSIS-CONCEPT.md`. Field+alpha is a surround fix on the **specimen**, not the instrument grammar. Do not millwork-as-if that photo were law.

## Comparative grammar (steal, don’t clone)

| Source | Steal | Don’t |
|--------|-------|-------|
| **VSTGUI / Camel CBitmap** | Named bitmaps stacked (`background` + widgets). Faceplate is a layer, not a flattened photo. | One edited JPEG of a synth on a desk |
| **Hardware mixer / analog faceplate** | The *panel* is the product. The bench is host chrome. | Photographing the bench as the UI |
| **FACE-SPEC v2** | Rack around paper. OEM hosts the editor. | Yellow LCD as the whole sit |
| **Plugin UIs that still work (2025–26)** | Lighting and metal *function*; layout breathes; analog grammar without the furniture | Screaming bolts, plastic 3D, wood table |
| **This sit’s own stack** | `SitRackView` already blits field-ish modules (banks, well, gate, send) | Farm-painting JOIN/WAKE/STATUS into `sit_chassis.png` |

Farm-paint flattened the instrument into the desk. Layers put the desk (field) *under* the faceplate again.

## Stack (back → front)

| Layer | Drawable | Role |
|-------|----------|------|
| 0 Letterbox | color `#12100E` | OEM hole |
| 1 **Field** | `sit_field.png` | Millwork case. Dark brushed metal. **Not oak.** |
| 2 **Face** | `sit_chassis.png` (alpha) / splash (alpha) | Cream instrument. Wood knocked to transparent. |
| 3 Modules | send, banks, well, gate | Existing CRect overlays |
| 4 Peek | phosphor draw | Instrument voice |

Do **not** bake field into face. Do **not** rectangle-fill verbs on the face as the design method (P1/P2 scars stay until a real millwork pass).

## Splash

Keep the splash **face** (gears, plaque, lamps). Replace surround with **field**, same as the rack. One instrument, two plates.

## Halt

`sit_field.png` + alpha face is the first layer drop. Farm-paint recanted. P3 cream-pills wait; they were farm-paint.
