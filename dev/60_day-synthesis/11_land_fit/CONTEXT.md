# 11_land_fit

**Not Yes. Not CURRENT.** Human **Go**. Archive chassis is identity. Stolen layers into existing holes. L4 parked. Farm-paint recanted. Do not start a second chassis.

## Inputs
- Layer 4: `../10_interaction_preview/output/FIT.md` (the map)
- Layer 3: SitRackView, SitCRects, MainActivity
- Law: `python/app_verify.py` 15/15

## Process
1. After Yes: stay `PLAN`, `labour` well = I did it / Not yet. Do not `goBank(NEXT)`.
2. Send overlay: one packing-slip line + listing. Lamp stays. JOIN/WAKE remain non-hits, not blit.
3. Draft chassis well: Kotlin **NOT ACTIVE** (violet), not cream pills / not a new PNG flood.
4. Receipt well: graphite (`QuietInk`).
5. Splash words already recanted (P1). Do not farm-paint `sit_chassis.png`.
6. Bump version. `app_verify`. Assemble Temurin 17 if present.

## Outputs
- Kotlin + versionName
- `output/RECEIPT.md`
- `app_verify` score
