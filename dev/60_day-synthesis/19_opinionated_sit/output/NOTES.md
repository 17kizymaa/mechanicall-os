# NOTES — opinionated sit `0.19.20-inbox-net`

**Not Yes.** Storm-0 `emulator-5554`. Confirm not tapped. Yes not two-tapped. Decide Not yet not tapped. Left Decide via RECEIPT bank (P0 hazard).

APK: `mechanicall-0.19.20-inbox-net-vc45.apk`  
Stills: `stills/`  
Handoff: `../18_loop_patches/output/HANDOFF.md`

Authorised First Plan is **PROPOSE only** on the seed pocket. Live CURRENT on glass stayed `Next look-only`.

---

## Open the app

`01-open.png` — already PLAN (seed bound). Splash missed. Phosphor: “This is the plan. Next look-only.” JOIN/WAKE lamps are back (14). GATE WAIT.

## Draft your First Plan (authorised)

Pushed `PROPOSE-CURRENT.md` then opened the writer.

- `05-draft-well.png` — chassis well is **one** violet NOT ACTIVE plate. **No extra buttons** (no LIVE/PROPOSED pills, no SAVE DRAFT). Phosphor: `NOT ACTIVE / live look-only / proposed strengthen-core-loop`.
- `06-draft-writer.png` — cream paper, chassis plaque + GATE around it. First Plan text visible. Not IsolatedDark. Not CURRENT.

## Decide an outcome (and why?)

Looked. Did not stamp.

- `07-decide.png` — Why strip + fat YES + **Not yet** under it. Phosphor: “Why. Then Yes. Not yet is enough.”
- `08-decide-why.png` — Why tap steals the **tube** (`why` EditText). Millwork strip stays a label.

**Outcome I would give, without Yes:** Not yet. Keep Next. Why: this pocket’s receipt already says Status/Approval **REJECTED** from a default `"not yet"` (P0). Yes would apply PROPOSE onto a CURRENT the sit already rejected by **navigation**. Models do not two-tap.

## Preview the full receipt — why it is useless

`09-receipt.png`

Phosphor + well show the same leftover markdown soup, `stripMd`’d:

- When 2026-09-08 15:34 UTC (not this walk)
- You said: Not yet
- Plan (Next): look-only
- Status: **REJECTED** · Approval: **REJECTED**
- Trail JSON in the well

Useless because:

1. It is not what happened **this** sit (we did not Yes, did not Not-yet-tap, did not labour).
2. It is a **file dump**, not a thermal strip of one said-line.
3. REJECTED was likely `leaveDecideWithoutYes` → `pocket_reject` (P0), i.e. the machine pretended a bank tap was a stamp.
4. Empty would have been honest. This leftover is a lie of completeness.

## Does the terminal sit cleanly on the chassis?

**Peek, yes. Dest, no.**

`04-plan-terminal.png` is identical to `02-plan.png`. Tap on the tube called `peekPage(0)`, not `openTerminal()`. CRT dest (chassis + `sit_crt`, 14) never appears. `openTerminal()` is dead. The phosphor peek **does** sit in the chassis glass (amber, truncated, not whole CURRENT). That is FIT peek. It is not a reader.

## Draft plate — appropriate buttons?

**None, and that is correct.** `05` is one NOT ACTIVE door. Cream pills recanted in 15. Writer is the whole-file editor. Do not add SAVE DRAFT / ALPHA. HANDOFF P6.

## Click the terminal on the Plan

Clicked. Nothing changed (`02` = `03` = `04`). P1a/P7.

## Folder icon — folder tree presentation

`10-folder.png` — packing slip. Listing: `.aether`, `CURRENT.md`, `DECISIONS.md`, `PROPOSE-CURRENT.md`, `RECEIPT.md`. Millwork `files only · not Yes`. Kotlin **`none`** on the left of that slogan. OEM FILES. Not a tree — a flat name list. Not Yes.

## Plan button logic

Five keys look like a form. Hits are `draftOrPeek` → `peekPage(n)` on PLAN (read-only TOC into `LawPages`). The peek **does not change the tube** because `lcdBody()` uses `idleLine`, not `pages[pageIndex]`. So the buttons are visually “press me” and mechanically invisible. They do **not** edit CURRENT. They do **not** open Draft. Keep that split (P5). Make TOC visible (P7) or open CRT (P1a). Do not turn them into a form.

---

## Tie to HANDOFF

Glass confirmed P0 (REJECTED leftover), P1 (dead CRT), P2 (`none`), P3 (useless receipt dump), P4 (Why in the tube), P6 (no draft buttons), P7 (invisible TOC).
