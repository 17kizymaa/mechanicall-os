# AUDIT-CONTINUE — session implementations vs this Continue

**Not Yes. Not CURRENT.** Previous millwork chat compacted; sitter session wrote FLAGS. This file is the audit so context is not lost.

## Sessions (what landed)

| When | Artifact | Implementation |
|------|----------|----------------|
| FIT stamp | `10_interaction_preview/output/FIT.md` | Archive chassis is identity. C rooms in SitCRects holes. Quiet Next *room* recanted. |
| 11 land | `0.19.17-fit` vc 42 | After Yes → PLAN `labour` feet. JOIN/WAKE **not blit** (P2 blanks). CRT IsolatedDark. Receipt graphite **on top of** baked well. |
| 13 sitter | `13_sit_sitter/output/FLAGS.md` | Walked storm-0. Never Yes. KEEP plaque/plan/send/WAIT. FLAG decide (no Not yet), receipt (stamp + text), phosphor markdown, bind house name, send `none` collision. JOIN/WAKE blanks KEEP *then*. |
| This Continue | human | Recant empty LEDs; receipt inner interchangeable; Decide needs No; CRT black covers chassis. Millwork-by-components. |

## This Continue vs FLAGS / FIT

| Bug | FLAGS | FIT | This Go |
|-----|-------|-----|---------|
| Empty JOIN/WAKE LEDs (rack + splash) | KEEP blanks | leftover-lab, do not blit | **Recant visual.** Wire original `sit_lamp_join` / `sit_lamp_wake` CBitmaps. Hits wired. Action still not Yes. |
| Receipt millwork obscures live receipt | FLAG 08: baked stamp + leftover md | graphite on well | **Inner interchangeable.** Metal frame stays; paper hole is live graphite. Not text-on-photo. |
| Decide only YES, no No | FLAG 06: blank metal, Not yet unreadable | Why + Yes + Not yet | **Not yet** on the blank plate (product No). Hit = leave without Yes. |
| CRT black covers chassis | PARK first sit | IsolatedDark dest | **Recant flood.** Chassis millwork under `sit_crt` module. IsolatedDark debug Reject in CURRENT. |

## Millwork-by-components (already half-true)

SitRackView already stacks named CBitmaps: field → chassis → lamps → banks → well → gate. The lie was: JOIN/WAKE decoded but not blit; wells are **full inner plates** (baked YES / RECEIPT stamp) not frame+live inner; CRT dest skipped the stack.

This stage: lamps on the stack; receipt inner is a hole; Decide No is live type on the existing plate; CRT uses the stack.

## Do not lose

- Quiet Next room recanted (PLAN labour). Unseen without Yes.
- L4 parked. Farm-paint recanted. Imagine ≠ spec.
- `app_verify` 15/15 is law, not beauty.
- Models never Yes. Never A33 as testers.
