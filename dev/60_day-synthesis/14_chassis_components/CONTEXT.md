# 14_chassis_components

**Not Yes.** Human Continue: wire original LEDs; receipt inner interchangeable; Decide Not yet; CRT keeps chassis. Never Confirm. Never farm-paint the freeze JPEG. Archive frame stays.

## Inputs
- Layer 4: `../13_sit_sitter/output/FLAGS.md` (session audit)
- Layer 4: `../10_interaction_preview/output/FIT.md` (geometry; JOIN/WAKE *visual* recanted this Go)
- Layer 3: SitRackView, SitCRects, nodpi `sit_lamp_*`, `sit_well_receipt`

## Process
1. Session audit → `output/AUDIT-CONTINUE.md` (FLAGS + this Continue; context not lost).
2. Blit JOIN/WAKE original lamp CBitmaps on rack **and** splash. Hits wired; action leftover-lab (not Yes).
3. Receipt well: alpha the paper inner (keep metal frame). Live graphite in the hole. Do not draw on the baked RECEIPT stamp.
4. Decide: readable **Not yet** on the blank plate under YES (the No). Hit = leave without Yes.
5. CRT dest: `drawRackMillwork` then `sit_crt` module. No IsolatedDark flood.
6. Version bump. `app_verify`. Assemble. Storm stills if emulator-5554.

## Outputs
- AUDIT-CONTINUE.md, RECEIPT.md, stills
- Kotlin + punched `sit_well_receipt.png` (archive first)
