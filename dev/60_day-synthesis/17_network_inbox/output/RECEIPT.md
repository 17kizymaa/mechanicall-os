# Receipt — 17_network_inbox

**Not Yes.** Next `you-decide-ritual` APPROVED. Preflight ALLOW. Confirm not tapped.

**APK:** `0.19.20-inbox-net` vc 45  
**Archive:** `~/.mechanicall/apk-archive/mechanicall-0.19.20-inbox-net-vc45.apk`  
**Law:** `app_verify` 15/15  
**Tests:** `tests.test_aether_inbox` + `tests.test_aether_pocket` OK (incl. default `inbox/`, drop → sitters, GATE percent)  
**Host:** storm-0 `emulator-5554` (never A33)

## Landed

1. **File transfer → `mechanicall-os/inbox`**
   - `python/aether_inbox.py` `inbox_root()` is repo `inbox/` (env `MECHANICALL_INBOX_ROOT` still wins).
   - Lab drop `python/aether_drop.py` stores `inbox/drop/` and unpacks a **copy** into `inbox/sitters/<id>/`.
   - Accept still skips live `CURRENT.md`. Operator CURRENT untouched (tested).
   - `inbox/README.md` committed; sitters/outbox/drop gitignored.

2. **JOIN / WAKE → networking dest** (`IsolatedMode.NETWORK`)
   - First tap opens the dest. Second JOIN uploads (`send_project`). Second WAKE pulls (`poll_incoming`).
   - Stand-in millwork = `sit_folder_dest` on `networkPlate` (above GATE). **New plate PNG follows.**
   - Send LED still S=1 packing-slip overlay. Not Yes.

3. **GATE → upload/download percent**
   - Pocket `.aether/transfer.json` + drop `inbox/drop/progress.json`.
   - `gate_state` overlays `percent` / `direction` (`up`/`down`).
   - Phosphor + bar on the trance-gate row when percent > 0. Idle GATE is honest empty / WAIT millwork.
   - LOAD/STREAM strings stay in source (B7).

4. **Comparative products named** — `output/PRODUCTS.md`  
   **Plan vs free-draft questioned** — `output/PLAN-VS-DRAFT.md` (no recant this pass).

## Stills (looked)

| File | Look |
|------|------|
| `stills/06-plan-after-fix.png` | Plan ticket + three lamps + five keys + GATE WAIT |
| `stills/07-join-network.png` | JOIN dest on chassis; phosphor “GATE idle / JOIN uploads … inbox. Not Yes.”; GATE millwork still visible |
| `stills/05-gate.png` | WAKE dest copy; GATE idle |

## FLAGS (millwork follows)

- Networking dest is the **folder plate**, not a new milled networking plate. Lamps disappear under it. Plan keys NEXT/KEEP/REJECT/LIMITS still peek below OEM FILES.
- Paper listing duplicates phosphor copy on the dest.
- GATE percent bar is latent until a real transfer (drop quiet on storm-0 → 0%).
- Plan well still reads as five **buttons** (see PLAN-VS-DRAFT). Hits remain peek-only.

## Not this pass

- Imagine a networking PNG
- Markwon
- Headscale mesh
- Yes / Confirm / paste CURRENT
- A33 USB look
