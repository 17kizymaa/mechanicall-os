# Preview framework — options (before millwork)

**Not Yes.** Live Next `you-decide-ritual`. Walk hits (`04`) exist. Millwork stays **this sprint**, after this phase. Website later. Confirm not tapped.

**Why 05 is preview, not paint:** millwork on a walk whose *peek* is still CRT-shaped will paint the wrong room. She needs to see Goal / NOT ACTIVE / One Next / receipt **as papers**, not as a tube, before we recycle plates.

**Selected:** **P=1** (2026-09-08). Contract: `PEEK.md`. Wire peeks = P2, not this halt.

Reply `P=…` (and optional `peek=…` if P1/P2).

---

### P1 — Contract only (markdown)

Write `PEEK.md`: one row per ritual room.

| Room | Chassis shows | Tap does | Never |
|------|---------------|----------|--------|
| Bind | folder name / “name a folder” | picker | Yes |
| Plan | truncated Goal / Next | stay on papers (or dest *paper*, not CRT) | whole CURRENT on glass |
| Draft | **NOT ACTIVE** + short proposed Next | writer | CURRENT rewrite |
| Decide | Why prompt; armed = Yes? | Why / two-tap Yes | Confirm; Publish |
| Quiet Next | **Your One Next** (one line) | I did it / Not yet | JOIN/WAKE |
| Receipt | empty is empty; else what happened | stay | fake agree |
| Send | lamp + overlay listing | stage; files only | Yes |

**Halt:** human stamps PEEK.md → `06_millwork_rooms` paints to it.

**Cost:** hours. **Recommend if:** you want millwork aimed.  
**Not:** code, stills, Imagine-as-spec.

---

### P2 — Contract + wire peeks (code, no new nodpi)

P1, then make `idleLine` / well ink match that table on the recycled host. Truncate. No CRT as first-sit (already). No new plates.

**Halt:** sideload when JDK 17 exists; else source + `app_verify`. Then millwork.

**Cost:** 05 rest of sitting + assemble later.  
**Recommend if:** the walk still *reads* like STATUS/LCD and you don’t want to millwork that lie.

---

### P3 — Still gallery as the brief

Assemble `0.19.12-walk-hits` (needs JDK 17), storm-0 or A33 USB look of each room. Those PNGs are the millwork brief: paint must beat these.

**Halt:** `stills/` + one-line FLAG per room → millwork.

**Cost:** blocked on JDK 17 / USB. Honest if you need eyes. USB ≠ LTE.

---

### P4 — Recycle-plate sketches (Pillow on archive nodpi)

Stamp room names / NOT ACTIVE / Yes / I did it onto existing chassis/wells. Output = millwork targets, not live APK. Imagine not used as spec.

**Halt:** sketched PNGs in `output/sketches/` → millwork copies from those.

**Cost:** art loop risk. **Not** if we just painted the old FLAG millwork.

---

### P5 — Peek + dest-reader (bigger)

Peek on chassis; tap opens a **paper dest** (not IsolatedDark, not CRT-as-the-room) for full CURRENT / PROPOSE / receipt. Dest millwork this sprint becomes that reader.

**Cost:** 1–2 days. Recants leftover “CRT dest is the reader.” Only if you still want a dest at all.

**Reject today unless you say so:** it’s a second architecture inside 05.

---

### P6 — Skip preview; millwork now

05 = `05_millwork_rooms` as already queued. Preview = look after paint.

**Cost:** lowest process, highest chance of painting LCD/STATUS again.

---

## Working default

**P1** if you want to pick millwork today. **P2** if the sitting still has hours and JDK isn’t the gate. **P3** when you can assemble. **P6** only if you’re done talking.

Mum–son: preview is what **she** sees before she stamps. Not a workshop dump. Not nag. Not a mascot.

F3 still open (recommend 1). Does not block P1.

---

## Pipeline if you pick

```
04_walk_hits     (done)
05_preview_framework  ← you are here (options)
06_millwork_rooms     (this sprint, after peek contract)
07_assemble / gauntlet
```
