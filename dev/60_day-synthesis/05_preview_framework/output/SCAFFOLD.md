# Next scaffolding — 05 remainder + Draft LLM

**Not Yes.** Time is short. Dual-Next Reject. Millwork still this sprint after PEEK stamp. Generate is **following** on live CURRENT until a human recants `generate-this-halt` **for Draft dest only**.

Reply `S=…` (and stamp PEEK if you haven’t).

---

### S1 — Ritual finish, Generate later (safest vs live CURRENT)

```
05  stamp PEEK.md
06  millwork rooms (elevation + recycled nodpi)
07  assemble / gauntlet
—— later Next ——
sit-bound-draft   (prose→sft-v4→merge PROPOSE)
```

**Keep:** `generate-this-halt` Reject until that later Next.  
**Cost:** honest to CURRENT. Draft LLM waits.  
**Use if:** you will not recant Generate this sitting.

---

### S2 — Same Next, Draft LLM **after** millwork (recommended if you want the track *and* paint)

```
05  stamp PEEK + this Draft-LLM contract
06  millwork rooms (paper Draft, fat Yes, quiet Next)
07  Draft dest Generate chore (sft-v4 + draft_rag_pack merge)
08  assemble
```

**Requires:** human recant of `generate-this-halt` **narrowed** to “chassis Generate / empty tap / model Yes” — Draft dest chore allowed. Still one Next: `you-decide-ritual`.  
**Cost:** this sprint stays APK-shaped. Desk must be up for 07; paper works if down.  
**Use if:** elevation + millwork first, LLM the same halt.

---

### S3 — Verify now, wire never this sitting (what this turn did)

```
05b PEEK.md (unstamped)
05e DRAFT-LLM.md + draft_rag_pack.py tests
06  millwork when PEEK stamped
Generate still following
```

**Keep:** no APK Generate. Corpus pack is real.  
**Cost:** lowest risk. You already have the packer.  
**Use if:** millwork is the clock.

---

### S4 — Wire Generate **now** on Draft dest (fast, fights PEEK)

Hook Draft dest `sit_gate` / a new chore hit → `agent_edit_propose` **after** replacing whole-tree pack with `draft_rag_pack` and merge-not-replace.

**Fights:** live Reject `generate-this-halt`; PEEK “GATE is waiting”; empty millwork clock.  
**Use only if** you recant those **today** and accept millwork slipping.

---

## Working default

**Selected: S=3** (2026-09-08). Verify-only. No APK Generate this sitting. Millwork is `06_millwork_rooms`. Isolated Draft LLM stays reverse-engineerable later (`DRAFT-LLM.md` + `python/draft_rag_pack.py`).

Chassis split to keep either S2 or S4 honest: **chassis GATE = ETA**; **Draft dest Generate = chore**; never Yes.

F3 still open (recommend 1). Website later. OpenRouter QC stays desk.

---

## Also later (not 05)

- Markwon on Plan/Draft/Receipt paper (`ELEVATION.md`)
- FileProvider share of RECEIPT / zip bag
- Headscale
- anphuni.com drafter (same job as sft-v4, **web never Yes**)
