# Draft room — prose → NOT ACTIVE (parked config, ritual grammar)

**Not Yes. Not Generate-on-chassis.** Live Next `you-decide-ritual` still has **Generate following** and Reject `generate-this-halt`. This file **verifies** the parked APK job and **corrects** it to Draft grammar. Wiring the APK is a scaffold pick (`SCAFFOLD.md`), not silent recant.

Parked law (Grok-drafted, not live): `examples/propose-current/CURRENT-sit-bound-draft.md` · `.aether/proposals/CURRENT-proposal-20260905-sit-bound-draft.md`

## Job (keep)

```
prose on Draft paper
→ Generate (Draft dest chore)
→ personal-llm-sft-v4 @ desk Ollama
→ folder-walk RAG (bound CURRENT + PROPOSE + receipt names)
→ curated few-shot CURRENT.md corpus
→ schema Objective/Next/Keep/Reject/Limits **merged** into PROPOSE
→ user prose stays
→ optional OpenRouter QC of schema only (desk, $10/w, key never in APK)
→ CURRENT unchanged
→ she stamps at Decide
```

Fail-closed if desk down. Paper still works. Stranger sideload does not need your Ollama.

## Already on disk (pull, don’t rewrite)

| Piece | Where | Parked behaviour | Ritual correction |
|-------|-------|------------------|-------------------|
| Model | `aether_pocket.agent_edit_propose` default `personal-llm-sft-v4:latest` | Desk Ollama | Keep. Propose/taste only. |
| Refuse public bind | `_refuse_public_ollama` | no `0.0.0.0` | Keep. USB ≠ LTE. |
| Desk hosts | `DEFAULT_DESK_HOSTS` myarch / 127.0.0.1 / LAN / Tailscale `100.90.85.68` | fail-over | Keep. |
| Tree pack | `pack_tree_context` 80k **whole folder** | D9=3 dump | **Recant.** Use `python/draft_rag_pack.py` (CURRENT+PROPOSE+RECEIPT + few-shot). No chroma. |
| Write | `agent_edit_propose` writes **full** PROPOSE | replace | **Recant.** Merge schema; **prose stays** (`write-schema-draft-replace-prose` already Reject in parked toy). |
| `write_schema_draft` | pocket | rebuilds a template body | Merge into existing PROPOSE; don’t clobber her sentences. |
| Chat path | `draft_chat` / ICM bubbles | leftover | **Not first-sit.** Draft is paper, not a chat app. |
| OpenRouter | `aether_llm.py` + Desktop `.env` | QC | Keep desk-only. Never in APK. `qc skipped` if cap/down. |
| Tests | `tests/test_aether_llm_personal.py` | sft-v4 pick | Keep. |
| RAG pack (new) | `python/draft_rag_pack.py` | — | Verified this sitting. No network. |

## Draft grammar (this sit)

From `PEEK.md` + mum–son:

- Peek: **NOT ACTIVE** + short proposed vs live Next.
- Tap: writer. `PROPOSE-CURRENT.md` only.
- Generate is **his labour on the Draft dest**, not chassis GATE, not Yes, not GET, not empty tap.
- Chassis GATE stays **waiting / ETA** (PEEK). Do not light GATE as a Generate door on first sit.
- Markwon (elevation): highlight the paper; not a Compose host.
- She never has to run Ollama. He does.

## Chassis altercation (archived vs ritual)

| Parked sit-bound-draft | Live ritual / PEEK | Verdict |
|------------------------|--------------------|---------|
| Generate = **Gate hit on Draft dest** (`sit_gate` blit) | Chassis GATE = ETA only; JOIN/WAKE leftover-lab | **Split:** chassis GATE stays wait. Draft **dest** may keep a Generate plate as a **chore** (type prose first). Not a first-sit lamp peer of Yes. |
| Isolated Draft = chassis millwork + GATE | Isolated Draft = writer; millwork later | Keep dest as paper. Recycle `sit_gate` **on dest only** if Generate is in this Next. |
| CRT darkened-room | CRT leftover-lab | Keep leftover. Not the Draft reader. |
| Send = settings overlay | Send = bag, files only | Ritual Send wins. Generate does not live on Send. |
| Visual live-vs-proposed fields | Elevation: violet NOT ACTIVE vs graphite live | Keep. |

## RAG corpus (curated)

**Bound (evidence, not law):** that folder’s `CURRENT.md`, `PROPOSE-CURRENT.md`, `RECEIPT.md` head.  
**Few-shot (3–8):** `examples/propose-current/` — prefer ritual + sit-bound-draft + template.  
**Never:** `.env`, keys, this operator tree as the bound pocket, chroma, whole-tree 80k dump.

```bash
python3 python/draft_rag_pack.py /path/to/bound-folder
python3 -m unittest tests.test_draft_rag_pack
```

## OpenRouter QC (parked, still following until scaffold)

Desk only. `$10/week` in Desktop `.env` as `OPENROUTER_WEEKLY_BUDGET_USD=10` (human adds). First `sk-or-` wins in `aether_fs`. May revise **schema block** of PROPOSE only.

## Must-not

- Generate writes CURRENT
- Empty tap fires
- Key in APK / git
- Vector DB
- Dual-Next (`sit-bound-draft` beside `you-decide-ritual`)
- Chat bubbles as the Draft face
- Chassis GATE as Yes or as the only Generate

Confirm not tapped.
