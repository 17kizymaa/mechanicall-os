# Implement — 0.7.0-core

**Not CURRENT. Not Yes.** Opening is not Yes.

Engine: `walk_stage` / `_apply_walk` / slim `draft_chat` (schema + 3 user turns + file **names**). Gate still skips Ollama. `clear_chat` wipes `DRAFT-CHAT.jsonl`.

Face: Client room deleted. Bottom rail `01 Bind · 02 Plan · 03 Chat · 04 Decide · 05 Receipt`. Hamburger is PROJECT files only. Receipt shows EVENTS. Chat shows `Turn:` and Clear thread.

Tests: 25 pocket + 13 client OK. Assemble SUCCESS `versionName=0.7.0-core` `versionCode=6`.
