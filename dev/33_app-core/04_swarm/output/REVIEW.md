# REVIEW — 0.7.0-core (2026-08-20)

**Not CURRENT. Not Yes.** Eight read-only reviewers after the core walk. Sideloaded `0.7.0-core` versionCode **6** on RZCW2038KHN. Glass walked this turn (`chat.png`, `chat-cleared.png`, `plan.png`, `receipt.png`, `bind.png`, `project-menu.png`). Confirm not tapped.

Live Next remains `people-app-phone-seat` (human Status REJECTED). Model did not approve.

## Scores (0–5)

| # | Dimension | Was (0.6.0-icm) | Now |
|---|-----------|----------------:|----:|
| 0 | Definitive core (the walk) | — | **3** |
| 1 | Client gone / events on Receipt | useless room | **4** |
| 2 | Local model not overwhelmed | 5 | **4** |
| 3 | ICM walk on glass | 2 | **4** |
| 4 | Protocol law | 4 | **4** |
| 5 | Workspace bridge | 2 | **3** |
| 6 | Selected directory | 3 | **3** |
| 7 | Skeptic: sat product | 1 | **2** (tree); **live glass exists** |

**Four-claim estimate:** local-AI **3** + selected dir **3** + chat-app **3** + workspace **3** = **12 / 20**. Same ballpark as swarm2 tree. Better chrome. Not a sat product.

## Live glass (skeptic missed this)

Skeptic scored as if 0.7.0 was never sat. It was, this turn:

- Title **03 Chat**. Subtitle *Walk. Chat is not Yes.*
- `Turn: show-plan`. Composer + Send. Rail **01–05**.
- PROJECT: walk sentence + protocol files. **Client is not there.**
- Clear thread emptied leftover `note only` / `ping` / timeout bubbles.
- Plan shows CAPITALISED fields (pocket Status REJECTED).
- Receipt is trail + EVENTS, not a client dump.
- Bind is 01 on the default pocket. Picker still labeled support.

## What was pending, and what moved

| Pending | Move |
|---------|------|
| Client useless | **Gone as a room.** Events on Receipt. `clientRun` still in FaceBridge (leak). |
| ICM stages only in Python | **On glass:** numbered rail + Turn chip + Clear thread + Open Decide. Still jumpable tabs. |
| Leftover DRAFT-CHAT.jsonl | **Clear thread works** (proven). Not auto-wipe. |
| Workspace = CURRENT sermon | Desk sees **file names**, not contents. Hamburger is still the CURRENT family. |
| Bind hidden as Support | Bind is **01**. Still not a gate; default pocket still pre-bound. |

## One conclusion

The phone seat now **says** it is Bind → Plan → Chat → Decide → Receipt, and the A33 shows that sentence. Client is not a Sit destination. The local model still gets a slim pack. Decide is still the only Yes. That is a core **label**. It is not yet a one-way walk, not yet their folder, and not yet a desk that answers.

## Human gate

- [ ] Sit a real slim Send once myarch is up  
- [ ] Make Bind a gate (not a skippable tab)  
- [ ] Leave 0.7.0-core as lab; do not treat this file as Yes  

Silence is never permission.
