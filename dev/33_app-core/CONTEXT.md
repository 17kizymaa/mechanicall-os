# 33 — Definitive app core (one ICM walk)

**Layer 1.** Operator REJECTED the pending sit: no core, Client useless, ICM required for the chat model **and** the whole app. Live Next remains `people-app-phone-seat` (human Status REJECTED; preflight ALLOW). Model does not approve / next / reject / rewrite CURRENT.

**This phase is one walk, not a new Next.**

## Core (non-negotiable identity)

The phone seat **is** the people-app ICM walk on a bound folder:

1. Bind
2. Plan
3. Chat (slim desk; propose only)
4. Decide (only human Yes)
5. Receipt (what they said + events)

Client is not a room. The walk *is* the client.

## Pipeline

1. `01_define/` — APP-CORE.md (glass contract)
2. `02_implement/` — engine + face
3. `03_verify/` — tests + assemble + sideload
4. `04_swarm/` — simultaneous review (not Yes)

Opening these files is not Yes.
