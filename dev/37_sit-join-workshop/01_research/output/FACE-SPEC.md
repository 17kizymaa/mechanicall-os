# FACE-SPEC — sit-join-workshop

**Not CURRENT. Not Yes.** Implements grill S1–S13 + proposed action-id `sit-join-workshop`. Chrome is Compose. Identity is **paper**, not VST.

## Walk
Unbound: **FOLDER** only (B1).  
After bind: compact **DESK strip** beside DIR (JOIN status + WAKE). Not a fifth rail page. Not inside Draft.  
Then: **PLAN** (published) · **DRAFT** (Suggesting) · **DECIDE** (Publish) · **RECEIPT**.

Gray outside a module overlay still closes that module (existing FILES rule). Strip stays.

## DESK strip (after bind)
| Control | States | Not |
|---------|--------|-----|
| JOIN | not-on-net / invited / connected | Yes |
| well | paste invite or auth URL (operator minted out-of-band) | secret store |
| WAKE | sleeping / waking / up | Yes; disabled until connected |
| lamp | GATE idle/warm/gate/quiet | typing dots |

JOIN consumes the paste via **userspace** libtailscale/tsnet (no Tailscale app, no VpnService). Probe MagicDNS `wol-pi` and `myarch:11434`. USB `127.0.0.1` is operator lab only.

## PLAN — published document
- Whole bound `CURRENT.md`. Objective and Next first; rest of file scrollable.
- CAPITALISED fields are **anchors** (jump), not the only atoms.
- Read-only as law. Optional “edit in Draft” does not write CURRENT.
- Last-pass wells may remain as a shortcut that still `writeSchemaDraft` (PROPOSE), labelled not Yes.

## DRAFT — Suggesting on a proposal copy
- Left/margin: live text (published). Inline: suggested inserts (purple), suggested deletes (strike).
- One hunk in focus (heading or selected span). Accept / Reject that hunk → **PROPOSE-CURRENT.md** only.
- Accept all remaining = all hunks into PROPOSE, still not Yes.
- Composer is **span-scoped instruction**, not a bubble log. SEND = desk inserts suggestions for the **focused hunk**; may also propose the rest of the **schema** as additional suggestions (S11 Other). GATE strip shows thinking.
- Last desk say is one line under the hunk, not a chat history.

## DECIDE
- Unchanged: two-tap + Why. Confirm with empty Why refused.
- Copy: Publish the proposal. Opening Decide is not Yes.

## RECEIPT
- What they already published. Empty is honest.

## Visual
- Paper: cream/ink, suggestion purple, LCD yellow only on GATE/Next lamp.
- No painted 360×560 plugin, no traffic-light VST, no fake WinDots.
- `resizeableActivity` stays. OEM pop-up optional (B9).
- Camel Space / genuine VST chrome: **later settings**, not this FACE-SPEC.

## Never
- Hunk accept writes CURRENT
- Schema auto-complete writes CURRENT
- Dump whole CURRENT into the 7B
- JOIN/WAKE/SEND as Yes
- Invite URL in git or APK
