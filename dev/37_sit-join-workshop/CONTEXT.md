# 37 — sit-join-workshop

**Layer 1.** Live Next `sit-join-workshop` APPROVED (human 2026-08-20T18:58:47Z). This workspace does not rewrite CURRENT. Models never approve. Walkers never Confirm.

One sit: userspace Tailscale JOIN after bind, WAKE via wol-pi, document workshop (Plan = published CURRENT.md, Draft = Suggesting on PROPOSE, Decide publishes), hunk-scoped 7B+LoRA ICM.

Grill (law of the spike, not CURRENT): `decision-tree.md` S1–S13. S6 recanted in favour of this action-id.

## Pipeline

1. `01_research/` — Docs/Word/Overleaf/HackMD + Tailscale userspace + hunk prompt contract → FACE-SPEC
2. `02_implement/` — APK chrome + libtailscale userspace + wol-pi wake + workshop + ICM (after human reviews 01)
3. `03_verify/` — tests + STORM-on-emulator; never A33; never Confirm

## Halt

Do not auto-advance. Human reviews each `output/`.
