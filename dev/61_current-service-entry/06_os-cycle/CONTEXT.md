## Inputs
- Layer 1: ../CONTEXT.md
- Live law: ../../CURRENT.md (`you-decide-os-cycle`)
- Layer 4: ../04_verify/output/RECEIPT.md (apex door live; receive was Render `src/data`)
- Site: /home/anphuni/sites/anphuni.com-repo/
- Behaviour: ../../python/you_decide_os.py

## Process
You are the **os-cycle** stage for Next `you-decide-os-cycle`.

Preflight must ALLOW. Do not `aether approve` / `aether next`. Do not overwrite live CURRENT.md. Do not Generate. Do not restart killed local :8787. Public drop stays HTTPS on anphuni.com.

1. Durable store = mechanicall-os `inbox/tickets/<id>/` (gitignored blobs).
2. Operate the live door: mint → drop.
3. Pull the offer into this tree (`you_decide_os.py pull`).
4. Bounded operator work here. Zip on the same ticket. Download ≠ Yes.
5. Receipt in this `output/`. Following proposal NOT ACTIVE.

## Outputs
- RECEIPT.md -> output/
- stills -> output/
- summary.md -> output/
