# 49 — aether-mcp-projection

**Live Next:** `aether-mcp-projection` APPROVED. Preflight allowed.  
**Not:** EdubaWare clone, tools named `approve`/`reject`/`next`, default-on install, sit-ui CURRENT, Funnel, Confirm.

## Pipeline
1. `01_spike/` — stdio MCP (`current`, `probe`, `propose_write`). Halt on `output/`. Human opts in to a client config.
