## Inputs
- Layer 1: `../CONTEXT.md` · root `CURRENT.md` Next `aether-mcp-projection`
- Layer 3: `python/aether_client.py` CLIENT_DENY · ALPHA-LIMITATIONS · CORE_PRINCIPLES
- Layer 4: leftover-compose-delete done

## Process
You are the **spike** stage.

Stdlib Python stdio MCP. Bind-root is one folder. Tools: `current`, `probe`, `propose_write` (`.aether/proposals/` only). Refuse `approve`/`reject`/`next`. Tests in `tests/test_aether_mcp.py`. Do not add the server to a default client config.

## Outputs
- `python/aether_mcp.py`
- `tests/test_aether_mcp.py`
- `output/TOOLS.md` · `output/summary.md`
