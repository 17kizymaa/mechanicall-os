# RECEIPT — 02b native rack

**Not Decide. Not Yes.** Preflight ALLOW `sit-mvp-rack`. Human “Continue with next!” = Go on FRAMEWORK.md.

## Done

- Host recant: Compose `SeatNav`/`LcdViewer` no longer drives the activity. `MainActivity` hosts `SitRackView` (one `View`, one CBitmap, CRect hits, LCD clipped to STATUS glass).
- Packed `drawable-nodpi/{sit_plan,sit_bind,sit_draft,sit_splash}.png` 1080×2138 from `01_plates/output/`. `sit_skin.png` kept.
- CRects measured from **paint** (`plan.png` LCD `(72,176,504,452)` preview → pack `(135,302,945,774)`). SKIN-LAYOUT thin-strip lcd discarded.
- Bind plate: SAF on paper/LCD. Draft: transparent `EditText` in wells, `writeSchemaDraft`. Decide: two-tap paper + why. GATE SEQ pages law. Title bar 4-way = PLAN/DRAFT/DECIDE/RECEIPT.
- `app_verify` **10/10**. `tests/test_app_verify.py` OK. A33 `--serial` still refused (exit 3).
- Sideload debug `0.18.0-sitrack` vc 23 on `RZCW2038KHN` via mbp-edge USB. **Not LTE.**

## Look (USB)

`output/look-bind.png` — millwork is `bind.png`. Amber idle line **inside** the STATUS glass. No Compose SEQ pits on the paper. No `LAW · OBJECTIVE` on the bezel.

NeuralBridge tree (forward 7474): one interactive node `Choose your folder` `[0,80,1080,2281]`. Total 6. No ChatHome essays.

## Not this stage

- tsnet AAR / AuthKey inject / zip carry (`03_mesh`)
- WAKE→wol-pi deploy (`04_doorbell`)
- CURRENT.md host-sentence apply (proposal `.aether/proposals/CURRENT-proposal-20260825-2114.md` — human)
- Git last is human
- Compose leftover files (`SeatNav.kt`, `LcdViewer.kt`, modules) still in the tree but unused by `MainActivity`

## Halt

Look is on the phone. Mesh is later. Silence is not mesh Go.
