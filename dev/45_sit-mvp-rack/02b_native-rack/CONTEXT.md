# 02b — native rack (SitRackView)

**Not mesh. Not doorbell. Not Yes.** Recant of `02_freeze` *host*: Compose overlay destroyed the imagined millwork. M2=1 and M7=1 stand (plates stay; Imagine is paint). The host is wrong.

## Inputs
- `../01_plates/output/{plan,draft,bind,splash,rack}.png`
- `../01_plates/output/{SKIN-LAYOUT.md,ZOOM.md}`
- `../02_freeze/output/{RECEIPT.md,NEURALBRIDGE.md}`
- USB look: `../02_freeze/output/look-usb-sit.png` (Compose SEQ pits on OBJECTIVE; LAW text on the bezel)
- Layer 1: `../CONTEXT.md`
- FACE-SPEC v2: `dev/38_sit-vst-headscale/01_research/output/FACE-SPEC.md` (no painted letterbox; `resizeableActivity`)
- BINARY-PIPELINE: `dev/39_sit-storm-factory/15_binary-skin/output/BINARY-PIPELINE.md` (CBitmap + CRect; steal pipeline not Camel pixels)

## Process
You are the implementer of **SitRackView**, not a Compose restyle.

1. Human reviews `output/FRAMEWORK.md`. **Halt. No assemble until Go.**
2. After Go: native `View` host. One `CBitmap`. Hits are integer CRects mapped through the **same** `Matrix` as the blit. LCD text is clipped to the glass. Draft fields are transparent `EditText`s in wells. Decide is a CRect two-tap. No `ChatHome` / `DecideModule` / `PluginDialogFrame` on the paper.
3. Look via `scripts/sit-look.sh` (mbp-edge USB `screencap`) and NeuralBridge `android_screenshot` once companion 0.1.1-capture is on the A33. Diff millwork vs plate PNG with LCD masked. USB is not LTE.

## Outputs
- `output/FRAMEWORK.md` (this stage's contract)
- After Go: SitRackView sources + look PNGs + RECEIPT.md

Never Confirm. Never Funnel. Never Camel bitmaps. Never Compose millwork-as-SOS.
