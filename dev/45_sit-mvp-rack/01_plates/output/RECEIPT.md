# RECEIPT — 01_plates (halt)

**Not Decide. Not assemble.** Preflight ALLOW `sit-mvp-rack`. Confirm not tapped. Funnel not up. No Camel bitmaps extracted.

Human Continue after CURRENT body for `sit-mvp-rack` APPROVED (reason on disk is crude; event exists). Stage 1 only.

## Produced

- `rack.png` `plan.png` `draft.png` `bind.png` `splash.png` — Imagine millwork, 576×1248, original plates (cream / gold LCD well / purple gate pits). Derived from one canonical via image_edit.
- `SKIN-LAYOUT.md` — pack CRects 1080×2138. Hits for JOIN / SEND / WAKE / banks / fields / TBC / GATE.
- This receipt.

## Not done (correct)

- No `assembleDebug`
- No `drawable-nodpi/sit_skin.png`
- No tsnet AAR
- No wol-pi deploy
- No AuthKey inject
- No BEHAVIOURS rewrite
- No CURRENT.md cleanup (live file still has proposal preamble + fenced body — **human** should paste the inner CURRENT; models do not overwrite)

## Correction (same stage)

Human: do **not** add a second terminal. In-frame zoom uses the **existing STATUS LCD**. `zoom-bezel.png` is a rejected fork. Contract: `ZOOM.md`. Previews: `lcd-idle.png` `lcd-objective.png` `lcd-next.png` `lcd-keep.png` (bytes blitted into the same glass via `scripts/composite_lcd.py`).

## Halt

Review `ZOOM.md` + LCD composites. Go = freeze stage (`02_freeze/`). Silence is not Go.
