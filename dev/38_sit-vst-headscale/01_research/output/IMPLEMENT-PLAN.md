# IMPLEMENT-PLAN — 02_implement (do not run until human says proceed to 02)

**Not CURRENT. Not Yes.** Ordered so the sit can JOIN a Headscale net, show LOAD, and still be paper inside a rack.

## Leftover from 37 (must fold in)

1. **gomobile tsnet JNI** — JOIN `connected` is a lie until this ships.
2. wol-pi wake script already exists; Headscale process is ops.
3. Hunk ICM already in `aether_pocket.py` — keep; do not dump whole CURRENT.

## Work packages (one sit, not four Nexts)

| # | Package | Files (likely) | Done when |
|---|---------|----------------|-----------|
| A | Rack chrome | `SeatTheme.kt` PluginWindow/Title/LCD/GateStrip | FACE-SPEC v2; B9 still true |
| B | GATE two-phase | `write_gate` + `GateStrip` + `draft_chat` LOAD before stream | `load` then `gate`; no `…` |
| C | Queue + nice | pocket queue JSON; document myarch `Nice=` | extra Send queues; never Yes |
| D | Headscale JOIN | Go tsnet wrapper + JNI + `join_accept` | connected = `Up`; fingerprint only |
| E | Read-twin GET | tsnet Listen GET-only; operator curl recipe | no POST Yes |
| F | visual-reviewer skill | `.grok/skills/visual-reviewer/SKILL.md` | file exists; unused until 03 |
| G | Tests | `tests/test_aether_pocket.py` + `app_verify.py` B7 | load state; dump-file still refuse |

## Suggested 02 order

1. A + B + tests (visible without Headscale running).
2. D (userspace) — largest; may land as a stub that *refuses* `connected` until JNI is linked (honest).
3. C queue.
4. E twin GET (needs D).
5. F skill file.
6. Halt for 03 STORM. Do not sideload to A33. Do not Confirm.

## Out of 02

- Standing up Headscale on wol-pi (ops receipt OK, no secrets).
- `SHARING.md` at repo root (04).
- Go-git / GitHub PRs (human **Go-git**; 00_git-pr-brief still empty).
- Bind-must-have-CURRENT.md (T8c parked).
- OIDC, Funnel, VpnService, Imagine-as-spec.

## Prompt contract

Keep `dev/37_sit-join-workshop/01_research/output/PROMPT-CONTRACT.md`. 02 does not reopen hunk ICM unless LOAD/queue forces a call-site change.
