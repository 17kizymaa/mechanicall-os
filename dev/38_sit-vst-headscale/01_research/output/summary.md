# summary — 01_research sit-vst-headscale

Live Next `sit-vst-headscale` APPROVED. Preflight ALLOW. Models did not approve. Walkers did not Confirm.

## Wrote

| File | Role |
|------|------|
| `VENDOR-NOTES.md` | Camel Space = trance **gate** rack; Headscale preauth; tsnet ControlURL; Ollama LOAD |
| `HEADSCALE-LAYOUT.md` | login-server must be public HTTPS before JOIN; JNI owns tsnet; key never stored |
| `GATE-CONTRACT.md` | quiet/idle/queued/load/STREAM(`gate`); 60s LOAD; one niced generate |
| `FACE-SPEC.md` | v2: rack around paper; LCD = Next+GATE; no letterbox |
| `TWIN-PROTOCOL.md` | GET-only watch; not `aether_pocket_serve` Yes face; draft of 04 `SHARING.md` |
| `VISUAL-REVIEWER.md` | T7 rubric for 03; install skill in 02 |
| `IMPLEMENT-PLAN.md` | 02 packages A–G |

## Honest gap (still)

libtailscale/tsnet JNI is **not** in the APK. 02 must ship it or keep JOIN at `invited` instead of lying with LAN Ollama.

## Halt

Human reviews `dev/38_sit-vst-headscale/01_research/output/`. Edit the spec in place if the rack/GATE split is wrong.

Say **proceed to 02** to implement. Do not start 03 STORM from here. Do not Go-git unless you say so. `00_git-pr-brief/` is still empty on purpose (S13 last).
