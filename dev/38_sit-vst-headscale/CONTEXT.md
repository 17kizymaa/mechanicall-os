# 38 — sit-vst-headscale

**Layer 1.** Live header **Next:** `sit-vst-headscale` · Phase APPROVE · Status APPROVED · preflight ALLOW (2026-08-20). Human `aether next` + `approve` already happened. Models never `approve` / `reject` / `next`. Walkers never Confirm. Commit ≠ Yes. **Go-git** is still a separate human line (S13).

One sit (from `examples/propose-current/CURRENT-sit-vst-headscale.md` + `decision-tree.md` T1–T8b): genuine VST/Camel Space **frame** around the paper workshop; Headscale + preauth JOIN (userspace); GATE **LOAD then STREAM**; one niced 7B generate; STORM iterate storm-0 with `/visual-reviewer`; operator **read-twin**. Fold leftover sit-join: tsnet JNI, wol-pi wake, hunk ICM.

**Hygiene (this tree, 2026-08-20):** live `CURRENT.md` header **Next** and body **Action id** both `sit-vst-headscale`. Phase APPROVE · Status APPROVED · Approval APPROVED (human `aether next` + `approve`). Agents do **not** rewrite CURRENT.

## Pipeline

0. `00_git-pr-brief/` — git-tree analysis + executor brief for 3–4 PRs. Halt.
1. `01_research/` — genuine VST/Camel Space refs + Headscale login-server layout + FACE-SPEC v2 (frame + paper) + GATE LOAD/STREAM contract. Human reviews `output/`.
2. `02_implement/` — frame chrome; Headscale+preauth JOIN; two-phase GATE; niced queue; read-twin; leftover tsnet userspace.
3. `03_storm/` — storm-0 iterate; `/app-reviewer` + `/visual-reviewer`; never Confirm; never A33; round cap 3.
4. `04_sharing/` — `SHARING.md` twin protocol (read-only). Git-in-pocket not shipped.

## Halt

Human **REJECTED** the per-stage halt (2026-08-20) and asked remaining folders to run as **one Next in one turn**. 02+03+04 (+00 brief) executed that way. Models still never `approve` / `next`. Do not `git commit` until the human says **Go-git**. Live CURRENT header may read REJECTED; Next remains `sit-vst-headscale`. Agents do not un-reject.
