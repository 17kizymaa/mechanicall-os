# EXECUTOR-BRIEF — sit-vst-headscale git (not Go-git yet)

**Not Yes.** Print-only groups live in `behaviours/print-pr-groups.py`. Do not `git add` / `commit` / `gh pr create` until the human says **Go-git**. Never commit `CURRENT.md`, `.aether/events.jsonl`, secrets, `android/app/build`, `local.properties`, `*.apk`.

Live header may read REJECTED; **Next** is still `sit-vst-headscale`. Do not `aether approve`.

## Slice (3–4 PRs, this repo’s small-PR habit)

1. **PR1 engine+tests** — `aether_pocket.py` (LOAD/queue/honest JOIN), `aether_twin.py`, tests, `app_verify` B7, visual-reviewer skill, wake/nice scripts.
2. **PR2 android lab (source only)** — `SeatTheme`/`SeatNav`/`FaceBridge`, `android/tsnet/README.md`, version `0.15.0-vst`. Rely on `android/.gitignore` for `app/build`.
3. **PR3 factory** — `dev/37_*`, `dev/38_*`, `decision-tree.md`, propose-current examples. Not live CURRENT.
4. **PR4 STORM+SHARING** — `03_storm/output`, `04_sharing/output`, repo `SHARING.md`.

Optional later: PARK archive of `dev/23`–`36`.

## Between PRs

Read-only inbox: `/sdcard/mechanicall/` or `~/mechanicall-twin/<sitter>/`. Do **not** bind mechanicall-os. Twin is GET (`python/aether_twin.py`).

## Executor must not

- Rewrite CURRENT
- Tap Confirm
- Share RZCW2038KHN
- Bake a preauth key
- Claim tsnet JNI is in the APK (`scripts/build-tsnet-aar.sh` exits 2)
