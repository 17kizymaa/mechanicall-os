# PR-MANIFEST

Run `python3 dev/38_sit-vst-headscale/00_git-pr-brief/behaviours/print-pr-groups.py` for the live file list. Groups: PR1 engine+tests, PR2 android source, PR3 factory, PR4 STORM+SHARING, PARK optional, HUMAN-ONLY never model-commit.

`print-pr-groups.py` exit 0 this sit (no never-commit path listed inside a PR group).
