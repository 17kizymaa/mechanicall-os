# Why dest-preview was not beautiful

**Not CURRENT. Not Yes.** Evidence from the 2026-09-05 sitting that shipped lab APK `0.19.4-dest-preview` vc 29. Live law after that sitting: Next `sit-dest-preview` **REJECTED**.

## What “beautiful” meant on disk

CURRENT Keep (still the body of live law) asked for: truncated STATUS peek; CRT dest as the reader (caption + body, phosphor kept); Draft dest = chassis millwork + GATE plate; Send+FILES one dest; title dim; Decide well millwork fill; rack GATE = ETA.

That is a **composition** halt. The sitting treated it as a **string + CRect** halt.

## Six causes (ranked)

### 1. The halt was law-shaped

Grill C-series ran ~19 rounds in the same conversation as implement (store vs bound-folder, dest doors, GATE, phosphor, printer, CRects). Human rejects in `.aether/events.jsonl`:

- 15:04Z — next Next will be `first-sit-patching`, merge with what Grok executed
- 15:10Z — merge `sit-bound-draft` too, **want a new name altogether**

Tokens went to Next hygiene (wrong file pasted as CURRENT, validate FAIL, approve, reject-to-rename) instead of dest millwork around the tube. FACE-SPEC plates already existed; the loop optimized **gate files**.

### 2. 15/15 rewards strings, not composition

`python/app_verify.py` **15/15** on this tree (2026-09-05 this session). It never opened a PNG.

Smoking guns in the scorer:

| Probe | What it rewards | What CURRENT Keep wanted |
|-------|-----------------|--------------------------|
| B4 | `"Field Objective"` and `"Field Next"` in `SitRackView.kt` | Draft dest millwork + GATE, recant ALPHA/BETA/GAMMA chrome |
| B11 | `"IsolatedDark" in rack` | Darkened-room **composed** `sit_crt`, not IsolatedDark debug as Need |
| B12 | `SitHit.Files` as a **different** hit; `"FILES stays other hit"` | Send **and** FILES open **one** dest |
| B13 | `IsolatedMode.DRAFT` + `"live vs proposed"` substring | Chassis millwork + `sit_gate` blit visible |

`SitRackView.drawDraftPeek` still paints the words `Field Objective · Field Next` into the well so B4 stays green. Recanting that dump **fails 15/15**. The scorer fights the look.

### 3. Skills/scripts score a deleted sit

Leftover Compose is **gone from disk** (`SeatNav.kt` / `SeatTheme.kt` / `LcdViewer.kt` absent). Contracts still name them:

- `.grok/skills/visual-reviewer/SKILL.md` Inputs: `SeatTheme.kt`, `SeatNav.kt`
- `dev/38_sit-vst-headscale/01_research/output/VISUAL-REVIEWER.md` same
- `.grok/workflows/actual-app-verification.rhai` reviewer prompt: “read SeatNav.kt, PlanModule.kt, ChatHome.kt…”
- FACE-SPEC v2 **V4**: LCD = Next + GATE only — dest millwork + 120-char peek is a different instrument

`scripts/sit-look.sh` wakes and captures **whatever is on the panel**. It does not launch the sit. Evidence: `dev/55_sit-dest-preview/01_look/output/look-after-send.png` is the A33 **launcher** (Notes / Brave / street wallpaper), timestamp 16:46, not millwork.

`scripts/storm-walk.sh` force-stops, starts MainActivity, dumps **one** `home` still. No dest walk.

`dev/52_sit-glass-polish/03_sdk-stills/walk_sdk_stills.py` still taps `draft_alpha` / `draft_beta`. Misses ALLOW, IME, DocumentsUI.

### 4. Imagine was used as a shortcut, then thrown away

CURRENT: Imagine = paint, human PNG before freeze. Dest-preview generated wrong-size plates, then Pillow-desaturated the title CRect and flood-filled Decide well from Plan cream. That is **CRect automation**, not a farm-paint pass. Phosphor was almost recanted as a bug; operator parked that (`CURRENT-proposal-20260905-park-visual-upgrades.md`). Colour change is a later visual-upgrade Next.

### 5. No look-backward factory

STORM + visual-reviewer exist and were **not** the execute loop. Execute was: Kotlin blit → 15/15 → Tailscale TCP ~40 KiB/s for 27 min → home-screen PNG. Missing loop: bind pocket → still every dest → score V-rubric / human FLAG → **one** millwork edit → reassemble until FLAG clears.

### 6. Send-path ate the look budget

`RECEIPT.md` in dest-preview: myarch `192.168.0.51/24` and mbp-edge `192.168.1.88/24` do not route. Only Tailscale. USB last hop is fast. Lab logistics were billed as “look.”

## What actually shipped (lab, not beauty)

- Truncate peek (`PREVIEW_CHARS = 120`); CRT dest caption+body; phosphor `#E6C14A` kept
- Draft dest: no IsolatedDark early-return (CRT dest still `canvas.drawColor(IsolatedDark)` then `sit_crt`)
- Send overlay cream panel + folder listing text (not millwork dest plate)
- Title dim + Decide well fill via Pillow on pack CRects
- APK archived; A33 install Success; Confirm not tapped
- Look PNG = launcher

SDK stills that **do** show dests (`dev/52_sit-glass-polish/03_sdk-stills/`) are the **previous** IsolatedDark era: CRT tube floating on black with `LAW  KEEP`; Draft dest = STATUS tube + ALPHA/BETA/GAMMA keys on black. Those stills are the look the human already rejected (2026-09-04 IsolatedDark). Dest-preview did not recapture dest stills on SDK or A33.

## Inference (not law)

A sitting that must both **rename Next** and **paint dests** will spend the budget on the rename. A scorer that greps `"IsolatedDark"` will keep IsolatedDark. A look script that does not launch the app will score the launcher. None of that is a model-taste failure. It is a factory that closes on the wrong artifact.
