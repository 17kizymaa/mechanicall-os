# Score gap — law probes vs look

**Not CURRENT.** Two scorers are required. Today only one exists, and it is pointed at leftover identities.

## `app_verify.py` (keep as law)

Deterministic source greps + optional uidump blob. Refuse serial `RZCW2038KHN`. 15/15 is **not** Yes.

Keep for: bind-first, propose-not-current, Decide-only Yes, operator-tree refuse, leftover Compose **gone**, Confirm never tapped.

Do **not** extend it to millwork beauty. Every time a look recant needed a string put back (B4 `Field Objective`), the law scorer ate the face.

### Probes that fight dest Keep

```
b4  = "Field Objective" in rack and "Field Next" in rack
b11 = "IsolatedMode.CRT" in nav and "IsolatedDark" in rack
b12 = ... "SitHit.Files" in nav   # evidence text: FILES stays other hit
b13 = "IsolatedMode.DRAFT" in nav and "live vs proposed" in rack
```

B11 **requires** IsolatedDark. CURRENT B11 Need is darkened-room composed tube, and Reject includes `isolated-dark-debug-as-product`. The mechanical Need and the visual Reject are the same token.

B12 mechanical Need is the **old** Send overlay vs FILES split. CURRENT B12 Need is one dest.

`leftover` join of SeatNav/SeatTheme/LcdViewer/… is `""` because those files are gone — B2/B9 PASS for absence, which is correct. Skills did not get the same recant.

## visual-reviewer (stale)

FACE-SPEC v2 rubric V1–V7. Inputs name **deleted** Compose. V4 (LCD = Next + GATE only) contradicts dest millwork peek (~120 chars of plan keys on chassis, full pages on CRT dest). V3 “paper score inside rack” is still the Draft dest goal, but the skill will not find `SitRackView.kt`.

FAIL-closed V2/V5/V3 can stay. Dest millwork needs extra ids (proposed D-row below), not a silent V4 recant in the old file.

## Walkers

| Script | What it captures | Beauty? |
|--------|------------------|---------|
| `scripts/sit-look.sh` | Wake + screencap via mbp-edge USB | Whatever is on the panel. Launcher is a “valid PNG.” |
| `scripts/sit-wake.sh` | Panel on | Does not launch PKG |
| `scripts/storm-walk.sh` | One `home` after `am start` | No dest, no bind, no FLAG |
| `walk_sdk_stills.py` | CRect tour including ALPHA/BETA | Misses ALLOW/IME/DocumentsUI; still IsolatedDark-era hits |
| `scripts/sit-send-smoke.sh` | B14 folder smoke | USB look optional; not millwork |

## Proposed second scorer (`look_verify.py`) — spec only this stage

Fail closed on missing PNG. Never tap Confirm. Refuse A33 serial in the **walker**; USB look is a later FLAG sitting, not this scorer’s default.

| id | Need | FAIL if |
|----|------|---------|
| L0 frame | PNG is the sit (chassis grain or dest millwork), not OEM launcher | Home widgets, wallpaper, Recents, lock |
| L1 peek | Chassis STATUS is truncated; no LcdPrev/LcdNext chrome on preview | Full file on glass; preview paging |
| L2 crt-dest | Tap glass → `sit_crt` millwork; caption + body; phosphor hue kept | IsolatedDark slab with no tube; `LAW ` dump; green terminal recant |
| L3 draft-dest | Chassis millwork + GATE plate visible | IsolatedDark early-return; ALPHA/BETA/GAMMA as the face |
| L4 folder-dest | Send and FILES land the same dest; OEM FILES is a **button** | Send is Yes; CRT-as-send; OEM manager dropped |
| L5 title | MECHANICALL plaque present, dimmed, not a control | Title missing; title opens a dest |
| L6 decide-well | `sit_well_decide` fills the well | Tiny plaque in empty alpha |
| L7 gate | Chassis GATE is ETA millwork | Gate opens CRT dest |
| L8 receipt | Empty is empty; not a CRT page | Receipt in `LawPages` CRT order as the face |

Mechanical hints (not the whole score): uidump package = sit; pixel mean of letterbox vs millwork; OCR/grep of `ALPHA`/`LAW ` on dest stills as FLAG; filename set must include `dest-crt` `dest-draft` `dest-folder` `rack-plan` `rack-decide`.

Human FLAG recants a green L-row. PASS is not Yes.

## Workflow gap

`.grok/workflows/actual-app-verification.rhai` fans out B1–B10 only (not B11–B15), prompts reviewers to read SeatNav. `.grok/workflows/product-review.rhai` is product-identity, not dest stills. There is no dest-stills workflow.
