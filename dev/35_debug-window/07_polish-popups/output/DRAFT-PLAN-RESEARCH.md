# DRAFT–PLAN research (separate swarm)

**Not Yes.** Live plan = CURRENT fields. Draft = PROPOSE-CURRENT.md. Chat never writes CURRENT.

## Hole

`draft_chat` already writes PROPOSE on propose. Plan showed only live CURRENT. ChatHome never called `readSchemaDraft`. Model `parsed_stage == "propose"` could write PROPOSE on a question.

## Contract (applied face-primary + one engine clamp)

1. See plan — live six fields.
2. Ask a change — local `chat_stage`; GATE strip thinks.
3. See proposed — chips + Plan rail from `read_schema_draft`; CURRENT bytes unchanged.
4. Open Plan / Decide — Decide is the only Yes.

Engine: write PROPOSE only if **local** stage is `propose`. Return `fields` + `schema_draft`. Prompt stays slim.

Not: chatbot identity, dump whole CURRENT into the prompt, model auto-approve.
