# RECEIPT — 0.12.0-polish

**Not Decide.** Confirm not tapped. Operator CURRENT not rewritten. Human REJECTED then Go. Preflight ALLOW on Next `mechanicall-debug-window`.

## Swarm first (parallel)

- UI: 4 explore reviewers → `UI-SWARM.md`
- Draft↔plan: 2 explore reviewers → `DRAFT-PLAN-RESEARCH.md`

## Named face fixes

| Ask | Did |
|-----|-----|
| AGAIN timeout + 2s crossfade | First tap arms; AGAIN/purple fades to YES/NOT YET over 2000ms; still armed until fade ends; timeout never Confirms. Why freezes the fade. |
| FILES CLOSE → RETURN | File viewer **RETURN** restores the list. List has no CLOSE (gray leaves). |
| Gray space closes the module | Overlay 86×70; `dismissOnClickOutside`; storm-0 tap bottom gray → overlay gone, PLAN back. |

## Polish from swarm (P0 slice)

Plan: Objective + Next first; proposed rail **DRAFT · not Yes**. Draft: field chips; Send is Send; no in-page Decide. Bind pads short. LCD is Next/Objective. Why is a plugin frame. Engine writes PROPOSE only on local `propose` stage (32 tests OK).

APK `0.12.0-polish` v13 on storm-0 and A33. Opening is not Yes.
