## Inputs
- Layer 0: live CURRENT `mechanicall-debug-window` (Phase SELECT, Status REJECTED, preflight ALLOW). Human said REJECTED then Go.
- Layer 4: `../06_bind-fit/output/{RECEIPT.md,WINDOW.md}` and live Kotlin face
- Layer 3: FACE-SPEC, `python/aether_pocket.py` draft_chat, PlanModule, ChatHome, DecideModule, PluginDialogFrame

## Process
You are fitting the rejected chrome under the same Next. Do not rewrite operator CURRENT. Do not tap Confirm. Do not `aether approve`.

Parallel (required):
1. UI definition + polish swarm (multiple reviewers). Write findings to output/.
2. Separate swarm: Draft must interact with the actual PLAN (fields / propose), not a chatbot. Research only until findings land.

Then implement (human named these):
- Decide AGAIN toggle: timeout + crossfade over 2 seconds.
- File viewer: CLOSE becomes RETURN to the FILES list.
- Tap the gray outside a pop-up module: close the module completely.

## Outputs
- UI-SWARM.md -> output/
- DRAFT-PLAN-RESEARCH.md -> output/
- RECEIPT.md -> output/
