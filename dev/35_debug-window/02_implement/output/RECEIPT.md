# RECEIPT — mechanicall-debug-window 0.9.0-window

**Not Decide.** Confirm was not tapped. CURRENT Next unchanged by this agent.

## Desk-hop

- `scripts/boot-desk.sh` — myarch Ollama idle, `personal-llm-sft-v4:latest` listed, private URLs printed.
- Edge **refuses ssh -R**. Hop is `scripts/desk-hop-proxy.py` on mbp-edge → `100.90.85.68:11434`, then `adb reverse tcp:11434`.
- Live Send: **Draft updated. Open Plan to read it.** GATE went **QUIET** after. `turn propose`.

## Window

- Compact plugin letterboxed on a dark host (does not fill the display).
- `resizeableActivity=true`.
- Yellow LCD, purple GATE 16-step, DRAFT/PLAN/DECIDE/RECEIPT, DIR slot.
- Simplified launcher: slab + disc PNG.

## STORM

- sdkmanager install **exit 0**: emulator + `system-images;android-34;google_apis;x86_64`.
- KVM usable.
- AVD `storm-0` created under `/tmp/storm-env/storm-0/avd`.
- Pocket seed copied. `scripts/storm-walk.sh` refuses RZCW2038KHN.
- Emulator **not booted this turn** (image install + AVD create done; first boot still pending).

APK: `versionName=0.9.0-window` `versionCode=8` on RZCW2038KHN.
