# 35 — mechanicall-debug-window (propose)

**Layer 1.** Operator asked for **one** Next covering desk-hop + STORM install + actual windowed Camel Space face. Live CURRENT is still `people-app-phone-seat` REJECTED until a **human** applies the proposal.

Model does not approve / next / rewrite CURRENT.

## Pipeline

1. `01_propose/` — FACE-SPEC + mark + proposal pointers  
2. `02_implement/` — **halt** until human applies CURRENT  

Opening these files is not Decide.
