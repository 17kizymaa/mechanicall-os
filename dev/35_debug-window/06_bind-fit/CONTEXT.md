## Inputs
- Layer 0: live CURRENT `mechanicall-debug-window` APPROVED (preflight ALLOW)
- Layer 4: operator answers — keep GATE; bind-first only module (one project); first-person demo CURRENT; sideload A33; fit glass; revert download-unstable window chrome
- Layer 3: `../01_propose/output/FACE-SPEC.md` (letterbox was a stand-in)
- Layer 4: `../05_client-face/output/RECEIPT.md`

## Process
You are fitting the sit under one Next.

1. GATE stays the debug instrument.
2. Unbound: BIND is the only module. Bound: one pocket path; DIR replaces, it does not stack.
3. Rewrite `examples/pocket-demo-client/CURRENT.md` to first person. Do not rewrite operator `CURRENT.md`.
4. Revert painted letterbox / fake traffic-light dots. Keep `resizeableActivity=true`. Fill the activity so taps and IME work. Do not add `<layout defaultWidth>` (that is what breaks outside Samsung freeform).
5. Sideload to RZCW2038KHN via edge. Never tap Confirm. Never `aether approve` / `next`.

## Outputs
- WINDOW.md -> output/ (is true windowed mode unstable outside A33?)
- RECEIPT.md -> output/
