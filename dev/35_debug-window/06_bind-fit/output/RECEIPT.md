# RECEIPT — 0.11.2-plan bind-first + fitted sit

**Not Decide.** Confirm not tapped. Operator `CURRENT.md` not rewritten. Next remains `mechanicall-debug-window`.

## Human answers applied

| Q | Did |
|---|-----|
| Keep GATE | Yes. Instrument on bind and on the four pages. |
| Bind-first, one project | Unbound: BIND is the only module (no PLAN/DRAFT/DECIDE/RECEIPT tabs). Bound: one path in `pocket_one`; DIR replaces. Engine `bind_folder` names/creates that folder. |
| First-person demo CURRENT | `examples/pocket-demo-client/CURRENT.md` — “Plan one afternoon of work I can say Yes to.” Operator CURRENT untouched. |
| Sideload A33 | `RZCW2038KHN` `versionName=0.11.2-plan` `versionCode=12`. Opening is not Yes. |

## Window (download / outside A33)

True OS freeform is **not** what made sideload fail. `resizeableActivity=true` is stable. The painted 360×560 letterbox + traffic-light dots was a fake window (IME ate Send). Reverted. Activity fills the sit. No `<layout defaultWidth>`. Detail: `WINDOW.md`.

Samsung pop-up on the A33 remains a human gesture. storm-0 is full-screen because the AVD is.

## Proof (never Confirm)

- storm-0 unbound: `bind.png` / `uidump-bind.xml` — FOLDER, GATE, no PLAN tabs.
- storm-0 after Bind `/sdcard/mechanicall-pocket`: `plan.png` — PLAN, first-person OBJECTIVE, DIR/FILES. Bind is not Yes.
- A33: `a33-bind.png` — same bind-first sit, GATE IDLE, DESK lamp.

## Not done by this agent

- Did not tap Yes / Confirm.
- Did not `aether approve` / `reject` / `next`.
- Did not rewrite operator CURRENT.
