# REVIEW — ICM chat (0.6.0-icm)

**Not CURRENT. Not Yes.** Eight read-only reviewers after the chat ICM fix. Sideloaded `0.6.0-icm` versionCode **5**.

| Dimension | Score | Note |
|-----------|------:|------|
| Local model not overwhelmed | **5** | Prompt is schema + last 3 user turns. Gate never calls the desk. |
| ICM chat stages | **2** | Engine classifies show-plan / propose / gate. Glass still six Sit rooms. |
| Chat-app chrome | **4** | Thread + composer, no desk URL field, imePadding. Not an inbox. |
| Protocol law | **4** | Chat is not Yes. “Use Decide.” CURRENT untouched. |
| Selected directory | **3** | Default pocket; picker still Support. |
| Workspace bridge | **2** | Desk sees schema, not the work tree. |
| Distribution honesty | **4** | Lab sideload, not Play Store. |
| Skeptic: skip live sit | **1** | Refute fails *before* this sideload; 0.6.0-icm is now on the A33. |

## What changed

Chat no longer stuffs full CURRENT + PROPOSE + the whole jsonl into `personal-llm-sft-v4`. Decision framework is local (`chat_stage`). Bubbles are short. Composer is Message + Send only.

Glass (`chat-icm.png`): *Opening is not a yes. Chat is not Yes. Desk sees a short plan, not the whole file.* Desk URL gone from the thread. Pocket still has leftover `note only` / `ping` / timeout bubbles (old jsonl).

24 pocket tests OK. Confirm not tapped.
