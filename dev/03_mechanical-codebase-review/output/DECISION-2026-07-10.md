# Stage 03 — Gate G2 decision record (2026-07-10)

Human decision, sprint 0710 Gate Hour, recorded by CC-6. Direction accepted as designed
in `mechanical-design.md`; three calls resolved:

1. **Level 1: SHIPPED.** The one-command mechanical review (gather → Grok Heavy API →
   persist review as FS artifact) is accepted as the stage 03 deliverable as-is.
2. **Level 2: APPROVED.** Tool-calling autonomy proceeds — native xAI API tool calling
   with a local Python tool loop (`read_file`, `grep_codebase`, `list_dir`), per the
   design's Level 2 section. Work stubbed on branch `sprint/0710-level2-stub`.
3. **MCP: MINIMAL-ONLY, skip for now.** Confirms the design's own decision: no MCP server
   for v1; Level 2 uses native tool calling. A minimal stdio `mcp_codebase_server.py`
   remains a future option only if it stays stdlib-Python, sidecar-logged, and cat-able.

Stage 03 status: **complete** (Level 1 shipped). Level 2 is new work under its own branch,
not a reopening of this stage.
