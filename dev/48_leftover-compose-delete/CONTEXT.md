# 48 — leftover-compose-delete

**Live Next:** `leftover-compose-delete` APPROVED. Preflight allowed.  
**Not:** assemble overlay-Compose, Funnel, MCP, sit-ui CURRENT, dirty-tree-as-PR, Confirm, `aether approve`/`next`.

## Pipeline
1. `01_delete/` — remove leftover Compose millwork; gradle without Compose; `app_verify` scores SitRackView only. Halt. No sideload unless human Go.

## Keep
SitRackView + FaceBridge + LawPages + SitCRects + OpenBoundFolder. Native host already on A33.
