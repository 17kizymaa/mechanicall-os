# 47 — Literary PR eval (research, not Yes)

**Not CURRENT. Not Yes. Not a recant.**  
**Live operator file:** root `CURRENT.md` — human 2026-08-28 ordered `take-forward` onto this file. `aether current validate` OK. Next pin `take-forward`. Models do not `aether approve` / `next`.

This factory is **evaluative research**: ingest the phone literary “PR”, map it onto mechanicall-os history, score what the metaphors want to reshape, and name what to take forward in the two active pockets. It does **not** implement SitRackView, assemble sit, merge PRs, or rewrite law.

## Two pockets (do not dual-Next)

| Pocket | Path | Claimed Next | Role |
|--------|------|--------------|------|
| Operator Domain | this repo | `sit-mvp-rack` (pin missing) | Protocol + APK factory |
| Sit UI sprint | `/home/anphuni/sit-ui-sprint` | `discuss-sit-ui` | Face law + plates; APK sources stay here |

Dual-concurrent-next remains Reject. This factory **reads** both; it does not give either a second Next.

## Pipeline

1. `01_ingest/` — pull phone session + Jake MCP captions + pin sources. **Done.**
2. `02_eval/` — evaluative reading + `PR-RUBRIC.md`. **Done.**
3. `03_pr-rubric-as-check/` — mechanical scorer + judged REVIEW of #7/#8/dirty tree. **Done.**
4. `04_take-forward/` — human-ordered apply onto operator `CURRENT.md`. **This stage.** Halt for `aether next`.

## Non-goals

- Do not `aether approve` / `next` / `reject`.
- Do not overwrite `CURRENT.md` again unless the human re-orders an apply.
- Do not assemble, sideload, or Confirm.
- Do not clone EdubaWare as core (`clone-edubaware-as-core` already Prohibited in parked propose).
- Do not treat this essay as a GitHub PR to merge.
