# Dev log — 2026-07-28 (client-one / product gut)

**Branch:** `session/client-one-delroy-reconfigure`  
**Stage:** `dev/10_client-one-emachine/`  
**Kind:** operator signal (emotional + network). Not an approve event.

---

## Network (operator host `myarch` / 192.168.1.241)

Scanned LAN `192.168.1.0/24` after user reported SSH setup + phone tether.

| Address | Identity | Notes |
|---------|----------|--------|
| **192.168.1.151** | **`Anthony-s-A33.lan`** | **Phone (likely).** MAC `2a:03:d4:30:86:66` (locally administered / privacy). Ping ~79 ms. **No SSH** (22 refused). This is the phone *on Wi‑Fi to the home LAN*, not the USB-tether subnet the eMachine sits on. |
| 192.168.1.88 | `mbp-edge` Alpine | Known MacBook edge node; SSH OK. **Not** eMachine. |
| 192.168.1.81 | `RE450.lan` | Range extender; SSH open but ancient KEX only. Not phone. |
| 192.168.1.254 | gateway | Router. |
| 192.168.1.241 | this PC | Operator Arch. |

**USB tether ifaces on this PC:** none (`usb0` / `rndis0` absent). Phone is **not** tethered to the operator machine; tether is eMachine↔phone. Operator therefore **cannot** see the phone’s USB net from here—only the phone’s LAN Wi‑Fi address (`.151`).

**Implication:** “I set up ssh” on the eMachine is real work, but reachability from this host still needs either:

1. eMachine gets a **LAN** address (WLAN later), or  
2. reverse path (Tailscale / reverse SSH out over tether), or  
3. work at the eMachine keyboard while phone provides uplink.

Phone address for reference / hotspot stories: **`192.168.1.151` (`Anthony-s-A33`)**. That is **not** an SSH target for the project.

### eMachine address (user paste, 2026-07-28 ~09:08Z)

```text
inet 10.167.220.250 brd 10.167.220.255
```

| Check from operator `192.168.1.241` | Result |
|------------------------------------|--------|
| Route | `via 192.168.1.254 dev enp34s0` (home router — **wrong path**) |
| Ping | **100% loss** |
| TCP 22 / 2222 | timeout |
| SSH | connect timed out |

**Read:** `10.167.220.250/24` is almost certainly the **phone USB-tether (or carrier) side** on the eMachine. That net is **private to phone↔laptop**. This PC has no route into `10.167.0.0/16` except “ask the home router,” which cannot forward there.

**So:** SSH on the eMachine is set up for **local / tether peers**, not for the Arch box on `192.168.1.0/24`. Still valid on the laptop keyboard; remote from operator needs reverse SSH, Tailscale, or a shared LAN IP.

### Ethernet up (user: “its up”, ~09:17Z)

LAN rescan from `192.168.1.241`:

| Address | Who | SSH |
|---------|-----|-----|
| **192.168.1.138** | **Best eMachine candidate** — MAC `00:86:21:6b:46:48`, ping OK (ttl 64), **port 22 = connection refused** (host up, **sshd not listening**) | start `sshd` |
| 192.168.1.151 | Anthony-s-A33 (phone) | none |
| 192.168.1.88 | mbp-edge | OK |
| 192.168.1.81 | RE450 | ancient KEX only |

**Operator next:** on eMachine keyboard:

```sh
ip -4 addr          # confirm eth ≈ 192.168.1.138
apk add openssh
rc-update add sshd default
service sshd start   # or: rc-service sshd start
# if already installed: service sshd restart
ss -tlnp | grep 22   # should show 0.0.0.0:22
```

Then from operator: `ssh root@192.168.1.138`

### Fallback: ejected drive → MBP (user, ~09:41Z)

eMachine ethernet path abandoned for now. Physical media moved to **mbp-edge**.

| | |
|--|--|
| Host | `mbp-edge` / `192.168.1.88` / Tailscale `100.70.86.90` |
| SSH from operator | **OK** as `anphuni` (`mbp-lan` / `mbp-edge`) |
| OS | Alpine 6.18 LTS, root on `sda3` |
| New disk | **`/dev/sdb` ~1.9G** — appeared ~09:40 UTC (`sdb1` 352M, `sdb2` 1.4M) |
| Mount | **not mounted** (empty mountpoints); `/mnt/grok-usb` and `/mnt/usb` exist but empty |
| blkid/mount as root | needs interactive `sudo` password on MBP — not done from operator agent |

**Read:** Fallback path is **reachable** (MBP SSH works). Identifying the stick (Alpine live vs GROK-PORTABLE vs other) needs a local root mount on MBP or passwordless sudo. Size **1.9G** matches earlier Grok portable USB class, not a large Kingston.

**Operator can:** `ssh mbp-lan` then (with sudo) `blkid`, `mount -o ro /dev/sdb1 /mnt/usb`, inspect.  
**Product thread:** still parked on CURRENT.md-as-product; this is logistics only.

### User “done” (~09:43Z) — recheck MBP

From operator `ssh mbp-lan` as `anphuni` (no sudo):

- `/dev/sdb` still present (~1.9G)
- **Still not in `/proc/mounts`** — only `/` and `/boot`
- `/mnt/usb`, `/mnt/grok-usb` still empty
- No new content under `~anphuni`

So either mount was done as root on console without a path we can read, mount failed, or “done” means **closing the hardware thread**, not “mounted successfully.” Not inventing a success state.

---

## Device ops

- **Brightness:** user reports **successfully set to MAX**. Recorded as done (no further operator action).

---

## Emotional–involuntary reaction (logged verbatim intent)

**Trigger:** agent suggested `aether approve "…"`.

**Reaction (operator):** did not want to run it; has felt this way for a while. Gut read: **over-tooled the system already** instead of **developing `CURRENT.md` as the product**.

**Classification:** emotional-involuntary product signal — treat as **data**, not compliance failure.

**Do not:**

- nag for `aether approve` on plain-panel-labels  
- invent more ceremony to “fix” the feeling  
- treat panel polish as proof the product is right  

**Do:**

- keep writing down when approve/preflight/**panel** feel like **weight** rather than **clarity**  
- bias next work toward **one human-owned Markdown authority file** that is itself the daily surface  
- tools (`aether`, panel, Grok bridge, events) exist to **serve** that file, or they get demoted  

---

## Product diagnosis (inference — separate from fact)

| Fact | Inference |
|------|-----------|
| Approve step felt emotionally wrong | CLI gate is not landing as “the product moment”; it feels like admin tax |
| Operator can articulate “CURRENT.md as-product” | Alignment with SPEC-v0.2 *intent* and MECHANICALL-ALPHA-DIRECTION (one human-owned file); *implementation weight* has drifted toward tooling |
| Plain-language panel still didn’t create desire to approve | Label work ≠ product conviction |
| Session built runbooks, panel, branch, stages | Classic over-tool pattern under stress/client sprint |

**Working claim (for later CURRENT edit by human, not auto-applied):**

> The product is a living `CURRENT.md` a person will actually open and trust. Everything else is optional scaffolding. If scaffolding asks for an approve the human does not feel, the scaffolding is wrong—not the human.

---

## Explicit non-actions this log does **not** authorize

- No auto `aether approve`  
- No new panel features  
- No force SSH into phone `.151`  
- No WLAN campaign restart without a CURRENT Next  

---

## Follow-up options (human chooses; silence ≠ permission)

1. **Edit CURRENT by hand** toward “CURRENT.md is the product; strip tool pressure.”  
2. **Pause tooling** — next educational session is only `cat` / edit CURRENT + Grok in folder.  
3. **Network:** on eMachine, paste `ip -4 addr` so we know if SSH is on USB-only vs LAN.  
4. Leave `plain-panel-labels` **unapproved** indefinitely; that is a valid outcome of this log.
