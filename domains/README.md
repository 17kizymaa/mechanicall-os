# domains/

**status: lab** — sample Domain folders only · not the product · may vanish  

Examples for local CURRENT experiments (`house-tv-desk`, `minimal-cli`).  
Core product is the authority protocol (`aether` + CURRENT), not these folders.  
See `docs/LAB-STATUS.md`.
